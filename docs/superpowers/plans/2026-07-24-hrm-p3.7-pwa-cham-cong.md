# P3.7 — PWA riêng cho vỏ chấm công (`/toi`) + seam bọc Capacitor — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho phép nhân viên cài vỏ chấm công `/toi` thành một app PWA riêng ("Chấm công", mở thẳng check-in), giữ nguyên app "Nhân sự" cho admin, và để sẵn seam để sau bọc Capacitor chỉ là thêm vỏ.

**Architecture:** Một build của `vite-plugin-pwa` chỉ sinh một manifest ("Nhân sự"). Ta thêm một manifest tĩnh thứ hai (`manifest.chamcong.webmanifest`, scope `/toi`) và **tráo `<link rel="manifest">` runtime** theo route: ở trong `/toi` thì trỏ manifest "Chấm công", ngoài ra trỏ manifest mặc định. Thêm `appTarget.ts` làm seam: build `VITE_APP_TARGET=cham-cong` boot thẳng `/toi/cham-cong` và tắt service worker khi chạy native (`isNativeApp()`).

**Tech Stack:** React 18 + react-router-dom v6.30, Vite + `vite-plugin-pwa`, Vitest 4 (env `node`, chọn `jsdom` per-file bằng docblock `// @vitest-environment jsdom`), `@testing-library/react` v16.

## Global Constraints

- Node 22 (`fe/.nvmrc` = v22.0.0); frontend dùng **npm** (`cd fe`). Chạy 1 test: `npx vitest run <path>`. Chạy tất cả: `npm run test`.
- Thư mục làm việc của mọi lệnh FE là `fe/` (repo root là thư mục cha). Đường dẫn trong plan là **tương đối `fe/`** trừ khi ghi rõ.
- Vitest env mặc định là `node`; file test cần DOM **phải** mở đầu bằng `// @vitest-environment jsdom` (dòng ĐẦU TIÊN của file).
- Manifest "Chấm công": `name: "Chấm công"`, `id: "/toi"`, `start_url: "/toi/cham-cong"`, `scope: "/toi"`, `theme_color: "#1F3864"`, dùng lại icon `pwa-192x192.png` / `pwa-512x512.png`.
- KHÔNG đụng: cấu hình `vite-plugin-pwa` trong `vite.config.ts`, manifest "Nhân sự", chiến lược cache (API vẫn NetworkOnly), logic đăng nhập & route con của `/toi`. KHÔNG tạo project Capacitor ở phase này. KHÔNG thêm offline queue.
- Commit sau mỗi task. Cuối message commit thêm:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

### Task 1: Seam `appTarget.ts` — target build + nhận biết native

**Files:**
- Create: `fe/src/appTarget.ts`
- Test: `fe/src/appTarget.test.ts`

**Interfaces:**
- Produces:
  - `type AppTarget = "full" | "cham-cong"`
  - `const APP_TARGET: AppTarget`
  - `const isChamCongApp: boolean`
  - `function getHomePath(): string` → `"/toi/cham-cong"` khi cham-cong, ngược lại `"/"`
  - `function isNativeApp(): boolean` → true khi chạy trong Capacitor native

- [ ] **Step 1: Viết test thất bại**

Tạo `fe/src/appTarget.test.ts`:

```ts
import { describe, it, expect, vi, afterEach } from 'vitest';

describe('appTarget', () => {
  afterEach(() => {
    vi.unstubAllEnvs();
    vi.resetModules();
    delete (globalThis as { Capacitor?: unknown }).Capacitor;
  });

  it('mặc định (không set VITE_APP_TARGET) là full, home = /', async () => {
    vi.stubEnv('VITE_APP_TARGET', '');
    vi.resetModules();
    const m = await import('./appTarget');
    expect(m.isChamCongApp).toBe(false);
    expect(m.getHomePath()).toBe('/');
  });

  it('VITE_APP_TARGET=cham-cong → isChamCongApp, home = /toi/cham-cong', async () => {
    vi.stubEnv('VITE_APP_TARGET', 'cham-cong');
    vi.resetModules();
    const m = await import('./appTarget');
    expect(m.isChamCongApp).toBe(true);
    expect(m.getHomePath()).toBe('/toi/cham-cong');
  });

  it('isNativeApp: false khi không có Capacitor', async () => {
    vi.resetModules();
    const m = await import('./appTarget');
    expect(m.isNativeApp()).toBe(false);
  });

  it('isNativeApp: true khi Capacitor.isNativePlatform() = true', async () => {
    (globalThis as { Capacitor?: unknown }).Capacitor = {
      isNativePlatform: () => true,
    };
    vi.resetModules();
    const m = await import('./appTarget');
    expect(m.isNativeApp()).toBe(true);
  });
});
```

- [ ] **Step 2: Chạy test để thấy FAIL**

Run: `cd fe && npx vitest run src/appTarget.test.ts`
Expected: FAIL — không import được `./appTarget` (file chưa tồn tại).

- [ ] **Step 3: Tạo `fe/src/appTarget.ts`**

```ts
/**
 * Seam để phân biệt app đang chạy ở dạng nào và có đang chạy trong Capacitor native không.
 * - Build web thường: VITE_APP_TARGET rỗng → "full", hành vi như cũ.
 * - Build để bọc Capacitor: `VITE_APP_TARGET=cham-cong npm run build` → boot thẳng chấm công.
 */
export type AppTarget = "full" | "cham-cong";

export const APP_TARGET: AppTarget =
  import.meta.env.VITE_APP_TARGET === "cham-cong" ? "cham-cong" : "full";

export const isChamCongApp = APP_TARGET === "cham-cong";

/** Trang chủ tùy theo target: app chấm công mở thẳng màn check-in. */
export function getHomePath(): string {
  return isChamCongApp ? "/toi/cham-cong" : "/";
}

/** True khi web bundle đang chạy trong vỏ Capacitor native (an toàn khi chưa cài Capacitor). */
export function isNativeApp(): boolean {
  const cap = (globalThis as { Capacitor?: { isNativePlatform?: () => boolean } })
    .Capacitor;
  return !!cap?.isNativePlatform?.();
}
```

- [ ] **Step 4: Chạy test để thấy PASS**

Run: `cd fe && npx vitest run src/appTarget.test.ts`
Expected: PASS (4 test).

- [ ] **Step 5: Commit**

```bash
cd /Users/os_anhvt/Documents/Dino/hrm
git add fe/src/appTarget.ts fe/src/appTarget.test.ts
git commit -m "feat(fe): seam appTarget (target build + isNativeApp) cho PWA/Capacitor

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Manifest tĩnh thứ hai `manifest.chamcong.webmanifest`

**Files:**
- Create: `fe/public/manifest.chamcong.webmanifest`
- Test: `fe/src/pwa/manifestChamCong.test.ts`

**Interfaces:**
- Produces: file tĩnh phục vụ tại `/manifest.chamcong.webmanifest` (Vite copy `public/` → gốc `dist/`).

- [ ] **Step 1: Viết test thất bại**

Tạo `fe/src/pwa/manifestChamCong.test.ts` (env `node` mặc định — đọc file bằng fs):

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import path from 'node:path';

describe('manifest.chamcong.webmanifest', () => {
  const raw = readFileSync(
    path.resolve(__dirname, '../../public/manifest.chamcong.webmanifest'),
    'utf-8',
  );
  const manifest = JSON.parse(raw);

  it('là JSON hợp lệ với đúng danh tính app chấm công', () => {
    expect(manifest.name).toBe('Chấm công');
    expect(manifest.id).toBe('/toi');
    expect(manifest.start_url).toBe('/toi/cham-cong');
    expect(manifest.scope).toBe('/toi');
    expect(manifest.display).toBe('standalone');
  });

  it('dùng lại icon PWA hiện có', () => {
    const srcs = manifest.icons.map((i: { src: string }) => i.src);
    expect(srcs).toContain('/pwa-192x192.png');
    expect(srcs).toContain('/pwa-512x512.png');
  });
});
```

- [ ] **Step 2: Chạy test để thấy FAIL**

Run: `cd fe && npx vitest run src/pwa/manifestChamCong.test.ts`
Expected: FAIL — `ENOENT` (file manifest chưa tồn tại).

- [ ] **Step 3: Tạo `fe/public/manifest.chamcong.webmanifest`**

```json
{
  "name": "Chấm công",
  "short_name": "Chấm công",
  "description": "Chấm công nhân viên",
  "lang": "vi",
  "id": "/toi",
  "start_url": "/toi/cham-cong",
  "scope": "/toi",
  "display": "standalone",
  "orientation": "portrait",
  "theme_color": "#1F3864",
  "background_color": "#ffffff",
  "icons": [
    { "src": "/pwa-192x192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/pwa-512x512.png", "sizes": "512x512", "type": "image/png" },
    { "src": "/pwa-512x512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

- [ ] **Step 4: Chạy test để thấy PASS**

Run: `cd fe && npx vitest run src/pwa/manifestChamCong.test.ts`
Expected: PASS (2 test).

- [ ] **Step 5: Commit**

```bash
cd /Users/os_anhvt/Documents/Dino/hrm
git add fe/public/manifest.chamcong.webmanifest fe/src/pwa/manifestChamCong.test.ts
git commit -m "feat(fe): manifest PWA riêng cho vỏ chấm công (scope /toi)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `employeeManifest.ts` — tráo link manifest theo path

**Files:**
- Create: `fe/src/pwa/employeeManifest.ts`
- Test: `fe/src/pwa/employeeManifest.test.ts`

**Interfaces:**
- Produces: `function applyManifestForPath(pathname: string): void`
  - path bắt đầu `/toi` → `<link rel="manifest">` href = `/manifest.chamcong.webmanifest`, meta `apple-mobile-web-app-title` = `"Chấm công"`.
  - ngược lại → href = `/manifest.webmanifest`, apple title = `"Nhân sự"`.
  - Tạo mới thẻ `<link>` / `<meta>` nếu thiếu; idempotent (không nhân đôi thẻ).

- [ ] **Step 1: Viết test thất bại**

Tạo `fe/src/pwa/employeeManifest.test.ts` (CẦN DOM — docblock ở dòng đầu):

```ts
// @vitest-environment jsdom
import { describe, it, expect, beforeEach } from 'vitest';
import { applyManifestForPath } from './employeeManifest';

const APPLE = 'meta[name="apple-mobile-web-app-title"]';

function href() {
  return document.querySelector<HTMLLinkElement>('link[rel="manifest"]')?.getAttribute('href');
}
function appleTitle() {
  return document.querySelector<HTMLMetaElement>(APPLE)?.getAttribute('content');
}

describe('applyManifestForPath', () => {
  beforeEach(() => {
    document.head.innerHTML = '';
  });

  it('trong /toi → manifest chấm công + apple title "Chấm công"', () => {
    applyManifestForPath('/toi/cham-cong');
    expect(href()).toBe('/manifest.chamcong.webmanifest');
    expect(appleTitle()).toBe('Chấm công');
  });

  it('ngoài /toi → manifest mặc định + apple title "Nhân sự"', () => {
    applyManifestForPath('/nhan-su/ho-so-nhan-vien');
    expect(href()).toBe('/manifest.webmanifest');
    expect(appleTitle()).toBe('Nhân sự');
  });

  it('tạo thẻ link mới khi document chưa có, không ném lỗi', () => {
    expect(document.querySelector('link[rel="manifest"]')).toBeNull();
    expect(() => applyManifestForPath('/toi')).not.toThrow();
    expect(href()).toBe('/manifest.chamcong.webmanifest');
  });

  it('idempotent: gọi hai lần không nhân đôi thẻ link/meta', () => {
    applyManifestForPath('/toi');
    applyManifestForPath('/toi');
    expect(document.querySelectorAll('link[rel="manifest"]').length).toBe(1);
    expect(document.querySelectorAll(APPLE).length).toBe(1);
  });

  it('tráo qua lại: /toi rồi / cập nhật đúng cùng thẻ', () => {
    applyManifestForPath('/toi');
    applyManifestForPath('/');
    expect(href()).toBe('/manifest.webmanifest');
    expect(appleTitle()).toBe('Nhân sự');
    expect(document.querySelectorAll('link[rel="manifest"]').length).toBe(1);
  });
});
```

- [ ] **Step 2: Chạy test để thấy FAIL**

Run: `cd fe && npx vitest run src/pwa/employeeManifest.test.ts`
Expected: FAIL — không import được `applyManifestForPath`.

- [ ] **Step 3: Tạo `fe/src/pwa/employeeManifest.ts`**

```ts
/**
 * Tráo <link rel="manifest"> và apple title theo route để vỏ nhân viên /toi cài được
 * thành app PWA riêng "Chấm công", còn phần còn lại vẫn là app "Nhân sự".
 * Thuần DOM, idempotent, an toàn khi thẻ chưa tồn tại (dev / test).
 */
const CHAMCONG_MANIFEST = "/manifest.chamcong.webmanifest";
const DEFAULT_MANIFEST = "/manifest.webmanifest";
const APPLE_TITLE = "apple-mobile-web-app-title";

function ensureManifestLink(): HTMLLinkElement {
  let link = document.querySelector<HTMLLinkElement>('link[rel="manifest"]');
  if (!link) {
    link = document.createElement("link");
    link.rel = "manifest";
    document.head.appendChild(link);
  }
  return link;
}

function ensureAppleTitleMeta(): HTMLMetaElement {
  let meta = document.querySelector<HTMLMetaElement>(`meta[name="${APPLE_TITLE}"]`);
  if (!meta) {
    meta = document.createElement("meta");
    meta.name = APPLE_TITLE;
    document.head.appendChild(meta);
  }
  return meta;
}

export function applyManifestForPath(pathname: string): void {
  const isEmployee = pathname.startsWith("/toi");
  ensureManifestLink().setAttribute(
    "href",
    isEmployee ? CHAMCONG_MANIFEST : DEFAULT_MANIFEST,
  );
  ensureAppleTitleMeta().setAttribute("content", isEmployee ? "Chấm công" : "Nhân sự");
}
```

- [ ] **Step 4: Chạy test để thấy PASS**

Run: `cd fe && npx vitest run src/pwa/employeeManifest.test.ts`
Expected: PASS (5 test).

- [ ] **Step 5: Commit**

```bash
cd /Users/os_anhvt/Documents/Dino/hrm
git add fe/src/pwa/employeeManifest.ts fe/src/pwa/employeeManifest.test.ts
git commit -m "feat(fe): tráo manifest theo route (/toi → app Chấm công)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: `ManifestSync` component + đấu nối vào `App` và `main.tsx`

**Files:**
- Create: `fe/src/pwa/ManifestSync.tsx`
- Test: `fe/src/pwa/ManifestSync.test.tsx`
- Modify: `fe/src/App.tsx` (thêm `<ManifestSync />` trong `<BrowserRouter>`)
- Modify: `fe/src/main.tsx` (gọi `applyManifestForPath` trước khi render)

**Interfaces:**
- Consumes: `applyManifestForPath` (Task 3).
- Produces: `ManifestSync` (default export) — component không render UI, gọi `applyManifestForPath(location.pathname)` mỗi khi path đổi. Phải đặt bên trong Router (dùng `useLocation`).

- [ ] **Step 1: Viết test thất bại**

Tạo `fe/src/pwa/ManifestSync.test.tsx`:

```tsx
// @vitest-environment jsdom
import { describe, it, expect, beforeEach } from 'vitest';
import { render } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import ManifestSync from './ManifestSync';

function href() {
  return document.querySelector<HTMLLinkElement>('link[rel="manifest"]')?.getAttribute('href');
}

describe('ManifestSync', () => {
  beforeEach(() => {
    document.head.innerHTML = '';
  });

  it('mount ở /toi/cham-cong → manifest chấm công', () => {
    render(
      <MemoryRouter initialEntries={['/toi/cham-cong']}>
        <ManifestSync />
      </MemoryRouter>,
    );
    expect(href()).toBe('/manifest.chamcong.webmanifest');
  });

  it('mount ở / → manifest mặc định', () => {
    render(
      <MemoryRouter initialEntries={['/']}>
        <ManifestSync />
      </MemoryRouter>,
    );
    expect(href()).toBe('/manifest.webmanifest');
  });
});
```

- [ ] **Step 2: Chạy test để thấy FAIL**

Run: `cd fe && npx vitest run src/pwa/ManifestSync.test.tsx`
Expected: FAIL — không import được `./ManifestSync`.

- [ ] **Step 3: Tạo `fe/src/pwa/ManifestSync.tsx`**

```tsx
import { useEffect } from "react";
import { useLocation } from "react-router-dom";
import { applyManifestForPath } from "./employeeManifest";

/** Cập nhật <link rel="manifest"> theo route hiện tại. Không render UI. */
export default function ManifestSync() {
  const { pathname } = useLocation();
  useEffect(() => {
    applyManifestForPath(pathname);
  }, [pathname]);
  return null;
}
```

- [ ] **Step 4: Chạy test để thấy PASS**

Run: `cd fe && npx vitest run src/pwa/ManifestSync.test.tsx`
Expected: PASS (2 test).

- [ ] **Step 5: Đấu `ManifestSync` vào `App.tsx`**

Trong `fe/src/App.tsx`, thêm import cạnh các import component:

```tsx
import ManifestSync from "./pwa/ManifestSync";
```

Đặt `<ManifestSync />` làm con đầu tiên bên trong `<BrowserRouter>` (nó cần context Router). Sửa khối:

```tsx
        <BrowserRouter>
          <AuthProvider>
```

thành:

```tsx
        <BrowserRouter>
          <ManifestSync />
          <AuthProvider>
```

- [ ] **Step 6: Gọi lần đầu trong `main.tsx`**

Sửa `fe/src/main.tsx` để manifest đúng ngay lần load đầu (trước khi React render):

```tsx
import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";
import "./common";
import { applyManifestForPath } from "./pwa/employeeManifest";

applyManifestForPath(window.location.pathname);

createRoot(document.getElementById("root")!).render(<App />);
```

- [ ] **Step 7: Chạy lại test + typecheck build nhẹ**

Run: `cd fe && npx vitest run src/pwa/ManifestSync.test.tsx src/pwa/employeeManifest.test.ts`
Expected: PASS toàn bộ.

- [ ] **Step 8: Commit**

```bash
cd /Users/os_anhvt/Documents/Dino/hrm
git add fe/src/pwa/ManifestSync.tsx fe/src/pwa/ManifestSync.test.tsx fe/src/App.tsx fe/src/main.tsx
git commit -m "feat(fe): đồng bộ manifest theo route + set manifest ngay khi load

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Boot thẳng chấm công khi target = cham-cong (seam Capacitor)

**Files:**
- Modify: `fe/src/App.tsx` (route index `/` và catch-all `*` tùy `isChamCongApp`)

**Interfaces:**
- Consumes: `isChamCongApp`, `getHomePath` (Task 1); `Navigate` (đã import sẵn trong App.tsx).
- Ghi chú test: nhánh quyết định đã được phủ bởi unit test `getHomePath` ở Task 1. Bước này là glue JSX mỏng, xác minh bằng typecheck/build (Step 3) — không mount toàn bộ `App` (kéo theo nhiều provider + network) chỉ để test một `<Navigate>`.

- [ ] **Step 1: Thêm import seam vào `App.tsx`**

Trong `fe/src/App.tsx`, thêm:

```tsx
import { isChamCongApp, getHomePath } from "./appTarget";
```

- [ ] **Step 2: Sửa route index `/` và catch-all `*`**

Sửa route index trong khối `MainLayout` (hiện là):

```tsx
                <Route index element={<TrangChuTheoQuyen />} />
```

thành:

```tsx
                <Route
                  index
                  element={
                    isChamCongApp ? (
                      <Navigate to={getHomePath()} replace />
                    ) : (
                      <TrangChuTheoQuyen />
                    )
                  }
                />
```

Sửa catch-all (hiện là):

```tsx
              <Route path="*" element={<NotFound />} />
```

thành:

```tsx
              <Route
                path="*"
                element={
                  isChamCongApp ? <Navigate to={getHomePath()} replace /> : <NotFound />
                }
              />
```

> Lý do: build native (Capacitor) load `index.html` ở gốc `/`; hai chỗ này đưa app về thẳng `/toi/cham-cong`. Build web thường (`full`) giữ nguyên hành vi cũ.

- [ ] **Step 3: Kiểm bằng build hai target**

Run (web thường vẫn build được):
`cd fe && npm run build`
Expected: build thành công, không lỗi TypeScript.

Run (target chấm công build được):
`cd fe && VITE_APP_TARGET=cham-cong npm run build`
Expected: build thành công. (Xác minh thủ công tùy chọn: `npm run preview` rồi mở `/` → tự nhảy `/toi/cham-cong`.)

- [ ] **Step 4: Commit**

```bash
cd /Users/os_anhvt/Documents/Dino/hrm
git add fe/src/App.tsx
git commit -m "feat(fe): target=cham-cong boot thẳng /toi/cham-cong (seam Capacitor)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Tắt service worker khi chạy native (seam Capacitor)

**Files:**
- Modify: `fe/src/components/shared/PWAUpdatePrompt.tsx`

**Interfaces:**
- Consumes: `isNativeApp` (Task 1).

- [ ] **Step 1: Thêm guard `isNativeApp()` vào effect đăng ký SW**

Trong `fe/src/components/shared/PWAUpdatePrompt.tsx`:

Thêm import:

```tsx
import { isNativeApp } from '../../appTarget';
```

Trong `useEffect`, thêm dòng đầu tiên trước khi khai báo `timer`/`registerSW`:

```tsx
  useEffect(() => {
    // Capacitor tự phục vụ asset offline — không đăng ký service worker trong native
    // để tránh xung đột scheme capacitor:// với Workbox.
    if (isNativeApp()) return;

    let timer: ReturnType<typeof setInterval> | undefined;
    // ...phần còn lại giữ nguyên...
```

- [ ] **Step 2: Xác minh không phá web (SW vẫn đăng ký)**

Run: `cd fe && npm run test`
Expected: toàn bộ test hiện có PASS (không có regression). `isNativeApp()` trả false trên web/test nên `registerSW` vẫn chạy như cũ.

- [ ] **Step 3: Commit**

```bash
cd /Users/os_anhvt/Documents/Dino/hrm
git add fe/src/components/shared/PWAUpdatePrompt.tsx
git commit -m "feat(fe): bỏ đăng ký service worker khi chạy Capacitor native

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Kiểm thử tổng & thủ công (sau Task 6)

- Chạy full: `cd fe && npm run test` → tất cả PASS.
- Build web: `cd fe && npm run build` → OK; `dist/index.html` vẫn có `<link rel="manifest" href="/manifest.webmanifest">`, và `dist/manifest.chamcong.webmanifest` tồn tại.
- QA thủ công (ghi lại, ngoài scope tự động):
  - Chrome Android mở `/toi/cham-cong` → "Cài đặt ứng dụng" hiện tên **Chấm công**, cài về mở thẳng check-in; mở `/` vẫn cài được app **Nhân sự** riêng.
  - iOS Safari "Thêm vào MH chính" từ `/toi` → tên **Chấm công**.
  - Build `VITE_APP_TARGET=cham-cong` + `npm run preview`: mở `/` tự nhảy `/toi/cham-cong`.

## Self-review (đã đối chiếu spec P3.7)

- §3.1 manifest tĩnh → Task 2. §3.2 module tráo link → Task 3. §3.3 đấu nối (main.tsx + effect theo route) → Task 4. §3.5 app-target/getHomePath + boot redirect → Task 1 + Task 5. §3.6 tắt SW native → Task 6. §4 test (employeeManifest + appTarget) → Task 1/2/3/4. §6 seam Capacitor → Task 1/5/6 (native project để phase sau — không làm, đúng spec). §7 deliverables khớp danh sách file các task.
- Không placeholder; mọi bước có code/lệnh cụ thể.
- Nhất quán tên: `applyManifestForPath`, `getHomePath`, `isChamCongApp`, `isNativeApp`, `ManifestSync` dùng đồng nhất giữa các task.

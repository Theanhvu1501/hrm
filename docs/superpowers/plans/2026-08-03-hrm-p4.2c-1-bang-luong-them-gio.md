# P4.2c-1 — Bảng lương thêm giờ (mẫu 03-LĐTL) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Màn "Bảng lương thêm giờ" riêng — tổng hợp tiền làm thêm theo kỳ từ đơn OT đã duyệt, sửa tay được, chốt kỳ để khoá số, và xuất Excel đúng biểu mẫu pháp định 03-LĐTL.

**Architecture:** Ba lớp. (1) Hàm thuần `tinhDongThemGio()` trong `libs/core` — nhận giờ theo loại + cấu hình, trả đơn giá và tiền từng loại; không DB, không NestJS. (2) Entity `DongLuongThemGio` + service tổng hợp/sửa/chốt trong `config-service`, cùng khuôn `DongLuong`/`BangLuong_Service` đã có. (3) Màn FE theo pattern CHandler, cột sinh động theo `heSoTra`, xuất Excel qua `exportReportExcel` sẵn có.

**Tech Stack:** NestJS 10 + TypeORM (MongoDB), Jest (backend); React 18 + Vite + antd + Vitest + ExcelJS (frontend); yarn (be), npm (fe); Node 22.

**Spec:** `docs/superpowers/specs/2026-08-03-hrm-p4.2c-tien-lam-them-gio-design.md` §2.3, §5

**Phụ thuộc:** P4.2b (đã ship 2026-08-03) — `phanBoOt` trên đơn, `heSoTra`/`uuTienLoai` trong cấu hình.

## Phạm vi: đây là plan 1/2 của P4.2c

| Plan | Nội dung | `KetQuaLuong` |
|---|---|---|
| **P4.2c-1 (plan này)** | Màn Bảng lương thêm giờ, tổng hợp / sửa tay / chốt kỳ, xuất 03-LĐTL | **không đụng** |
| P4.2c-2 (sau) | `TIEN_OT` đọc số đã chốt, tách miễn thuế, cột TN miễn thuế gộp, phí công đoàn, mở khoá `cheDoBu`, đánh dấu `kyLuongTra` | đổi |

Tách vì ranh giới sạch: hết plan này, kế toán đã có bảng thanh toán tiền làm thêm và biểu mẫu pháp định để bàn giao, mà **bảng lương chính chưa đổi một con số nào** — deploy được không thể sai lương. Plan 2 mới nối vào và mới đụng thuế.

## Global Constraints

- **Node 22 bắt buộc.** `cd be && nvm use v22.0.0`; `cd fe && nvm use`. Không `nvm use` thì `node` không tồn tại trong shell này.
- **KHÔNG đụng `be/libs/core/src/luong/tinh-luong.ts` và `KetQuaLuong`.** Mọi thay đổi ở đó là P4.2c-2. Vi phạm là làm sai lương ngay lúc deploy plan này.
- **Không làm tròn tiền trên bảng thêm giờ.** Dòng dữ liệu thật trong sheet `LÀM THÊM GIỜ` là `1.503.906,25` — có phần thập phân. `lamTronTheo()` chỉ áp khi con số này thành khoản `TIEN_OT` trong bảng lương chính (P4.2c-2).
- **Đơn giá:** `donGiaNgay = luongThang / congChuan`, `donGiaGio = donGiaNgay / soGioMoiNgay`. Ca kiểm chuẩn lấy từ sheet: `5.500.000 / 24 = 229.166,67` → `/ 8 = 28.645,83` → `35 giờ × 28.645,83 × 1,5 = 1.503.906,25`.
- **Cột sinh động theo `heSoTra`**, không cứng bốn nhóm. Cứng bốn nhóm là ném đi toàn bộ phần cấu hình hoá của P4.2b.
- **Quyền:** dùng lại `/luong/bang-luong:xem|sua|them` — **không mở module quyền mới**, để không phát sinh bước bắt buộc `ops/grant-quyen-module-moi.ts` lúc deploy.
- **Thêm trường vào entity thì PHẢI khai trong DTO** — `main.ts` bật `forbidNonWhitelisted`, quên khai là 400 cho cả form.
- **Commit sau mỗi task**, message tiếng Việt theo lệ repo.

---

## File Structure

| File | Trách nhiệm | Task |
|---|---|---|
| `be/libs/entities/src/luong/dong-luong-them-gio.entity.ts` | entity + kiểu `DongThemGioTheoLoai` | 1 |
| `be/libs/core/src/luong/tinh-them-gio.ts` | hàm thuần `tinhDongThemGio()` | 2 |
| `be/apps/config-service/src/bang-luong/them-gio.service.ts` | tổng hợp / sửa / chốt | 3, 4 |
| `be/apps/config-service/src/bang-luong/them-gio.controller.ts` | route + quyền | 4 |
| `be/apps/config-service/src/bang-luong/dto/cap-nhat-dong-them-gio.dto.ts` | DTO sửa tay | 4 |
| `fe/src/services/bangLuongThemGioService.ts` | client | 5 |
| `fe/src/pages/luong/bang-luong-them-gio/**` | màn hình (CHandler) | 5 |
| `fe/src/pages/luong/bang-luong-them-gio/xuat03LDTL.ts` | dựng `ReportSheet` cho 03-LĐTL | 6 |

Service tách file riêng (`them-gio.service.ts`) chứ không nhét vào `bang-luong.service.ts`: file đó đã ~600 dòng và lo bảng lương chính; hai thứ chốt kỳ độc lập nhau nên tách theo trách nhiệm là đúng.

---

## Task 1: Entity `DongLuongThemGio`

**Files:**
- Create: `be/libs/entities/src/luong/dong-luong-them-gio.entity.ts`
- Modify: `be/libs/entities/src/luong/index.ts` (hoặc barrel tương đương — kiểm bằng `grep -rn "dong-luong.entity" be/libs/entities/src`)

**Interfaces:**
- Consumes: `BaseEntity` (`be/libs/entities/src/base.entity.ts`)
- Produces:
  ```ts
  export interface DongThemGioTheoLoai { soGio: number; heSo: number; thanhTien: number }
  export class DongLuongThemGio extends BaseEntity {
    thang: string; employeeId: string; employeeName?: string; employeeCode?: string;
    luongThang: number; congChuan: number; soGioMoiNgay: number;
    donGiaNgay: number; donGiaGio: number;
    theoLoai: Record<string, DongThemGioTheoLoai>;
    tongTien: number; gioNghiBu: number; tienNghiBu: number; thucNhan: number;
    gioOtHetHan: number; suaTay: boolean;
    trangThai: string; isActive: boolean;
  }
  ```

- [ ] **Step 1: Tạo entity**

```ts
import { Entity, Column } from 'typeorm';
import { BaseEntity } from '../base.entity';

/** Một nhóm cột trên mẫu 03-LĐTL: Số giờ + Thành tiền của một loại ngày. */
export interface DongThemGioTheoLoai {
  soGio: number;
  /** Hệ số TRẢ TIỀN đã dùng — snapshot, không tra lại cấu hình khi đọc. */
  heSo: number;
  thanhTien: number;
}

/**
 * Một dòng bảng thanh toán tiền làm thêm giờ (mẫu 03-LĐTL) của MỘT nhân viên
 * trong MỘT kỳ.
 *
 * Tách khỏi `DongLuong` chứ không thêm cột vào đó: hai bảng chốt kỳ ĐỘC LẬP
 * (kế toán chốt bảng thêm giờ trước, rồi mới tổng hợp bảng lương chính đọc số
 * đã chốt — xem spec P4.2c §5), và mẫu 03-LĐTL là sản phẩm bàn giao riêng có
 * chỗ ký.
 *
 * Tiền ở đây KHÔNG làm tròn: dòng thật trong sheet `LÀM THÊM GIỜ` là
 * 1.503.906,25. Làm tròn chỉ xảy ra khi con số này thành khoản `TIEN_OT` trong
 * bảng lương chính (P4.2c-2), qua `lamTronTheo()`.
 */
@Entity('dong_luong_them_gio')
export class DongLuongThemGio extends BaseEntity {
  @Column() thang: string; // 'YYYY-MM'
  @Column() employeeId: string;
  @Column({ nullable: true }) employeeName?: string;
  @Column({ nullable: true }) employeeCode?: string;

  // ── Snapshot mẫu số của đơn giá ──────────────────────────────────────────
  @Column({ default: 0 }) luongThang: number;
  @Column({ default: 26 }) congChuan: number;
  @Column({ default: 8 }) soGioMoiNgay: number;
  @Column({ default: 0 }) donGiaNgay: number;
  @Column({ default: 0 }) donGiaGio: number;

  /** Khoá = mã loại ngày (`ngay_thuong`, `ngay_dem`, …), theo `uuTienLoai`. */
  @Column('json', { nullable: true }) theoLoai: Record<string, DongThemGioTheoLoai>;

  @Column({ default: 0 }) tongTien: number;

  /** Giờ nghỉ bù đã dùng trong kỳ — cột thông tin trên biểu mẫu, xem Task 3. */
  @Column({ default: 0 }) gioNghiBu: number;
  @Column({ default: 0 }) tienNghiBu: number;
  @Column({ default: 0 }) thucNhan: number;

  /** Giờ quy đổi từ quỹ hết hạn `quy_ra_tien` — P4.2c-2 mới trả tiền. */
  @Column({ default: 0 }) gioOtHetHan: number;

  /**
   * Kế toán đã sửa tay dòng này. `tongHop()` chạy lại KHÔNG ghi đè dòng đã
   * sửa tay — cùng quy ước `nhapTheoKy` của bảng lương chính: tổng hợp lại là
   * thao tác thường ngày, và ghi đè số kế toán vừa sửa là mất việc của họ.
   */
  @Column({ default: false }) suaTay: boolean;

  @Column({ default: 'nhap' }) trangThai: string; // nhap|chot
  @Column({ default: true }) isActive: boolean;
}

export interface DongLuongThemGioEntities {
  DongLuongThemGio: typeof DongLuongThemGio;
}
declare module '../entities' {
  interface Entities extends DongLuongThemGioEntities {}
}
```

- [ ] **Step 2: Export ra barrel**

Tìm nơi `DongLuong` được export và thêm dòng tương ứng:

```bash
cd be && grep -rn "dong-luong.entity" libs/entities/src
```

Thêm `export * from './luong/dong-luong-them-gio.entity';` ngay cạnh dòng của `dong-luong.entity`.

- [ ] **Step 3: Kiểm biên dịch**

```bash
cd be && nvm use v22.0.0 && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | grep -c ""
```
Expected: đúng **74** dòng — bằng baseline lỗi có sẵn của repo (toàn bộ nằm ở spec files của `auth-service`/`bang-luong.service.spec`/`libs/core`, không liên quan). Nhiều hơn 74 nghĩa là entity mới gây lỗi.

- [ ] **Step 4: Commit**

```bash
git add be/libs/entities/src/luong/
git commit -m "feat(be): entity DongLuongThemGio cho bảng thanh toán tiền làm thêm

Tách khỏi DongLuong vì hai bảng chốt kỳ độc lập: kế toán chốt bảng thêm giờ
trước, bảng lương chính mới đọc số đã chốt. Tiền không làm tròn ở tầng này —
dòng thật trong mẫu 03-LĐTL là 1.503.906,25."
```

---

## Task 2: Hàm thuần `tinhDongThemGio()`

**Files:**
- Create: `be/libs/core/src/luong/tinh-them-gio.ts`
- Modify: `be/libs/core/src/index.ts` (barrel — kiểm bằng `grep -rn "tinh-luong" be/libs/core/src/index.ts`)
- Test: `be/libs/core/src/luong/tinh-them-gio.spec.ts`

**Interfaces:**
- Consumes: `DongThemGioTheoLoai` (Task 1, từ `@app/entities`)
- Produces:
  ```ts
  export interface DauVaoThemGio {
    luongThang: number;
    congChuan: number;
    soGioMoiNgay: number;
    /** Giờ TRẢ TIỀN theo loại, đã lọc theo `cheDoBu` ở tầng service. */
    gioTheoLoai: Record<string, number>;
    heSoTra: Record<string, number>;
    /** Hệ số áp thực tế = heSoTra − 1 khi cheDoBu là `nghi_bu_va_chenh`. */
    truMotDonVi: boolean;
  }
  export interface KetQuaThemGio {
    donGiaNgay: number;
    donGiaGio: number;
    theoLoai: Record<string, DongThemGioTheoLoai>;
    tongTien: number;
  }
  export function tinhDongThemGio(dv: DauVaoThemGio): KetQuaThemGio;
  ```

- [ ] **Step 1: Viết test**

Tạo `be/libs/core/src/luong/tinh-them-gio.spec.ts`:

```ts
import { tinhDongThemGio } from './tinh-them-gio';

const HE_SO = { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 };

describe('tinhDongThemGio', () => {
  it('CA KIỂM CHUẨN từ sheet LÀM THÊM GIỜ: 5.500.000, 35 giờ ngày thường', () => {
    const kq = tinhDongThemGio({
      luongThang: 5_500_000,
      congChuan: 24,
      soGioMoiNgay: 8,
      gioTheoLoai: { ngay_thuong: 35 },
      heSoTra: HE_SO,
      truMotDonVi: false,
    });

    expect(kq.donGiaNgay).toBeCloseTo(229_166.6667, 4);
    expect(kq.donGiaGio).toBeCloseTo(28_645.8333, 4);
    expect(kq.theoLoai.ngay_thuong.thanhTien).toBeCloseTo(1_503_906.25, 2);
    expect(kq.tongTien).toBeCloseTo(1_503_906.25, 2);
  });

  it('cộng nhiều loại, mỗi loại một hệ số', () => {
    const kq = tinhDongThemGio({
      luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
      gioTheoLoai: { ngay_thuong: 10, ngay_le: 8 },
      heSoTra: HE_SO, truMotDonVi: false,
    });

    const g = 5_500_000 / 24 / 8;
    expect(kq.theoLoai.ngay_thuong).toEqual({
      soGio: 10, heSo: 1.5, thanhTien: 10 * g * 1.5,
    });
    expect(kq.theoLoai.ngay_le).toEqual({
      soGio: 8, heSo: 3.0, thanhTien: 8 * g * 3.0,
    });
    expect(kq.tongTien).toBeCloseTo(10 * g * 1.5 + 8 * g * 3.0, 6);
  });

  it('truMotDonVi: chế độ nghỉ bù + trả chênh chỉ trả phần vượt 1.0', () => {
    const kq = tinhDongThemGio({
      luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
      gioTheoLoai: { ngay_le: 8 },
      heSoTra: HE_SO, truMotDonVi: true,
    });

    const g = 5_500_000 / 24 / 8;
    // Phần 1.0 đã vào quỹ nghỉ bù, chỉ trả 3.0 − 1.0 = 2.0.
    expect(kq.theoLoai.ngay_le.heSo).toBe(2.0);
    expect(kq.theoLoai.ngay_le.thanhTien).toBeCloseTo(8 * g * 2.0, 6);
  });

  it('truMotDonVi không đẩy hệ số xuống ÂM khi hệ số cấu hình < 1', () => {
    // Hệ số < 1 là cấu hình sai nhưng DTO chỉ chặn ≤ 0, nên vẫn tới được đây.
    // Trả tiền âm là trừ vào lương người lao động vì họ làm thêm giờ.
    const kq = tinhDongThemGio({
      luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
      gioTheoLoai: { ngay_thuong: 10 },
      heSoTra: { ngay_thuong: 0.5 }, truMotDonVi: true,
    });

    expect(kq.theoLoai.ngay_thuong.heSo).toBe(0);
    expect(kq.theoLoai.ngay_thuong.thanhTien).toBe(0);
  });

  it('loại có 0 giờ vẫn hiện trong theoLoai — biểu mẫu cần in đủ cột', () => {
    const kq = tinhDongThemGio({
      luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
      gioTheoLoai: { ngay_thuong: 10, ngay_le: 0 },
      heSoTra: HE_SO, truMotDonVi: false,
    });

    expect(kq.theoLoai.ngay_le).toEqual({ soGio: 0, heSo: 3.0, thanhTien: 0 });
  });

  it('congChuan hoặc soGioMoiNgay = 0 thì đơn giá 0, KHÔNG Infinity', () => {
    // Chia cho 0 ra Infinity; Infinity ghi vào DB rồi hiện lên phiếu lương.
    const kq = tinhDongThemGio({
      luongThang: 5_500_000, congChuan: 0, soGioMoiNgay: 0,
      gioTheoLoai: { ngay_thuong: 10 },
      heSoTra: HE_SO, truMotDonVi: false,
    });

    expect(kq.donGiaNgay).toBe(0);
    expect(kq.donGiaGio).toBe(0);
    expect(kq.tongTien).toBe(0);
  });

  it('gioTheoLoai rỗng thì tổng 0, theoLoai rỗng', () => {
    const kq = tinhDongThemGio({
      luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
      gioTheoLoai: {}, heSoTra: HE_SO, truMotDonVi: false,
    });

    expect(kq.tongTien).toBe(0);
    expect(kq.theoLoai).toEqual({});
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test libs/core/src/luong/tinh-them-gio.spec.ts
```
Expected: FAIL — `Cannot find module './tinh-them-gio'`.

- [ ] **Step 3: Viết hàm**

Tạo `be/libs/core/src/luong/tinh-them-gio.ts`:

```ts
import type { DongThemGioTheoLoai } from '@app/entities';

export interface DauVaoThemGio {
  luongThang: number;
  congChuan: number;
  soGioMoiNgay: number;
  /** Giờ TRẢ TIỀN theo loại — đã lọc theo `cheDoBu` ở tầng service. */
  gioTheoLoai: Record<string, number>;
  heSoTra: Record<string, number>;
  /** `cheDoBu = 'nghi_bu_va_chenh'`: chỉ trả phần vượt 1.0. */
  truMotDonVi: boolean;
}

export interface KetQuaThemGio {
  donGiaNgay: number;
  donGiaGio: number;
  theoLoai: Record<string, DongThemGioTheoLoai>;
  tongTien: number;
}

/**
 * Tiền làm thêm của MỘT nhân viên trong MỘT kỳ, tách theo loại ngày.
 *
 * Công thức đơn giá khớp đúng dòng dữ liệu thật trong sheet `LÀM THÊM GIỜ`:
 * 5.500.000 / 24 = 229.166,67 (mức lương ngày) / 8 = 28.645,83 (mức lương giờ),
 * 35 giờ × 28.645,83 × 1,5 = 1.503.906,25.
 *
 * KHÔNG làm tròn — mẫu 03-LĐTL in tới phần thập phân. Làm tròn chỉ xảy ra khi
 * con số này thành khoản `TIEN_OT` của bảng lương chính (P4.2c-2).
 *
 * Mẫu số 0 trả đơn giá 0 chứ không `Infinity`: `congChuan`/`soGioMoiNgay` là
 * cấu hình do người dùng nhập, và `Infinity` ghi vào DB rồi hiện thẳng lên
 * phiếu lương của người lao động.
 */
export function tinhDongThemGio(dv: DauVaoThemGio): KetQuaThemGio {
  const donGiaNgay = dv.congChuan > 0 ? dv.luongThang / dv.congChuan : 0;
  const donGiaGio = dv.soGioMoiNgay > 0 ? donGiaNgay / dv.soGioMoiNgay : 0;

  const theoLoai: Record<string, DongThemGioTheoLoai> = {};
  let tongTien = 0;

  for (const [loai, soGio] of Object.entries(dv.gioTheoLoai)) {
    const heSoGoc = dv.heSoTra[loai] ?? 0;
    // `Math.max(0, …)`: hệ số cấu hình < 1 (DTO chỉ chặn ≤ 0) sẽ cho hệ số áp
    // ÂM ở chế độ trả chênh — tức TRỪ tiền của người lao động vì họ làm thêm.
    const heSo = dv.truMotDonVi ? Math.max(0, heSoGoc - 1) : heSoGoc;
    const thanhTien = soGio * donGiaGio * heSo;
    theoLoai[loai] = { soGio, heSo, thanhTien };
    tongTien += thanhTien;
  }

  return { donGiaNgay, donGiaGio, theoLoai, tongTien };
}
```

- [ ] **Step 4: Export ra barrel `@app/core`**

```bash
cd be && grep -n "tinh-luong" libs/core/src/index.ts
```

Thêm `export * from './luong/tinh-them-gio';` ngay dưới dòng của `tinh-luong`.

- [ ] **Step 5: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test libs/core
```
Expected: PASS toàn bộ.

- [ ] **Step 6: Commit**

```bash
git add be/libs/core/src/
git commit -m "feat(be): hàm thuần tinhDongThemGio — tiền làm thêm tách theo loại ngày

Ca kiểm chuẩn lấy thẳng từ sheet LÀM THÊM GIỜ của chủ sản phẩm:
5.500.000/24/8 = 28.645,83; 35 giờ x 1,5 = 1.503.906,25.

Không làm tròn ở tầng này — mẫu 03-LĐTL in tới phần thập phân.
Mẫu số 0 trả đơn giá 0 chứ không Infinity: congChuan/soGioMoiNgay do người
dùng nhập, và Infinity sẽ hiện thẳng lên phiếu lương."
```

---

## Task 3: `ThemGio_Service.tongHop()`

**Files:**
- Create: `be/apps/config-service/src/bang-luong/them-gio.service.ts`
- Create: `be/apps/config-service/src/bang-luong/them-gio.service.spec.ts`
- Modify: `be/apps/config-service/src/bang-luong/bang-luong.module.ts`

**Interfaces:**
- Consumes: `tinhDongThemGio` (Task 2), `DongLuongThemGio` (Task 1), `AttendanceRequest`, `Employee`, `CauHinhLuong`
- Produces: `ThemGio_Service.tongHop(thang: string): Promise<DongLuongThemGio[]>`

**Ba quyết định của task này, ghi rõ vì chúng quyết định con số ra tiền:**

1. **Nguồn giờ theo `cheDoBu`** (spec §2.3):

| `cheDoBu` | Giờ đưa vào `gioTheoLoai` | `truMotDonVi` |
|---|---|---|
| `chi_nghi_bu` | rỗng — giờ đã vào quỹ | `false` |
| `chi_tien` | toàn bộ `phanBoOt` của đơn đã duyệt trong kỳ | `false` |
| `nhan_vien_chon` | chỉ đơn có `hinhThucBu === 'tien'` | `false` |
| `nghi_bu_va_chenh` | toàn bộ | `true` |

2. **Cột "Số ngày nghỉ bù" trên biểu mẫu là THÔNG TIN, `tienNghiBu` luôn 0.** Biểu mẫu gốc trừ tiền nghỉ bù vào Thực nhận vì nó giả định công ty trả tiền OT rồi lấy lại phần đã nghỉ bù. Trong cả bốn chế độ của ta, **một giờ công không bao giờ vừa vào quỹ vừa được trả tiền** (`nghi_bu_va_chenh` tách 1.0 / phần chênh, không chồng nhau), nên trừ thêm là trừ hai lần. Vì vậy `tienNghiBu = 0` và `thucNhan = tongTien`; cột vẫn in số giờ để khớp biểu mẫu pháp định.
   → **Điểm cần chủ sản phẩm xác nhận trước khi chạy thật.** Ghi vào phần "Rủi ro" cuối plan.

3. **Không ghi đè dòng đã `chot` hoặc đã `suaTay`** — cùng quy ước `tongHop()` của bảng lương chính.

- [ ] **Step 1: Viết test**

Tạo `be/apps/config-service/src/bang-luong/them-gio.service.spec.ts`:

```ts
import { Test } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import {
  AttendanceRequest,
  CauHinhLuong,
  DongLuongThemGio,
  Employee,
} from '@app/entities';
import { ThemGio_Service } from './them-gio.service';

function repoGia(khoiTao: any[] = []) {
  const rows: any[] = [...khoiTao];
  let seq = 0;
  return {
    rows,
    find: jest.fn(async ({ where }: any = {}) =>
      rows.filter((r) =>
        Object.entries(where ?? {}).every(([k, v]) => String(r[k]) === String(v)),
      ),
    ),
    findOne: jest.fn(
      async ({ where }: any = {}) =>
        rows.find((r) =>
          Object.entries(where ?? {}).every(([k, v]) => String(r[k]) === String(v)),
        ) ?? null,
    ),
    create: jest.fn((x: any) => ({ ...x })),
    save: jest.fn(async (x: any) => {
      if (!x._id) {
        seq += 1;
        x._id = seq.toString(16).padStart(24, '0');
        rows.push(x);
        return x;
      }
      const i = rows.findIndex((r) => String(r._id) === String(x._id));
      if (i >= 0) rows[i] = x;
      else rows.push(x);
      return x;
    }),
  };
}

const NV = {
  _id: '650000000000000000000101',
  employeeId: 'NV0001',
  hoTen: 'Đào Thị Kiều Oanh',
  luongThoaThuan: 5_500_000,
  isActive: true,
};

const CAU_HINH = {
  congChuan: 24,
  soGioMoiNgay: 8,
  lamThem: {
    cheDoBu: 'chi_tien',
    heSoTra: { ngay_thuong: 1.5, ngay_nghi: 2, ngay_le: 3, ngay_dem: 1.5 },
    heSoTichQuy: { ngay_thuong: 1.5, ngay_nghi: 2, ngay_le: 3, ngay_dem: 1.5 },
    khungGioDem: { tu: '22:00', den: '06:00' },
    uuTienLoai: ['ngay_le', 'ngay_nghi', 'ngay_dem', 'ngay_thuong'],
    mienThueChenh: ['ngay_dem'],
    soThangHanDung: null,
    khiHetHan: 'quy_ra_tien',
  },
};

const donOt = (over: any = {}) => ({
  _id: over._id ?? 'd1',
  loaiDon: 'lam_them_gio',
  trangThai: 'da_duyet',
  isActive: true,
  employeeId: NV._id,
  ngay: '2026-06-10',
  soGioOt: 35,
  loaiNgayOt: 'ngay_thuong',
  heSoOt: 1.5,
  phanBoOt: [
    { loaiNgayOt: 'ngay_thuong', soGio: 35, heSoTra: 1.5, heSoTichQuy: 1.5 },
  ],
  ...over,
});

async function dungService(opts: { don?: any[]; dong?: any[]; cauHinh?: any } = {}) {
  const donRepo = repoGia(opts.don);
  const dongRepo = repoGia(opts.dong);
  const nvRepo = repoGia([NV]);
  const chRepo = repoGia([opts.cauHinh ?? CAU_HINH]);

  const mod = await Test.createTestingModule({
    providers: [
      ThemGio_Service,
      { provide: getRepositoryToken(DongLuongThemGio), useValue: dongRepo },
      { provide: getRepositoryToken(AttendanceRequest), useValue: donRepo },
      { provide: getRepositoryToken(Employee), useValue: nvRepo },
      { provide: getRepositoryToken(CauHinhLuong), useValue: chRepo },
    ],
  }).compile();

  return { service: mod.get(ThemGio_Service), donRepo, dongRepo };
}

describe('ThemGio_Service.tongHop', () => {
  it('CA KIỂM CHUẨN: 35 giờ ngày thường, lương 5.500.000 → 1.503.906,25', async () => {
    const { service, dongRepo } = await dungService({ don: [donOt()] });

    await service.tongHop('2026-06');

    const d = dongRepo.rows[0];
    expect(d.donGiaGio).toBeCloseTo(28_645.8333, 4);
    expect(d.theoLoai.ngay_thuong.soGio).toBe(35);
    expect(d.tongTien).toBeCloseTo(1_503_906.25, 2);
    expect(d.thucNhan).toBeCloseTo(1_503_906.25, 2);
  });

  it('chỉ lấy đơn ĐÃ DUYỆT, đúng tháng, đúng loại đơn', async () => {
    const { service, dongRepo } = await dungService({
      don: [
        donOt({ _id: 'd1' }),
        donOt({ _id: 'd2', trangThai: 'cho_duyet' }),
        donOt({ _id: 'd3', ngay: '2026-07-01' }),
        donOt({ _id: 'd4', loaiDon: 'nghi_phep' }),
        donOt({ _id: 'd5', isActive: false }),
      ],
    });

    await service.tongHop('2026-06');

    // Chỉ d1 được tính ⇒ đúng 35 giờ, không phải 175.
    expect(dongRepo.rows[0].theoLoai.ngay_thuong.soGio).toBe(35);
  });

  it('cộng giờ từ NHIỀU đơn và nhiều loại trong phanBoOt', async () => {
    const { service, dongRepo } = await dungService({
      don: [
        donOt({ _id: 'd1', phanBoOt: [
          { loaiNgayOt: 'ngay_thuong', soGio: 2, heSoTra: 1.5, heSoTichQuy: 1.5 },
          { loaiNgayOt: 'ngay_dem', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 },
        ] }),
        donOt({ _id: 'd2', phanBoOt: [
          { loaiNgayOt: 'ngay_dem', soGio: 3, heSoTra: 1.5, heSoTichQuy: 1.5 },
        ] }),
      ],
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].theoLoai.ngay_thuong.soGio).toBe(2);
    expect(dongRepo.rows[0].theoLoai.ngay_dem.soGio).toBe(7);
  });

  it('đơn cũ chưa backfill (không có phanBoOt) rơi về loaiNgayOt/soGioOt', async () => {
    const { service, dongRepo } = await dungService({
      don: [donOt({ phanBoOt: undefined, soGioOt: 6, loaiNgayOt: 'ngay_le' })],
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].theoLoai.ngay_le.soGio).toBe(6);
  });

  it('cheDoBu = chi_nghi_bu → không trả đồng nào (giờ đã vào quỹ)', async () => {
    const { service, dongRepo } = await dungService({
      don: [donOt()],
      cauHinh: { ...CAU_HINH, lamThem: { ...CAU_HINH.lamThem, cheDoBu: 'chi_nghi_bu' } },
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].tongTien).toBe(0);
    expect(dongRepo.rows[0].theoLoai).toEqual({});
  });

  it('cheDoBu = nhan_vien_chon → chỉ đơn hinhThucBu = tien', async () => {
    const { service, dongRepo } = await dungService({
      don: [
        donOt({ _id: 'd1', hinhThucBu: 'tien' }),
        donOt({ _id: 'd2', hinhThucBu: 'nghi_bu' }),
        donOt({ _id: 'd3' }), // không khai ⇒ không phải 'tien' ⇒ bỏ
      ],
      cauHinh: { ...CAU_HINH, lamThem: { ...CAU_HINH.lamThem, cheDoBu: 'nhan_vien_chon' } },
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].theoLoai.ngay_thuong.soGio).toBe(35);
  });

  it('cheDoBu = nghi_bu_va_chenh → hệ số áp là heSoTra − 1', async () => {
    const { service, dongRepo } = await dungService({
      don: [donOt({ phanBoOt: [
        { loaiNgayOt: 'ngay_le', soGio: 8, heSoTra: 3, heSoTichQuy: 1 },
      ] })],
      cauHinh: { ...CAU_HINH, lamThem: { ...CAU_HINH.lamThem, cheDoBu: 'nghi_bu_va_chenh' } },
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].theoLoai.ngay_le.heSo).toBe(2);
  });

  it('công ty CHƯA khai lamThem → không tạo dòng nào, không ném', async () => {
    const { service, dongRepo } = await dungService({
      don: [donOt()],
      cauHinh: { congChuan: 24 }, // không có lamThem
    });

    await expect(service.tongHop('2026-06')).resolves.toEqual([]);
    expect(dongRepo.rows).toHaveLength(0);
  });

  it('KHÔNG ghi đè dòng đã chốt', async () => {
    const { service, dongRepo } = await dungService({
      don: [donOt()],
      dong: [{
        _id: 'x1', thang: '2026-06', employeeId: NV._id,
        trangThai: 'chot', tongTien: 999, isActive: true,
      }],
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].tongTien).toBe(999);
  });

  it('KHÔNG ghi đè dòng kế toán đã sửa tay', async () => {
    const { service, dongRepo } = await dungService({
      don: [donOt()],
      dong: [{
        _id: 'x1', thang: '2026-06', employeeId: NV._id,
        trangThai: 'nhap', suaTay: true, tongTien: 777, isActive: true,
      }],
    });

    await service.tongHop('2026-06');

    expect(dongRepo.rows[0].tongTien).toBe(777);
  });

  it('chạy lại KHÔNG tạo dòng thứ hai cho cùng {thang, employeeId}', async () => {
    const { service, dongRepo } = await dungService({ don: [donOt()] });

    await service.tongHop('2026-06');
    await service.tongHop('2026-06');

    expect(dongRepo.rows).toHaveLength(1);
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong/them-gio.service.spec.ts
```
Expected: FAIL — `Cannot find module './them-gio.service'`.

- [ ] **Step 3: Viết service**

Tạo `be/apps/config-service/src/bang-luong/them-gio.service.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import {
  AttendanceRequest,
  CauHinhLuong,
  DongLuongThemGio,
  Employee,
} from '@app/entities';
import { tinhDongThemGio } from '@app/core';

/** Cấu hình làm thêm đã resolve, hoặc `null` khi công ty chưa khai. */
interface CauHinhThemGio {
  congChuan: number;
  soGioMoiNgay: number;
  cheDoBu: string;
  heSoTra: Record<string, number>;
  uuTienLoai: string[];
}

@Injectable()
export class ThemGio_Service {
  private readonly logger = new Logger(ThemGio_Service.name);

  constructor(
    @InjectRepository(DongLuongThemGio)
    private readonly repo: Repository<DongLuongThemGio>,
    @InjectRepository(AttendanceRequest)
    private readonly donRepo: Repository<AttendanceRequest>,
    @InjectRepository(Employee)
    private readonly nvRepo: Repository<Employee>,
    @InjectRepository(CauHinhLuong)
    private readonly cauHinhRepo: Repository<CauHinhLuong>,
  ) {}

  /**
   * `null` = công ty chưa khai `lamThem`. Cùng cửa mà `QuyGio_Service` dùng để
   * quyết có tích quỹ hay không — tích và trả tiền phải luôn đồng thanh trả
   * lời "tính năng đã bật hay chưa", nếu không hai bên lệch pha.
   */
  private async layCauHinh(): Promise<CauHinhThemGio | null> {
    const rows = await this.cauHinhRepo.find({});
    const ch: any = rows[0];
    if (!ch?.lamThem) return null;
    return {
      congChuan: ch.congChuan ?? 26,
      soGioMoiNgay: ch.soGioMoiNgay ?? 8,
      cheDoBu: ch.lamThem.cheDoBu,
      heSoTra: ch.lamThem.heSoTra ?? {},
      uuTienLoai: ch.lamThem.uuTienLoai ?? [],
    };
  }

  /** Đơn OT của MỘT nhân viên được TRẢ TIỀN trong kỳ, theo `cheDoBu`. */
  private locDonTraTien(
    dons: AttendanceRequest[],
    cheDoBu: string,
  ): AttendanceRequest[] {
    if (cheDoBu === 'chi_nghi_bu') return [];
    if (cheDoBu === 'nhan_vien_chon') {
      return dons.filter((d) => d.hinhThucBu === 'tien');
    }
    return dons; // chi_tien, nghi_bu_va_chenh
  }

  /** Cộng giờ theo loại từ `phanBoOt`; đơn cũ chưa backfill rơi về scalar. */
  private congGioTheoLoai(dons: AttendanceRequest[]): Record<string, number> {
    const gio: Record<string, number> = {};
    for (const don of dons) {
      if (don.phanBoOt?.length) {
        for (const p of don.phanBoOt) {
          gio[p.loaiNgayOt] = (gio[p.loaiNgayOt] ?? 0) + p.soGio;
        }
      } else if (don.loaiNgayOt && don.soGioOt) {
        gio[don.loaiNgayOt] = (gio[don.loaiNgayOt] ?? 0) + don.soGioOt;
      }
    }
    return gio;
  }

  /**
   * Tổng hợp bảng thanh toán tiền làm thêm của một kỳ.
   *
   * Bỏ qua dòng đã `chot` (snapshot bất biến) và dòng kế toán đã `suaTay`
   * (tổng hợp lại là thao tác thường ngày; ghi đè số họ vừa sửa là mất việc
   * của họ) — cùng quy ước `BangLuong_Service.tongHop()`.
   */
  async tongHop(thang: string): Promise<DongLuongThemGio[]> {
    const ch = await this.layCauHinh();
    if (!ch) {
      this.logger.warn(
        `Bỏ qua tổng hợp bảng lương thêm giờ kỳ ${thang}: công ty chưa khai cấu hình làm thêm`,
      );
      return [];
    }

    const employees = await this.nvRepo.find({ where: { isActive: true } as any });
    const tatCaDon = await this.donRepo.find({
      where: { loaiDon: 'lam_them_gio', trangThai: 'da_duyet', isActive: true } as any,
    });
    const donTrongKy = tatCaDon.filter((d) => (d.ngay ?? '').startsWith(thang));

    const rows: DongLuongThemGio[] = [];

    for (const emp of employees) {
      const employeeId = String((emp as any)._id);
      const cu = (
        await this.repo.find({ where: { thang, employeeId } as any })
      )[0];

      if (cu && (cu.trangThai === 'chot' || cu.suaTay)) {
        rows.push(cu);
        continue;
      }

      const donCuaNV = this.locDonTraTien(
        donTrongKy.filter((d) => String(d.employeeId) === employeeId),
        ch.cheDoBu,
      );

      const kq = tinhDongThemGio({
        luongThang: emp.luongThoaThuan ?? 0,
        congChuan: ch.congChuan,
        soGioMoiNgay: ch.soGioMoiNgay,
        gioTheoLoai: this.congGioTheoLoai(donCuaNV),
        heSoTra: ch.heSoTra,
        truMotDonVi: ch.cheDoBu === 'nghi_bu_va_chenh',
      });

      const row =
        cu ??
        this.repo.create({
          thang,
          employeeId,
          employeeName: emp.hoTen,
          employeeCode: emp.employeeId,
          trangThai: 'nhap',
          suaTay: false,
          isActive: true,
        } as Partial<DongLuongThemGio>);

      row.luongThang = emp.luongThoaThuan ?? 0;
      row.congChuan = ch.congChuan;
      row.soGioMoiNgay = ch.soGioMoiNgay;
      row.donGiaNgay = kq.donGiaNgay;
      row.donGiaGio = kq.donGiaGio;
      row.theoLoai = kq.theoLoai;
      row.tongTien = kq.tongTien;

      // Cột "Số ngày nghỉ bù" của mẫu 03-LĐTL là THÔNG TIN. Biểu mẫu gốc trừ
      // nó vào Thực nhận vì giả định công ty trả tiền OT rồi lấy lại phần đã
      // nghỉ bù. Trong cả bốn chế độ của ta, một giờ công KHÔNG BAO GIỜ vừa
      // vào quỹ vừa được trả tiền (`nghi_bu_va_chenh` tách 1.0 / phần chênh,
      // không chồng nhau), nên trừ thêm là trừ hai lần.
      row.gioNghiBu = 0;
      row.tienNghiBu = 0;
      row.thucNhan = kq.tongTien;

      // P4.2c-2 mới đọc quỹ hết hạn và trả tiền cho nó.
      row.gioOtHetHan = row.gioOtHetHan ?? 0;

      rows.push(await this.repo.save(row));
    }

    return rows;
  }
}
```

- [ ] **Step 4: Nối vào module**

Trong `be/apps/config-service/src/bang-luong/bang-luong.module.ts`, thêm `DongLuongThemGio` vào `DatabaseModule.forFeature([...])` (đã có `AttendanceRequest`, `CauHinhLuong`, `Employee` từ P4.2b), thêm `ThemGio_Service` vào `providers` và `exports`.

- [ ] **Step 5: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong
```
Expected: PASS toàn bộ.

- [ ] **Step 6: Commit**

```bash
git add be/apps/config-service/src/bang-luong/
git commit -m "feat(be): tổng hợp bảng lương thêm giờ theo kỳ

Nguồn giờ lọc theo cheDoBu đúng bảng spec P4.2c §2.3. Không ghi đè dòng đã
chốt hoặc kế toán đã sửa tay — cùng quy ước tongHop() của bảng lương chính.

tienNghiBu = 0 và thucNhan = tongTien: biểu mẫu gốc trừ tiền nghỉ bù vì giả
định trả tiền OT rồi lấy lại phần đã nghỉ, nhưng trong cả bốn chế độ của ta
một giờ công không bao giờ vừa vào quỹ vừa được trả tiền, nên trừ thêm là
trừ hai lần. Cột vẫn in số giờ để khớp mẫu pháp định."
```

---

## Task 4: Sửa tay, chốt kỳ, controller

**Files:**
- Modify: `be/apps/config-service/src/bang-luong/them-gio.service.ts`
- Create: `be/apps/config-service/src/bang-luong/dto/cap-nhat-dong-them-gio.dto.ts`
- Modify: `be/apps/config-service/src/bang-luong/dto/index.ts`
- Create: `be/apps/config-service/src/bang-luong/them-gio.controller.ts`
- Create: `be/apps/config-service/src/bang-luong/them-gio.controller.spec.ts`
- Modify: `be/apps/config-service/src/bang-luong/bang-luong.module.ts`
- Test: `be/apps/config-service/src/bang-luong/them-gio.service.spec.ts`

**Interfaces:**
- Consumes: `ThemGio_Service.tongHop` (Task 3)
- Produces:
  ```ts
  danhSach(thang: string): Promise<DongLuongThemGio[]>
  capNhatDong(id: string, dto: CapNhatDongThemGioDto): Promise<DongLuongThemGio>
  chot(thang: string): Promise<{ soDong: number }>
  moLai(thang: string): Promise<{ soDong: number }>
  ```
  Routes (`@Controller('bang-luong-them-gio')`):
  | Method | Path | Quyền |
  |---|---|---|
  | GET | `/` (query `thang`) | `/luong/bang-luong:xem` |
  | POST | `tong-hop` | `/luong/bang-luong:them` |
  | PATCH | `chot` | `/luong/bang-luong:sua` |
  | PATCH | `mo-lai` | `/luong/bang-luong:sua` |
  | PATCH | `:id` | `/luong/bang-luong:sua` |

- [ ] **Step 1: Viết test service**

Thêm vào cuối `them-gio.service.spec.ts`:

```ts
describe('ThemGio_Service — sửa tay và chốt kỳ', () => {
  const dongMau = (over: any = {}) => ({
    _id: 'x1', thang: '2026-06', employeeId: NV._id,
    luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
    donGiaNgay: 229_166.6667, donGiaGio: 28_645.8333,
    theoLoai: { ngay_thuong: { soGio: 35, heSo: 1.5, thanhTien: 1_503_906.25 } },
    tongTien: 1_503_906.25, gioNghiBu: 0, tienNghiBu: 0,
    thucNhan: 1_503_906.25, gioOtHetHan: 0,
    suaTay: false, trangThai: 'nhap', isActive: true,
    ...over,
  });

  it('sửa số giờ một loại → tính lại thành tiền, tổng và thực nhận', async () => {
    const { service, dongRepo } = await dungService({ dong: [dongMau()] });

    const kq = await service.capNhatDong('x1', {
      theoLoai: { ngay_thuong: 10 },
    } as any);

    const g = 5_500_000 / 24 / 8;
    expect(kq.theoLoai.ngay_thuong.soGio).toBe(10);
    expect(kq.theoLoai.ngay_thuong.thanhTien).toBeCloseTo(10 * g * 1.5, 6);
    expect(kq.tongTien).toBeCloseTo(10 * g * 1.5, 6);
    expect(kq.thucNhan).toBeCloseTo(10 * g * 1.5, 6);
  });

  it('sửa tay bật cờ suaTay để tổng hợp lại không ghi đè', async () => {
    const { service } = await dungService({ dong: [dongMau()] });

    const kq = await service.capNhatDong('x1', {
      theoLoai: { ngay_thuong: 10 },
    } as any);

    expect(kq.suaTay).toBe(true);
  });

  it('KHÔNG cho sửa dòng đã chốt', async () => {
    const { service } = await dungService({
      dong: [dongMau({ trangThai: 'chot' })],
    });

    await expect(
      service.capNhatDong('x1', { theoLoai: { ngay_thuong: 10 } } as any),
    ).rejects.toThrow(/đã chốt/);
  });

  it('chốt kỳ đổi mọi dòng nhap sang chot', async () => {
    const { service, dongRepo } = await dungService({
      dong: [dongMau({ _id: 'x1' }), dongMau({ _id: 'x2', employeeId: 'nv2' })],
    });

    await expect(service.chot('2026-06')).resolves.toEqual({ soDong: 2 });
    expect(dongRepo.rows.every((r: any) => r.trangThai === 'chot')).toBe(true);
  });

  it('mở lại kỳ đổi chot về nhap', async () => {
    const { service, dongRepo } = await dungService({
      dong: [dongMau({ trangThai: 'chot' })],
    });

    await expect(service.moLai('2026-06')).resolves.toEqual({ soDong: 1 });
    expect(dongRepo.rows[0].trangThai).toBe('nhap');
  });

  it('chốt kỳ KHÔNG đụng kỳ khác', async () => {
    const { service, dongRepo } = await dungService({
      dong: [dongMau({ _id: 'x1' }), dongMau({ _id: 'x2', thang: '2026-07' })],
    });

    await service.chot('2026-06');

    expect(dongRepo.rows.find((r: any) => r._id === 'x2').trangThai).toBe('nhap');
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong/them-gio.service.spec.ts
```
Expected: FAIL — `service.capNhatDong is not a function`.

- [ ] **Step 3: Viết DTO**

Tạo `be/apps/config-service/src/bang-luong/dto/cap-nhat-dong-them-gio.dto.ts`:

```ts
import { IsObject, IsOptional, IsNumber, Min } from 'class-validator';

/**
 * Kế toán sửa SỐ GIỜ từng loại, không sửa thẳng thành tiền — thành tiền là
 * kết quả tính lại từ giờ × đơn giá × hệ số. Cho sửa tiền trực tiếp là mở
 * đường cho một con số không giải thích được bằng bất kỳ phép tính nào.
 */
export class CapNhatDongThemGioDto {
  /** { ngay_thuong: 10, ngay_dem: 4 } — khoá là mã loại ngày. */
  @IsOptional() @IsObject() theoLoai?: Record<string, number>;

  @IsOptional() @IsNumber() @Min(0) gioNghiBu?: number;
}
```

Thêm `export * from './cap-nhat-dong-them-gio.dto';` vào `dto/index.ts`.

- [ ] **Step 4: Viết `capNhatDong` / `chot` / `moLai` / `danhSach`**

Thêm vào `ThemGio_Service`:

```ts
  async danhSach(thang: string): Promise<DongLuongThemGio[]> {
    return this.repo.find({ where: { thang, isActive: true } as any });
  }

  /**
   * Sửa tay số GIỜ từng loại rồi tính lại tiền. Không nhận thành tiền trực
   * tiếp: một con số tiền không suy ra được từ giờ × đơn giá × hệ số là con số
   * không ai đối soát được về sau.
   */
  async capNhatDong(
    id: string,
    dto: CapNhatDongThemGioDto,
  ): Promise<DongLuongThemGio> {
    const { ObjectId } = await import('mongodb');
    const row = await this.repo.findOne({
      where: { _id: new ObjectId(id) as any },
    });
    if (!row) throw new NotFoundException(`Không tìm thấy dòng ${id}`);
    if (row.trangThai === 'chot') {
      throw new BadRequestException(
        'Dòng đã chốt — mở lại kỳ trước khi sửa',
      );
    }

    const ch = await this.layCauHinh();
    if (!ch) {
      throw new BadRequestException('Công ty chưa khai cấu hình làm thêm');
    }

    if (dto.theoLoai) {
      const kq = tinhDongThemGio({
        luongThang: row.luongThang,
        congChuan: row.congChuan,
        soGioMoiNgay: row.soGioMoiNgay,
        gioTheoLoai: dto.theoLoai,
        heSoTra: ch.heSoTra,
        truMotDonVi: ch.cheDoBu === 'nghi_bu_va_chenh',
      });
      row.donGiaNgay = kq.donGiaNgay;
      row.donGiaGio = kq.donGiaGio;
      row.theoLoai = kq.theoLoai;
      row.tongTien = kq.tongTien;
      row.thucNhan = kq.tongTien - row.tienNghiBu;
    }

    if (dto.gioNghiBu !== undefined) row.gioNghiBu = dto.gioNghiBu;

    row.suaTay = true;
    return this.repo.save(row);
  }

  async chot(thang: string): Promise<{ soDong: number }> {
    return this.doiTrangThai(thang, 'nhap', 'chot');
  }

  async moLai(thang: string): Promise<{ soDong: number }> {
    return this.doiTrangThai(thang, 'chot', 'nhap');
  }

  private async doiTrangThai(
    thang: string,
    tu: string,
    den: string,
  ): Promise<{ soDong: number }> {
    const rows = await this.repo.find({
      where: { thang, trangThai: tu, isActive: true } as any,
    });
    for (const r of rows) {
      r.trangThai = den;
      await this.repo.save(r);
    }
    return { soDong: rows.length };
  }
```

Thêm vào import đầu file: `BadRequestException, NotFoundException` từ `@nestjs/common`, và `CapNhatDongThemGioDto` từ `./dto`.

- [ ] **Step 5: Chạy test service, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong/them-gio.service.spec.ts
```
Expected: PASS.

- [ ] **Step 6: Viết controller + test phân quyền**

Tạo `be/apps/config-service/src/bang-luong/them-gio.controller.ts`:

```ts
import {
  Controller, Get, Post, Patch, Body, Param, Query, UseGuards,
} from '@nestjs/common';
import { ThemGio_Service } from './them-gio.service';
import { CapNhatDongThemGioDto } from './dto';
import { JwtGuard, PermissionGuard, Permissions } from '@app/auth';

/**
 * Bảng thanh toán tiền làm thêm giờ (mẫu 03-LĐTL).
 *
 * Dùng lại quyền `/luong/bang-luong:*` chứ không mở module quyền mới: đây là
 * dữ liệu lương, cùng nhóm rủi ro với bảng lương chính, và thêm module là thêm
 * một bước bắt buộc `ops/grant-quyen-module-moi.ts` lúc deploy.
 *
 * Route có đoạn đường dẫn CỐ ĐỊNH (`tong-hop`, `chot`, `mo-lai`) phải khai
 * TRƯỚC route `:id` — nếu không NestJS khớp các chuỗi đó thành `:id`, cùng bẫy
 * đã ghi ở `bang-luong.controller.ts`.
 */
@Controller('bang-luong-them-gio')
@UseGuards(JwtGuard)
export class ThemGio_Controller {
  constructor(private readonly themGio_Service: ThemGio_Service) {}

  @Get()
  @UseGuards(PermissionGuard)
  @Permissions('/luong/bang-luong:xem')
  async danhSach(@Query('thang') thang: string) {
    const data = await this.themGio_Service.danhSach(thang);
    return { success: true, data };
  }

  @Post('tong-hop')
  @UseGuards(PermissionGuard)
  @Permissions('/luong/bang-luong:them')
  async tongHop(@Body('thang') thang: string) {
    const data = await this.themGio_Service.tongHop(thang);
    return { success: true, data };
  }

  @Patch('chot')
  @UseGuards(PermissionGuard)
  @Permissions('/luong/bang-luong:sua')
  async chot(@Body('thang') thang: string) {
    const data = await this.themGio_Service.chot(thang);
    return { success: true, data };
  }

  @Patch('mo-lai')
  @UseGuards(PermissionGuard)
  @Permissions('/luong/bang-luong:sua')
  async moLai(@Body('thang') thang: string) {
    const data = await this.themGio_Service.moLai(thang);
    return { success: true, data };
  }

  @Patch(':id')
  @UseGuards(PermissionGuard)
  @Permissions('/luong/bang-luong:sua')
  async capNhatDong(
    @Param('id') id: string,
    @Body() body: CapNhatDongThemGioDto,
  ) {
    const data = await this.themGio_Service.capNhatDong(id, body);
    return { success: true, data };
  }
}
```

Tạo `them-gio.controller.spec.ts` theo đúng khuôn `bang-luong.controller.spec.ts` (chép cấu trúc file đó, đổi controller và bảng quyền):

```ts
const BANG_QUYEN: Array<[string, RequestMethod, string]> = [
  ['danhSach', RequestMethod.GET, '/luong/bang-luong:xem'],
  ['tongHop', RequestMethod.POST, '/luong/bang-luong:them'],
  ['chot', RequestMethod.PATCH, '/luong/bang-luong:sua'],
  ['moLai', RequestMethod.PATCH, '/luong/bang-luong:sua'],
  ['capNhatDong', RequestMethod.PATCH, '/luong/bang-luong:sua'],
];
```

Bài test cuối của khuôn đó (`bảng quyền phủ đúng toàn bộ route`) là bài quan trọng nhất — nó chặn việc thêm route mà quên khai quyền.

Đăng ký `ThemGio_Controller` vào `controllers` của `bang-luong.module.ts`.

- [ ] **Step 7: Chạy toàn bộ backend**

```bash
cd be && nvm use v22.0.0 && yarn test && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | grep -c ""
```
Expected: test PASS (trừ `auth-service` "Property 8: Password Hashing" — test này nằm sát mốc timeout 5s của Jest, đỏ cả trên `main`, không phải hồi quy); tsc đúng **74** dòng.

- [ ] **Step 8: Commit**

```bash
git add be/apps/config-service/src/bang-luong/
git commit -m "feat(be): sửa tay, chốt kỳ và API cho bảng lương thêm giờ

Kế toán sửa SỐ GIỜ, không sửa thẳng thành tiền — một con số tiền không suy ra
được từ giờ x đơn giá x hệ số là con số không ai đối soát được về sau.

Dùng lại quyền /luong/bang-luong:* thay vì mở module quyền mới, để không phát
sinh bước bắt buộc ops/grant-quyen-module-moi.ts lúc deploy."
```

---

## Task 5: Màn "Bảng lương thêm giờ" (FE)

**Files:**
- Create: `fe/src/services/bangLuongThemGioService.ts`
- Create: `fe/src/pages/luong/bang-luong-them-gio/BangLuongThemGioPage.tsx`
- Create: `fe/src/pages/luong/bang-luong-them-gio/BangLuongThemGioPage.state.ts`
- Create: `fe/src/pages/luong/bang-luong-them-gio/BangLuongThemGioHandlerContext.tsx`
- Create: `fe/src/pages/luong/bang-luong-them-gio/bangLuongThemGioHandler.ts`
- Create: `fe/src/pages/luong/bang-luong-them-gio/sub-handler/{init,tong-hop,chot,cap-nhat}/*.{handler,event}.ts`
- Create: `fe/src/pages/luong/bang-luong-them-gio/components/BangThemGioTable.tsx`
- Create: `fe/src/pages/luong/bang-luong-them-gio/BangLuongThemGioPage.render.test.tsx`
- Modify: router + sidebar (tìm bằng `grep -rn "bang-luong" fe/src/routes fe/src/components/layout`)

**Interfaces:**
- Consumes: API Task 4
- Produces: route `/luong/bang-luong-them-gio`

**Quy tắc bắt buộc:** đọc `fe/HANDLER_GUIDE.md` trước khi viết — màn này theo pattern CHandler như `bang-luong`. **Chép cấu trúc từ `fe/src/pages/luong/bang-luong/`** (cùng vòng đời tổng hợp → sửa → chốt) thay vì tự nghĩ ra cấu trúc mới.

- [ ] **Step 1: Viết service FE**

Tạo `fe/src/services/bangLuongThemGioService.ts`:

```ts
import { ServiceBase } from './serviceBase'; // kiểm tên thật bằng: grep -n "extends ServiceBase" fe/src/services/cauHinhLuongService.ts

export interface DongThemGioTheoLoai {
  soGio: number;
  heSo: number;
  thanhTien: number;
}

export interface DongLuongThemGio {
  id?: string;
  _id?: string;
  thang: string;
  employeeId: string;
  employeeName?: string;
  employeeCode?: string;
  luongThang: number;
  congChuan: number;
  soGioMoiNgay: number;
  donGiaNgay: number;
  donGiaGio: number;
  theoLoai: Record<string, DongThemGioTheoLoai>;
  tongTien: number;
  gioNghiBu: number;
  tienNghiBu: number;
  thucNhan: number;
  gioOtHetHan: number;
  suaTay: boolean;
  trangThai: 'nhap' | 'chot';
}

class BangLuongThemGioService extends ServiceBase {
  constructor() {
    super({ endpoint: '/config/bang-luong-them-gio' });
  }

  async danhSach(thang: string): Promise<DongLuongThemGio[]> {
    return super.get<DongLuongThemGio[]>({ params: { thang } } as never);
  }

  async tongHop(thang: string): Promise<DongLuongThemGio[]> {
    return super.post<DongLuongThemGio[]>({ thang }, { endpoint: '/tong-hop' } as never);
  }

  async chot(thang: string): Promise<{ soDong: number }> {
    return super.patch<{ soDong: number }>({ thang }, { endpoint: '/chot' } as never);
  }

  async moLai(thang: string): Promise<{ soDong: number }> {
    return super.patch<{ soDong: number }>({ thang }, { endpoint: '/mo-lai' } as never);
  }

  async capNhatDong(
    id: string,
    dto: { theoLoai?: Record<string, number>; gioNghiBu?: number },
  ): Promise<DongLuongThemGio> {
    return super.patch<DongLuongThemGio>(dto, { endpoint: `/${id}` } as never);
  }
}

export const bangLuongThemGioService = new BangLuongThemGioService();
```

**Trước khi viết:** mở `fe/src/services/cauHinhLuongService.ts` và một service có `post`/`patch` (vd `grep -rln "super.patch" fe/src/services`) để lấy ĐÚNG chữ ký `ServiceBase`. Chữ ký ở trên là phác thảo — dùng chữ ký thật của repo, đừng ép repo theo phác thảo này.

- [ ] **Step 2: Viết test render**

Tạo `BangLuongThemGioPage.render.test.tsx` theo khuôn `fe/src/pages/luong/bang-luong/BangLuongPage.render.test.tsx` (chép phần mock `matchMedia`/`ResizeObserver`/`AuthContext`):

```tsx
const DONG = {
  _id: 'x1', thang: '2026-06', employeeId: 'nv1',
  employeeName: 'Đào Thị Kiều Oanh', employeeCode: 'NV0001',
  luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
  donGiaNgay: 229_166.67, donGiaGio: 28_645.83,
  theoLoai: {
    ngay_thuong: { soGio: 35, heSo: 1.5, thanhTien: 1_503_906.25 },
    ngay_dem: { soGio: 0, heSo: 1.5, thanhTien: 0 },
  },
  tongTien: 1_503_906.25, gioNghiBu: 0, tienNghiBu: 0,
  thucNhan: 1_503_906.25, gioOtHetHan: 0,
  suaTay: false, trangThai: 'nhap' as const,
};

describe('Màn Bảng lương thêm giờ', () => {
  it('hiện một nhóm cột cho MỖI loại ngày trong dữ liệu', async () => {
    vi.spyOn(bangLuongThemGioService, 'danhSach').mockResolvedValue([DONG]);
    render(<BangLuongThemGioPage />);

    expect(await screen.findByText('Ngày thường')).toBeTruthy();
    expect(screen.getByText('Buổi đêm')).toBeTruthy();
    expect(screen.getByText('Đào Thị Kiều Oanh')).toBeTruthy();
  });

  it('hiện đúng con số của mẫu 03-LĐTL', async () => {
    vi.spyOn(bangLuongThemGioService, 'danhSach').mockResolvedValue([DONG]);
    render(<BangLuongThemGioPage />);

    await screen.findByText('Đào Thị Kiều Oanh');
    // Định dạng tiền của repo — kiểm bằng renderTien trong BangLuongTable.tsx.
    expect(screen.getAllByText(/1\.503\.906/).length).toBeGreaterThan(0);
  });

  it('kỳ đã chốt thì khoá sửa', async () => {
    vi.spyOn(bangLuongThemGioService, 'danhSach').mockResolvedValue([
      { ...DONG, trangThai: 'chot' as const },
    ]);
    render(<BangLuongThemGioPage />);

    await screen.findByText('Đào Thị Kiều Oanh');
    expect(screen.getByText(/Đã chốt/)).toBeTruthy();
  });

  it('không có dòng nào thì báo rỗng, không vỡ', async () => {
    vi.spyOn(bangLuongThemGioService, 'danhSach').mockResolvedValue([]);
    render(<BangLuongThemGioPage />);

    expect(await screen.findByText(/Chưa có dữ liệu|Không có dữ liệu/)).toBeTruthy();
  });
});
```

- [ ] **Step 3: Chạy test, xác nhận ĐỎ**

```bash
cd fe && nvm use && npm run test -- src/pages/luong/bang-luong-them-gio
```
Expected: FAIL — không tìm thấy module.

- [ ] **Step 4: Dựng màn hình**

Chép cấu trúc từ `fe/src/pages/luong/bang-luong/` (page + state + context + handler + 4 sub-handler), đổi service và cột. Bảng dựng cột **động** từ tập khoá `theoLoai` gộp của mọi dòng:

```tsx
// Gộp khoá của MỌI dòng, không lấy từ dòng đầu: nhân viên đầu tiên có thể
// không làm ca đêm, và cột "Buổi đêm" vẫn phải in ra đúng biểu mẫu.
const cacLoai = Array.from(
  new Set(dsDong.flatMap((d) => Object.keys(d.theoLoai ?? {}))),
);
```

Nhãn tiếng Việt lấy từ `NHAN_LOAI_NGAY` đã có ở `@/services/cauHinhLuongService`; loại không có nhãn hiện chính khoá (cùng quy ước màn Cấu hình lương).

Mỗi loại là một nhóm hai cột con `Số giờ` / `Thành tiền`, dùng `children` của antd Table column. Cột cuối: `Tổng cộng tiền` | `Số giờ nghỉ bù` | `Thực nhận`.

- [ ] **Step 5: Thêm route + mục sidebar**

```bash
cd fe && grep -rn "bang-luong" src/routes src/components/layout src/config 2>/dev/null | head
```

Thêm route `/luong/bang-luong-them-gio` và mục sidebar **giữa** "Cấu hình lương" và "Bảng lương" (spec §5). Gate bằng cùng quyền `/luong/bang-luong:xem` mà mục "Bảng lương" đang dùng.

- [ ] **Step 6: Chạy test, xác nhận XANH**

```bash
cd fe && nvm use && npm run test && ./node_modules/.bin/tsc --noEmit
```
Expected: test PASS, tsc không lỗi.

- [ ] **Step 7: Commit**

```bash
git add fe/src
git commit -m "feat(fe): màn Bảng lương thêm giờ — tổng hợp, sửa tay, chốt kỳ

Cột sinh động theo tập loại ngày có trong dữ liệu, gộp từ MỌI dòng chứ không
lấy từ dòng đầu: nhân viên đầu tiên có thể không làm ca đêm mà cột Buổi đêm
vẫn phải in đúng biểu mẫu."
```

---

## Task 6: Xuất Excel mẫu 03-LĐTL

**Files:**
- Create: `fe/src/pages/luong/bang-luong-them-gio/xuat03LDTL.ts`
- Create: `fe/src/pages/luong/bang-luong-them-gio/xuat03LDTL.test.ts`
- Modify: `fe/src/pages/luong/bang-luong-them-gio/BangLuongThemGioPage.tsx` (nút Xuất)

**Interfaces:**
- Consumes: `DongLuongThemGio` (Task 5), `ReportSheet`/`ReportCol`/`NUM_FMT`/`buildReportWorkbook`/`exportReportExcel` từ `@/utils/exportReportExcel`
- Produces: `dung03LDTL(thang, dsDong, tenCongTy): ReportSheet`

Biểu mẫu là **sản phẩm bàn giao có chỗ ký**, không phải một bảng dữ liệu — phần đầu đề, số hiệu mẫu và khối chữ ký phải in đúng.

- [ ] **Step 1: Viết test**

Tạo `xuat03LDTL.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { dung03LDTL } from './xuat03LDTL';
import { buildReportWorkbook, leafCols } from '@/utils/exportReportExcel';

const DONG = {
  _id: 'x1', thang: '2026-06', employeeId: 'nv1',
  employeeName: 'Đào Thị Kiều Oanh', employeeCode: 'NV0001',
  luongThang: 5_500_000, congChuan: 24, soGioMoiNgay: 8,
  donGiaNgay: 229_166.67, donGiaGio: 28_645.83,
  theoLoai: {
    ngay_thuong: { soGio: 35, heSo: 1.5, thanhTien: 1_503_906.25 },
    ngay_dem: { soGio: 0, heSo: 1.5, thanhTien: 0 },
  },
  tongTien: 1_503_906.25, gioNghiBu: 0, tienNghiBu: 0,
  thucNhan: 1_503_906.25, gioOtHetHan: 0,
  suaTay: false, trangThai: 'nhap' as const,
};

describe('dung03LDTL', () => {
  it('tiêu đề và số hiệu mẫu đúng biểu mẫu pháp định', () => {
    const s = dung03LDTL('2026-06', [DONG], 'Công ty cổ phần Master CEO');

    expect(s.title).toMatch(/BẢNG THANH TOÁN TIỀN LÀM THÊM GIỜ/);
    expect(s.meta?.join(' ')).toMatch(/Mẫu số: 03 - LĐTL/);
    expect(s.meta?.join(' ')).toMatch(/Công ty cổ phần Master CEO/);
  });

  it('mỗi loại ngày là một nhóm hai cột Số giờ / Thành tiền', () => {
    const s = dung03LDTL('2026-06', [DONG], 'X');
    const nhom = s.columns.find((c) => c.header === 'Ngày thường');

    expect(nhom?.children?.map((c) => c.header)).toEqual([
      'Số giờ',
      'Thành tiền',
    ]);
  });

  it('in ĐỦ cột kể cả loại có 0 giờ — khớp mẫu pháp định', () => {
    const s = dung03LDTL('2026-06', [DONG], 'X');
    expect(s.columns.some((c) => c.header === 'Buổi đêm')).toBe(true);
  });

  it('dòng dữ liệu mang đúng con số kiểm chuẩn', () => {
    const s = dung03LDTL('2026-06', [DONG], 'X');
    const r = s.rows.find((x) => x.cells?.hoTen === 'Đào Thị Kiều Oanh');

    expect(r?.cells?.luongThang).toBe(5_500_000);
    expect(r?.cells?.donGiaGio).toBeCloseTo(28_645.83, 2);
    expect(r?.cells?.['ngay_thuong__soGio']).toBe(35);
    expect(r?.cells?.['ngay_thuong__thanhTien']).toBeCloseTo(1_503_906.25, 2);
    expect(r?.cells?.thucNhan).toBeCloseTo(1_503_906.25, 2);
  });

  it('có dòng TỔNG cộng đúng theo chiều dọc', () => {
    const s = dung03LDTL('2026-06', [DONG, { ...DONG, _id: 'x2', employeeName: 'B' }], 'X');
    const tong = s.rows.find((r) => r.fill === 'total');

    expect(tong?.cells?.thucNhan).toBeCloseTo(2 * 1_503_906.25, 2);
  });

  it('có khối chữ ký Người lập / Kế toán / Giám đốc', () => {
    const s = dung03LDTL('2026-06', [DONG], 'X');
    const chuoi = JSON.stringify(s.rows);

    expect(chuoi).toMatch(/Người lập/);
    expect(chuoi).toMatch(/Kế toán/);
    expect(chuoi).toMatch(/Giám đốc/);
  });

  it('dựng được workbook thật, không ném', () => {
    const s = dung03LDTL('2026-06', [DONG], 'X');
    expect(() => buildReportWorkbook([s])).not.toThrow();
    expect(leafCols(s.columns).length).toBeGreaterThan(5);
  });

  it('không có dòng nào vẫn dựng được sheet (in mẫu trống để ký tay)', () => {
    const s = dung03LDTL('2026-06', [], 'X');
    expect(() => buildReportWorkbook([s])).not.toThrow();
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd fe && nvm use && npm run test -- src/pages/luong/bang-luong-them-gio/xuat03LDTL.test.ts
```
Expected: FAIL — không tìm thấy `dung03LDTL`.

- [ ] **Step 3: Viết `dung03LDTL`**

```ts
import {
  NUM_FMT,
  type ReportCol,
  type ReportRow,
  type ReportSheet,
} from '@/utils/exportReportExcel';
import { NHAN_LOAI_NGAY } from '@/services/cauHinhLuongService';
import type { DongLuongThemGio } from '@/services/bangLuongThemGioService';

/**
 * Dựng mô hình sheet cho mẫu số 03-LĐTL "Bảng thanh toán tiền làm thêm giờ".
 *
 * Đây là BIỂU MẪU PHÁP ĐỊNH có chỗ ký (Người lập / Kế toán / Giám đốc), không
 * phải một bảng dữ liệu — đầu đề, số hiệu mẫu và khối chữ ký phải in đúng.
 *
 * Cột sinh động theo tập loại ngày có trong dữ liệu, và in ĐỦ kể cả loại 0
 * giờ: in cột trống đúng biểu mẫu tốt hơn là bỏ cột rồi khác mẫu.
 */
export function dung03LDTL(
  thang: string,
  dsDong: DongLuongThemGio[],
  tenCongTy: string,
): ReportSheet {
  const cacLoai = Array.from(
    new Set(dsDong.flatMap((d) => Object.keys(d.theoLoai ?? {}))),
  );

  const nhomLoai: ReportCol[] = cacLoai.map((loai) => ({
    key: loai,
    header: NHAN_LOAI_NGAY[loai] ?? loai,
    children: [
      { key: `${loai}__soGio`, header: 'Số giờ', align: 'right' },
      {
        key: `${loai}__thanhTien`,
        header: 'Thành tiền',
        align: 'right',
        numFmt: NUM_FMT,
        width: 16,
      },
    ],
  }));

  const columns: ReportCol[] = [
    { key: 'stt', header: 'Số TT', width: 7, align: 'center' },
    { key: 'hoTen', header: 'Họ và tên', width: 26 },
    { key: 'luongThang', header: 'Tiền lương tháng', align: 'right', numFmt: NUM_FMT, width: 16 },
    {
      key: 'mucLuong',
      header: 'Mức lương',
      children: [
        { key: 'donGiaNgay', header: 'Ngày', align: 'right', numFmt: NUM_FMT, width: 14 },
        { key: 'donGiaGio', header: 'Giờ', align: 'right', numFmt: NUM_FMT, width: 14 },
      ],
    },
    ...nhomLoai,
    { key: 'tongTien', header: 'Tổng cộng tiền', align: 'right', numFmt: NUM_FMT, width: 16 },
    { key: 'gioNghiBu', header: 'Số giờ nghỉ bù', align: 'right', width: 14 },
    { key: 'thucNhan', header: 'Thực nhận', align: 'right', numFmt: NUM_FMT, width: 16 },
  ];

  const rows: ReportRow[] = dsDong.map((d, i) => {
    const cells: Record<string, string | number | null> = {
      stt: i + 1,
      hoTen: d.employeeName ?? d.employeeCode ?? '',
      luongThang: d.luongThang,
      donGiaNgay: d.donGiaNgay,
      donGiaGio: d.donGiaGio,
      tongTien: d.tongTien,
      gioNghiBu: d.gioNghiBu,
      thucNhan: d.thucNhan,
    };
    for (const loai of cacLoai) {
      cells[`${loai}__soGio`] = d.theoLoai?.[loai]?.soGio ?? 0;
      cells[`${loai}__thanhTien`] = d.theoLoai?.[loai]?.thanhTien ?? 0;
    }
    return { cells };
  });

  const cong = (lay: (d: DongLuongThemGio) => number) =>
    dsDong.reduce((s, d) => s + lay(d), 0);

  const oTong: Record<string, string | number | null> = {
    hoTen: 'Tổng cộng',
    tongTien: cong((d) => d.tongTien),
    gioNghiBu: cong((d) => d.gioNghiBu),
    thucNhan: cong((d) => d.thucNhan),
  };
  for (const loai of cacLoai) {
    oTong[`${loai}__soGio`] = cong((d) => d.theoLoai?.[loai]?.soGio ?? 0);
    oTong[`${loai}__thanhTien`] = cong((d) => d.theoLoai?.[loai]?.thanhTien ?? 0);
  }
  rows.push({ cells: oTong, bold: true, fill: 'total' });

  // Khối chữ ký — phần làm nó thành sản phẩm bàn giao chứ không phải báo cáo.
  rows.push({ spacer: true });
  rows.push({ cells: { hoTen: 'Người lập', tongTien: 'Kế toán', thucNhan: 'Giám đốc' }, bold: true });
  rows.push({
    cells: {
      hoTen: '(ký và ghi rõ họ tên)',
      tongTien: '(ký và ghi rõ họ tên)',
      thucNhan: '(ký và ghi rõ họ tên)',
    },
  });

  const [nam, thangSo] = thang.split('-');
  return {
    name: 'LÀM THÊM GIỜ',
    title: 'BẢNG THANH TOÁN TIỀN LÀM THÊM GIỜ',
    meta: [tenCongTy, 'Mẫu số: 03 - LĐTL', `Tháng ${thangSo} năm ${nam}`],
    columns,
    rows,
  };
}
```

- [ ] **Step 4: Nối nút Xuất vào màn hình**

Thêm nút "Xuất 03-LĐTL" cạnh nút Chốt kỳ:

```tsx
onClick={() =>
  exportReportExcel(
    `03-LDTL-${thang}`,
    [dung03LDTL(thang, dsDong, tenCongTy)],
  )
}
```

`tenCongTy` lấy từ nguồn tên công ty sẵn có trong FE — tìm bằng `grep -rn "tenCongTy\|companyName" fe/src | head`. Không có thì để chuỗi rỗng và ghi TODO **trong runbook**, không phải trong mã.

- [ ] **Step 5: Chạy test, xác nhận XANH**

```bash
cd fe && nvm use && npm run test && ./node_modules/.bin/tsc --noEmit
```
Expected: PASS, tsc sạch.

- [ ] **Step 6: Commit**

```bash
git add fe/src/pages/luong/bang-luong-them-gio/
git commit -m "feat(fe): xuất Excel mẫu 03-LĐTL từ bảng lương thêm giờ

Biểu mẫu pháp định có chỗ ký Người lập / Kế toán / Giám đốc — là sản phẩm bàn
giao, không phải bảng dữ liệu, nên đầu đề và số hiệu mẫu in đúng.

In đủ cột kể cả loại ngày 0 giờ: cột trống đúng mẫu tốt hơn bỏ cột rồi khác
mẫu pháp định."
```

---

## Rủi ro và điểm cần chủ sản phẩm xác nhận

1. **`tienNghiBu = 0`, `thucNhan = tongTien`** (Task 3). Mẫu 03-LĐTL gốc trừ tiền nghỉ bù vào Thực nhận. Lập luận để đặt 0: trong cả bốn chế độ `cheDoBu`, một giờ công không bao giờ vừa vào quỹ nghỉ bù vừa được trả tiền, nên trừ thêm là trừ hai lần. **Cần xác nhận trước khi in bảng thật cho kế toán ký.** Nếu chủ sản phẩm muốn trừ thật, đổi ở đúng một chỗ: `row.thucNhan = kq.tongTien - row.tienNghiBu` và cho `tienNghiBu = gioNghiBu × donGiaGio`.

2. **Bảng này chưa nối vào bảng lương chính.** Hết plan này, tiền làm thêm CHƯA vào `thucLinh` của ai. Đó là P4.2c-2 — phải nói rõ với kế toán để họ không tưởng đã trả.

3. **Công ty chưa bật quỹ giờ làm thêm** (đo trên production 2026-08-03). `tongHop()` trả mảng rỗng cho tới khi HR bật ở màn Cấu hình lương.

---

## Self-Review

**1. Spec coverage** (spec P4.2c, phần thuộc plan này):

| Mục spec | Task |
|---|---|
| §2 công thức đơn giá + ca kiểm chuẩn 1.503.906,25 | 2 |
| §2.3 nguồn giờ theo `cheDoBu` (bảng 4 chế độ) | 3 |
| §5 màn riêng, vòng đời tổng hợp → sửa tay → chốt → xuất | 3, 4, 5, 6 |
| §5 nhóm cột sinh động theo `heSoTra` | 5, 6 |
| §5 cột "Số ngày nghỉ bù" quy đổi ở tầng hiển thị | 3 (quyết định), 6 (in) |
| §5 dùng lại `exportReportExcel`, khối chữ ký | 6 |

Mục spec **không** thuộc plan này (đã ghi ở bảng phạm vi đầu file): §2 `TIEN_OT`/`gioOtHetHan` ra tiền, §2.1, §2.2, §3, §4 phí công đoàn, §6, §7 — tất cả sang P4.2c-2.

**2. Placeholder scan** — không có "TBD"/"TODO"/"tương tự Task N". Ba chỗ yêu cầu đọc mã hiện có trước khi viết (chữ ký `ServiceBase` ở Task 5 Step 1, khuôn controller spec ở Task 4 Step 6, router/sidebar ở Task 5 Step 5) đều nêu **lệnh grep cụ thể** để tìm, kèm code đầy đủ cho phần mới.

**3. Type consistency**

- `DongThemGioTheoLoai { soGio, heSo, thanhTien }` — khai Task 1, dùng nguyên vẹn ở Task 2 (`KetQuaThemGio.theoLoai`), Task 3 (`row.theoLoai`), Task 5 (FE), Task 6 (`d.theoLoai[loai].soGio/.thanhTien`). ✓
- `tinhDongThemGio(DauVaoThemGio): KetQuaThemGio` — Task 2 khai, Task 3 và Task 4 gọi với đúng 6 trường. ✓
- `truMotDonVi` (không phải `truMot`/`chiTraChenh`) dùng nhất quán Task 2/3/4. ✓
- DTO `CapNhatDongThemGioDto.theoLoai` là `Record<string, number>` (SỐ GIỜ), khác `DongLuongThemGio.theoLoai` là `Record<string, DongThemGioTheoLoai>` — **cố ý khác nhau**, đã ghi lý do ở Task 4 Step 3; test Task 4 Step 1 truyền `{ ngay_thuong: 10 }` đúng dạng số. ✓
- `dung03LDTL(thang, dsDong, tenCongTy)` — Task 6 khai và Task 6 Step 4 gọi đúng thứ tự tham số. ✓

# P4.5 — Lịch làm việc tuần mức công ty + gỡ rào bảng công — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho phép khai lịch làm việc tuần ở mức công ty (mặc định T2–T6) để bảng công không còn coi T7/CN là ngày làm việc, gỡ rào chốt bảng công và mở đường lên bảng lương.

**Architecture:** Thêm một entity singleton `CauHinhChamCong` theo đúng khuôn `CauHinhLuong` (tự sinh từ seed lần đọc đầu tiên, không migration). Một hàm thuần `lichTuanApDung(emp, chung)` giải quyết thừa kế 3 tầng (khai riêng → lịch công ty → mọi ngày). Bốn module đang đọc `emp.ngayLamViecTrongTuan` nạp cấu hình một lần rồi truyền lịch **đã giải quyết** vào các hàm thuần sẵn có — `suy-ky-hieu.ts`, `luat-don.ts`, `luat-phep.ts` giữ nguyên chữ ký và quy ước nội bộ.

**Tech Stack:** NestJS 11 + TypeORM (driver Mongo) + Jest 30 (BE, yarn, Node 22) · React + Vite + antd + CHandler/RxJS + Vitest (FE, npm, Node 22).

## Global Constraints

- Spec nguồn: `docs/superpowers/specs/2026-08-05-hrm-p4.5-lich-tuan-cong-ty-design.md`. Mọi quyết định nghiệp vụ lấy từ đó, không tự phát minh.
- Lịch tuần mã hoá là `number[]`, `0=CN … 6=T7`. Mặc định công ty là `[1, 2, 3, 4, 5]` (T2–T6).
- Quy tắc thừa kế 3 tầng, **không được rút gọn**: khai riêng có phần tử → dùng nó; rỗng → lịch công ty; rỗng nốt → `undefined` (nơi gọi giữ đáy "mọi ngày là ngày làm việc"). Không bao giờ suy ra "nghỉ tất cả các ngày".
- Guard cho route mới: `PermissionGuard` + `@Permissions(...)`. **Cấm** `AdminGuard` và `@Roles(...)` (`RoleGuard` là no-op).
- Khoá quyền: `/cham-cong/cau-hinh:xem` và `/cham-cong/cau-hinh:sua`.
- DTO backend bật `forbidNonWhitelisted` — thiếu khai một trường là 400 cả form.
- Không gọi `layCauHinh()` bên trong vòng lặp nhân viên/ngày. Nạp một lần trước vòng lặp.
- Chạy test BE: `cd be && yarn test <đường-dẫn-hoặc-tên-file>`. Chạy test FE: `cd fe && npm run test`.
- Node 22 cho cả hai (`nvm use` trong mỗi thư mục).
- Commit sau mỗi task. Thông điệp commit tiếng Việt, kết thúc bằng dòng `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

---

## File Structure

**Tạo mới (BE):**

| File | Trách nhiệm |
|---|---|
| `be/libs/entities/src/cham-cong/cau-hinh-cham-cong.entity.ts` | Entity singleton `CauHinhChamCong` |
| `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.seed.ts` | Hằng `CAU_HINH_CHAM_CONG_MAC_DINH` |
| `be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.ts` | Hàm thuần `lichTuanApDung()` — dùng chung cho 4 module |
| `be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.spec.ts` | Test hàm thuần |
| `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.service.ts` | `layCauHinh()` / `lichTuanChung()` / `capNhat()` |
| `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.service.spec.ts` | Test service |
| `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.controller.ts` | `GET` / `PUT /config/cau-hinh-cham-cong` |
| `be/apps/config-service/src/cau-hinh-cham-cong/dto/cap-nhat-cau-hinh-cham-cong.dto.ts` | DTO ghi |
| `be/apps/config-service/src/cau-hinh-cham-cong/dto/index.ts` | Re-export DTO |
| `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.module.ts` | Wiring, `exports: [CauHinhChamCong_Service]` |

**Sửa (BE):** `cham-cong/index.ts` (export entity) · `config-service.module.ts` · `bang-cong/suy-ky-hieu.ts` (+`.spec`) · `bang-cong/bang-cong.service.ts` (+`.module`, +`.spec`) · `quy-phep/quy-phep.service.ts` (+`.module`) · `don-cham-cong/don-cham-cong.service.ts` (+`.module`) · `ban-ghi-cham-cong/ban-ghi-cham-cong.service.ts` (+`.module`) · `libs/core/src/permissions/all-permissions.ts` · `ops/grant-quyen-module-moi.ts`

**Tạo mới (FE):** `services/cauHinhChamCongService.ts` · `pages/cham-cong/cau-hinh/` (handler, context, state, page, sub-handler init+luu)
**Sửa (FE):** `pages/loadable.tsx` · `App.tsx` · `components/layout/MainLayout.tsx` · `config/routePermissions.ts` · `pages/nhan-su/ho-so-nhan-vien/components/form/tabs/ChamCongTab.tsx`

---

## Task 1: Entity + module `cau-hinh-cham-cong` + khoá quyền

**Files:**
- Create: `be/libs/entities/src/cham-cong/cau-hinh-cham-cong.entity.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.seed.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.service.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.controller.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/dto/cap-nhat-cau-hinh-cham-cong.dto.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/dto/index.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.module.ts`
- Modify: `be/libs/entities/src/cham-cong/index.ts`
- Modify: `be/apps/config-service/src/config-service.module.ts`
- Modify: `be/libs/core/src/permissions/all-permissions.ts:17-25`
- Modify: `ops/grant-quyen-module-moi.ts`
- Test: `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.service.spec.ts`

**Interfaces:**
- Consumes: `BaseEntity` từ `@app/entities`, `DatabaseModule.forFeature([...])` từ `@app/database`, `JwtGuard`/`PermissionGuard`/`Permissions` từ `@app/auth`.
- Produces:
  - `class CauHinhChamCong extends BaseEntity { ngayLamViecTrongTuan: number[]; isActive: boolean }`
  - `const CAU_HINH_CHAM_CONG_MAC_DINH: Partial<CauHinhChamCong>`
  - `class CauHinhChamCong_Service` với `layCauHinh(): Promise<CauHinhChamCong>`, `lichTuanChung(): Promise<number[] | undefined>`, `capNhat(dto: CapNhatCauHinhChamCongDto): Promise<CauHinhChamCong>`
  - `class CauHinhChamCong_Module` (export service — Task 3/4/5 import module này)

- [ ] **Step 1: Viết test thất bại cho service**

Tạo `be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.service.spec.ts`:

```ts
import { Test, TestingModule } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import { CauHinhChamCong } from '@app/entities';
import { CauHinhChamCong_Service } from './cau-hinh-cham-cong.service';
import { CAU_HINH_CHAM_CONG_MAC_DINH } from './cau-hinh-cham-cong.seed';

describe('CauHinhChamCong_Service', () => {
  let service: CauHinhChamCong_Service;
  let store: Partial<CauHinhChamCong>[];
  let repo: { find: jest.Mock; create: jest.Mock; save: jest.Mock };

  beforeEach(async () => {
    store = [];
    repo = {
      find: jest.fn(async () => store.filter((r) => r.isActive !== false)),
      create: jest.fn((x) => ({ ...x })),
      save: jest.fn(async (x) => {
        const co = store.find((r) => r === x);
        if (!co) store.push(x);
        return x;
      }),
    };

    const mod: TestingModule = await Test.createTestingModule({
      providers: [
        CauHinhChamCong_Service,
        { provide: getRepositoryToken(CauHinhChamCong), useValue: repo },
      ],
    }).compile();

    service = mod.get(CauHinhChamCong_Service);
  });

  it('tự tạo bản ghi từ seed T2–T6 khi DB chưa có gì', async () => {
    const ch = await service.layCauHinh();
    expect(ch.ngayLamViecTrongTuan).toEqual([1, 2, 3, 4, 5]);
    expect(CAU_HINH_CHAM_CONG_MAC_DINH.ngayLamViecTrongTuan).toEqual([1, 2, 3, 4, 5]);
    expect(repo.save).toHaveBeenCalledTimes(1);
    expect(store).toHaveLength(1);
  });

  it('lần đọc thứ hai dùng lại bản ghi cũ, không tạo thêm', async () => {
    await service.layCauHinh();
    await service.layCauHinh();
    expect(store).toHaveLength(1);
    expect(repo.save).toHaveBeenCalledTimes(1);
  });

  it('lichTuanChung() trả mảng lịch của bản ghi', async () => {
    expect(await service.lichTuanChung()).toEqual([1, 2, 3, 4, 5]);
  });

  it('lichTuanChung() trả undefined khi HR bỏ trống hết — nơi gọi giữ đáy "mọi ngày là ngày làm việc"', async () => {
    await service.capNhat({ ngayLamViecTrongTuan: [] });
    expect(await service.lichTuanChung()).toBeUndefined();
  });

  it('capNhat() ghi đè lịch và trả bản ghi đã lưu', async () => {
    const ch = await service.capNhat({ ngayLamViecTrongTuan: [1, 2, 3, 4, 5, 6] });
    expect(ch.ngayLamViecTrongTuan).toEqual([1, 2, 3, 4, 5, 6]);
    expect((await service.layCauHinh()).ngayLamViecTrongTuan).toEqual([1, 2, 3, 4, 5, 6]);
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận nó fail**

```bash
cd be && nvm use && yarn test cau-hinh-cham-cong.service
```

Kỳ vọng: FAIL — `Cannot find module './cau-hinh-cham-cong.service'`.

- [ ] **Step 3: Tạo entity**

`be/libs/entities/src/cham-cong/cau-hinh-cham-cong.entity.ts`:

```ts
import { Entity, Column } from 'typeorm';
import { BaseEntity } from '../base.entity';

/**
 * Cấu hình chấm công mức CÔNG TY — một bản ghi duy nhất, cùng khuôn singleton
 * với `CauHinhLuong` (tự sinh từ seed lần đọc đầu tiên, không migration).
 *
 * Tồn tại vì trước P4.5 lịch làm việc tuần CHỈ có ở mức từng nhân viên, nên
 * ai chưa được HR khai tay thì T7/CN bị coi là ngày làm việc ⇒ mỗi người 4–5
 * ô chưa xử lý mỗi tháng ⇒ không chốt được bảng công ⇒ không lên được lương.
 */
@Entity('cau_hinh_cham_cong')
export class CauHinhChamCong extends BaseEntity {
  /** 0=CN … 6=T7. Rỗng = HR cố ý bỏ trống ⇒ rơi về đáy "mọi ngày là ngày làm việc". */
  @Column('json', { nullable: true }) ngayLamViecTrongTuan: number[];
  @Column({ default: true }) isActive: boolean;
}

export interface CauHinhChamCongEntities { CauHinhChamCong: typeof CauHinhChamCong; }
declare module '../entities' { interface Entities extends CauHinhChamCongEntities {} }
```

Thêm vào cuối `be/libs/entities/src/cham-cong/index.ts`:

```ts
export * from './cau-hinh-cham-cong.entity';
```

- [ ] **Step 4: Tạo seed**

`be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.seed.ts`:

```ts
import type { CauHinhChamCong } from '@app/entities';

/**
 * Mặc định T2–T6 (0=CN … 6=T7).
 *
 * Đây là chỗ GỠ RÀO: vì bản ghi tự sinh từ seed này ở lần đọc đầu tiên, ngay
 * lần Tổng hợp bảng công đầu tiên sau deploy — HR không cần vào màn nào,
 * không cần bấm gì — T7/CN đã hết bị coi là ngày làm việc.
 */
export const CAU_HINH_CHAM_CONG_MAC_DINH: Partial<CauHinhChamCong> = {
  ngayLamViecTrongTuan: [1, 2, 3, 4, 5],
  isActive: true,
};
```

- [ ] **Step 5: Tạo service**

`be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.service.ts`:

```ts
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { CauHinhChamCong } from '@app/entities';
import { CapNhatCauHinhChamCongDto } from './dto';
import { CAU_HINH_CHAM_CONG_MAC_DINH } from './cau-hinh-cham-cong.seed';

@Injectable()
export class CauHinhChamCong_Service {
  constructor(
    @InjectRepository(CauHinhChamCong)
    private readonly repo: Repository<CauHinhChamCong>,
  ) {}

  /**
   * Bản ghi singleton. Chưa có thì tạo từ seed rồi lưu lại — cùng khuôn
   * `BangLuong_Service.layCauHinh()`. Không có migration, không có seed script.
   */
  async layCauHinh(): Promise<CauHinhChamCong> {
    const rows = await this.repo.find({ where: { isActive: true } as any });
    if (rows.length > 0) return rows[0];

    const created = this.repo.create({ ...CAU_HINH_CHAM_CONG_MAC_DINH });
    return this.repo.save(created);
  }

  /**
   * Lịch tuần chung của công ty, hoặc `undefined` khi HR cố ý bỏ trống.
   *
   * Trả `undefined` chứ không `[]`: nơi gọi (`lichTuanApDung`) dùng
   * `undefined` làm tín hiệu rơi xuống đáy "mọi ngày đều là ngày làm việc".
   * Trả `[]` sẽ khiến các hàm thuần hạ nguồn hiểu thành "không ngày nào là
   * ngày làm việc" — xoá sạch công của cả công ty.
   */
  async lichTuanChung(): Promise<number[] | undefined> {
    const lich = (await this.layCauHinh()).ngayLamViecTrongTuan;
    return lich && lich.length > 0 ? lich : undefined;
  }

  async capNhat(dto: CapNhatCauHinhChamCongDto): Promise<CauHinhChamCong> {
    const item = await this.layCauHinh();
    Object.assign(item, dto);
    return this.repo.save(item);
  }
}
```

- [ ] **Step 6: Tạo DTO**

`be/apps/config-service/src/cau-hinh-cham-cong/dto/cap-nhat-cau-hinh-cham-cong.dto.ts`:

```ts
import { IsArray, IsInt, IsOptional, Max, Min } from 'class-validator';

/**
 * `forbidNonWhitelisted` đang bật ở `main.ts` — trường nào không khai ở đây
 * mà lọt vào body là 400 cho CẢ form, không phải bỏ qua im lặng.
 */
export class CapNhatCauHinhChamCongDto {
  /** 0=CN … 6=T7. Cho phép mảng rỗng: đó là "HR cố ý bỏ trống". */
  @IsOptional()
  @IsArray()
  @IsInt({ each: true })
  @Min(0, { each: true })
  @Max(6, { each: true })
  ngayLamViecTrongTuan?: number[];
}
```

`be/apps/config-service/src/cau-hinh-cham-cong/dto/index.ts`:

```ts
export * from './cap-nhat-cau-hinh-cham-cong.dto';
```

- [ ] **Step 7: Chạy test service, xác nhận PASS**

```bash
cd be && yarn test cau-hinh-cham-cong.service
```

Kỳ vọng: PASS, 5 test.

- [ ] **Step 8: Tạo controller**

`be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.controller.ts`:

```ts
import { Body, Controller, Get, Put, UseGuards } from '@nestjs/common';
import { JwtGuard, PermissionGuard, Permissions } from '@app/auth';
import { CauHinhChamCong_Service } from './cau-hinh-cham-cong.service';
import { CapNhatCauHinhChamCongDto } from './dto';

/**
 * Lịch làm việc tuần mức công ty ảnh hưởng TOÀN BỘ bảng công và mẫu số tính
 * phép năm — nên tách quyền riêng `/cham-cong/cau-hinh:*`, không dùng ké
 * quyền bảng công.
 *
 * `PermissionGuard`, KHÔNG `AdminGuard` (chỉ khớp `vaiTro` viết hoa đúng
 * 'ADMIN'/'SUPER_ADMIN', mà tên vai trò do từng tenant tự đặt) và KHÔNG
 * `@Roles(...)` (`RoleGuard` hiện là no-op). Cùng khuôn
 * `bang-luong.controller.ts`.
 */
@Controller('cau-hinh-cham-cong')
@UseGuards(JwtGuard)
export class CauHinhChamCong_Controller {
  constructor(private readonly service: CauHinhChamCong_Service) {}

  @Get()
  @UseGuards(PermissionGuard)
  @Permissions('/cham-cong/cau-hinh:xem')
  async layCauHinh() {
    const data = await this.service.layCauHinh();
    return { success: true, data };
  }

  @Put()
  @UseGuards(PermissionGuard)
  @Permissions('/cham-cong/cau-hinh:sua')
  async capNhat(@Body() body: CapNhatCauHinhChamCongDto) {
    const data = await this.service.capNhat(body);
    return { success: true, data };
  }
}
```

- [ ] **Step 9: Tạo module và đăng ký vào config-service**

`be/apps/config-service/src/cau-hinh-cham-cong/cau-hinh-cham-cong.module.ts`:

```ts
import { Module } from '@nestjs/common';
import { CauHinhChamCong } from '@app/entities';
import { DatabaseModule } from '@app/database';
import { CauHinhChamCong_Service } from './cau-hinh-cham-cong.service';
import { CauHinhChamCong_Controller } from './cau-hinh-cham-cong.controller';

@Module({
  imports: [DatabaseModule.forFeature([CauHinhChamCong])],
  controllers: [CauHinhChamCong_Controller],
  providers: [CauHinhChamCong_Service],
  exports: [CauHinhChamCong_Service],
})
export class CauHinhChamCong_Module {}
```

Trong `be/apps/config-service/src/config-service.module.ts`, thêm import và thêm vào mảng `imports` (đặt cạnh `QuyGio_Module`):

```ts
import { CauHinhChamCong_Module } from './cau-hinh-cham-cong/cau-hinh-cham-cong.module';
```

```ts
    QuyGio_Module,
    CauHinhChamCong_Module,
```

- [ ] **Step 10: Khai khoá quyền mới**

Trong `be/libs/core/src/permissions/all-permissions.ts`, thêm vào `PERMISSION_MODULES` ngay sau `'/cham-cong/quy-gio'`:

```ts
  '/cham-cong/quy-gio',
  '/cham-cong/cau-hinh',
```

Trong `ops/grant-quyen-module-moi.ts`, đổi `MODULE_CAN_CAP` thành:

```ts
const MODULE_CAN_CAP = ['/cham-cong/cau-hinh'];
```

và cập nhật chú thích ngay trên nó, thêm một dòng:

```
 * Đợt P4.5 (Cấu hình chấm công): cấp `/cham-cong/cau-hinh`.
```

- [ ] **Step 11: Build BE để bắt lỗi kiểu**

```bash
cd be && yarn build:config
```

Kỳ vọng: build thành công, không lỗi TypeScript.

- [ ] **Step 12: Commit**

```bash
git add be/libs/entities/src/cham-cong/ be/apps/config-service/src/cau-hinh-cham-cong/ \
        be/apps/config-service/src/config-service.module.ts \
        be/libs/core/src/permissions/all-permissions.ts ops/grant-quyen-module-moi.ts
git commit -m "feat(be): entity + module Cấu hình chấm công, seed lịch tuần T2-T6

Bản ghi singleton tự sinh từ seed ở lần đọc đầu tiên nên không cần
migration: ngay lần Tổng hợp bảng công đầu tiên sau deploy, T7/CN đã
hết bị coi là ngày làm việc mà HR không phải bấm gì.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: Hàm thuần `lichTuanApDung()` + cảnh báo `LAM_NGOAI_LICH_TUAN`

**Files:**
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.ts`
- Create: `be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.spec.ts`
- Modify: `be/apps/config-service/src/bang-cong/suy-ky-hieu.ts:12-38` (thêm mã cảnh báo), `:154-156` (dòng 2 bảng ưu tiên)
- Test: `be/apps/config-service/src/bang-cong/suy-ky-hieu.spec.ts` (thêm describe mới)

**Interfaces:**
- Consumes: không gì từ task trước (thuần, độc lập).
- Produces:
  - `function lichTuanApDung(emp: { ngayLamViecTrongTuan?: number[] } | null | undefined, chung: number[] | undefined): number[] | undefined` — Task 3, 4, 5 gọi hàm này.
  - `MA_CANH_BAO.LAM_NGOAI_LICH_TUAN = 'lam_ngoai_lich_tuan'`

- [ ] **Step 1: Viết test thất bại cho `lichTuanApDung()`**

Tạo `be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.spec.ts`:

```ts
import { lichTuanApDung } from './lich-tuan';

describe('lichTuanApDung', () => {
  const CHUNG = [1, 2, 3, 4, 5];

  it('khai riêng thắng lịch chung', () => {
    expect(lichTuanApDung({ ngayLamViecTrongTuan: [1, 2, 3, 4, 5, 6] }, CHUNG))
      .toEqual([1, 2, 3, 4, 5, 6]);
  });

  it('khai riêng rỗng thì rơi về lịch chung', () => {
    expect(lichTuanApDung({ ngayLamViecTrongTuan: [] }, CHUNG)).toEqual(CHUNG);
  });

  it('khai riêng undefined thì rơi về lịch chung', () => {
    expect(lichTuanApDung({}, CHUNG)).toEqual(CHUNG);
  });

  it('nhân viên null/undefined vẫn rơi về lịch chung, không ném', () => {
    expect(lichTuanApDung(null, CHUNG)).toEqual(CHUNG);
    expect(lichTuanApDung(undefined, CHUNG)).toEqual(CHUNG);
  });

  it('cả hai rỗng thì trả undefined — đáy "mọi ngày là ngày làm việc", KHÔNG phải "nghỉ tất cả"', () => {
    expect(lichTuanApDung({}, undefined)).toBeUndefined();
    expect(lichTuanApDung({ ngayLamViecTrongTuan: [] }, [])).toBeUndefined();
  });

  it('Chủ nhật (0) trong khai riêng không bị hiểu nhầm là falsy', () => {
    expect(lichTuanApDung({ ngayLamViecTrongTuan: [0] }, CHUNG)).toEqual([0]);
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận nó fail**

```bash
cd be && yarn test lich-tuan
```

Kỳ vọng: FAIL — `Cannot find module './lich-tuan'`.

- [ ] **Step 3: Viết `lich-tuan.ts`**

```ts
/**
 * Giải quyết lịch làm việc tuần THỰC SỰ áp cho một nhân viên — hàm thuần,
 * không DB, không NestJS.
 *
 * Tồn tại để 4 module (bảng công, quỹ phép, đơn từ, bản ghi chấm công) dùng
 * CHUNG một quy tắc thừa kế thay vì mỗi nơi tự đoán. Các hàm thuần hạ nguồn
 * (`suy-ky-hieu.ts`, `luat-don.ts`, `luat-phep.ts`) KHÔNG biết gì về cấu hình
 * công ty — chúng nhận lịch đã giải quyết và giữ nguyên quy ước cũ của mình.
 */

/**
 * Ba tầng, theo đúng thứ tự:
 *
 * 1. Nhân viên khai riêng (có phần tử) → dùng nó.
 * 2. Rỗng/chưa đặt → lịch chung của công ty.
 * 3. Công ty cũng rỗng → `undefined`.
 *
 * Tầng 3 trả `undefined` chứ KHÔNG `[]`, và đó là điểm dễ sửa sai nhất file
 * này: mọi hàm hạ nguồn quy ước "lịch rỗng/undefined = chưa cấu hình ⇒ mọi
 * ngày đều là ngày làm việc". Trả `[]` sẽ được hiểu ngược lại thành "không
 * ngày nào là ngày làm việc" ở những nhánh chỉ kiểm `includes()`, tức là âm
 * thầm xoá sạch công của cả công ty.
 */
export function lichTuanApDung(
  emp: { ngayLamViecTrongTuan?: number[] } | null | undefined,
  chung: number[] | undefined,
): number[] | undefined {
  const rieng = emp?.ngayLamViecTrongTuan;
  if (rieng && rieng.length > 0) return rieng;
  if (chung && chung.length > 0) return chung;
  return undefined;
}
```

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd be && yarn test lich-tuan
```

Kỳ vọng: PASS, 6 test.

- [ ] **Step 5: Viết test thất bại cho cảnh báo `LAM_NGOAI_LICH_TUAN`**

Thêm vào cuối `be/apps/config-service/src/bang-cong/suy-ky-hieu.spec.ts` (bên trong `describe('suyKyHieuNgay', ...)` ngoài cùng, hoặc ở cấp cao nhất của file — miễn `suyKyHieuNgay` và `MA_CANH_BAO` đã được import sẵn ở đầu file):

```ts
describe('ngày ngoài lịch làm việc trong tuần', () => {
  // 2026-08-08 là thứ Bảy; lịch T2–T6 nên hôm đó không phải ngày làm việc.
  const T7 = '2026-08-08';
  const LICH_T2_T6 = [1, 2, 3, 4, 5];

  const nen = {
    ngay: T7,
    ngayLamViecTrongTuan: LICH_T2_T6,
    laNgayLe: false,
    donNghi: null,
    coChamVao: false,
    coChamRa: false,
    coBanGhiNgoaiVung: false,
  };

  it('không có gì thì để trống, không cảnh báo, không chặn chốt', () => {
    const kq = suyKyHieuNgay({ ...nen });
    expect(kq.kyHieu).toBeNull();
    expect(kq.canhBao).toEqual([]);
    expect(kq.chuaXuLy).toBe(false);
  });

  it('CÓ chấm công thì cảnh báo LAM_NGOAI_LICH_TUAN nhưng vẫn không chặn chốt', () => {
    const kq = suyKyHieuNgay({ ...nen, coChamVao: true, coChamRa: true });
    expect(kq.kyHieu).toBeNull();
    expect(kq.canhBao).toEqual([MA_CANH_BAO.LAM_NGOAI_LICH_TUAN]);
    expect(kq.chuaXuLy).toBe(false);
  });

  it('chỉ có lượt chấm RA (ca đêm / nhập bù một đầu) cũng cảnh báo', () => {
    const kq = suyKyHieuNgay({ ...nen, coChamRa: true });
    expect(kq.canhBao).toEqual([MA_CANH_BAO.LAM_NGOAI_LICH_TUAN]);
  });

  it('đơn nghỉ dài ngày bắc qua cuối tuần KHÔNG đẻ cảnh báo rác', () => {
    const kq = suyKyHieuNgay({
      ...nen,
      donNghi: { loaiDon: 'nghi_phep', loaiNghi: 'phep_nam', laNuaNgay: false },
    });
    expect(kq.kyHieu).toBeNull();
    expect(kq.canhBao).toEqual([]);
    expect(kq.chuaXuLy).toBe(false);
  });

  it('lịch undefined (chưa cấu hình gì) giữ nguyên hành vi cũ: T7 vẫn là ngày làm việc', () => {
    const kq = suyKyHieuNgay({ ...nen, ngayLamViecTrongTuan: undefined, coChamVao: true, coChamRa: true });
    expect(kq.kyHieu).toBe('X');
  });
});
```

- [ ] **Step 6: Chạy test để xác nhận nó fail**

```bash
cd be && yarn test suy-ky-hieu
```

Kỳ vọng: FAIL — `MA_CANH_BAO.LAM_NGOAI_LICH_TUAN` là `undefined` nên test "CÓ chấm công" so sánh `['lam_ngoai_lich_tuan']` với `[]` và trượt.

- [ ] **Step 7: Thêm mã cảnh báo vào `suy-ky-hieu.ts`**

Trong object `MA_CANH_BAO` (`be/apps/config-service/src/bang-cong/suy-ky-hieu.ts:12-38`), thêm ngay trước dấu `} as const;`:

```ts
  /**
   * Có CHẤM CÔNG vào ngày ngoài lịch làm việc trong tuần (vd đi làm thứ Bảy
   * để chạy deadline). Ô vẫn để trống — giờ hôm đó thuộc về cột làm thêm,
   * không phải một ngày công — nhưng phải la lên: nếu im lặng thì một ngày
   * công thật biến mất khỏi lương mà không ai nhìn thấy. Cùng triết lý
   * TRUOC_NGAY_VAO_LAM / SAU_NGAY_NGHI_VIEC.
   *
   * `chuaXuLy: false` là CỐ Ý — cảnh báo, KHÔNG chặn chốt. Chặn ở đây là
   * dựng lại đúng cái rào mà P4.5 sinh ra để gỡ.
   */
  LAM_NGOAI_LICH_TUAN: 'lam_ngoai_lich_tuan',
```

- [ ] **Step 8: Sửa dòng 2 của bảng ưu tiên**

Thay khối ở `suy-ky-hieu.ts:154-156`:

```ts
  // Dòng 2: ngoài lịch làm việc trong tuần. Đi làm Chủ nhật vẫn để trống —
  // giờ hôm đó đi vào cột làm thêm với hệ số 2.0, không phải một ngày công.
  if (!laNgayLamViec(input.ngay, input.ngayLamViecTrongTuan)) return khongTinh();
```

bằng:

```ts
  // Dòng 2: ngoài lịch làm việc trong tuần. Đi làm Chủ nhật vẫn để trống —
  // giờ hôm đó đi vào cột làm thêm với hệ số 2.0, không phải một ngày công.
  //
  // Điều kiện cảnh báo là CHẤM CÔNG, cố ý KHÔNG dùng `coBangChung()` (vốn
  // tính cả `donNghi`): `gomTheoNgay()` trải đơn nghỉ ra từng ngày trong
  // khoảng, nên một đơn phép T5→T3 tuần sau phủ cả T7 lẫn CN. Dùng
  // `coBangChung()` là mỗi đơn dài ngày đẻ hai cảnh báo rác, trong khi
  // `luat-don.ts` vốn đã loại đúng những ngày đó khỏi `soNgayNghi` — không
  // có công nào mất, không có gì để la.
  if (!laNgayLamViec(input.ngay, input.ngayLamViecTrongTuan)) {
    if (input.coChamVao || input.coChamRa) {
      return {
        kyHieu: null,
        canhBao: [MA_CANH_BAO.LAM_NGOAI_LICH_TUAN],
        chuaXuLy: false,
      };
    }
    return khongTinh();
  }
```

- [ ] **Step 9: Chạy toàn bộ test bảng công, xác nhận PASS**

```bash
cd be && yarn test bang-cong && yarn test suy-ky-hieu
```

Kỳ vọng: PASS toàn bộ. Nếu có test cũ trong `suy-ky-hieu.spec.ts` khẳng định ngày ngoài lịch tuần **có chấm công** trả `canhBao: []`, đó là test mô tả hành vi vừa được thay — cập nhật nó sang kỳ vọng mới (`[MA_CANH_BAO.LAM_NGOAI_LICH_TUAN]`), đừng nới lỏng assert.

- [ ] **Step 10: Commit**

```bash
git add be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.ts \
        be/apps/config-service/src/cau-hinh-cham-cong/lich-tuan.spec.ts \
        be/apps/config-service/src/bang-cong/suy-ky-hieu.ts \
        be/apps/config-service/src/bang-cong/suy-ky-hieu.spec.ts
git commit -m "feat(be): lichTuanApDung() + cảnh báo đi làm ngoài lịch tuần

Hàm thuần giải quyết thừa kế 3 tầng để 4 module dùng chung một quy tắc.
Kèm cảnh báo LAM_NGOAI_LICH_TUAN để công thứ Bảy không biến mất im lặng
khi tổng hợp lại tháng cũ; cảnh báo, không chặn chốt.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: Đấu lịch công ty vào bảng công

**Files:**
- Modify: `be/apps/config-service/src/bang-cong/bang-cong.service.ts` (constructor, và 3 điểm gọi: `:232`, `:420`, `:486`)
- Modify: `be/apps/config-service/src/bang-cong/bang-cong.module.ts`
- Test: `be/apps/config-service/src/bang-cong/bang-cong.service.spec.ts`

**Interfaces:**
- Consumes: `CauHinhChamCong_Service.lichTuanChung()` (Task 1), `lichTuanApDung()` (Task 2).
- Produces: không có API mới. Hành vi mới: `generate()` không sinh ô `chuaXuLy` cho T7/CN.

- [ ] **Step 1: Viết test thất bại**

File `bang-cong.service.spec.ts` có sẵn helper `dungService()` (dòng 565) dựng `TestingModule` cho mọi test `generate()`. Thêm provider mới **vào chính helper đó**, ngay sau `{ provide: QuyPhep_Service, useValue: quyPhep }`:

```ts
      { provide: CauHinhChamCong_Service, useValue: cauHinhChamCong },
```

và khai `cauHinhChamCong` cùng chỗ với `quyPhep` (dòng 581), cho phép từng test ghi đè qua `opts`:

```ts
  // `khongCoLichChung` là cờ RIÊNG chứ không phải `lichChung: []`: service
  // thật trả `undefined` (không phải `[]`) khi công ty bỏ trống, và chính
  // sự khác biệt đó là thứ giữ đáy "mọi ngày là ngày làm việc". Mock bằng
  // `[]` sẽ test một giá trị mà production không bao giờ tạo ra.
  const cauHinhChamCong = {
    lichTuanChung: jest
      .fn()
      .mockResolvedValue(
        opts.khongCoLichChung ? undefined : (opts.lichChung ?? [1, 2, 3, 4, 5]),
      ),
  };
```

Thêm `lichChung?: number[]` và `khongCoLichChung?: boolean` vào type của `opts` (dòng 566-573), và `cauHinhChamCong` vào object `return` (dòng 600-609).

Import cần thêm ở đầu file:

```ts
import { CauHinhChamCong_Service } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.service';
```

Thêm hai test vào `describe('BangCong_Service.generate — tự sinh ký hiệu (P3.9)', ...)`:

```ts
  // 2026-08 có 31 ngày: 21 ngày T2–T6 và 10 ngày T7/CN (T7: 1,8,15,22,29;
  // CN: 2,9,16,23,30). Trước P4.5 cả 31 ngày đều là ô chưa xử lý.
  it('NV không khai lịch riêng thì theo lịch công ty T2–T6: T7/CN hết là ô trống', async () => {
    const { service } = await dungService({
      nhanVien: [{ ...HO_SO_NV1, ngayLamViecTrongTuan: undefined }],
    });

    const tomTat = await service.generate('2026-08');

    expect(tomTat.soOTrong).toBe(21);
  });

  it('khai riêng trên hồ sơ vẫn thắng lịch công ty', async () => {
    // HO_SO_NV1 khai sẵn [1,2,3,4,5,6] — làm cả thứ Bảy. Chỉ 5 ngày Chủ
    // nhật rơi ra ngoài ⇒ 26 ô trống, không phải 21.
    const { service } = await dungService({ nhanVien: [HO_SO_NV1] });

    const tomTat = await service.generate('2026-08');

    expect(tomTat.soOTrong).toBe(26);
  });

  it('công ty bỏ trống lịch thì giữ đáy cũ: mọi ngày đều là ngày làm việc', async () => {
    const { service } = await dungService({
      nhanVien: [{ ...HO_SO_NV1, ngayLamViecTrongTuan: undefined }],
      khongCoLichChung: true,
    });

    const tomTat = await service.generate('2026-08');

    expect(tomTat.soOTrong).toBe(31);
  });
```

- [ ] **Step 2: Chạy test để xác nhận nó fail**

```bash
cd be && yarn test bang-cong.service
```

Kỳ vọng: FAIL — DI báo thiếu provider `CauHinhChamCong_Service`, hoặc `soOTrong` ra 31 thay vì 21.

- [ ] **Step 3: Inject service vào `BangCong_Service`**

Thêm import ở đầu `bang-cong.service.ts`:

```ts
import { CauHinhChamCong_Service } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.service';
import { lichTuanApDung } from '../cau-hinh-cham-cong/lich-tuan';
```

Thêm tham số cuối cùng của constructor:

```ts
    private readonly cauHinhChamCong_Service: CauHinhChamCong_Service,
```

- [ ] **Step 4: Sửa 3 điểm gọi**

Trong `generate()` — nạp **một lần**, ngay sau khối `Promise.all([...])` (khoảng dòng 148), trước vòng lặp nhân viên:

```ts
    // Nạp MỘT LẦN cho cả tháng × toàn bộ nhân viên. Gọi trong vòng lặp là
    // 200 NV × 31 ngày lượt đọc DB cho một lần bấm nút.
    const lichChung = await this.cauHinhChamCong_Service.lichTuanChung();
```

Rồi ở `:232` đổi:

```ts
          ngayLamViecTrongTuan: emp.ngayLamViecTrongTuan,
```

thành:

```ts
          ngayLamViecTrongTuan: lichTuanApDung(emp, lichChung),
```

Ở `:420` và `:486` (hai hàm khác, mỗi hàm xử lý một nhân viên) thêm ngay trước lời gọi `suyKyHieuNgay(...)` tương ứng:

```ts
    const lichChung = await this.cauHinhChamCong_Service.lichTuanChung();
```

và đổi dòng `ngayLamViecTrongTuan: emp?.ngayLamViecTrongTuan,` thành:

```ts
      ngayLamViecTrongTuan: lichTuanApDung(emp, lichChung),
```

Ở `:486` lời gọi nằm trong vòng lặp ngày — đặt câu `const lichChung = ...` **ngoài** vòng lặp đó.

- [ ] **Step 5: Đăng ký module**

Trong `be/apps/config-service/src/bang-cong/bang-cong.module.ts`, thêm import và thêm `CauHinhChamCong_Module` vào mảng `imports`:

```ts
import { CauHinhChamCong_Module } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.module';
```

- [ ] **Step 6: Chạy test, xác nhận PASS**

```bash
cd be && yarn test bang-cong
```

Kỳ vọng: PASS toàn bộ, kể cả các test `generate()` cũ.

- [ ] **Step 7: Commit**

```bash
git add be/apps/config-service/src/bang-cong/
git commit -m "feat(be): bảng công đọc lịch tuần mức công ty

T7/CN hết bị đếm vào soOTrong nên finalize() không còn bị chặn oan.
Nạp cấu hình một lần ngoài vòng lặp, không phải mỗi NV mỗi ngày.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: Đấu lịch công ty vào quỹ phép

**Files:**
- Modify: `be/apps/config-service/src/quy-phep/quy-phep.service.ts` (constructor, và 5 điểm gọi: `:263`, `:282`, `:329`, `:421`, `:550`)
- Modify: `be/apps/config-service/src/quy-phep/quy-phep.module.ts`
- Test: `be/apps/config-service/src/quy-phep/quy-phep.service.spec.ts`

**Interfaces:**
- Consumes: `CauHinhChamCong_Service.lichTuanChung()` (Task 1), `lichTuanApDung()` (Task 2).
- Produces: không có API mới. `soNgayLamViecCuaThang()` giờ nhận mẫu số đúng (~22 thay vì ~30).

**Bối cảnh phải đọc trước khi sửa:** `quy-phep.service.ts:495` đã ghi bất biến *"KHÔNG nhánh nào hạ `soNgayDuocCap`"*, và `capMotNam()` thoát sớm khi quỹ đã tồn tại. Nghĩa là task này **không** làm tụt quỹ đã cấp — nó chỉ sửa mẫu số cho quỹ cấp mới. Đừng thêm bất kỳ nhánh tính lại nào.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `quy-phep.service.spec.ts`. Provider mới cần thêm vào mọi `Test.createTestingModule` trong file:

```ts
{
  provide: CauHinhChamCong_Service,
  useValue: { lichTuanChung: jest.fn(async () => [1, 2, 3, 4, 5]) },
},
```

Chữ ký thật (dòng 503): `tichPhepTheoThang(thang: string, nguoiThucHien: string, dsBangCong?: Timesheet[]): Promise<{ soNguoiDuocTich, soNguoiKhongDat, soNguoiDaTich }>`. Tham số thứ ba nhận thẳng danh sách bảng công nên **không phải giả repo Timesheet** — dùng nó.

Test mới:

```ts
it('mẫu số tích phép tháng lẻ dùng lịch công ty T2–T6, không phải cả 31 ngày', async () => {
  // 2026-08 có 21 ngày T2–T6 ⇒ ngưỡng NĐ145 Đ66.2 (50%) là 10.5 công.
  // Bảng công đã CHỐT với 12 công hợp lệ ⇒ ĐẠT ⇒ được tích.
  // Nếu mẫu số vẫn là 31 ngày (hành vi cũ) thì ngưỡng là 15.5 ⇒ KHÔNG đạt.
  const kq = await service.tichPhepTheoThang('2026-08', 'test', [
    {
      employeeId: EMP1,
      thang: '2026-08',
      trangThai: 'chot',
      soNgayCong: 12,
      soNgayOm: 0,
    } as any,
  ]);

  expect(kq.soNguoiDuocTich).toBe(1);
  expect(kq.soNguoiKhongDat).toBe(0);
});
```

Dựng kèm: một `Employee` `_id` = `EMP1`, **không** khai `ngayLamViecTrongTuan`; một `LeaveBalance` năm 2026 của `EMP1` với `thangDaTich: []`, `soNgayDuocCap: 0`, `canCuCap: { mucCaNam: 12 }`, `isActive: true`. Dùng lại đúng cơ chế repo giả mà `quy-phep.service.spec.ts` đang dùng cho các test `tichPhepTheoThang` sẵn có.

- [ ] **Step 2: Chạy test để xác nhận nó fail**

```bash
cd be && yarn test quy-phep.service
```

Kỳ vọng: FAIL — thiếu provider, hoặc `soNguoiDuocTich` ra 0.

- [ ] **Step 3: Inject service**

Thêm import ở đầu `quy-phep.service.ts`:

```ts
import { CauHinhChamCong_Service } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.service';
import { lichTuanApDung } from '../cau-hinh-cham-cong/lich-tuan';
```

Thêm tham số cuối constructor:

```ts
    private readonly cauHinhChamCong_Service: CauHinhChamCong_Service,
```

- [ ] **Step 4: Sửa 5 điểm gọi**

Mẫu chung: nạp `const lichChung = await this.cauHinhChamCong_Service.lichTuanChung();` ở đầu hàm bao ngoài (ngoài mọi vòng lặp và ngoài `Promise.all(ds.map(...))`), rồi thay:

| Dòng | Trước | Sau |
|---|---|---|
| `:263` (`capMotNam`) | `ngayLamViecTrongTuan: nv.ngayLamViecTrongTuan,` | `ngayLamViecTrongTuan: lichTuanApDung(nv, lichChung),` |
| `:282` (`capMotNam`, `thangTheoLich`) | `ngayLamViecTrongTuan: nv.ngayLamViecTrongTuan,` | `ngayLamViecTrongTuan: lichTuanApDung(nv, lichChung),` |
| `:329` (`xemTruocCapPhepDauNam`) | `ngayLamViecTrongTuan: nv.ngayLamViecTrongTuan,` | `ngayLamViecTrongTuan: lichTuanApDung(nv, lichChung),` |
| `:421` (dự kiến phép) | `ngayLamViecTrongTuan: nv?.ngayLamViecTrongTuan,` | `ngayLamViecTrongTuan: lichTuanApDung(nv, lichChung),` |
| `:550` (`tichPhepTheoThang`) | `ngayLamViecTrongTuan: nhanVien.get(employeeId)?.ngayLamViecTrongTuan,` | `ngayLamViecTrongTuan: lichTuanApDung(nhanVien.get(employeeId), lichChung),` |

`capMotNam()` là hàm private nhận một `nv` — nạp `lichChung` ngay đầu hàm đó (hai điểm `:263` và `:282` dùng chung một biến). `xemTruocCapPhepDauNam()` gọi `ds.map(async ...)`: nạp **trước** `Promise.all`, không trong callback.

- [ ] **Step 5: Đăng ký module**

Trong `quy-phep.module.ts` thêm vào `imports`:

```ts
import { CauHinhChamCong_Module } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.module';
```

`CauHinhChamCong_Module` không phụ thuộc module nào khác nên **không** cần `forwardRef()`.

- [ ] **Step 6: Chạy test, xác nhận PASS**

```bash
cd be && yarn test quy-phep
```

Kỳ vọng: PASS toàn bộ, kể cả `luat-phep.spec.ts` (file đó không bị sửa — đúng ý đồ).

- [ ] **Step 7: Commit**

```bash
git add be/apps/config-service/src/quy-phep/
git commit -m "fix(be): mẫu số tích phép tháng lẻ theo lịch công ty

quy-phep.service.ts:495 vốn đã ghi 'mẫu số là số ngày làm việc bình
thường của CÔNG TY', nhưng chưa có lịch công ty nên đang lấy ~30 ngày
thay vì ~22, đẩy ngưỡng NĐ145 Đ66.2 lên gần gấp rưỡi so với luật.

Không nhánh nào hạ soNgayDuocCap; quỹ đã cấp không đổi.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: Đấu lịch công ty vào đơn từ + bản ghi chấm công

**Files:**
- Modify: `be/apps/config-service/src/don-cham-cong/don-cham-cong.service.ts` (constructor, `cacNgayTruPhep()` tại `:150-167`, và 3 điểm truyền vào `luat-don` tại `:247`, `:320`, `:335`)
- Modify: `be/apps/config-service/src/don-cham-cong/don-cham-cong.module.ts`
- Modify: `be/apps/config-service/src/ban-ghi-cham-cong/ban-ghi-cham-cong.service.ts:452-466` (`suyNgayNghi()`)
- Modify: `be/apps/config-service/src/ban-ghi-cham-cong/ban-ghi-cham-cong.module.ts`
- Test: `be/apps/config-service/src/don-cham-cong/don-cham-cong.service.spec.ts`, `be/apps/config-service/src/ban-ghi-cham-cong/ban-ghi-cham-cong.service.spec.ts`

**Interfaces:**
- Consumes: `CauHinhChamCong_Service.lichTuanChung()` (Task 1), `lichTuanApDung()` (Task 2).
- Produces: không có API mới. Đơn nghỉ bắc qua cuối tuần trừ đúng số ngày; đi muộn/về sớm không còn tính vào T7/CN.

- [ ] **Step 1: Viết test thất bại cho đơn từ**

Thêm provider vào mọi `Test.createTestingModule` trong `don-cham-cong.service.spec.ts`:

```ts
{
  provide: CauHinhChamCong_Service,
  useValue: { lichTuanChung: jest.fn(async () => [1, 2, 3, 4, 5]) },
},
```

`cacNgayTruPhep()` là **private** (dòng 149), được gọi từ dòng 375 trong luồng `create()`. Đường public duy nhất là `service.create(dto: CreateDonChamCongDto)` (dòng 344) — test qua đó và kiểm `soNgayNghi` trên đơn trả về:

```ts
it('đơn nghỉ phép T6→T2 chỉ trừ 2 ngày khi công ty nghỉ T7/CN', async () => {
  // 2026-08-07 là thứ Sáu, 2026-08-10 là thứ Hai; 08 và 09 là T7/CN.
  // NV không khai lịch riêng ⇒ theo lịch công ty T2–T6 ⇒ trừ T6 + T2 = 2.
  const don = await service.create({
    employeeId: EMP1,
    loaiDon: 'nghi_phep',
    loaiNghi: 'phep_nam',
    ngay: '2026-08-07',
    denNgay: '2026-08-10',
    lyDo: 'test',
  } as any);

  expect(don.soNgayNghi).toBe(2);
});
```

Nhân viên dùng cho test này phải **không** khai `ngayLamViecTrongTuan` (nếu fixture `Employee` sẵn có trong file đã khai, ghi đè bằng `undefined` cho riêng test này). Dùng lại đúng cơ chế mock repo/quỹ phép mà các test `create()` sẵn có trong file đang dùng.

- [ ] **Step 2: Chạy test để xác nhận nó fail**

```bash
cd be && yarn test don-cham-cong.service
```

Kỳ vọng: FAIL — thiếu provider, hoặc trả 4 thay vì 2.

- [ ] **Step 3: Sửa `don-cham-cong.service.ts`**

Thêm import:

```ts
import { CauHinhChamCong_Service } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.service';
import { lichTuanApDung } from '../cau-hinh-cham-cong/lich-tuan';
```

Thêm tham số cuối constructor:

```ts
    private readonly cauHinhChamCong_Service: CauHinhChamCong_Service,
```

Trong `cacNgayTruPhep()` (`:150-167`), thay hai dòng:

```ts
    const daCauHinh =
      !!emp.ngayLamViecTrongTuan && emp.ngayLamViecTrongTuan.length > 0;
```

bằng:

```ts
    const lich = lichTuanApDung(emp, await this.cauHinhChamCong_Service.lichTuanChung());
    const daCauHinh = !!lich && lich.length > 0;
```

và ở cuối hàm thay `return emp.ngayLamViecTrongTuan!.includes(thu);` bằng:

```ts
      return lich!.includes(thu);
```

Với `:247`, `:320`, `:335` — cả ba đều nằm trong cùng một hàm tính số ngày nghỉ / loại ngày. Nạp một lần ở đầu hàm đó:

```ts
    const lichLamViec = lichTuanApDung(emp, await this.cauHinhChamCong_Service.lichTuanChung());
```

rồi đổi cả ba dòng `ngayLamViecTrongTuan: emp.ngayLamViecTrongTuan,` thành:

```ts
        ngayLamViecTrongTuan: lichLamViec,
```

- [ ] **Step 4: Sửa `ban-ghi-cham-cong.service.ts`**

Thêm cùng hai import và tham số constructor như trên. Thay `suyNgayNghi()` (`:452-466`) bằng:

```ts
  /**
   * Ngày lễ, hoặc ngày ngoài lịch làm việc của NV.
   *
   * Lịch lấy theo thứ tự: khai riêng trên hồ sơ → lịch chung công ty →
   * rỗng cả hai. Rỗng cả hai vẫn KHÔNG suy ra "mọi ngày đều là ngày nghỉ",
   * vì như thế sẽ âm thầm tắt việc tính đi muộn cho toàn bộ công ty.
   */
  private async suyNgayNghi(emp: Employee, ngay: string): Promise<boolean> {
    const le = await this.ngayLe_Service.timTheoNgay(ngay);
    if (le) return true;

    const lich = lichTuanApDung(emp, await this.cauHinhChamCong_Service.lichTuanChung());
    if (!lich || lich.length === 0) return false;

    return !lich.includes(thuTrongTuanCuaNgay(ngay));
  }
```

- [ ] **Step 5: Đăng ký module cho cả hai**

Thêm `CauHinhChamCong_Module` vào `imports` của `don-cham-cong.module.ts` và `ban-ghi-cham-cong.module.ts`:

```ts
import { CauHinhChamCong_Module } from '../cau-hinh-cham-cong/cau-hinh-cham-cong.module';
```

- [ ] **Step 6: Thêm test cho bản ghi chấm công**

Thêm provider vào `TestingModule` của `ban-ghi-cham-cong.service.spec.ts`:

```ts
{
  provide: CauHinhChamCong_Service,
  useValue: { lichTuanChung: jest.fn().mockResolvedValue([1, 2, 3, 4, 5]) },
},
```

File này đã có test `'đánh dấu laNgayNghi khi ngày ngoài lịch làm việc của NV'` (dòng 306) dùng `USER`/`DTO`/`NV` và `nhanVien.resolveEmployeeFromUser.mockResolvedValue(...)`. Thêm test mới ngay sau nó, cùng idiom:

```ts
    it('NV không khai lịch riêng thì theo lịch công ty', async () => {
      const [nam, thang, ngay] = homNayVN().split('-').map(Number);
      const thuHomNay = new Date(Date.UTC(nam, thang - 1, ngay)).getUTCDay();
      const laCuoiTuan = thuHomNay === 0 || thuHomNay === 6;

      // Bỏ hẳn khai riêng ⇒ rơi về lịch chung T2–T6 đã mock ở provider.
      nhanVien.resolveEmployeeFromUser.mockResolvedValue({
        ...NV,
        ngayLamViecTrongTuan: undefined,
      });

      // Ngày chạy test rơi vào T7/CN thì hôm nay LÀ ngày nghỉ, ngược lại
      // thì không — khẳng định theo lịch chung chứ không cứng một giá trị,
      // vì `homNayVN()` đọc ngày thật của máy chạy test.
      expect((await service.checkIn(USER, DTO)).laNgayNghi).toBe(laCuoiTuan);
    });
```

- [ ] **Step 7: Chạy test, xác nhận PASS**

```bash
cd be && yarn test don-cham-cong && yarn test ban-ghi-cham-cong
```

Kỳ vọng: PASS toàn bộ.

- [ ] **Step 8: Chạy toàn bộ test BE**

```bash
cd be && yarn test
```

Kỳ vọng: PASS toàn bộ. Lưu ý: các test bcrypt của `auth-service` nằm sát mốc timeout 5s của Jest và có thể trượt tuỳ tải máy — nếu chỉ chúng đỏ, xác minh bằng cách chạy lại trên commit trước khi kết luận là do task này.

- [ ] **Step 9: Commit**

```bash
git add be/apps/config-service/src/don-cham-cong/ be/apps/config-service/src/ban-ghi-cham-cong/
git commit -m "feat(be): đơn từ + bản ghi chấm công đọc lịch tuần công ty

Đơn nghỉ bắc qua cuối tuần trừ đúng số ngày phép; đi muộn/về sớm không
còn bị tính cho T7/CN. Bốn module giờ cùng tin MỘT sự thật về lịch làm
việc của một người.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: Màn hình Cấu hình chấm công (FE)

**Files:**
- Create: `fe/src/services/cauHinhChamCongService.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/cauHinhChamCongHandler.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/CauHinhChamCongHandlerContext.tsx`
- Create: `fe/src/pages/cham-cong/cau-hinh/CauHinhChamCongPage.state.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/CauHinhChamCongPage.tsx`
- Create: `fe/src/pages/cham-cong/cau-hinh/sub-handler/index.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/sub-handler/init/init.event.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/sub-handler/init/init.handler.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/sub-handler/luu/luu.event.ts`
- Create: `fe/src/pages/cham-cong/cau-hinh/sub-handler/luu/luu.handler.ts`
- Modify: `fe/src/pages/loadable.tsx`
- Modify: `fe/src/App.tsx`
- Modify: `fe/src/components/layout/MainLayout.tsx`
- Modify: `fe/src/config/routePermissions.ts`
- Test: `fe/src/pages/cham-cong/cau-hinh/CauHinhChamCongPage.render.test.tsx`

**Interfaces:**
- Consumes: `GET`/`PUT /config/cau-hinh-cham-cong` (Task 1); quyền `/cham-cong/cau-hinh:xem|:sua` (Task 1).
- Produces: `interface CauHinhChamCong { id?: string; ngayLamViecTrongTuan: number[] }` và `cauHinhChamCongService` — Task 7 dùng lại service này.

- [ ] **Step 1: Viết service**

`fe/src/services/cauHinhChamCongService.ts`:

```ts
import { ServiceBase } from './base/service-base';

/** Cấu hình chấm công mức công ty. Khớp `CauHinhChamCong` bên BE. */
export interface CauHinhChamCong {
  id?: string;
  /** 0=CN … 6=T7. */
  ngayLamViecTrongTuan: number[];
}

class CauHinhChamCongService extends ServiceBase {
  constructor() {
    super({ endpoint: '/config/cau-hinh-cham-cong' });
  }

  async get(): Promise<CauHinhChamCong> {
    return super.get<CauHinhChamCong>();
  }

  /**
   * `forbidNonWhitelisted` ở `CapNhatCauHinhChamCongDto` chặn 400 nếu body có
   * field ngoài whitelist — mà object trong state đọc từ server còn kèm
   * `id`/`_id`/`tenantId`/`createdAt`/`updatedAt`/`isActive`. Lọc trước khi PUT.
   */
  async update(dto: Partial<CauHinhChamCong>): Promise<CauHinhChamCong> {
    const { id, _id, tenantId, createdAt, updatedAt, isActive, ...payload } =
      dto as Record<string, unknown>;
    return super.put<CauHinhChamCong>(payload);
  }
}

export const cauHinhChamCongService = new CauHinhChamCongService();
```

- [ ] **Step 2: Viết handler + context + state**

`cauHinhChamCongHandler.ts`:

```ts
import { CHanlder } from "@/common";
import { BaseStates } from "@/common/c-handler/core/actions/c-state.action";
import "./sub-handler";

export interface CauHinhChamCongEvents {}
export interface CauHinhChamCongStates extends BaseStates {}

export class CauHinhChamCongHandler extends CHanlder<
  CauHinhChamCongEvents,
  CauHinhChamCongStates
> {
  constructor() {
    super("cau-hinh-cham-cong-context");
  }
}
```

`CauHinhChamCongPage.state.ts`:

```ts
import { BaseStates } from "@/common/c-handler/core/actions/c-state.action";
import { CauHinhChamCong } from "@/services/cauHinhChamCongService";

export interface CauHinhChamCongPageStates extends BaseStates {
  cauHinh: CauHinhChamCong | null;
  dangTai: boolean;
  dangLuu: boolean;
}

declare module "./cauHinhChamCongHandler" {
  interface CauHinhChamCongStates extends CauHinhChamCongPageStates {}
}
```

`CauHinhChamCongHandlerContext.tsx` — chép nguyên `fe/src/pages/luong/cau-hinh-luong/CauHinhLuongHandlerContext.tsx`, đổi mọi `CauHinhLuong` thành `CauHinhChamCong`:

```tsx
import { createContext, useContext, useState, ReactNode } from "react";
import { CauHinhChamCongHandler, CauHinhChamCongStates } from "./cauHinhChamCongHandler";
import { useChandlerState } from "@/common/c-handler/hooks/use-chandler-state";
import { StateKey, StateValue } from "@/common/c-handler/core/actions/c-state.action";

const CauHinhChamCongHandlerContext = createContext<CauHinhChamCongHandler | null>(null);

export function CauHinhChamCongHandlerProvider({ children }: { children: ReactNode }) {
  const [handler] = useState(() => new CauHinhChamCongHandler());
  return (
    <CauHinhChamCongHandlerContext.Provider value={handler}>
      {children}
    </CauHinhChamCongHandlerContext.Provider>
  );
}

export function useCauHinhChamCongHandler() {
  const handler = useContext(CauHinhChamCongHandlerContext);
  if (!handler) {
    throw new Error(
      "useCauHinhChamCongHandler must be used within CauHinhChamCongHandlerProvider"
    );
  }
  return handler;
}

export function useCauHinhChamCongState<K extends StateKey<CauHinhChamCongStates>>(
  key: K,
  initialValue?: StateValue<CauHinhChamCongStates, K>
) {
  const handler = useCauHinhChamCongHandler();
  return useChandlerState<CauHinhChamCongStates, K>(key, handler, initialValue);
}
```

- [ ] **Step 3: Viết sub-handler init + luu**

`sub-handler/index.ts`:

```ts
import "./init/init.handler";
import "./luu/luu.handler";
```

`sub-handler/init/init.event.ts`:

```ts
import { BaseEvents } from "@/common";

export interface InitEvent extends BaseEvents {
  init: { params: Record<string, never>; result: void };
}

declare module "../../cauHinhChamCongHandler" {
  interface CauHinhChamCongEvents extends InitEvent {}
}
```

`sub-handler/init/init.handler.ts`:

```ts
import { message } from "antd";
import { HandlerDecorator, RegisterHandler } from "@/common";
import { CSubHanlder } from "@/common/c-handler/core/sub-handler.ts/sub-handler";
import { apiErrorMessage } from "@/config/api";
import { cauHinhChamCongService } from "@/services/cauHinhChamCongService";
import "./init.event";

@RegisterHandler("cau-hinh-cham-cong-context")
export class InitHandler extends CSubHanlder {
  @HandlerDecorator("init")
  async init(): Promise<void> {
    this.setState("dangLuu", false);
    this.setState("dangTai", true);
    try {
      const cauHinh = await cauHinhChamCongService.get();
      // Bồi mảng rỗng ngay khi vào state: repo KHÔNG có ErrorBoundary, nên
      // một `.map()` trên undefined ở component là trắng TOÀN trang.
      this.setState("cauHinh", {
        ...cauHinh,
        ngayLamViecTrongTuan: cauHinh.ngayLamViecTrongTuan ?? [],
      });
    } catch (error) {
      console.error("Tải cấu hình chấm công lỗi:", error);
      this.setState("cauHinh", null);
      message.error(
        apiErrorMessage(error, "Không thể tải cấu hình chấm công. Vui lòng thử lại.")
      );
    } finally {
      this.setState("dangTai", false);
    }
  }
}
```

`sub-handler/luu/luu.event.ts`:

```ts
import { BaseEvents } from "@/common";
import { CauHinhChamCong } from "@/services/cauHinhChamCongService";

export interface LuuEvent extends BaseEvents {
  luu: { params: { cauHinh: CauHinhChamCong }; result: void };
}

declare module "../../cauHinhChamCongHandler" {
  interface CauHinhChamCongEvents extends LuuEvent {}
}
```

`sub-handler/luu/luu.handler.ts`:

```ts
import { message } from "antd";
import { HandlerDecorator, RegisterHandler } from "@/common";
import { CSubHanlder } from "@/common/c-handler/core/sub-handler.ts/sub-handler";
import { apiErrorMessage } from "@/config/api";
import { cauHinhChamCongService } from "@/services/cauHinhChamCongService";
import type { CauHinhChamCong } from "@/services/cauHinhChamCongService";
import "./luu.event";

@RegisterHandler("cau-hinh-cham-cong-context")
export class LuuHandler extends CSubHanlder {
  @HandlerDecorator("luu")
  async luu(params: { cauHinh: CauHinhChamCong }): Promise<void> {
    this.setState("dangLuu", true);
    try {
      const updated = await cauHinhChamCongService.update(params.cauHinh);
      this.setState("cauHinh", {
        ...updated,
        ngayLamViecTrongTuan: updated.ngayLamViecTrongTuan ?? [],
      });
      message.success("Lưu cấu hình chấm công thành công!");
    } catch (error) {
      console.error("Lưu cấu hình chấm công lỗi:", error);
      message.error(apiErrorMessage(error, "Không thể lưu cấu hình chấm công"));
    } finally {
      this.setState("dangLuu", false);
    }
  }
}
```

- [ ] **Step 4: Viết trang**

`CauHinhChamCongPage.tsx`:

```tsx
import { useEffect } from "react";
import { Alert, Button, Card, Checkbox, Empty, Spin } from "antd";
import { SaveOutlined } from "@ant-design/icons";
import {
  CauHinhChamCongHandlerProvider,
  useCauHinhChamCongHandler,
  useCauHinhChamCongState,
} from "./CauHinhChamCongHandlerContext";
import { usePagePermission } from "@/hooks/usePagePermission";
import type { CauHinhChamCong } from "@/services/cauHinhChamCongService";
import "./CauHinhChamCongPage.state";

const NGAY_TRONG_TUAN_OPTIONS = [
  { label: "Thứ 2", value: 1 },
  { label: "Thứ 3", value: 2 },
  { label: "Thứ 4", value: 3 },
  { label: "Thứ 5", value: 4 },
  { label: "Thứ 6", value: 5 },
  { label: "Thứ 7", value: 6 },
  { label: "Chủ nhật", value: 0 },
];

function CauHinhChamCongPageInner() {
  const handler = useCauHinhChamCongHandler();
  // Setter lấy từ chính hook (cùng cách `HangSoEditor.tsx` đang làm) —
  // KHÔNG gọi `handler.setState(...)` trực tiếp từ component.
  const [cauHinh, setCauHinh] = useCauHinhChamCongState(
    "cauHinh",
    null as CauHinhChamCong | null
  );
  const [dangTai] = useCauHinhChamCongState("dangTai", false);
  const [dangLuu] = useCauHinhChamCongState("dangLuu", false);
  const { canEdit } = usePagePermission("/cham-cong/cau-hinh");

  useEffect(() => {
    handler.executeEvent("init", {});
  }, [handler]);

  const handleLuu = () => {
    if (!cauHinh) return;
    handler.executeEvent("luu", { cauHinh });
  };

  const handleDoiLich = (value: number[]) => {
    if (!cauHinh) return;
    setCauHinh({ ...cauHinh, ngayLamViecTrongTuan: value });
  };

  return (
    <div className="space-y-3">
      <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
        <div>
          <h1 className="text-2xl font-bold text-foreground">Cấu hình chấm công</h1>
          <p className="text-muted-foreground">
            Lịch làm việc trong tuần áp dụng cho toàn công ty
          </p>
        </div>
        {canEdit && (
          <Button
            type="primary"
            icon={<SaveOutlined />}
            loading={dangLuu}
            disabled={!cauHinh}
            onClick={handleLuu}
          >
            Lưu cấu hình
          </Button>
        )}
      </div>

      <Spin spinning={dangTai}>
        {cauHinh ? (
          <Card>
            <label className="block mb-2 text-sm font-medium">
              Ngày làm việc trong tuần
            </label>
            <Checkbox.Group
              options={NGAY_TRONG_TUAN_OPTIONS}
              value={cauHinh.ngayLamViecTrongTuan ?? []}
              onChange={(v) => handleDoiLich(v as number[])}
              disabled={!canEdit}
            />
            <div className="mt-3 text-xs text-gray-500">
              Ngày không tích sẽ để trống trên bảng công (không tính công, không
              chặn chốt), và không tính vào số ngày làm việc chuẩn của tháng khi
              xét ngưỡng tích phép năm. Nhân viên có lịch khác thì khai riêng ở
              hồ sơ — khai riêng luôn thắng lịch chung.
            </div>
            {(cauHinh.ngayLamViecTrongTuan ?? []).length === 0 && (
              <Alert
                type="warning"
                showIcon
                className="mt-3"
                message="Chưa tích ngày nào"
                description="Bỏ trống hoàn toàn nghĩa là chưa cấu hình: hệ thống sẽ coi MỌI ngày đều là ngày làm việc, kể cả T7 và Chủ nhật. Đó thường không phải điều bạn muốn."
              />
            )}
          </Card>
        ) : (
          !dangTai && (
            <Card>
              <Empty description="Không thể tải cấu hình chấm công" />
            </Card>
          )
        )}
      </Spin>
    </div>
  );
}

const CauHinhChamCongPage: React.FC = () => {
  return (
    <CauHinhChamCongHandlerProvider>
      <CauHinhChamCongPageInner />
    </CauHinhChamCongHandlerProvider>
  );
};

export default CauHinhChamCongPage;
```

- [ ] **Step 5: Nối route, menu, quyền**

`fe/src/pages/loadable.tsx` — thêm:

```tsx
export const CauHinhChamCongPage = loadable(() => import('./cham-cong/cau-hinh/CauHinhChamCongPage'), {
  fallback: <PageLoader />
});
```

`fe/src/App.tsx` — thêm `CauHinhChamCongPage` vào khối import từ `./pages/loadable`, và thêm route trong nhóm `<Route path="cham-cong">`:

```tsx
                  <Route
                    path="cau-hinh"
                    element={
                      <ProtectedRoute requiredPermission="/cham-cong/cau-hinh:xem">
                        <CauHinhChamCongPage />
                      </ProtectedRoute>
                    }
                  />
```

`fe/src/config/routePermissions.ts` — thêm cạnh các dòng `/cham-cong/*`:

```ts
  '/cham-cong/cau-hinh': '/cham-cong/cau-hinh:xem',
```

`fe/src/components/layout/MainLayout.tsx` — thêm biến quyền cạnh nhóm ở `:171-179`:

```tsx
  const canViewCauHinhChamCong = hasPermission('/cham-cong/cau-hinh:xem') || user?.isSuperAdmin;
```

và mục menu **cuối** nhóm Chấm công, ngay sau mục `/cham-cong/ban-ghi` (cấu hình đứng cuối, sau các mục nghiệp vụ dùng hằng ngày):

```tsx
        ...(canViewCauHinhChamCong ? [{
          key: "/cham-cong/cau-hinh",
          icon: <SettingOutlined />,
          label: "Cấu hình chấm công",
        }] : []),
```

Thêm `SettingOutlined` vào khối import `@ant-design/icons` của file nếu chưa có.

- [ ] **Step 6: Viết test render**

`fe/src/pages/cham-cong/cau-hinh/CauHinhChamCongPage.render.test.tsx` — chép khuôn từ `fe/src/pages/luong/cau-hinh-luong/CauHinhLuongPage.render.test.tsx` (đọc file đó để lấy đúng cách mock service và AuthContext), khẳng định:

```tsx
it('hiện 7 ô ngày và tích sẵn T2–T6 từ server', async () => {
  render(<CauHinhChamCongPage />);
  expect(await screen.findByText('Thứ 2')).toBeInTheDocument();
  expect(screen.getByText('Chủ nhật')).toBeInTheDocument();
  expect((screen.getByLabelText('Thứ 2') as HTMLInputElement).checked).toBe(true);
  expect((screen.getByLabelText('Chủ nhật') as HTMLInputElement).checked).toBe(false);
});

it('cảnh báo khi không tích ngày nào', async () => {
  // mock service trả { ngayLamViecTrongTuan: [] }
  render(<CauHinhChamCongPage />);
  expect(await screen.findByText('Chưa tích ngày nào')).toBeInTheDocument();
});
```

- [ ] **Step 7: Chạy test FE, xác nhận PASS**

```bash
cd fe && nvm use && npm run test
```

Kỳ vọng: PASS toàn bộ.

- [ ] **Step 8: Commit**

```bash
git add fe/src/services/cauHinhChamCongService.ts fe/src/pages/cham-cong/cau-hinh/ \
        fe/src/pages/loadable.tsx fe/src/App.tsx \
        fe/src/components/layout/MainLayout.tsx fe/src/config/routePermissions.ts
git commit -m "feat(fe): màn Cấu hình chấm công — lịch làm việc tuần công ty

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: Sửa chú thích lịch tuần trên hồ sơ nhân viên

**Files:**
- Modify: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/ChamCongTab.tsx:100-119`
- Test: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/ChamCongTab.render.test.tsx` (tạo nếu chưa có)

**Interfaces:**
- Consumes: `cauHinhChamCongService` (Task 6).
- Produces: không có.

Dòng chú thích hiện tại **nói ngược với hành vi mới** — để nguyên là để lại một lời nói dối trong sản phẩm:

> "Bỏ trống nghĩa là chưa cấu hình — hệ thống sẽ không coi ngày nào là ngày nghỉ (không phải nghỉ tất cả các ngày)."

- [ ] **Step 1: Viết test thất bại**

```tsx
it('chú thích lịch tuần nêu đúng lịch chung công ty đọc từ server', async () => {
  // mock cauHinhChamCongService.get() → { ngayLamViecTrongTuan: [1,2,3,4,5] }
  renderChamCongTab();
  expect(
    await screen.findByText(/theo lịch chung của công ty \(T2, T3, T4, T5, T6\)/)
  ).toBeInTheDocument();
});
```

- [ ] **Step 2: Chạy test để xác nhận nó fail**

```bash
cd fe && npm run test ChamCongTab
```

Kỳ vọng: FAIL — chuỗi chưa tồn tại.

- [ ] **Step 3: Sửa component**

Thêm state + fetch trong `ChamCongTab()`, cạnh hai `useEffect` sẵn có:

```tsx
  const [lichChung, setLichChung] = useState<number[] | null>(null);
```

trong `useEffect` đã có, thêm:

```tsx
    cauHinhChamCongService
      .get()
      .then((ch) => setLichChung(ch.ngayLamViecTrongTuan ?? []))
      .catch((e) => {
        console.error("Tải cấu hình chấm công lỗi:", e);
        setLichChung(null);
      });
```

Thêm helper trên component:

```tsx
const TEN_THU: Record<number, string> = {
  0: "CN", 1: "T2", 2: "T3", 3: "T4", 4: "T5", 5: "T6", 6: "T7",
};

/** [1,2,3,4,5] → "T2, T3, T4, T5, T6". Rỗng/lỗi mạng → null (ẩn phần trong ngoặc). */
function moTaLich(lich: number[] | null): string | null {
  if (!lich || lich.length === 0) return null;
  return [...lich].sort((a, b) => (a === 0 ? 7 : a) - (b === 0 ? 7 : b))
    .map((t) => TEN_THU[t])
    .join(", ");
}
```

Thay khối chú thích ở `:115-118`:

```tsx
        <div className="mt-1 text-xs text-gray-500">
          Bỏ trống = theo lịch chung của công ty
          {moTaLich(lichChung) ? ` (${moTaLich(lichChung)})` : ""}. Chỉ khai ở
          đây khi người này làm khác lịch chung.
        </div>
```

Không hard-code chuỗi "T2–T6": lịch công ty sửa được ở màn Cấu hình chấm công, hard-code là để lại đúng loại lời nói dối mà chính dòng này đang mắc. Lỗi mạng → `lichChung` là `null` → phần trong ngoặc biến mất, câu vẫn đọc được.

Thêm import:

```tsx
import { cauHinhChamCongService } from "@/services/cauHinhChamCongService";
```

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd fe && npm run test
```

Kỳ vọng: PASS toàn bộ.

- [ ] **Step 5: Build FE để bắt lỗi kiểu**

```bash
cd fe && npm run build
```

Kỳ vọng: build thành công.

- [ ] **Step 6: Commit**

```bash
git add fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/
git commit -m "fix(fe): chú thích lịch tuần trên hồ sơ nêu đúng lịch công ty

Dòng cũ nói 'bỏ trống = không coi ngày nào là ngày nghỉ' — sai kể từ
P4.5. Đọc lịch chung từ server thay vì hard-code chuỗi.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Sau khi xong plan — việc vận hành (không phải task code)

1. Deploy BE + FE.
2. **Bắt buộc:** chạy trong container `nhan-su-be`:
   ```bash
   MONGODB_URI=... MONGODB_DATABASE=nhan_su npx ts-node ops/grant-quyen-module-moi.ts --dry-run
   MONGODB_URI=... MONGODB_DATABASE=nhan_su npx ts-node ops/grant-quyen-module-moi.ts
   ```
   Không chạy thì màn Cấu hình chấm công 403 với mọi vai trò.
3. Vào **Chấm công → Cấu hình chấm công**, xác nhận đã tích sẵn T2–T6.
4. Với tháng 6/2026 rồi 7/2026: **Mở lại** (nếu đã chốt) → **Tổng hợp bảng công** → rà các ô cảnh báo vàng `LAM_NGOAI_LICH_TUAN` (đó là ngày công thứ Bảy sắp mất — sửa tay thành `X` hoặc yêu cầu nộp đơn làm thêm) → xử nốt ô trống thật → **Chốt**.
5. Sau đó mới tổng hợp được bảng lương. Nhắc lại: đơn giá ngày vẫn chia cho `congChuan = 24` cho tới P4.8.

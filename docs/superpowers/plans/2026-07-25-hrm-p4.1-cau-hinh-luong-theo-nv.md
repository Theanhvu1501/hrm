# P4.1 — Cấu hình lương riêng theo NLĐ + HĐLĐ thứ 2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho phép config `congChuan` / tỷ lệ thử việc / tỷ lệ + căn cứ BHXH **riêng từng nhân viên** (để trống = kế thừa `CauHinhLuong`), thêm cờ **HĐLĐ thứ 2** (NLĐ không bị trừ BHXH, không giảm trừ gia cảnh, công ty chịu 0,5%) và đưa **chi phí BH công ty** vào kết quả engine.

**Architecture:** Engine `tinhDongLuong(dv, ch)` giữ nguyên chữ ký và vẫn thuần. Phần cá nhân hoá nằm ở một hàm gộp cấu hình chạy **trước** engine: `ganCauHinhRieng(ch, ov) → CauHinhLuongData`. `bang-luong.service` gọi hàm này cho mỗi NV rồi mới chạy engine hai lần (khai báo / thực tế) trên cấu hình đã gộp, và snapshot giá trị đã resolve vào `DongLuong.cauHinhApDung` để kỳ đã chốt tính lại vẫn ra đúng số.

**Tech Stack:** NestJS + TypeORM (MongoDB) + Jest (`be/`, Node 22, yarn) — React + Vite + antd + react-hook-form + Vitest (`fe/`, npm).

**Spec:** `docs/superpowers/specs/2026-07-25-hrm-p4.1-cau-hinh-luong-theo-nv-design.md`

## Global Constraints

- **Không hằng số nghiệp vụ trong code.** Mọi con số (21,5% / 0,5% / 24 / 85%) chỉ được xuất hiện trong `cau-hinh-luong.seed.ts` hoặc trong test. Engine đọc từ cấu hình.
- **Dùng `??`, KHÔNG dùng `||`** cho mọi giá trị số/boolean đọc từ BE hoặc từ override: `0` và `false` là giá trị hợp lệ (tỷ lệ thử việc 0%, không đóng BH).
- **Đơn vị tỷ lệ:** lưu 0..1 ở BE và trong mọi payload. FE nhập/hiện theo % và quy đổi `/100` khi gửi, `×100` khi nạp.
- **`thucLinh` không đổi.** Chi phí BH công ty là số quản trị, không phải khoản trừ của NLĐ.
- **Tương thích ngược:** NV không config riêng ⇒ kết quả y nguyên như trước. Không migration dữ liệu. Dòng `DongLuong` đã `chot` không bị tính lại.
- **Guard:** không thêm route mới ⇒ không chạy lại `ops/grant-quyen-module-moi.ts`. Giữ khuôn `JwtGuard` + `PermissionGuard` + `@Permissions(...)`.
- **Node 20 trên image Docker** (dev là Node 22) — không dùng API chỉ có từ Node 21+.
- Chạy test BE: `cd be && npx jest <đường dẫn spec>`. Chạy test FE: `cd fe && npx vitest run <đường dẫn test>`.

---

### Task 1: Sửa DTO nhân viên — bug chặn 400 (`forbidNonWhitelisted`)

`config-service/src/main.ts:50` bật `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })`, nhưng `CreateEmployeeDto` **không khai** bất kỳ trường lương nào. Hệ quả: tab "Lương" trong Hồ sơ NV gửi `PUT /nhan-vien/:id` → **400**, không lưu được gì. Task này phải xong trước mọi task khác, nếu không cả spec cũng không lưu được.

**Files:**
- Modify: `be/apps/config-service/src/nhan-vien/dto/create-employee.dto.ts`
- Modify: `be/apps/config-service/src/bang-luong/dto/cap-nhat-cau-hinh-luong.dto.ts`
- Test: `be/apps/config-service/src/nhan-vien/dto/create-employee.dto.spec.ts` (đã tồn tại — thêm describe mới)

**Interfaces:**
- Consumes: không gì (task đầu).
- Produces: `CauHinhLuongRiengDto` (4 trường optional: `congChuan?: number`, `thuViecTyLe?: number`, `bhxhTyLe?: number`, `bhxhCanCu?: 'MUC_KHAI_BAO' | 'LUONG_THOA_THUAN'`); `CreateEmployeeDto` nhận thêm 9 trường: `luongThoaThuan?`, `mucKhaiBao?`, `phuCapCoDinh?`, `soNguoiPhuThuoc?`, `dongBH?`, `thoiVu?`, `camKet?`, `hopDongThu2?: boolean`, `cauHinhLuongRieng?: CauHinhLuongRiengDto`.

- [ ] **Step 1: Viết test thất bại — dùng chính `ValidationPipe` của app**

Test hiện có gọi `validate(dto)` trực tiếp, **không** bắt được lỗi `forbidNonWhitelisted` (lỗi đó xảy ra ở tầng pipe, không ở tầng `validate`). Thêm vào cuối `create-employee.dto.spec.ts`:

```ts
import { ArgumentMetadata, ValidationPipe } from '@nestjs/common';

/**
 * Bug đã từng xảy ra: 7 trường lương của P4-A có trong entity + FE nhưng
 * KHÔNG có trong DTO, mà app bật forbidNonWhitelisted → PUT /nhan-vien/:id
 * trả 400 và tab Lương im lặng không lưu được. `validate(dto)` không thấy
 * lỗi này vì nó xảy ra ở tầng ValidationPipe. Test dưới dựng đúng pipe của
 * `main.ts` để canh.
 */
describe('CreateEmployeeDto — qua ValidationPipe như main.ts', () => {
  const pipe = new ValidationPipe({
    whitelist: true,
    transform: true,
    forbidNonWhitelisted: true,
  });
  const meta: ArgumentMetadata = {
    type: 'body',
    metatype: CreateEmployeeDto,
  };

  it('không chặn payload đầy đủ trường lương (P4-A + P4.1)', async () => {
    const payload = {
      ...BASE_HOP_LE,
      luongThoaThuan: 15_000_000,
      mucKhaiBao: 5_500_000,
      phuCapCoDinh: 500_000,
      soNguoiPhuThuoc: 2,
      dongBH: true,
      thoiVu: false,
      camKet: false,
      hopDongThu2: true,
      cauHinhLuongRieng: {
        congChuan: 26,
        thuViecTyLe: 0.9,
        bhxhTyLe: 0.105,
        bhxhCanCu: 'LUONG_THOA_THUAN',
      },
    };

    const ketQua = await pipe.transform(payload, meta);

    expect(ketQua).toMatchObject(payload);
  });

  it('giữ được giá trị false/0 (không bị coi là thiếu)', async () => {
    const ketQua = await pipe.transform(
      { ...BASE_HOP_LE, dongBH: false, luongThoaThuan: 0, cauHinhLuongRieng: { thuViecTyLe: 0 } },
      meta,
    );

    expect(ketQua.dongBH).toBe(false);
    expect(ketQua.luongThoaThuan).toBe(0);
    expect(ketQua.cauHinhLuongRieng?.thuViecTyLe).toBe(0);
  });

  it('vẫn chặn trường lạ ngoài whitelist', async () => {
    await expect(
      pipe.transform({ ...BASE_HOP_LE, khongCoTruongNay: 1 }, meta),
    ).rejects.toThrow();
  });

  it('từ chối tỷ lệ ngoài [0,1] và bhxhCanCu lạ', async () => {
    await expect(
      pipe.transform({ ...BASE_HOP_LE, cauHinhLuongRieng: { thuViecTyLe: 90 } }, meta),
    ).rejects.toThrow();
    await expect(
      pipe.transform({ ...BASE_HOP_LE, cauHinhLuongRieng: { bhxhCanCu: 'LUNG_TUNG' } }, meta),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận thất bại**

Run: `cd be && npx jest apps/config-service/src/nhan-vien/dto/create-employee.dto.spec.ts`
Expected: FAIL — ca đầu báo `BadRequestException: property luongThoaThuan should not exist` (chính là bug đang có trên production).

- [ ] **Step 3: Thêm các trường vào `create-employee.dto.ts`**

Bổ sung import (giữ nguyên phần import cũ, thêm những cái còn thiếu):

```ts
import {
  IsString, IsNotEmpty, IsOptional, IsEmail, IsIn, IsArray, IsObject,
  IsInt, IsBoolean, IsNumber, ValidateNested, Min, Max,
} from 'class-validator';
import { Type } from 'class-transformer';
```

Thêm class override **trước** `CreateEmployeeDto`:

```ts
/**
 * Override cấu hình lương cho riêng một NV. Trường vắng mặt = kế thừa
 * `CauHinhLuong` của công ty (xem `ganCauHinhRieng`). Tỷ lệ lưu 0..1.
 */
export class CauHinhLuongRiengDto {
  @IsOptional()
  @IsNumber()
  @Min(1)
  congChuan?: number;

  @IsOptional()
  @IsNumber()
  @Min(0)
  @Max(1)
  thuViecTyLe?: number;

  @IsOptional()
  @IsNumber()
  @Min(0)
  @Max(1)
  bhxhTyLe?: number;

  @IsOptional()
  @IsIn(['MUC_KHAI_BAO', 'LUONG_THOA_THUAN'])
  bhxhCanCu?: 'MUC_KHAI_BAO' | 'LUONG_THOA_THUAN';
}
```

Thêm vào cuối `CreateEmployeeDto` (sau `choPhepChamNgoaiVung`):

```ts
  // ── Lương (P4-A) ── entity + FE đã có từ P4-A nhưng DTO thì chưa: thiếu
  // ở đây là 400 thẳng vì app bật `forbidNonWhitelisted` (main.ts).
  @IsOptional()
  @IsNumber()
  @Min(0)
  luongThoaThuan?: number;

  @IsOptional()
  @IsNumber()
  @Min(0)
  mucKhaiBao?: number;

  @IsOptional()
  @IsNumber()
  @Min(0)
  phuCapCoDinh?: number;

  @IsOptional()
  @IsInt()
  @Min(0)
  soNguoiPhuThuoc?: number;

  @IsOptional()
  @IsBoolean()
  dongBH?: boolean;

  @IsOptional()
  @IsBoolean()
  thoiVu?: boolean;

  @IsOptional()
  @IsBoolean()
  camKet?: boolean;

  // ── Lương (P4.1) ──
  /** HĐLĐ thứ 2: NLĐ không đóng BH tại đây, không giảm trừ gia cảnh tại đây,
   *  công ty chỉ chịu BHTNLĐ-BNN (`bhCongTy.tyLeHopDongThu2`). */
  @IsOptional()
  @IsBoolean()
  hopDongThu2?: boolean;

  @IsOptional()
  @IsObject()
  @ValidateNested()
  @Type(() => CauHinhLuongRiengDto)
  cauHinhLuongRieng?: CauHinhLuongRiengDto;
```

- [ ] **Step 4: Cho phép `bhCongTy` trong `CapNhatCauHinhLuongDto`**

Đọc file `be/apps/config-service/src/bang-luong/dto/cap-nhat-cau-hinh-luong.dto.ts` và thêm trường theo đúng style các trường object đang có ở đó (`bhxh`, `quyTacThoiVu`…):

```ts
  @IsOptional()
  @IsObject()
  bhCongTy?: { tyLe: number; tyLeHopDongThu2: number };
```

- [ ] **Step 5: Chạy test để xác nhận PASS**

Run: `cd be && npx jest apps/config-service/src/nhan-vien/dto/create-employee.dto.spec.ts`
Expected: PASS toàn bộ (cả describe `choPhepChamNgoaiVung` cũ).

- [ ] **Step 6: Commit**

```bash
git add be/apps/config-service/src/nhan-vien/dto/create-employee.dto.ts \
        be/apps/config-service/src/nhan-vien/dto/create-employee.dto.spec.ts \
        be/apps/config-service/src/bang-luong/dto/cap-nhat-cau-hinh-luong.dto.ts
git commit -m "fix(be): khai đủ trường lương trong CreateEmployeeDto — tab Lương đang bị 400

forbidNonWhitelisted chặn 7 trường lương của P4-A (luongThoaThuan, mucKhaiBao,
phuCapCoDinh, soNguoiPhuThuoc, dongBH, thoiVu, camKet) vì DTO không khai. Thêm
luôn hopDongThu2 + cauHinhLuongRieng của P4.1 và bhCongTy cho cấu hình lương.
Test dựng đúng ValidationPipe của main.ts để canh — validate(dto) không thấy
lỗi tầng pipe."
```

---

### Task 2: `CauHinhLuongRieng` + `ganCauHinhRieng`

**Files:**
- Modify: `be/libs/entities/src/luong/luong.types.ts` (thêm `CauHinhLuongRieng`, `CauHinhLuongApDung`)
- Create: `be/libs/core/src/luong/gan-cau-hinh-rieng.ts`
- Create: `be/libs/core/src/luong/gan-cau-hinh-rieng.spec.ts`
- Modify: `be/libs/core/src/index.ts` (thêm export)

**Interfaces:**
- Consumes: `CauHinhLuongData` (đã có trong `@app/entities`).
- Produces: `ganCauHinhRieng(ch: CauHinhLuongData, ov?: CauHinhLuongRieng): CauHinhLuongData` — export từ `@app/core`. Types `CauHinhLuongRieng` (4 trường optional như Task 1) và `CauHinhLuongApDung = Required<CauHinhLuongRieng>` — export từ `@app/entities`.

- [ ] **Step 1: Thêm type vào `luong.types.ts`**

Thêm vào cuối file `be/libs/entities/src/luong/luong.types.ts`:

```ts
/**
 * Override cấu hình lương cho RIÊNG một NV. Trường `undefined` = kế thừa
 * `CauHinhLuong` của công ty.
 *
 * Chỉ chứa tham số mang tính THỎA THUẬN riêng từng người. Tham số là LUẬT
 * (giảm trừ, bậc thuế, quy tắc thời vụ/cam kết) và quy ước trình bày
 * (`lamTron`) cố ý KHÔNG có ở đây — xem spec P4.1 §4.
 */
export interface CauHinhLuongRieng {
  congChuan?: number;
  thuViecTyLe?: number; // 0..1, cùng đơn vị CauHinhLuong.thuViec.tyLe
  bhxhTyLe?: number; // 0..1
  bhxhCanCu?: 'MUC_KHAI_BAO' | 'LUONG_THOA_THUAN';
}

/** Giá trị đã resolve (không còn `undefined`) — snapshot vào `DongLuong`. */
export type CauHinhLuongApDung = Required<CauHinhLuongRieng>;
```

- [ ] **Step 2: Viết test thất bại**

Create `be/libs/core/src/luong/gan-cau-hinh-rieng.spec.ts`:

```ts
import { ganCauHinhRieng } from './gan-cau-hinh-rieng';
import { CauHinhLuongData } from '@app/entities';

function cauHinh(): CauHinhLuongData {
  return {
    mucKhaiBaoMacDinh: 5_500_000,
    congChuan: 24,
    khoanLuong: [],
    giamTruBanThan: 15_500_000,
    giamTruNPT: 6_200_000,
    bhxh: { tyLe: 0.105, canCu: 'MUC_KHAI_BAO' },
    bacThue: [{ den: null, suat: 0.1 }],
    thuViec: { tyLe: 0.85 },
    quyTacThoiVu: { tyLe: 0.1, nguong: 2_000_000 },
    quyTacCamKet: { mienThue: true },
    bhCongTy: { tyLe: 0.215, tyLeHopDongThu2: 0.005 },
    lamTron: 1000,
  };
}

describe('ganCauHinhRieng', () => {
  it('không có override → giữ nguyên mọi giá trị', () => {
    const ch = cauHinh();
    expect(ganCauHinhRieng(ch, undefined)).toEqual(ch);
    expect(ganCauHinhRieng(ch, {})).toEqual(ch);
  });

  it('override từng trường một — chỉ trường đó đổi', () => {
    const ch = cauHinh();
    expect(ganCauHinhRieng(ch, { congChuan: 26 })).toEqual({ ...ch, congChuan: 26 });
    expect(ganCauHinhRieng(ch, { thuViecTyLe: 0.9 })).toEqual({ ...ch, thuViec: { tyLe: 0.9 } });
    expect(ganCauHinhRieng(ch, { bhxhTyLe: 0.095 })).toEqual({
      ...ch,
      bhxh: { tyLe: 0.095, canCu: 'MUC_KHAI_BAO' },
    });
    expect(ganCauHinhRieng(ch, { bhxhCanCu: 'LUONG_THOA_THUAN' })).toEqual({
      ...ch,
      bhxh: { tyLe: 0.105, canCu: 'LUONG_THOA_THUAN' },
    });
  });

  it('override nhiều trường cùng lúc', () => {
    const r = ganCauHinhRieng(cauHinh(), {
      congChuan: 26,
      thuViecTyLe: 0.9,
      bhxhTyLe: 0,
      bhxhCanCu: 'LUONG_THOA_THUAN',
    });
    expect(r.congChuan).toBe(26);
    expect(r.thuViec.tyLe).toBe(0.9);
    expect(r.bhxh).toEqual({ tyLe: 0, canCu: 'LUONG_THOA_THUAN' });
  });

  it('tỷ lệ 0 là HỢP LỆ — không bị `||` nuốt thành mặc định', () => {
    const r = ganCauHinhRieng(cauHinh(), { thuViecTyLe: 0, bhxhTyLe: 0 });
    expect(r.thuViec.tyLe).toBe(0);
    expect(r.bhxh.tyLe).toBe(0);
  });

  it('giá trị vô lý → bỏ qua, rơi về cấu hình chung', () => {
    const ch = cauHinh();
    expect(ganCauHinhRieng(ch, { congChuan: 0 }).congChuan).toBe(24);
    expect(ganCauHinhRieng(ch, { congChuan: -5 }).congChuan).toBe(24);
    expect(ganCauHinhRieng(ch, { congChuan: NaN }).congChuan).toBe(24);
    expect(ganCauHinhRieng(ch, { thuViecTyLe: 1.5 }).thuViec.tyLe).toBe(0.85);
    expect(ganCauHinhRieng(ch, { thuViecTyLe: -0.1 }).thuViec.tyLe).toBe(0.85);
    expect(ganCauHinhRieng(ch, { bhxhTyLe: 90 }).bhxh.tyLe).toBe(0.105);
    expect(
      ganCauHinhRieng(ch, { bhxhCanCu: 'LUNG_TUNG' as never }).bhxh.canCu,
    ).toBe('MUC_KHAI_BAO');
  });

  it('không mutate cấu hình gốc', () => {
    const ch = cauHinh();
    const goc = JSON.parse(JSON.stringify(ch));
    ganCauHinhRieng(ch, { congChuan: 26, thuViecTyLe: 0.9, bhxhTyLe: 0.2 });
    expect(ch).toEqual(goc);
  });
});
```

- [ ] **Step 3: Chạy test để xác nhận thất bại**

Run: `cd be && npx jest libs/core/src/luong/gan-cau-hinh-rieng.spec.ts`
Expected: FAIL — `Cannot find module './gan-cau-hinh-rieng'`.

- [ ] **Step 4: Viết implementation**

Create `be/libs/core/src/luong/gan-cau-hinh-rieng.ts`:

```ts
import type { CauHinhLuongData, CauHinhLuongRieng } from '@app/entities';

/** Số dương hữu hạn, ngược lại `undefined` để caller rơi về mặc định. */
function soDuong(v: unknown): number | undefined {
  return typeof v === 'number' && Number.isFinite(v) && v > 0 ? v : undefined;
}

/** Tỷ lệ trong [0,1]. `0` là giá trị HỢP LỆ (thử việc 0%, BH 0%). */
function tyLe(v: unknown): number | undefined {
  return typeof v === 'number' && Number.isFinite(v) && v >= 0 && v <= 1
    ? v
    : undefined;
}

function canCu(v: unknown): CauHinhLuongData['bhxh']['canCu'] | undefined {
  return v === 'MUC_KHAI_BAO' || v === 'LUONG_THOA_THUAN' ? v : undefined;
}

/**
 * Gộp cấu hình lương RIÊNG của một NV lên cấu hình chung của công ty, chạy
 * TRƯỚC `tinhDongLuong`. Nhờ tách ra đây, engine giữ nguyên chữ ký và vẫn
 * thuần — nó không cần biết gì về `Employee`.
 *
 * Trả về BẢN SAO, không mutate `ch`: cùng một `ch` được gộp lại cho từng NV
 * trong một vòng lặp tổng hợp kỳ, mutate là làm hỏng những NV sau.
 *
 * Giá trị override vô lý (công chuẩn ≤ 0, tỷ lệ ngoài [0,1], căn cứ lạ) bị
 * BỎ QUA và rơi về cấu hình chung — chọn "tính bằng số của công ty" thay vì
 * ném lỗi giữa lúc chạy kỳ lương cho toàn bộ nhân sự.
 */
export function ganCauHinhRieng(
  ch: CauHinhLuongData,
  ov?: CauHinhLuongRieng,
): CauHinhLuongData {
  const o = ov ?? {};
  return {
    ...ch,
    congChuan: soDuong(o.congChuan) ?? ch.congChuan,
    thuViec: { ...ch.thuViec, tyLe: tyLe(o.thuViecTyLe) ?? ch.thuViec.tyLe },
    bhxh: {
      ...ch.bhxh,
      tyLe: tyLe(o.bhxhTyLe) ?? ch.bhxh.tyLe,
      canCu: canCu(o.bhxhCanCu) ?? ch.bhxh.canCu,
    },
  };
}
```

- [ ] **Step 5: Export từ `@app/core`**

Modify `be/libs/core/src/index.ts` — thêm dòng ngay dưới `export * from './luong/tinh-luong';`:

```ts
export * from './luong/gan-cau-hinh-rieng';
```

- [ ] **Step 6: Chạy test để xác nhận PASS**

Run: `cd be && npx jest libs/core/src/luong/gan-cau-hinh-rieng.spec.ts`
Expected: PASS (6 ca).

Lưu ý: ca test dùng `bhCongTy` trong `cauHinh()` — trường này thêm vào type ở Task 3. `ts-jest` ở repo này chỉ transpile (không type-check) nên test vẫn chạy xanh; đừng bỏ trường đó ra.

- [ ] **Step 7: Commit**

```bash
git add be/libs/entities/src/luong/luong.types.ts \
        be/libs/core/src/luong/gan-cau-hinh-rieng.ts \
        be/libs/core/src/luong/gan-cau-hinh-rieng.spec.ts \
        be/libs/core/src/index.ts
git commit -m "feat(be): ganCauHinhRieng — gộp cấu hình lương riêng của NV lên cấu hình chung

Trường undefined = kế thừa; giá trị vô lý (công chuẩn <= 0, tỷ lệ ngoài [0,1])
rơi về cấu hình chung thay vì ném lỗi giữa kỳ lương. Trả bản sao, không mutate."
```

---

### Task 3: Engine — chi phí BH công ty + HĐLĐ thứ 2

**Files:**
- Modify: `be/libs/entities/src/luong/luong.types.ts` (`CauHinhLuongData.bhCongTy`, `DauVaoDongLuong.hopDongThu2`, `KetQuaLuong.chiPhiBHCongTy` + `.tongChiPhiCongTy`)
- Modify: `be/libs/entities/src/luong/cau-hinh-luong.entity.ts`
- Modify: `be/apps/config-service/src/bang-luong/cau-hinh-luong.seed.ts`
- Modify: `be/libs/core/src/luong/tinh-luong.ts:91-117`
- Test: `be/libs/core/src/luong/tinh-luong.spec.ts`

**Interfaces:**
- Consumes: `tinhDongLuong(dv, ch)` (chữ ký **không đổi**).
- Produces: `DauVaoDongLuong.hopDongThu2: boolean`; `KetQuaLuong.chiPhiBHCongTy: number` và `.tongChiPhiCongTy: number`; `CauHinhLuongData.bhCongTy: { tyLe: number; tyLeHopDongThu2: number }`.

- [ ] **Step 1: Viết test thất bại**

Trong `be/libs/core/src/luong/tinh-luong.spec.ts`: thêm `bhCongTy: { tyLe: 0.215, tyLeHopDongThu2: 0.005 }` vào object trả về của helper `cauHinh()` (trước `...over`), thêm `hopDongThu2: false` vào helper `dauVao()` (trước `nhapTheoKy: {}`), rồi thêm describe mới ở cuối file:

```ts
describe('chi phí BH công ty', () => {
  it('có đóng BH, HĐ thường → tỷ lệ chuẩn trên căn cứ đóng', () => {
    const r = tinhDongLuong(dauVao({ dongBH: true }), cauHinh());
    expect(r.chiPhiBHCongTy).toBe(lamTronTheo(0.215 * 5_500_000, 1000));
  });

  it('không đóng BH, HĐ thường → 0', () => {
    expect(tinhDongLuong(dauVao({ dongBH: false }), cauHinh()).chiPhiBHCongTy).toBe(0);
  });

  it('HĐLĐ thứ 2 → chỉ tỷ lệ BHTNLĐ-BNN, BẤT KỂ dongBH', () => {
    const mong = lamTronTheo(0.005 * 5_500_000, 1000);
    expect(
      tinhDongLuong(dauVao({ hopDongThu2: true, dongBH: true }), cauHinh()).chiPhiBHCongTy,
    ).toBe(mong);
    expect(
      tinhDongLuong(dauVao({ hopDongThu2: true, dongBH: false }), cauHinh()).chiPhiBHCongTy,
    ).toBe(mong);
  });

  it('đọc tỷ lệ từ CẤU HÌNH, không số cứng', () => {
    const ch = cauHinh({ bhCongTy: { tyLe: 0.3, tyLeHopDongThu2: 0.01 } });
    expect(tinhDongLuong(dauVao({ dongBH: true }), ch).chiPhiBHCongTy).toBe(
      lamTronTheo(0.3 * 5_500_000, 1000),
    );
    expect(tinhDongLuong(dauVao({ hopDongThu2: true }), ch).chiPhiBHCongTy).toBe(
      lamTronTheo(0.01 * 5_500_000, 1000),
    );
  });

  it('theo căn cứ LUONG_THOA_THUAN thì tính trên base', () => {
    const ch = cauHinh({ bhxh: { tyLe: 0.105, canCu: 'LUONG_THOA_THUAN' } });
    const r = tinhDongLuong(dauVao({ base: 20_000_000, dongBH: true }), ch);
    expect(r.chiPhiBHCongTy).toBe(lamTronTheo(0.215 * 20_000_000, 1000));
  });

  it('tongChiPhiCongTy = tongThuNhap + chiPhiBHCongTy', () => {
    const r = tinhDongLuong(dauVao({ dongBH: true }), cauHinh());
    expect(r.tongChiPhiCongTy).toBe(r.tongThuNhap + r.chiPhiBHCongTy);
  });

  it('không làm đổi thucLinh (hồi quy)', () => {
    const dv = dauVao({ dongBH: true });
    const r = tinhDongLuong(dv, cauHinh());
    expect(r.thucLinh).toBe(r.tongThuNhap - r.bhxh - r.thue - dv.tamUng - dv.khauTruKhac);
  });
});

describe('HĐLĐ thứ 2', () => {
  it('không trừ BHXH của NLĐ dù dongBH = true', () => {
    const r = tinhDongLuong(dauVao({ hopDongThu2: true, dongBH: true }), cauHinh());
    expect(r.bhxh).toBe(0);
  });

  it('không giảm trừ gia cảnh, vẫn chạy bậc thuế lũy tiến', () => {
    const dv = dauVao({
      base: 30_000_000,
      hopDongThu2: true,
      soNguoiPhuThuoc: 2,
      congThuong: 24,
    });
    const r = tinhDongLuong(dv, cauHinh());

    expect(r.giamTru).toBe(0);
    expect(r.thuNhapTinhThue).toBe(r.tongThuNhap - r.thuNhapMienThue);
    expect(r.thue).toBe(lamTronTheo(thueLuyTien(r.thuNhapTinhThue, BAC_MAC_DINH), 1000));
    expect(r.thue).toBeGreaterThan(0);
  });

  it('camKet thắng hopDongThu2 → thuế 0', () => {
    const r = tinhDongLuong(
      dauVao({ base: 30_000_000, hopDongThu2: true, camKet: true }),
      cauHinh(),
    );
    expect(r.thue).toBe(0);
  });

  it('thoiVu thắng hopDongThu2 → khấu trừ theo quyTacThoiVu, không lũy tiến', () => {
    const r = tinhDongLuong(
      dauVao({ base: 30_000_000, hopDongThu2: true, thoiVu: true }),
      cauHinh(),
    );
    const tnCT = r.tongThuNhap - r.thuNhapMienThue;
    expect(r.thue).toBe(lamTronTheo(0.1 * tnCT, 1000));
    expect(r.giamTru).toBe(0);
  });

  it('HĐ thường (mặc định) không đổi hành vi cũ', () => {
    const r = tinhDongLuong(dauVao({ base: 30_000_000, dongBH: true, soNguoiPhuThuoc: 1 }), cauHinh());
    expect(r.giamTru).toBe(15_500_000 + 6_200_000);
    expect(r.bhxh).toBe(lamTronTheo(0.105 * 5_500_000, 1000));
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận thất bại**

Run: `cd be && npx jest libs/core/src/luong/tinh-luong.spec.ts`
Expected: FAIL — `r.chiPhiBHCongTy` là `undefined`, `r.bhxh` vẫn > 0 khi `hopDongThu2`.

- [ ] **Step 3: Thêm trường vào `luong.types.ts`**

Trong `CauHinhLuongData`, thêm sau dòng `quyTacCamKet`:

```ts
  /** Phần BẢO HIỂM CÔNG TY chịu (không trừ vào lương NLĐ). `tyLeHopDongThu2`
   *  dùng khi NV là HĐLĐ thứ 2 — công ty chỉ đóng BHTNLĐ-BNN. */
  bhCongTy: { tyLe: number; tyLeHopDongThu2: number };
```

Trong `DauVaoDongLuong`, thêm sau `camKet: boolean;`:

```ts
  /** HĐLĐ thứ 2 (NLĐ đã có HĐ chính ở nơi khác). */
  hopDongThu2: boolean;
```

Trong `KetQuaLuong`, thêm sau `thucLinh: number;`:

```ts
  /** BH phần công ty chịu — KHÔNG trừ vào `thucLinh`. */
  chiPhiBHCongTy: number;
  /** Tổng chi phí công ty bỏ ra cho NV này = tongThuNhap + chiPhiBHCongTy. */
  tongChiPhiCongTy: number;
```

- [ ] **Step 4: Thêm cột vào entity + seed**

`be/libs/entities/src/luong/cau-hinh-luong.entity.ts` — thêm sau dòng `bhxh`:

```ts
  @Column('json', { nullable: true }) bhCongTy: { tyLe: number; tyLeHopDongThu2: number };
```

`be/apps/config-service/src/bang-luong/cau-hinh-luong.seed.ts` — thêm sau dòng `bhxh:`:

```ts
  bhCongTy: { tyLe: 0.215, tyLeHopDongThu2: 0.005 },
```

- [ ] **Step 5: Sửa engine `tinh-luong.ts`**

Thay khối tính BHXH (hiện `tinh-luong.ts:91-95`) bằng:

```ts
  // Cơ sở đóng BHXH hiện là lựa chọn `canCu` cố định (MUC_KHAI_BAO | base);
  // KhoanLuong.vaoBHXH chưa được cộng dồn ở đây — dành cho một task sau.
  const baseBHXH =
    ch.bhxh.canCu === 'MUC_KHAI_BAO' ? dv.mucKhaiBao : dv.base;

  // HĐLĐ thứ 2: BHXH/BHYT/BHTN đã đóng ở nơi thứ nhất nên NLĐ không bị trừ
  // tại đây, dù HR có tích `dongBH`.
  const bhxh =
    dv.dongBH && !dv.hopDongThu2
      ? lamTronTheo(ch.bhxh.tyLe * baseBHXH, ch.lamTron)
      : 0;

  // Phần CÔNG TY chịu — không trừ vào `thucLinh`, chỉ để quản trị chi phí.
  // Với HĐLĐ thứ 2, công ty vẫn phải đóng BHTNLĐ-BNN nên tỷ lệ này áp BẤT KỂ
  // `dongBH`: HR sẽ không tích `dongBH` cho người này, mà nghĩa vụ 0,5% thì
  // vẫn còn — buộc theo `dongBH` là làm cờ HĐ thứ 2 mất tác dụng đúng ở ca
  // phổ biến nhất.
  const tyLeBHCongTy = dv.hopDongThu2
    ? ch.bhCongTy.tyLeHopDongThu2
    : dv.dongBH
      ? ch.bhCongTy.tyLe
      : 0;
  const chiPhiBHCongTy = lamTronTheo(tyLeBHCongTy * baseBHXH, ch.lamTron);
```

Trong nhánh `else` của khối thuế (hiện `tinh-luong.ts:108-114`), thay dòng tính `giamTru`:

```ts
  } else {
    // HĐLĐ thứ 2: giảm trừ gia cảnh chỉ được đăng ký ở MỘT nơi — đã dùng ở
    // công ty thứ nhất, nên tại đây giảm trừ = 0 nhưng vẫn chạy lũy tiến.
    giamTru = dv.hopDongThu2
      ? 0
      : ch.giamTruBanThan + dv.soNguoiPhuThuoc * ch.giamTruNPT;
    thuNhapTinhThue = Math.max(
      0,
      tongThuNhap - thuNhapMienThue - bhxh - giamTru,
    );
    thue = lamTronTheo(thueLuyTien(thuNhapTinhThue, ch.bacThue), ch.lamTron);
  }
```

Trong object `return`, thêm 2 trường (sau `thucLinh`):

```ts
    chiPhiBHCongTy,
    tongChiPhiCongTy: tongThuNhap + chiPhiBHCongTy,
```

- [ ] **Step 6: Chạy test để xác nhận PASS**

Run: `cd be && npx jest libs/core/src/luong/tinh-luong.spec.ts`
Expected: PASS toàn bộ, kể cả các ca cũ (thứ tự ưu tiên `camKet` > `thoiVu` > `hopDongThu2` giữ nguyên cấu trúc if/else nên ca cũ không đổi).

- [ ] **Step 7: Commit**

```bash
git add be/libs/entities/src/luong/luong.types.ts \
        be/libs/entities/src/luong/cau-hinh-luong.entity.ts \
        be/apps/config-service/src/bang-luong/cau-hinh-luong.seed.ts \
        be/libs/core/src/luong/tinh-luong.ts \
        be/libs/core/src/luong/tinh-luong.spec.ts
git commit -m "feat(be): engine tính chi phí BH công ty + quy tắc HĐLĐ thứ 2

HĐ thứ 2: NLĐ không bị trừ BHXH tại đây, giảm trừ gia cảnh = 0 (đã đăng ký ở
nơi thứ nhất) nhưng vẫn lũy tiến; công ty chỉ chịu bhCongTy.tyLeHopDongThu2
(0,5% BHTNLĐ-BNN) bất kể dongBH. Ưu tiên camKet > thoiVu > hopDongThu2.
thucLinh không đổi."
```

---

### Task 4: Entity NV/dòng lương + áp cấu hình riêng khi tổng hợp kỳ

**Files:**
- Modify: `be/libs/entities/src/nhan-su/employee.entity.ts:44-52`
- Modify: `be/libs/entities/src/luong/dong-luong.entity.ts`
- Modify: `be/apps/config-service/src/bang-luong/bang-luong.service.ts`
- Test: `be/apps/config-service/src/bang-luong/bang-luong.service.spec.ts`

**Interfaces:**
- Consumes: `ganCauHinhRieng` (Task 2), `CauHinhLuongRieng`/`CauHinhLuongApDung` (Task 2), `DauVaoDongLuong.hopDongThu2` + `CauHinhLuongData.bhCongTy` (Task 3).
- Produces: `Employee.cauHinhLuongRieng?: CauHinhLuongRieng`, `Employee.hopDongThu2: boolean`; `DongLuong.cauHinhApDung: CauHinhLuongApDung`, `DongLuong.hopDongThu2: boolean`. FE (Task 5/7) đọc đúng tên này.

- [ ] **Step 1: Viết test thất bại**

Đọc `bang-luong.service.spec.ts` để dùng lại đúng khuôn mock repo có sẵn ở đó (đừng dựng khuôn mới), rồi thêm describe:

```ts
describe('cấu hình lương riêng theo NV', () => {
  it('áp override của NV vào cả hai mức khai báo/thực tế', async () => {
    // NV có congChuan 26 + thử việc 90% thay vì 24 + 85% của cấu hình chung.
    const emp = {
      _id: 'e1', hoTen: 'A', employeeId: 'NV0001',
      luongThoaThuan: 15_000_000, mucKhaiBao: 5_500_000,
      congThuViec: 0, isActive: true,
      cauHinhLuongRieng: { congChuan: 26, thuViecTyLe: 0.9 },
    };
    // ... dựng mock theo khuôn có sẵn: employeeRepo.find → [emp],
    //     timesheetRepo.find → [{ thang, employeeId: 'e1', soNgayCong: 26 }],
    //     dongLuongRepo.find → [], save → (row) => row

    const rows = await service.tongHop('2026-07');

    const chGoc = { ...CAU_HINH_LUONG_MAC_DINH, bhCongTy: { tyLe: 0.215, tyLeHopDongThu2: 0.005 } };
    const chNV = ganCauHinhRieng(chGoc, emp.cauHinhLuongRieng);
    expect(rows[0].khaiBao).toEqual(
      tinhDongLuong(
        {
          base: 5_500_000, mucKhaiBao: 5_500_000,
          congThuong: 26, congThuViec: 0, congKhac: 0,
          phuCapCoDinh: 0, soNguoiPhuThuoc: 0, tamUng: 0, khauTruKhac: 0,
          dongBH: false, thoiVu: false, camKet: false, hopDongThu2: false,
          nhapTheoKy: {},
        },
        chNV,
      ),
    );
  });

  it('NV không có override → dùng cấu hình chung (không đổi hành vi cũ)', async () => {
    // dựng emp KHÔNG có cauHinhLuongRieng; kỳ vọng khaiBao khớp tinhDongLuong
    // với cấu hình chung, y như test P4 hiện có.
  });

  it('ghi cauHinhApDung là giá trị ĐÃ resolve và copy hopDongThu2 từ hồ sơ', async () => {
    // emp = { ..., hopDongThu2: true, cauHinhLuongRieng: { congChuan: 26 } }
    const rows = await service.tongHop('2026-07');
    expect(rows[0].cauHinhApDung).toEqual({
      congChuan: 26,
      thuViecTyLe: 0.85, // kế thừa cấu hình chung
      bhxhTyLe: 0.105,
      bhxhCanCu: 'MUC_KHAI_BAO',
    });
    expect(rows[0].hopDongThu2).toBe(true);
  });

  it('HĐ thứ 2: dòng lương có chiPhiBHCongTy theo tỷ lệ 0,5% và bhxh = 0', async () => {
    // emp = { ..., hopDongThu2: true, dongBH: true }
    const rows = await service.tongHop('2026-07');
    expect(rows[0].khaiBao.bhxh).toBe(0);
    expect(rows[0].khaiBao.chiPhiBHCongTy).toBe(lamTronTheo(0.005 * 5_500_000, 1000));
  });

  it('capNhatDong dùng cauHinhApDung của dòng, KHÔNG đọc lại Employee', async () => {
    // dòng đã có cauHinhApDung = { congChuan: 26, thuViecTyLe: 0.9,
    //   bhxhTyLe: 0.105, bhxhCanCu: 'MUC_KHAI_BAO' }, hopDongThu2 = false
    const row = await service.capNhatDong('d1', { tamUng: 1_000_000 });

    expect(employeeRepo.find).not.toHaveBeenCalled();
    expect(employeeRepo.findOne).not.toHaveBeenCalled();
    // số tính lại phải theo congChuan 26 của snapshot
    expect(row.khaiBao).toEqual(
      tinhDongLuong(/* dv từ snapshot dòng */, ganCauHinhRieng(chGoc, row.cauHinhApDung)),
    );
  });
});

describe('layCauHinh', () => {
  it('backfill bhCongTy từ seed khi bản ghi cũ thiếu, và LƯU lại', async () => {
    // cauHinhRepo.find → [{ _id: 'c1', ...CAU_HINH_LUONG_MAC_DINH, bhCongTy: undefined }]
    const ch = await service.layCauHinh();

    expect(ch.bhCongTy).toEqual(CAU_HINH_LUONG_MAC_DINH.bhCongTy);
    expect(cauHinhRepo.save).toHaveBeenCalled();
  });

  it('không ghi lại khi bản ghi đã có bhCongTy', async () => {
    // cauHinhRepo.find → [{ ...MAC_DINH, bhCongTy: { tyLe: 0.2, tyLeHopDongThu2: 0.004 } }]
    await service.layCauHinh();
    expect(cauHinhRepo.save).not.toHaveBeenCalled();
  });
});
```

Điền phần `// ...` bằng đúng cách dựng mock đã có trong file. Mỗi `it` phải có assertion thật — không để lại comment thay cho code.

- [ ] **Step 2: Chạy test để xác nhận thất bại**

Run: `cd be && npx jest apps/config-service/src/bang-luong/bang-luong.service.spec.ts`
Expected: FAIL — `rows[0].cauHinhApDung` là `undefined`; `layCauHinh` không backfill.

- [ ] **Step 3: Thêm cột vào `employee.entity.ts`**

Thêm import type ở đầu file:

```ts
import type { CauHinhLuongRieng } from '../luong/luong.types';
```

Thêm vào nhóm `// ── Lương (P4) ──` (sau `camKet`):

```ts
  // ── Lương (P4.1) ──
  /**
   * Override cấu hình lương cho riêng NV này — trường để trống thì kế thừa
   * `CauHinhLuong` của công ty (xem `ganCauHinhRieng`). Chỉ chứa tham số
   * mang tính thỏa thuận riêng; tham số là luật (giảm trừ, bậc thuế) cố ý
   * KHÔNG override được.
   */
  @Column('json', { nullable: true }) cauHinhLuongRieng?: CauHinhLuongRieng;
  /**
   * NV làm ở 2 công ty và ĐÂY là HĐLĐ thứ 2: BHXH/BHYT/BHTN đã đóng ở nơi
   * thứ nhất (NLĐ không bị trừ tại đây), giảm trừ gia cảnh cũng đăng ký ở đó
   * (tại đây giảm trừ = 0, vẫn lũy tiến), công ty chỉ chịu BHTNLĐ-BNN.
   */
  @Column({ default: false }) hopDongThu2: boolean;
```

- [ ] **Step 4: Thêm cột snapshot vào `dong-luong.entity.ts`**

Sửa import type:

```ts
import type { CauHinhLuongApDung, KetQuaLuong } from './luong.types';
```

Thêm vào khối `// Snapshot đầu vào` (sau `camKet`):

```ts
  @Column({ default: false }) hopDongThu2: boolean;
  /**
   * Cấu hình đã RESOLVE cho NV này lúc tổng hợp (không còn `undefined`).
   * Lưu để (a) đọc một dòng lương là biết ngay số nào đã dùng, (b) sửa khoản
   * biến động tính lại không phải đọc lại hồ sơ NV — hồ sơ có thể đã đổi.
   */
  @Column('json', { nullable: true }) cauHinhApDung: CauHinhLuongApDung;
```

- [ ] **Step 5: Sửa `bang-luong.service.ts`**

(a) Import thêm:

```ts
import { ganCauHinhRieng, tinhDongLuong } from '@app/core';
import type {
  CauHinhLuongApDung, CauHinhLuongData, DauVaoDongLuong,
} from '@app/entities';
```

(b) `layCauHinh()` — backfill `bhCongTy` cho bản ghi tạo trước P4.1. Thay phần trả về bản ghi có sẵn:

```ts
    const rows = await this.cauHinhRepo.find({ where: { isActive: true } });
    const row = rows[0] as CauHinhLuong | undefined;
    if (row) {
      // Bản ghi tạo trước P4.1 không có khối `bhCongTy`; engine khai trường
      // này BẮT BUỘC nên backfill từ seed rồi lưu lại, thay vì để engine
      // phòng thủ `?? 0` (sẽ âm thầm báo chi phí công ty = 0).
      if (!row.bhCongTy) {
        row.bhCongTy = CAU_HINH_LUONG_MAC_DINH.bhCongTy;
        return this.cauHinhRepo.save(row);
      }
      return row;
    }
```

(c) `SnapshotChung` — thêm 2 trường:

```ts
  hopDongThu2: boolean;
  cauHinhApDung: CauHinhLuongApDung;
```

(d) `buildDauVao` — truyền cờ mới:

```ts
      camKet: sn.camKet,
      hopDongThu2: sn.hopDongThu2,
      nhapTheoKy: sn.nhapTheoKy,
```

(e) Thêm helper cạnh `buildDauVao`:

```ts
  /** Ảnh chụp cấu hình ĐÃ resolve cho một NV — nguồn duy nhất để tính lại. */
  private toCauHinhApDung(ch: CauHinhLuongData): CauHinhLuongApDung {
    return {
      congChuan: ch.congChuan,
      thuViecTyLe: ch.thuViec.tyLe,
      bhxhTyLe: ch.bhxh.tyLe,
      bhxhCanCu: ch.bhxh.canCu,
    };
  }
```

(f) `tongHop` — trong vòng lặp, sau khi có `mucKhaiBao`/`luongThoaThuan`, gộp cấu hình riêng và dùng `chNV` cho **cả hai** lần chạy engine:

```ts
      // Cấu hình riêng của NV gộp lên cấu hình chung; `ch` KHÔNG bị mutate
      // nên NV sau trong vòng lặp vẫn xuất phát từ cấu hình công ty.
      const chNV = ganCauHinhRieng(ch, emp.cauHinhLuongRieng);

      const snapshot: SnapshotChung = {
        // ... các trường cũ giữ nguyên ...
        hopDongThu2: !!emp.hopDongThu2,
        cauHinhApDung: this.toCauHinhApDung(chNV),
      };

      const khaiBao = tinhDongLuong(this.buildDauVao(mucKhaiBao, mucKhaiBao, snapshot), chNV);
      const thucTe = tinhDongLuong(this.buildDauVao(luongThoaThuan, mucKhaiBao, snapshot), chNV);
```

và ghi 2 trường mới vào `row` (cạnh `row.camKet = snapshot.camKet;`):

```ts
      row.hopDongThu2 = snapshot.hopDongThu2;
      row.cauHinhApDung = snapshot.cauHinhApDung;
```

(g) `capNhatDong` — tính lại theo **snapshot của dòng**, không đọc lại `Employee`:

```ts
    const chEntity = await this.layCauHinh();
    // Cố ý dùng `item.cauHinhApDung` (snapshot của dòng) chứ không đọc lại
    // Employee: sửa một khoản biến động không được kéo theo thay đổi cấu hình
    // riêng mà HR vừa sửa trên hồ sơ giữa kỳ.
    const ch = ganCauHinhRieng(this.toCauHinhData(chEntity), item.cauHinhApDung);

    const snapshot: SnapshotChung = {
      // ... các trường cũ giữ nguyên ...
      hopDongThu2: item.hopDongThu2,
      cauHinhApDung: item.cauHinhApDung,
    };
```

- [ ] **Step 6: Chạy test để xác nhận PASS**

Run: `cd be && npx jest apps/config-service/src/bang-luong`
Expected: PASS toàn bộ (cả các ca P4 cũ trong `bang-luong.service.spec.ts` và `bang-luong.controller.spec.ts`).

- [ ] **Step 7: Chạy toàn bộ test BE + build**

Run: `cd be && npx jest && npx tsc -p tsconfig.json --noEmit`
Expected: test PASS; `tsc` không lỗi (bắt các chỗ object literal thiếu trường mới — `ts-jest` chỉ transpile nên test xanh không đủ để yên tâm).

- [ ] **Step 8: Commit**

```bash
git add be/libs/entities/src/nhan-su/employee.entity.ts \
        be/libs/entities/src/luong/dong-luong.entity.ts \
        be/apps/config-service/src/bang-luong/bang-luong.service.ts \
        be/apps/config-service/src/bang-luong/bang-luong.service.spec.ts
git commit -m "feat(be): áp cấu hình lương riêng của NV khi tổng hợp kỳ + snapshot cauHinhApDung

tongHop gộp Employee.cauHinhLuongRieng lên cấu hình chung rồi chạy engine hai
mức trên cấu hình đã gộp; lưu giá trị đã resolve vào DongLuong.cauHinhApDung
để capNhatDong tính lại không phải đọc lại hồ sơ. layCauHinh backfill bhCongTy
cho bản ghi tạo trước P4.1."
```

---

### Task 5: FE — Hồ sơ NV: tab Lương nhận cấu hình riêng + cờ HĐ thứ 2

**Files:**
- Modify: `fe/src/services/employeeService.ts` (interface `Employee`, `CreateEmployeeDto`, hàm `transform`)
- Modify: `fe/src/services/cauHinhLuongService.ts` (thêm `bhCongTy` vào interface `CauHinhLuong`)
- Modify: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/HoSoNhanVienForm.state.ts`
- Modify: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/HoSoNhanVienForm.tsx:18-86`
- Create: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/luongTab.convert.ts`
- Create: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/luongTab.convert.test.ts`
- Modify: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/hoSoNhanVienForm.convert.ts`
- Modify: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/hoSoNhanVienForm.convert.test.ts`
- Modify: `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/LuongTab.tsx`

**Interfaces:**
- Consumes: BE fields `cauHinhLuongRieng` / `hopDongThu2` (Task 4), `bhCongTy` (Task 3).
- Produces: `cauHinhLuongRiengToDto(values): Pick<CreateEmployeeDto, 'cauHinhLuongRieng' | 'hopDongThu2'>`; form values thêm `hopDongThu2: boolean` và **4 trường phẳng**: `orCongChuan?: number`, `orThuViecPhanTram?: number`, `orBhxhPhanTram?: number`, `orBhxhCanCu?: 'MUC_KHAI_BAO' | 'LUONG_THOA_THUAN'`.

**Ghi chú thiết kế:** form giữ 4 ô **phẳng** (`or*`) chứ không nest, vì `react-hook-form` + `Controller` làm việc với đường dẫn phẳng gọn hơn, và ô % ở form là **phần trăm** (90) còn payload là **tỷ lệ** (0.9). Việc gộp lại thành object + quy đổi `/100` nằm gọn trong `luongTab.convert.ts` để test được — sai ở đây hoàn toàn vô hình trên màn hình (form vẫn báo "Cập nhật thành công").

- [ ] **Step 1: Viết test thất bại cho hàm convert**

Create `fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/luongTab.convert.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { cauHinhLuongRiengToDto } from "./luongTab.convert";
import type { HoSoNhanVienFormValues } from "../HoSoNhanVienForm.state";

const BASE = {} as HoSoNhanVienFormValues;

describe("cauHinhLuongRiengToDto", () => {
  it("không nhập gì → cauHinhLuongRieng = {} (xoá được cấu hình riêng cũ)", () => {
    expect(cauHinhLuongRiengToDto({ ...BASE })).toEqual({
      hopDongThu2: false,
      cauHinhLuongRieng: {},
    });
  });

  it("quy đổi % sang tỷ lệ 0..1", () => {
    const r = cauHinhLuongRiengToDto({
      ...BASE,
      orCongChuan: 26,
      orThuViecPhanTram: 90,
      orBhxhPhanTram: 10.5,
      orBhxhCanCu: "LUONG_THOA_THUAN",
    });
    expect(r.cauHinhLuongRieng).toEqual({
      congChuan: 26,
      thuViecTyLe: 0.9,
      bhxhTyLe: 0.105,
      bhxhCanCu: "LUONG_THOA_THUAN",
    });
  });

  it("0% là giá trị hợp lệ, không bị bỏ qua", () => {
    const r = cauHinhLuongRiengToDto({ ...BASE, orThuViecPhanTram: 0 });
    expect(r.cauHinhLuongRieng).toEqual({ thuViecTyLe: 0 });
  });

  it("ô để trống thì KHÔNG có khoá đó trong payload (kế thừa cấu hình chung)", () => {
    const r = cauHinhLuongRiengToDto({ ...BASE, orCongChuan: 26 });
    expect(r.cauHinhLuongRieng).toEqual({ congChuan: 26 });
    expect(r.cauHinhLuongRieng).not.toHaveProperty("thuViecTyLe");
  });

  it("hopDongThu2 giữ đúng false/true, không nuốt false", () => {
    expect(cauHinhLuongRiengToDto({ ...BASE, hopDongThu2: false }).hopDongThu2).toBe(false);
    expect(cauHinhLuongRiengToDto({ ...BASE, hopDongThu2: true }).hopDongThu2).toBe(true);
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận thất bại**

Run: `cd fe && npx vitest run src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/luongTab.convert.test.ts`
Expected: FAIL — không resolve được `./luongTab.convert`.

- [ ] **Step 3: Viết `luongTab.convert.ts`**

```ts
import type { CreateEmployeeDto } from "@/services/employeeService";
import type { HoSoNhanVienFormValues } from "../HoSoNhanVienForm.state";

/** `undefined`/`null`/NaN → không đưa khoá vào payload (nghĩa: kế thừa). */
function co(v: number | undefined | null): boolean {
  return typeof v === "number" && Number.isFinite(v);
}

/**
 * Gộp 4 ô "cấu hình riêng" phẳng của form thành object `cauHinhLuongRieng` +
 * cờ `hopDongThu2` cho DTO.
 *
 * Hai quy tắc dễ làm sai và hoàn toàn vô hình trên màn hình:
 *
 * 1. Ô % ở form là PHẦN TRĂM (90), payload là TỶ LỆ (0.9) — quy đổi đúng một
 *    chỗ là ở đây. BE từ chối tỷ lệ ngoài [0,1] nên gửi 90 là 400.
 * 2. Ô để trống phải KHÔNG có khoá trong payload ("kế thừa cấu hình chung"),
 *    trong khi `0` là giá trị hợp lệ phải gửi đi. Dùng `Number.isFinite`,
 *    KHÔNG dùng `if (v)` — `0` sẽ bị rơi mất.
 *
 * Luôn trả về `cauHinhLuongRieng` (kể cả `{}`) để HR xoá trắng các ô rồi lưu
 * là thực sự bỏ được cấu hình riêng: `JSON.stringify` loại khoá `undefined`
 * khỏi body, và BE `update` là `Object.assign` — gửi `undefined` = "giữ
 * nguyên giá trị cũ", không phải "xoá".
 */
export function cauHinhLuongRiengToDto(
  values: HoSoNhanVienFormValues,
): Pick<CreateEmployeeDto, "cauHinhLuongRieng" | "hopDongThu2"> {
  const rieng: NonNullable<CreateEmployeeDto["cauHinhLuongRieng"]> = {};

  if (co(values.orCongChuan)) rieng.congChuan = values.orCongChuan as number;
  if (co(values.orThuViecPhanTram))
    rieng.thuViecTyLe = (values.orThuViecPhanTram as number) / 100;
  if (co(values.orBhxhPhanTram))
    rieng.bhxhTyLe = (values.orBhxhPhanTram as number) / 100;
  if (values.orBhxhCanCu) rieng.bhxhCanCu = values.orBhxhCanCu;

  return { hopDongThu2: values.hopDongThu2 ?? false, cauHinhLuongRieng: rieng };
}

/** Nghịch đảo: từ hồ sơ BE về giá trị form (tỷ lệ → %). */
export function cauHinhLuongRiengToForm(
  rieng: CreateEmployeeDto["cauHinhLuongRieng"],
): Pick<
  HoSoNhanVienFormValues,
  "orCongChuan" | "orThuViecPhanTram" | "orBhxhPhanTram" | "orBhxhCanCu"
> {
  return {
    orCongChuan: rieng?.congChuan,
    orThuViecPhanTram: co(rieng?.thuViecTyLe)
      ? (rieng?.thuViecTyLe as number) * 100
      : undefined,
    orBhxhPhanTram: co(rieng?.bhxhTyLe)
      ? (rieng?.bhxhTyLe as number) * 100
      : undefined,
    orBhxhCanCu: rieng?.bhxhCanCu,
  };
}
```

- [ ] **Step 4: Thêm ca test cho `cauHinhLuongRiengToForm`**

Thêm vào `luongTab.convert.test.ts`:

```ts
import { cauHinhLuongRiengToForm } from "./luongTab.convert";

describe("cauHinhLuongRiengToForm", () => {
  it("tỷ lệ → %, trường vắng mặt để undefined", () => {
    expect(cauHinhLuongRiengToForm({ congChuan: 26, thuViecTyLe: 0.9 })).toEqual({
      orCongChuan: 26,
      orThuViecPhanTram: 90,
      orBhxhPhanTram: undefined,
      orBhxhCanCu: undefined,
    });
  });

  it("undefined/rỗng → tất cả undefined", () => {
    expect(cauHinhLuongRiengToForm(undefined)).toEqual({
      orCongChuan: undefined,
      orThuViecPhanTram: undefined,
      orBhxhPhanTram: undefined,
      orBhxhCanCu: undefined,
    });
  });

  it("tỷ lệ 0 → 0%, không thành undefined", () => {
    expect(cauHinhLuongRiengToForm({ thuViecTyLe: 0 }).orThuViecPhanTram).toBe(0);
  });
});
```

- [ ] **Step 5: Chạy test convert để xác nhận PASS**

Run: `cd fe && npx vitest run src/pages/nhan-su/ho-so-nhan-vien/components/form/tabs/luongTab.convert.test.ts`
Expected: PASS (8 ca).

- [ ] **Step 6: Thêm trường vào service + form state**

`fe/src/services/employeeService.ts` — thêm type export và các trường:

```ts
export interface CauHinhLuongRieng {
  congChuan?: number;
  thuViecTyLe?: number; // 0..1
  bhxhTyLe?: number; // 0..1
  bhxhCanCu?: 'MUC_KHAI_BAO' | 'LUONG_THOA_THUAN';
}
```

- trong `interface Employee`, sau `camKet: boolean;`:
  ```ts
  hopDongThu2: boolean;
  cauHinhLuongRieng?: CauHinhLuongRieng;
  ```
- trong `interface CreateEmployeeDto`, sau `camKet?: boolean;`:
  ```ts
  hopDongThu2?: boolean;
  cauHinhLuongRieng?: CauHinhLuongRieng;
  ```
- trong `transform`, sau `camKet: (x.camKet as boolean) ?? false,`:
  ```ts
      hopDongThu2: (x.hopDongThu2 as boolean) ?? false,
      cauHinhLuongRieng: (x.cauHinhLuongRieng as CauHinhLuongRieng) ?? undefined,
  ```

`HoSoNhanVienForm.state.ts` — thêm vào `HoSoNhanVienFormValues` sau `camKet: boolean;`:

```ts
  hopDongThu2: boolean;
  // 4 ô "cấu hình riêng" — phẳng cho react-hook-form; ô % ở đây là PHẦN TRĂM
  // (90), quy đổi sang tỷ lệ 0..1 trong `luongTab.convert.ts`.
  orCongChuan?: number;
  orThuViecPhanTram?: number;
  orBhxhPhanTram?: number;
  orBhxhCanCu?: "MUC_KHAI_BAO" | "LUONG_THOA_THUAN";
```

- [ ] **Step 7: Nối vào `DEFAULT_VALUES` / `toFormValues` / `toCreateEmployeeDto`**

`HoSoNhanVienForm.tsx` — thêm vào `DEFAULT_VALUES` (sau `camKet: false,`):

```ts
  hopDongThu2: false,
  orCongChuan: undefined,
  orThuViecPhanTram: undefined,
  orBhxhPhanTram: undefined,
  orBhxhCanCu: undefined,
```

và trong `toFormValues`, sau `camKet: employee.camKet ?? false,`:

```ts
    hopDongThu2: employee.hopDongThu2 ?? false,
    ...cauHinhLuongRiengToForm(employee.cauHinhLuongRieng),
```

(thêm import `import { cauHinhLuongRiengToForm } from "./tabs/luongTab.convert";`)

`hoSoNhanVienForm.convert.ts` — thêm import và spread ở cuối object trả về:

```ts
import { cauHinhLuongRiengToDto } from "./tabs/luongTab.convert";
// ...
    camKet: values.camKet ?? false,

    // Cấu hình riêng + cờ HĐLĐ thứ 2 (tab Lương). Tách hàm để test được —
    // xem docblock `cauHinhLuongRiengToDto` về hai bẫy %/khoá vắng mặt.
    ...cauHinhLuongRiengToDto(values),
```

- [ ] **Step 8: Thêm ca vào test convert tổng**

Thêm vào `hoSoNhanVienForm.convert.test.ts` (dùng đúng khuôn `values` mẫu đã có trong file đó):

```ts
  it("gộp cấu hình riêng + hopDongThu2 vào DTO", () => {
    const dto = toCreateEmployeeDto({
      ...VALUES_MAU,
      hopDongThu2: true,
      orCongChuan: 26,
      orThuViecPhanTram: 90,
    });

    expect(dto.hopDongThu2).toBe(true);
    expect(dto.cauHinhLuongRieng).toEqual({ congChuan: 26, thuViecTyLe: 0.9 });
  });

  it("không nhập cấu hình riêng → gửi {} để xoá được cấu hình cũ", () => {
    const dto = toCreateEmployeeDto({ ...VALUES_MAU });
    expect(dto.cauHinhLuongRieng).toEqual({});
    expect(dto.hopDongThu2).toBe(false);
  });
```

- [ ] **Step 9: Sửa UI `LuongTab.tsx`**

Thêm checkbox HĐ thứ 2 vào `<Col span={24}>` đang chứa `dongBH`/`thoiVu`/`camKet` (cùng khuôn `Controller` + `Checkbox` như 3 cái kia), nhãn:

```
HĐLĐ thứ 2 (công ty chỉ đóng 0,5% BHTNLĐ-BNN; không giảm trừ gia cảnh tại đây)
```

Thêm mục cấu hình riêng ở cuối `<Row>`. Đọc cấu hình chung để làm placeholder:

```tsx
import { useEffect, useState } from "react";
import { Select, Divider } from "antd";
import { cauHinhLuongService, type CauHinhLuong } from "@/services/cauHinhLuongService";

// ... trong LuongTab():
  const [chung, setChung] = useState<CauHinhLuong | null>(null);
  useEffect(() => {
    // Chỉ để hiện placeholder "đang dùng số nào" — lỗi/chưa tải xong thì
    // placeholder trống, KHÔNG chặn form lưu.
    cauHinhLuongService.get().then(setChung).catch(() => setChung(null));
  }, []);

  const ph = (v?: number) => (v === undefined ? "" : `${v} (theo cấu hình)`);
```

và JSX (4 ô, mỗi ô `<Col span={12}>` hoặc `span={6}` tuỳ chỗ, dùng `Controller` như các ô sẵn có):

```tsx
      <Col span={24}>
        <Divider orientation="left" plain style={{ marginBottom: 8 }}>
          Cấu hình riêng — để trống là theo Cấu hình lương
        </Divider>
      </Col>
      <Col span={6}>
        <Form.Item label="Công chuẩn / tháng">
          <Controller
            name="orCongChuan"
            control={control}
            render={({ field }) => (
              <InputNumber
                style={{ width: "100%" }}
                min={1}
                placeholder={ph(chung?.congChuan)}
                {...field}
              />
            )}
          />
        </Form.Item>
      </Col>
      <Col span={6}>
        <Form.Item label="Tỷ lệ thử việc">
          <Controller
            name="orThuViecPhanTram"
            control={control}
            render={({ field }) => (
              <InputNumber
                style={{ width: "100%" }}
                min={0}
                max={100}
                addonAfter="%"
                placeholder={ph(chung ? chung.thuViec.tyLe * 100 : undefined)}
                {...field}
              />
            )}
          />
        </Form.Item>
      </Col>
      <Col span={6}>
        <Form.Item label="Tỷ lệ BHXH (NLĐ đóng)">
          <Controller
            name="orBhxhPhanTram"
            control={control}
            render={({ field }) => (
              <InputNumber
                style={{ width: "100%" }}
                min={0}
                max={100}
                addonAfter="%"
                placeholder={ph(chung ? chung.bhxh.tyLe * 100 : undefined)}
                {...field}
              />
            )}
          />
        </Form.Item>
      </Col>
      <Col span={6}>
        <Form.Item label="Căn cứ đóng BH">
          <Controller
            name="orBhxhCanCu"
            control={control}
            render={({ field }) => (
              <Select
                style={{ width: "100%" }}
                allowClear
                placeholder={
                  chung?.bhxh.canCu === "LUONG_THOA_THUAN"
                    ? "Lương thoả thuận (theo cấu hình)"
                    : "Mức khai báo (theo cấu hình)"
                }
                options={[
                  { value: "MUC_KHAI_BAO", label: "Mức khai báo" },
                  { value: "LUONG_THOA_THUAN", label: "Lương thoả thuận" },
                ]}
                {...field}
              />
            )}
          />
        </Form.Item>
      </Col>
```

- [ ] **Step 10: Chạy test FE + typecheck**

Run: `cd fe && npx vitest run src/pages/nhan-su && npx tsc -p tsconfig.json --noEmit`
Expected: PASS; `tsc` không lỗi.

- [ ] **Step 11: Commit**

```bash
git add fe/src/services/employeeService.ts \
        fe/src/pages/nhan-su/ho-so-nhan-vien/components/form/
git commit -m "feat(fe): tab Lương — cấu hình riêng theo NV + cờ HĐLĐ thứ 2

4 ô để trống là kế thừa Cấu hình lương (placeholder hiện số chung đang dùng).
Quy đổi %/tỷ lệ và quy tắc 'ô trống = không gửi khoá, 0 = gửi' gom vào
luongTab.convert.ts để test được — sai ở đó vô hình trên màn hình."
```

---

### Task 6: FE — Cấu hình lương: khối BH công ty đóng

**Files:**
- Modify: `fe/src/services/cauHinhLuongService.ts` (interface `CauHinhLuong`)
- Modify: `fe/src/pages/luong/cau-hinh-luong/components/HangSoEditor.tsx`
- Test: `fe/src/pages/luong/cau-hinh-luong/CauHinhLuongPage.render.test.tsx`

**Interfaces:**
- Consumes: `CauHinhLuongData.bhCongTy` (Task 3).
- Produces: `CauHinhLuong.bhCongTy: { tyLe: number; tyLeHopDongThu2: number }` trong `cauHinhLuongService`.

- [ ] **Step 1: Viết test thất bại**

Đọc `CauHinhLuongPage.render.test.tsx` để dùng lại đúng khuôn mock service + render sẵn có, rồi thêm:

```tsx
  it("tab Hằng số hiện 2 ô tỷ lệ BH công ty đóng theo % từ cấu hình", async () => {
    // mock cauHinhLuongService.get trả về cấu hình có
    //   bhCongTy: { tyLe: 0.215, tyLeHopDongThu2: 0.005 }
    render(<CauHinhLuongPage />);
    await screen.findByText("Khoản lương");

    fireEvent.click(screen.getByText("Hằng số"));

    expect(screen.getByDisplayValue("21.5")).toBeInTheDocument();
    expect(screen.getByDisplayValue("0.5")).toBeInTheDocument();
  });
```

- [ ] **Step 2: Chạy test để xác nhận thất bại**

Run: `cd fe && npx vitest run src/pages/luong/cau-hinh-luong/CauHinhLuongPage.render.test.tsx`
Expected: FAIL — không tìm thấy ô `21.5`.

- [ ] **Step 3: Thêm `bhCongTy` vào interface FE**

`fe/src/services/cauHinhLuongService.ts`, trong `interface CauHinhLuong` sau dòng `bhxh`:

```ts
  /** Phần BH công ty chịu — `tyLeHopDongThu2` áp khi NV là HĐLĐ thứ 2. */
  bhCongTy: { tyLe: number; tyLeHopDongThu2: number };
```

- [ ] **Step 4: Thêm 2 ô vào `HangSoEditor.tsx`**

Thêm sau `<Col>` "Căn cứ đóng BHXH" (dùng đúng khuôn `FieldLabel` + `InputNumber` `addonAfter="%"` như các ô tỷ lệ có sẵn):

```tsx
      <Col span={8}>
        <FieldLabel>BH công ty đóng (HĐ thường)</FieldLabel>
        <InputNumber
          className="w-full"
          min={0}
          max={100}
          addonAfter="%"
          value={(cauHinh.bhCongTy?.tyLe ?? 0) * 100}
          disabled={!canEdit}
          onChange={(v) =>
            capNhat({
              bhCongTy: {
                tyLe: (v ?? 0) / 100,
                tyLeHopDongThu2: cauHinh.bhCongTy?.tyLeHopDongThu2 ?? 0,
              },
            })
          }
        />
      </Col>
      <Col span={8}>
        <FieldLabel>BH công ty đóng (HĐLĐ thứ 2)</FieldLabel>
        <InputNumber
          className="w-full"
          min={0}
          max={100}
          addonAfter="%"
          value={(cauHinh.bhCongTy?.tyLeHopDongThu2 ?? 0) * 100}
          disabled={!canEdit}
          onChange={(v) =>
            capNhat({
              bhCongTy: {
                tyLe: cauHinh.bhCongTy?.tyLe ?? 0,
                tyLeHopDongThu2: (v ?? 0) / 100,
              },
            })
          }
        />
      </Col>
```

Dùng `?.`/`?? 0` ở đây vì tenant chưa từng lưu `bhCongTy` (bản ghi tạo trước P4.1) vẫn có thể mở màn này trước khi BE backfill.

- [ ] **Step 5: Chạy test để xác nhận PASS**

Run: `cd fe && npx vitest run src/pages/luong/cau-hinh-luong`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add fe/src/services/cauHinhLuongService.ts \
        fe/src/pages/luong/cau-hinh-luong/
git commit -m "feat(fe): Cấu hình lương — 2 ô tỷ lệ BH công ty đóng (HĐ thường / HĐLĐ thứ 2)"
```

---

### Task 7: FE — Bảng lương: cột chi phí BH công ty + dấu cấu hình riêng

**Files:**
- Modify: `fe/src/services/bangLuongService.ts` (`KetQuaLuong`, `DongLuong`, `transform`)
- Modify: `fe/src/pages/luong/bang-luong/components/BangLuongTable.tsx`
- Test: `fe/src/pages/luong/bang-luong/BangLuongPage.render.test.tsx`

**Interfaces:**
- Consumes: `DongLuong.cauHinhApDung` / `.hopDongThu2` (Task 4), `KetQuaLuong.chiPhiBHCongTy` / `.tongChiPhiCongTy` (Task 3).
- Produces: không gì (task cuối của phần code).

- [ ] **Step 1: Viết test thất bại**

Đọc `BangLuongPage.render.test.tsx` để dùng lại khuôn mock `bangLuongService` + render, rồi thêm:

```tsx
  it("hiện cột chi phí BH công ty", async () => {
    // mock danhSach trả 1 dòng có khaiBao.chiPhiBHCongTy = 1_183_000
    render(<BangLuongPage />);
    expect(await screen.findByText("CP BH công ty")).toBeInTheDocument();
    expect(await screen.findByText("1.183.000")).toBeInTheDocument();
  });

  it("dòng cũ thiếu chiPhiBHCongTy hiện 0, không vỡ", async () => {
    // mock dòng có khaiBao KHÔNG có chiPhiBHCongTy
    render(<BangLuongPage />);
    expect(await screen.findByText("CP BH công ty")).toBeInTheDocument();
  });

  it("gắn nhãn HĐ2 cho dòng hopDongThu2", async () => {
    // mock dòng có hopDongThu2: true
    render(<BangLuongPage />);
    expect(await screen.findByText("HĐ2")).toBeInTheDocument();
  });
```

- [ ] **Step 2: Chạy test để xác nhận thất bại**

Run: `cd fe && npx vitest run src/pages/luong/bang-luong/BangLuongPage.render.test.tsx`
Expected: FAIL — không có text "CP BH công ty".

- [ ] **Step 3: Thêm trường vào `bangLuongService.ts`**

Trong `interface KetQuaLuong`, sau `thucLinh: number;`:

```ts
  /** Dòng chốt trước P4.1 không có 2 trường này → đọc `?? 0`. */
  chiPhiBHCongTy?: number;
  tongChiPhiCongTy?: number;
```

Thêm type + trường vào `DongLuong` (sau `camKet: boolean;`):

```ts
  hopDongThu2: boolean;
  cauHinhApDung?: {
    congChuan: number;
    thuViecTyLe: number;
    bhxhTyLe: number;
    bhxhCanCu: 'MUC_KHAI_BAO' | 'LUONG_THOA_THUAN';
  };
```

Trong `transform`, sau `camKet: ...`:

```ts
      hopDongThu2: (x.hopDongThu2 as boolean) ?? false,
      cauHinhApDung: (x.cauHinhApDung as DongLuong['cauHinhApDung']) ?? undefined,
```

- [ ] **Step 4: Thêm cột + nhãn vào `BangLuongTable.tsx`**

(a) Thêm vào `ketQuaColumns`, ngay trước cột "Thực lĩnh":

```tsx
      {
        title: "CP BH công ty",
        key: "chiPhiBHCongTy",
        width: 130,
        align: "right",
        render: (_: unknown, record: DongLuong) =>
          // `?? 0`: dòng chốt trước P4.1 không có trường này. 0 ở đây nghĩa
          // "chưa ghi nhận", không phải "công ty không đóng gì".
          renderTien(record[tabDangXem]?.chiPhiBHCongTy ?? 0),
      },
```

(b) Trong cột "Họ tên", thêm nhãn sau tên. Import `Tag` từ antd:

```tsx
        render: (value: string | undefined, record: DongLuong) => (
          <span>
            {thieuCongHoacLuong(record) && (
              <Tooltip title="Thiếu công hoặc chưa khai báo lương thoả thuận">
                <WarningOutlined style={{ color: "#faad14", marginRight: 6 }} />
              </Tooltip>
            )}
            {value || "-"}
            {record.hopDongThu2 && (
              <Tooltip title="HĐLĐ thứ 2: không trừ BHXH, không giảm trừ gia cảnh tại đây; công ty chỉ đóng BHTNLĐ-BNN">
                <Tag color="purple" style={{ marginLeft: 6 }}>HĐ2</Tag>
              </Tooltip>
            )}
            {moTaCauHinhRieng(record) && (
              <Tooltip title={moTaCauHinhRieng(record)}>
                <Tag color="blue" style={{ marginLeft: 6 }}>riêng</Tag>
              </Tooltip>
            )}
          </span>
        ),
```

(c) Thêm helper trong component (cần cấu hình chung để so sánh — `BangLuongPage.state` đã có state `khoanLuong` đọc từ cấu hình; thêm state `cauHinhChung` cùng nguồn nếu chưa có, hoặc so sánh với `cauHinh` mà handler đã tải):

```tsx
  const [cauHinhChung] = useBangLuongState("cauHinhChung", null as CauHinhLuong | null);

  /** Liệt kê tham số của dòng khác cấu hình chung — để HR biết vì sao số lệch
   *  so với người khác. Rỗng (undefined) khi trùng hoặc chưa tải được cấu hình. */
  const moTaCauHinhRieng = (dong: DongLuong): string | undefined => {
    const ap = dong.cauHinhApDung;
    if (!ap || !cauHinhChung) return undefined;
    const khac: string[] = [];
    if (ap.congChuan !== cauHinhChung.congChuan)
      khac.push(`Công chuẩn ${ap.congChuan}`);
    if (ap.thuViecTyLe !== cauHinhChung.thuViec.tyLe)
      khac.push(`Thử việc ${ap.thuViecTyLe * 100}%`);
    if (ap.bhxhTyLe !== cauHinhChung.bhxh.tyLe)
      khac.push(`BHXH ${ap.bhxhTyLe * 100}%`);
    if (ap.bhxhCanCu !== cauHinhChung.bhxh.canCu)
      khac.push(
        `Căn cứ ${ap.bhxhCanCu === "MUC_KHAI_BAO" ? "mức khai báo" : "lương thoả thuận"}`,
      );
    return khac.length ? `Cấu hình riêng: ${khac.join(", ")}` : undefined;
  };
```

Nếu `BangLuongPage.state.ts` / `bangLuongHandler.ts` chưa lưu cả object cấu hình (chỉ lưu `khoanLuong`), thêm state `cauHinhChung: CauHinhLuong | null` và set nó trong cùng nhánh handler đang gọi `cauHinhLuongService.get()` — không thêm request mới.

(d) `moTaCauHinhRieng` dùng trong `columns` `useMemo` ⇒ thêm `cauHinhChung` vào deps.

**Cẩn thận với `summary`:** `Table.Summary.Row` hiện dùng `colSpan={columns.length - 2}` rồi 2 ô Thuế/Thực lĩnh. Cột mới chèn **trước** "Thực lĩnh" nên phải sửa lại: `colSpan={columns.length - 3}` và thêm một `Table.Summary.Cell` cho tổng CP BH công ty:

```tsx
  const tongChiPhiBH = danhSach.reduce(
    (sum, dong) => sum + (dong[tabDangXem]?.chiPhiBHCongTy ?? 0), 0);
```

```tsx
                <Table.Summary.Cell index={0} colSpan={columns.length - 3}>
                  <strong>Tổng cộng</strong>
                </Table.Summary.Cell>
                <Table.Summary.Cell index={1} align="right">
                  <strong>{renderTien(tongThue)}</strong>
                </Table.Summary.Cell>
                <Table.Summary.Cell index={2} align="right">
                  <strong>{renderTien(tongChiPhiBH)}</strong>
                </Table.Summary.Cell>
                <Table.Summary.Cell index={3} align="right">
                  <strong>{renderTien(tongThucLinh)}</strong>
                </Table.Summary.Cell>
```

- [ ] **Step 5: Chạy test để xác nhận PASS**

Run: `cd fe && npx vitest run src/pages/luong`
Expected: PASS.

- [ ] **Step 6: Chạy toàn bộ test FE + typecheck**

Run: `cd fe && npx vitest run && npx tsc -p tsconfig.json --noEmit`
Expected: PASS toàn bộ; `tsc` không lỗi.

- [ ] **Step 7: Commit**

```bash
git add fe/src/services/bangLuongService.ts fe/src/pages/luong/bang-luong/
git commit -m "feat(fe): Bảng lương — cột CP BH công ty + nhãn HĐ2/cấu hình riêng

Nhãn liệt kê tham số lệch so với cấu hình chung để HR biết vì sao số của NV
này khác người khác. Dòng chốt trước P4.1 đọc `?? 0`, không vỡ."
```

---

### Task 8: Deploy

**Files:** không sửa code. Theo `.claude/skills/deploy.md`.

- [ ] **Step 1: Xác nhận cây làm việc sạch và test xanh**

```bash
cd be && npx jest && npx tsc -p tsconfig.json --noEmit
cd ../fe && npx vitest run && npx tsc -p tsconfig.json --noEmit
cd .. && git status --short
```
Expected: test PASS cả hai bên, `tsc` không lỗi, `git status` không còn file lương nào chưa commit.

- [ ] **Step 2: Tag ảnh rollback TRƯỚC khi build**

```bash
ssh kt "cd /root/chimseo/nhan-su && \
  docker tag nhan-su-be:latest nhan-su-be:rollback-20260725 && \
  docker tag nhan-su-fe:latest nhan-su-fe:rollback-20260725"
```

- [ ] **Step 3: Sync code**

```bash
rsync -az --delete --exclude='node_modules' --exclude='.superpowers' \
  --exclude='env/*.env' --exclude='.env-cmdrc' \
  /Users/os_anhvt/Documents/Dino/hrm/be/ kt:/root/chimseo/nhan-su/be/
rsync -az --delete --exclude='node_modules' --exclude='.superpowers' \
  /Users/os_anhvt/Documents/Dino/hrm/fe/ kt:/root/chimseo/nhan-su/fe/
rsync -az /Users/os_anhvt/Documents/Dino/hrm/docker-compose.production.yml kt:/root/chimseo/nhan-su/
```

- [ ] **Step 4: Build + restart**

```bash
ssh kt "cd /root/chimseo/nhan-su && \
  docker compose -f docker-compose.production.yml build --no-cache && \
  docker compose -f docker-compose.production.yml up -d"
```

- [ ] **Step 5: Kiểm tra health + smoke test**

```bash
ssh kt "sleep 10 && docker ps --filter name=nhan-su --format 'table {{.Names}}\t{{.Status}}'"
ssh kt "docker exec nhan-su-be curl -s http://localhost:3000/api/config/ngay-le"
```
Expected: BE `(healthy)`; smoke test trả `{"success":false,"error":{"code":"UNAUTHORIZED",...}}` (đúng — thiếu token). Nếu BE không healthy: `ssh kt "docker logs nhan-su-be --tail 50"`, và rollback theo `.claude/skills/deploy.md` nếu cần.

- [ ] **Step 6: Kiểm tra thật trên giao diện**

Mở `https://nhansu.masterceo.com.vn`, rồi:
1. Hồ sơ NV → sửa một NV → tab **Lương** → nhập Công chuẩn 26, Tỷ lệ thử việc 90%, tích **HĐLĐ thứ 2** → **Cập nhật** → mở lại hồ sơ đó xác nhận số đã lưu (đây chính là ca đang bị 400 trước Task 1).
2. Cấu hình lương → tab **Hằng số** → thấy 2 ô BH công ty đóng (21,5% / 0,5%) → sửa rồi **Lưu cấu hình** → tải lại thấy giữ.
3. Bảng lương → chọn tháng → **Tổng hợp** → NV vừa sửa có nhãn **HĐ2** + **riêng**, cột **CP BH công ty** hiện số theo 0,5%.

Không sửa được ⇒ dừng, xem log BE, đừng "sửa nhanh" trực tiếp trên server.

---

## Self-Review

**Spec coverage:**

| Spec | Task |
|---|---|
| §3 Task tiên quyết (DTO 400) | Task 1 |
| §4 phân loại tham số riêng/chung | Task 2 (type `CauHinhLuongRieng` chỉ chứa 4 trường) |
| §4.1 `bhCongTy` + seed | Task 3 |
| §4.2 `Employee.cauHinhLuongRieng` + `hopDongThu2` | Task 4 |
| §5.1 `ganCauHinhRieng` | Task 2 |
| §5.2 (a) bảo hiểm, (b) thuế, thứ tự ưu tiên cờ | Task 3 |
| §5.3 `KetQuaLuong` 2 trường mới | Task 3 |
| §6 snapshot, `capNhatDong`, `layCauHinh` backfill, tương thích ngược | Task 4 |
| §7 DTO backend | Task 1 |
| §8 FE LuongTab / CauHinhLuongPage / BangLuongTable | Task 5 / 6 / 7 |
| §9 kiểm thử | Task 2 Step 2, Task 3 Step 1, Task 4 Step 1, Task 1 Step 1 |

**Nhất quán tên gọi:** `ganCauHinhRieng`, `CauHinhLuongRieng`, `CauHinhLuongApDung`, `cauHinhLuongRieng` (Employee), `cauHinhApDung` (DongLuong), `hopDongThu2`, `bhCongTy.{tyLe,tyLeHopDongThu2}`, `chiPhiBHCongTy`, `tongChiPhiCongTy` — dùng đúng một dạng ở mọi task.

**Điểm cần chú ý khi thực thi:**
- `ts-jest` chỉ transpile ⇒ test xanh **không** đảm bảo kiểu đúng. Task 4 Step 7 và Task 5/7 có bước `tsc --noEmit` để bắt object literal thiếu trường mới.
- Task 4 và Task 7 yêu cầu **đọc file test/handler có sẵn** để dùng lại khuôn mock, không dựng khuôn mới.
- Task 7 sửa `colSpan` của `Table.Summary` — chèn cột mà quên chỗ này là lệch hết hàng tổng cộng.

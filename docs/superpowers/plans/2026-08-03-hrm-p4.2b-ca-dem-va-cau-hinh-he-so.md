# P4.2b — Ca đêm và cấu hình hoá hệ số làm thêm — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Đơn làm thêm giờ được chẻ theo khung giờ đêm và phân bổ đúng loại ngày, với mọi hệ số đọc từ cấu hình theo từng công ty thay vì hằng số trong mã nguồn — và quỹ giờ/quỹ phép giữ chỗ nguyên tử để số dư không bao giờ xuống âm.

**Architecture:** Ba lớp tách bạch. (1) Hàm thuần trong `luat-don.ts` / `luat-quy-gio.ts` — không DB, không NestJS, nhận bảng tra hệ số làm tham số. (2) Service `don-cham-cong` gọi hàm thuần rồi **snapshot** kết quả vào `AttendanceRequest.phanBoOt` ngay lúc tạo đơn. (3) Quỹ giờ/phép chuyển từ read-then-write sang compare-and-swap có thử lại, giữ nguyên toàn bộ kiến trúc callback `doi`/`kiem`/`dauSo` hiện có.

**Tech Stack:** NestJS 10 + TypeORM (MongoDB), Jest (backend); React 18 + Vite + antd + Vitest (frontend); yarn (be), npm (fe); Node 22 (`nvm use` — cả `be/.nvmrc` và `fe/.nvmrc` đều là `v22.0.0`).

**Spec:** `docs/superpowers/specs/2026-08-03-hrm-p4.2b-ca-dem-va-cau-hinh-he-so-design.md`

## Global Constraints

- **Node 22 bắt buộc.** Mọi lệnh backend chạy sau `cd be && nvm use v22.0.0`; frontend sau `cd fe && nvm use`. Không có `nvm use` thì `node` không tồn tại trong shell này.
- **Hết phase này KHÔNG đồng nào ra tiền.** `KetQuaLuong` không được thêm/sửa/xoá trường nào. `libs/core/src/luong/tinh-luong.ts` không được đụng tới. Mọi thứ liên quan `TIEN_OT`, cột TN miễn thuế, phí công đoàn là P4.2c.
- **Không làm tròn số giờ trước khi nhân hệ số.** Xem docblock `SO_LE_GIO` (`be/apps/config-service/src/quy-gio/luat-quy-gio.ts:42-72`): làm tròn trước khi nhân biến 2h20′ × 3,0 = 7,00 thành 2,33 × 3 = 6,99. `phanBoOt[].soGio` lưu chính xác; `lamTronGio()` chỉ gọi **sau** phép nhân.
- **Bất biến bắt buộc:** `Σ phanBoOt[].soGio === tinhSoGioOt(gioTu, gioDen)`, so sánh **chính xác** (`toBe`), không phải `toBeCloseTo`.
- **Không đọc hệ số bằng `if/else`.** Mọi nơi tra hệ số đi qua `traHeSo(bang, loai)`. Thêm một loại ngày phải là thêm một dòng cấu hình, không phải một nhánh mã.
- **Ngày/giờ tính trên trục UTC thuần** — `Date.UTC` + `getUTC*`, không `new Date("YYYY-MM-DD").getDay()`. Lý do ở `luat-don.ts:5-9`.
- **Giá trị seed mặc định** (chốt với chủ sản phẩm 2026-08-03, đều đổi được bằng cấu hình):
  ```
  heSoTra:       { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 }
  heSoTichQuy:   { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 }
  khungGioDem:   { tu: '22:00', den: '06:00' }
  uuTienLoai:    ['ngay_le', 'ngay_nghi', 'ngay_dem', 'ngay_thuong']
  mienThueChenh: ['ngay_dem']
  ```
- **Thêm trường vào entity thì PHẢI khai trong DTO.** `main.ts` bật `forbidNonWhitelisted`; quên khai là 400 cho **cả form** cấu hình lương, không riêng trường mới.
- **Commit sau mỗi task**, message tiếng Việt theo lệ repo (`feat(be):`, `fix(be):`, `feat(fe):`, `test(be):`).

---

## File Structure

| File | Trách nhiệm | Task |
|---|---|---|
| `be/apps/config-service/src/quy-gio/quy-gio.service.ts` | CAS nguyên tử cho quỹ giờ | 1 |
| `be/apps/config-service/src/quy-phep/quy-phep.service.ts` | CAS nguyên tử cho quỹ phép | 2 |
| `be/libs/entities/src/luong/luong.types.ts` | `CauHinhLamThem` — bảng tra mở | 3 |
| `be/apps/config-service/src/don-cham-cong/luat-don.ts` | hằng số mặc định, `traHeSo`, `suyLoaiNgay`, `chiaGioOtTheoLoai`, `gopPhanBoOt` | 3, 4 |
| `be/apps/config-service/src/quy-gio/luat-quy-gio.ts` | `HeSoTichQuy` thành `Record`, `gioTichTuDonOt` đọc `phanBoOt` | 3, 5 |
| `be/apps/config-service/src/bang-luong/cau-hinh-luong.seed.ts` | seed 4 trường mới | 3 |
| `be/libs/entities/src/cham-cong/attendance-request.entity.ts` | kiểu `PhanBoOt` + cột `phanBoOt` | 6 |
| `be/apps/config-service/src/don-cham-cong/don-cham-cong.service.ts` | nối hàm thuần vào lúc tạo đơn | 6 |
| `be/apps/config-service/src/bang-luong/dto/cap-nhat-cau-hinh-luong.dto.ts` | validate cấu hình mới | 7 |
| `ops/backfill-phan-bo-ot.ts` | backfill đơn cũ | 8 |
| `fe/src/services/cauHinhLuongService.ts` | kiểu + mặc định phía FE | 9 |
| `fe/src/pages/luong/cau-hinh-luong/components/LamThemEditor.tsx` | bảng hệ số + khung giờ đêm | 9 |

**Thứ tự phụ thuộc:** Task 1 và 2 độc lập với phần còn lại (làm trước theo spec §5). Task 3 mở đường cho 4→5→6. Task 7 phụ thuộc 3. Task 8 phụ thuộc 6. Task 9 phụ thuộc 3 và 7.

---

## Task 1: Giữ chỗ nguyên tử — quỹ giờ

**Files:**
- Modify: `be/apps/config-service/src/quy-gio/quy-gio.service.ts:20-24` (mã lỗi), `:478-519` (`apDung`), `:526-542` (doc-comment `giuCho`)
- Test: `be/apps/config-service/src/quy-gio/quy-gio.service.spec.ts:6-31` (repo giả), cuối file (test mới)

**Interfaces:**
- Consumes: (không có — task đầu tiên)
- Produces: `MA_LOI_QUY_GIO.DANG_SUA_DONG_THOI = 'QUY_GIO_DANG_SUA_DONG_THOI'`. Hành vi `apDung()` không đổi với lời gọi tuần tự; ném `ConflictException` mã trên khi CAS trượt 5 lần.

**Bối cảnh:** `apDung()` hôm nay là `findOne` → sửa trong bộ nhớ → `save()`. Doc-comment ở `:529-541` đã ghi nhận đúng lỗ hổng này và cố ý hoãn lại. P4.2c cho bảng lương đọc số dư nên không hoãn được nữa: hai đơn nộp đồng thời cùng đọc một `soGioConLai`, cùng thấy đủ, cùng giữ chỗ → số dư âm → dòng lương âm. `doiSoat()` không bắt được vì sổ và số dư cùng sai như nhau.

**Cách làm:** compare-and-swap, **không** đổi hình dạng callback `doi`/`kiem`/`dauSo`. Đọc bản ghi, tính giá trị mới bằng đúng callback cũ, rồi ghi bằng `findOneAndUpdate` có điều kiện là *ba số dư đọc được lúc đầu*. Ai đó chen vào giữa thì filter không khớp → đọc lại và tính lại. Đây là lý do giữ được toàn bộ `giuCho`/`nhaCho`/`chuyenSangDaDung`/`hoanTraDaDung` không sửa một dòng.

- [ ] **Step 1: Mở rộng repo giả trong spec để có `findOneAndUpdate`**

Sửa `repoGia()` ở `quy-gio.service.spec.ts:6-31` — thêm `manager` vào object trả về (giữ nguyên `find`/`findOne`/`create`/`save`):

```ts
function repoGia(khoiTao: any[] = []) {
  const rows = [...khoiTao];
  const findOneAndUpdate = jest.fn(
    async (filter: any, update: any, _opts?: any) => {
      // Bắt lỗi fixture thiếu `_id` cho ồn ào, thay vì để `undefined === undefined`
      // khớp nhầm hàng đầu tiên và làm test xanh giả.
      if (!('_id' in filter) || filter._id === undefined) {
        throw new Error('repoGia.findOneAndUpdate: filter thiếu _id — fixture phải khai _id');
      }
      const i = rows.findIndex((r) =>
        Object.entries(filter).every(([k, v]) => r[k] === v),
      );
      if (i < 0) return null;
      rows[i] = { ...rows[i], ...(update.$set ?? {}) };
      return rows[i];
    },
  );
  return {
    rows,
    findOneAndUpdate,
    find: jest.fn(async ({ where }: any = {}) =>
      rows.filter((r) =>
        Object.entries(where ?? {}).every(([k, v]) => r[k] === v),
      ),
    ),
    findOne: jest.fn(async ({ where }: any = {}) =>
      rows.find((r) =>
        Object.entries(where ?? {}).every(([k, v]) => r[k] === v),
      ) ?? null,
    ),
    create: jest.fn((x: any) => ({ ...x })),
    save: jest.fn(async (x: any) => {
      const i = rows.findIndex((r) => r._id && r._id === x._id);
      if (i >= 0) rows[i] = x;
      else {
        x._id = x._id ?? `id-${rows.length + 1}`;
        rows.push(x);
      }
      return x;
    }),
    manager: { getMongoRepository: () => ({ findOneAndUpdate }) },
  };
}
```

- [ ] **Step 2: Viết test CAS trượt — hai đơn đồng thời không được cùng giữ chỗ**

Thêm vào cuối `quy-gio.service.spec.ts`:

```ts
describe('giữ chỗ nguyên tử (P4.2b Task 1)', () => {
  it('CAS trượt thì đọc lại và tính lại trên số dư MỚI', async () => {
    const { service, quyRepo } = await dungService({
      quy: [
        {
          _id: 'b1', employeeId: 'nv1', kyTich: '2026-01', soGioTich: 10,
          soGioDaDung: 0, soGioDangChoDuyet: 0, soGioConLai: 10,
          hanDung: '2026-07-31', trangThai: 'dang_hieu_luc',
        },
      ],
    });

    // Mô phỏng một đơn KHÁC vừa giữ 8 giờ ngay giữa lúc ta đọc và lúc ta ghi:
    // lần findOneAndUpdate đầu tiên trượt, và trước khi ta đọc lại thì số dư
    // đã bị đơn kia ăn mất.
    const that = quyRepo.findOneAndUpdate;
    let lanDau = true;
    quyRepo.manager.getMongoRepository = () => ({
      findOneAndUpdate: async (filter: any, update: any, opts: any) => {
        if (lanDau) {
          lanDau = false;
          quyRepo.rows[0].soGioDangChoDuyet = 8;
          quyRepo.rows[0].soGioConLai = 2;
          return null; // filter không khớp nữa
        }
        return that(filter, update, opts);
      },
    });

    await expect(
      service.giuCho('nv1', [{ balanceId: 'b1', kyTich: '2026-01', soGio: 4 }], 'don1', 'nv1'),
    ).rejects.toMatchObject({ response: { code: 'QUY_GIO_KHONG_DU_SO_DU' } });

    // Số dư giữ nguyên đúng phần đơn kia đã giữ — không có 12/10 giờ nào bị giữ.
    expect(quyRepo.rows[0].soGioDangChoDuyet).toBe(8);
  });

  it('ghi qua findOneAndUpdate chứ không qua save()', async () => {
    const { service, quyRepo } = await dungService({
      quy: [
        {
          _id: 'b1', employeeId: 'nv1', kyTich: '2026-01', soGioTich: 10,
          soGioDaDung: 0, soGioDangChoDuyet: 0, soGioConLai: 10,
          hanDung: '2026-07-31', trangThai: 'dang_hieu_luc',
        },
      ],
    });

    await service.giuCho('nv1', [{ balanceId: 'b1', kyTich: '2026-01', soGio: 4 }], 'don1', 'nv1');

    expect(quyRepo.findOneAndUpdate).toHaveBeenCalled();
    expect(quyRepo.save).not.toHaveBeenCalled();
    expect(quyRepo.rows[0].soGioDangChoDuyet).toBe(4);
    expect(quyRepo.rows[0].soGioConLai).toBe(6);
  });
});
```

- [ ] **Step 3: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/quy-gio/quy-gio.service.spec.ts
```
Expected: FAIL — `expect(quyRepo.findOneAndUpdate).toHaveBeenCalled()` nhận 0 lần gọi (code hiện dùng `save()`).

- [ ] **Step 4: Thêm mã lỗi CAS**

Sửa `quy-gio.service.ts:21-24`:

```ts
export const MA_LOI_QUY_GIO = {
  KHONG_DU_SO_DU: 'QUY_GIO_KHONG_DU_SO_DU',
  DA_TIEU_KHONG_THU_HOI_DUOC: 'QUY_GIO_DA_TIEU',
  /** CAS trượt liên tiếp — quỹ đang bị nhiều request sửa cùng lúc. */
  DANG_SUA_DONG_THOI: 'QUY_GIO_DANG_SUA_DONG_THOI',
} as const;

/** Số lần đọc-lại-tính-lại trước khi bỏ cuộc. 5 là quá đủ cho tranh chấp thật
 *  (hai người bấm nộp cùng giây); vượt 5 nghĩa là có gì đó sai hệ thống chứ
 *  không phải tranh chấp bình thường, và thử mãi chỉ kéo dài request. */
const SO_LAN_THU_LAI_CAS = 5;
```

- [ ] **Step 5: Viết `capNhatCas()` và sửa `apDung()`**

Thêm method private ngay trên `apDung()` trong `quy-gio.service.ts`:

```ts
/**
 * Ghi số dư bằng compare-and-swap: filter chứa ĐÚNG ba con số đã đọc, nên
 * bất kỳ ai chen vào giữa đọc và ghi cũng làm filter trượt.
 *
 * Đi qua `getMongoRepository()` thẳng (không qua proxy tenant của
 * @app/database — proxy không chặn `findOneAndUpdate`), nên filter phải tự
 * đủ chặt. `_id` là khoá toàn cục duy nhất nên không cần thêm `tenantId` —
 * cùng lập luận đã ghi ở `nhan-vien.service.ts:43-48`.
 */
private async capNhatCas(
  id: unknown,
  truoc: { soGioTich: number; soGioDaDung: number; soGioDangChoDuyet: number },
  sau: OvertimeBalance,
): Promise<boolean> {
  const mongoRepo = this.repo.manager.getMongoRepository(
    OvertimeBalance,
  ) as unknown as import('typeorm').MongoRepository<OvertimeBalance>;

  const kq = await (mongoRepo as any).findOneAndUpdate(
    { _id: id, ...truoc },
    {
      $set: {
        soGioTich: sau.soGioTich,
        soGioDaDung: sau.soGioDaDung,
        soGioDangChoDuyet: sau.soGioDangChoDuyet,
        soGioConLai: sau.soGioConLai,
      },
    },
    { returnDocument: 'after' },
  );
  return kq != null;
}
```

Thay thân vòng lặp của `apDung()` (`:489-518`) bằng:

```ts
for (const p of phanBo) {
  let daXong = false;

  for (let lan = 0; lan < SO_LAN_THU_LAI_CAS && !daXong; lan++) {
    const quy = await this.repo.findOne({
      where: { employeeId, kyTich: p.kyTich } as any,
    });
    if (!quy) { daXong = true; break; }

    const loi = kiem?.(quy, p.soGio);
    if (loi) {
      throw new ConflictException({
        code: MA_LOI_QUY_GIO.KHONG_DU_SO_DU,
        message: loi,
      });
    }

    // Chụp TRƯỚC khi `doi()` sửa `quy` tại chỗ — đây là vế so sánh của CAS.
    const truoc = {
      soGioTich: quy.soGioTich,
      soGioDaDung: quy.soGioDaDung,
      soGioDangChoDuyet: quy.soGioDangChoDuyet,
    };

    const thucTe = doi(quy, p.soGio);
    if (boQuaKhiKhongDoi && thucTe === 0) { daXong = true; break; }

    this.tinhLaiConLai(quy);

    if (!(await this.capNhatCas((quy as any)._id, truoc, quy))) {
      continue; // ai đó vừa đổi số dư — đọc lại, `kiem()` chạy lại trên số mới
    }

    await this.ghiSo({
      balanceId: String((quy as any)._id),
      employeeId,
      kyTich: p.kyTich,
      soGio: dauSo(thucTe),
      lyDo,
      requestId,
      nguoiThucHien,
    });
    daXong = true;
  }

  if (!daXong) {
    throw new ConflictException({
      code: MA_LOI_QUY_GIO.DANG_SUA_DONG_THOI,
      message: `Quỹ kỳ ${p.kyTich} đang được sửa đồng thời, vui lòng thử lại`,
    });
  }
}
```

- [ ] **Step 6: Cập nhật doc-comment `giuCho()` — bỏ phần ghi nhận giới hạn đã hết đúng**

Trong `quy-gio.service.ts`, thay khối `GIỚI HẠN CÒN LẠI (review round, …)` (`:529-541`) bằng:

```
   * NGUYÊN TỬ (P4.2b Task 1): `apDung()` ghi bằng compare-and-swap — filter
   * chứa đúng ba số dư đã đọc, nên hai đơn nộp đồng thời không thể cùng giữ
   * chỗ trên một số dư. Bên trượt đọc lại và chạy `kiem()` trên số dư MỚI,
   * nên nó rớt đúng chỗ đáng rớt thay vì đẩy số dư xuống âm. Guard
   * `demRongTheoLyDo()` vẫn cần và vẫn giữ: nó chặn lớp lỗi KHÁC (gọi lại
   * cùng `requestId`), thứ mà CAS không nhìn thấy vì hai lần gọi đó thật sự
   * đọc hai số dư khác nhau và cả hai đều hợp lệ.
```

- [ ] **Step 7: Chạy toàn bộ suite quỹ giờ, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/quy-gio
```
Expected: PASS toàn bộ. Nếu test cũ đỏ với `filter thiếu _id` → fixture đó thiếu `_id`, thêm `_id: 'bN'` vào đúng fixture (đây là lỗi fixture thật, không phải hồi quy).

- [ ] **Step 8: Chạy các suite gọi gián tiếp quỹ giờ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong
```
Expected: PASS. `don-cham-cong.quy-gio.tich-hop.spec.ts` dựng repo giả riêng — nếu nó đỏ vì thiếu `manager`, chép nguyên khối `manager: { getMongoRepository: () => ({ findOneAndUpdate }) }` từ Step 1 sang repo giả của file đó.

- [ ] **Step 9: Commit**

```bash
git add be/apps/config-service/src/quy-gio/ be/apps/config-service/src/don-cham-cong/
git commit -m "fix(be): quỹ giờ giữ chỗ nguyên tử bằng CAS — số dư không còn xuống âm được

apDung() là read-then-write; hai đơn nộp đồng thời cùng đọc một soGioConLai,
cùng qua kiem(), cùng giữ chỗ. doiSoat() không bắt được vì sổ và số dư cùng
sai như nhau. P4.2c cho bảng lương đọc số dư nên số dư âm sẽ thành dòng
lương âm.

CAS giữ nguyên kiến trúc callback doi/kiem/dauSo — giuCho/nhaCho/
chuyenSangDaDung/hoanTraDaDung không sửa một dòng."
```

---

## Task 2: Giữ chỗ nguyên tử — quỹ phép

**Files:**
- Modify: `be/apps/config-service/src/quy-phep/quy-phep.service.ts:549-571` (`giuCho`), khối `MA_LOI_QUY_PHEP`
- Test: `be/apps/config-service/src/quy-phep/quy-phep.service.spec.ts`

**Interfaces:**
- Consumes: khuôn CAS từ Task 1 (chép sang, không dùng chung — hai service không có lớp cha chung, và ép ra một lớp cha chỉ vì 20 dòng là phức tạp hơn phần tiết kiệm được)
- Produces: `MA_LOI_QUY_PHEP.DANG_SUA_DONG_THOI = 'QUY_PHEP_DANG_SUA_DONG_THOI'`

**Bối cảnh:** `quy-phep.giuCho()` (`:555-570`) là read-then-write y hệt, chỉ khác tên trường (`soNgay*` thay `soGio*`) và không có lớp `apDung()` trung gian — nó sửa thẳng trong vòng lặp. Spec §5 nói rõ: làm một quỹ mà bỏ quỹ kia là để nguyên cái bẫy ở chỗ khác, và quỹ phép còn dễ chạm hơn vì ai cũng nghỉ phép.

- [ ] **Step 1: Thêm `manager.getMongoRepository` vào repo giả của spec quỹ phép**

Mở `be/apps/config-service/src/quy-phep/quy-phep.service.spec.ts`, tìm hàm dựng repo giả (cùng khuôn với `quy-gio`), thêm y hệt Step 1 của Task 1: biến `findOneAndUpdate` khai bằng `jest.fn`, ném lỗi khi filter thiếu `_id`, và `manager: { getMongoRepository: () => ({ findOneAndUpdate }) }` trong object trả về.

- [ ] **Step 2: Viết test CAS cho quỹ phép**

Thêm vào cuối `quy-phep.service.spec.ts` (dùng đúng helper dựng service sẵn có trong file — tên hàm và hình dạng fixture lấy theo các test đã có ở đó):

```ts
describe('giữ chỗ nguyên tử (P4.2b Task 2)', () => {
  it('ghi qua findOneAndUpdate chứ không qua save()', async () => {
    const { service, quyRepo } = await dungService({
      quy: [
        {
          _id: 'q1', employeeId: 'nv1', nam: 2026, loaiQuy: 'phep_nam',
          soNgayDuocCap: 12, soNgayDaDung: 0, soNgayDangChoDuyet: 0,
          soNgayConLai: 12, hanDung: '2027-03-31', trangThai: 'dang_hieu_luc',
        },
      ],
    });

    await service.giuCho('nv1', [{ balanceId: 'q1', nam: 2026, soNgay: 3 }], 'don1', 'nv1');

    expect(quyRepo.findOneAndUpdate).toHaveBeenCalled();
    expect(quyRepo.save).not.toHaveBeenCalled();
    expect(quyRepo.rows[0].soNgayDangChoDuyet).toBe(3);
    expect(quyRepo.rows[0].soNgayConLai).toBe(9);
  });

  it('CAS trượt thì đọc lại và rớt trên số dư MỚI', async () => {
    const { service, quyRepo } = await dungService({
      quy: [
        {
          _id: 'q1', employeeId: 'nv1', nam: 2026, loaiQuy: 'phep_nam',
          soNgayDuocCap: 12, soNgayDaDung: 0, soNgayDangChoDuyet: 0,
          soNgayConLai: 12, hanDung: '2027-03-31', trangThai: 'dang_hieu_luc',
        },
      ],
    });

    const that = quyRepo.findOneAndUpdate;
    let lanDau = true;
    quyRepo.manager.getMongoRepository = () => ({
      findOneAndUpdate: async (f: any, u: any, o: any) => {
        if (lanDau) {
          lanDau = false;
          quyRepo.rows[0].soNgayDangChoDuyet = 11;
          quyRepo.rows[0].soNgayConLai = 1;
          return null;
        }
        return that(f, u, o);
      },
    });

    await expect(
      service.giuCho('nv1', [{ balanceId: 'q1', nam: 2026, soNgay: 3 }], 'don1', 'nv1'),
    ).rejects.toMatchObject({ response: { code: 'QUY_PHEP_KHONG_DU_SO_DU' } });

    expect(quyRepo.rows[0].soNgayDangChoDuyet).toBe(11);
  });
});
```

- [ ] **Step 3: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/quy-phep/quy-phep.service.spec.ts
```
Expected: FAIL — `findOneAndUpdate` chưa được gọi lần nào.

- [ ] **Step 4: Thêm mã lỗi + hằng số thử lại**

Trong `quy-phep.service.ts`, thêm vào khối `MA_LOI_QUY_PHEP`:

```ts
  /** CAS trượt liên tiếp — quỹ đang bị nhiều request sửa cùng lúc. */
  DANG_SUA_DONG_THOI: 'QUY_PHEP_DANG_SUA_DONG_THOI',
```

và ngay dưới khối đó:

```ts
/** Xem lập luận ở `quy-gio.service.ts` — hai quỹ cố ý giữ cùng con số. */
const SO_LAN_THU_LAI_CAS = 5;
```

- [ ] **Step 5: Viết `capNhatCas()` cho quỹ phép**

Thêm method private trong `QuyPhep_Service` (đặt ngay trên `giuCho`):

```ts
/**
 * CAS cho quỹ phép — cùng khuôn `quy-gio.service.ts.capNhatCas()`. Cố ý chép
 * chứ không trừu tượng hoá: hai service không có lớp cha chung, và tên trường
 * (`soNgay*` / `soGio*`) khác nhau nên một lớp cha sẽ phải nhận map tên
 * trường — phức tạp hơn hai mươi dòng nó tiết kiệm được.
 */
private async capNhatCas(
  id: unknown,
  truoc: { soNgayDuocCap: number; soNgayDaDung: number; soNgayDangChoDuyet: number },
  sau: LeaveBalance,
): Promise<boolean> {
  const mongoRepo = this.repo.manager.getMongoRepository(
    LeaveBalance,
  ) as unknown as import('typeorm').MongoRepository<LeaveBalance>;

  const kq = await (mongoRepo as any).findOneAndUpdate(
    { _id: id, ...truoc },
    {
      $set: {
        soNgayDuocCap: sau.soNgayDuocCap,
        soNgayDaDung: sau.soNgayDaDung,
        soNgayDangChoDuyet: sau.soNgayDangChoDuyet,
        soNgayConLai: sau.soNgayConLai,
      },
    },
    { returnDocument: 'after' },
  );
  return kq != null;
}
```

- [ ] **Step 6: Viết lại `giuCho()` theo vòng CAS**

Thay thân `giuCho()` (`:555-570`) bằng:

```ts
for (const p of phanBo) {
  let daXong = false;

  for (let lan = 0; lan < SO_LAN_THU_LAI_CAS && !daXong; lan++) {
    const quy = await this.layQuyTheoId(employeeId, p.balanceId);

    if (
      quy.soNgayDangChoDuyet + p.soNgay >
      quy.soNgayDuocCap - quy.soNgayDaDung
    ) {
      throw new ConflictException({
        code: MA_LOI_QUY_PHEP.KHONG_DU_SO_DU,
        message: `Quỹ ${quy.nam} không còn đủ để giữ chỗ`,
      });
    }

    const truoc = {
      soNgayDuocCap: quy.soNgayDuocCap,
      soNgayDaDung: quy.soNgayDaDung,
      soNgayDangChoDuyet: quy.soNgayDangChoDuyet,
    };

    quy.soNgayDangChoDuyet += p.soNgay;
    quy.soNgayConLai =
      quy.soNgayDuocCap - quy.soNgayDaDung - quy.soNgayDangChoDuyet;

    if (await this.capNhatCas((quy as any)._id, truoc, quy)) daXong = true;
  }

  if (!daXong) {
    throw new ConflictException({
      code: MA_LOI_QUY_PHEP.DANG_SUA_DONG_THOI,
      message: `Quỹ phép đang được sửa đồng thời, vui lòng thử lại`,
    });
  }
}
```

Kiểm `LeaveBalance` đã có trong danh sách import ở đầu file; nếu chưa, thêm vào import từ `@app/entities`.

- [ ] **Step 7: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/quy-phep
```
Expected: PASS. Fixture nào đỏ vì `filter thiếu _id` thì thêm `_id` cho fixture đó.

- [ ] **Step 8: Cập nhật doc-comment `giuCho()` của quỹ phép**

Tìm đoạn doc-comment ghi nhận giới hạn read-then-write (khối mà `quy-gio.service.ts:536-538` trỏ tới bằng câu *"Đây đúng là giới hạn mà `quy-phep.service.ts` đã ghi nhận"*) và thay bằng ghi chú nguyên tử tương tự Task 1 Step 6, đổi `soGio*` → `soNgay*`.

- [ ] **Step 9: Chạy toàn bộ suite backend**

```bash
cd be && nvm use v22.0.0 && yarn test
```
Expected: PASS. Ngoại lệ đã biết: `auth-service` có test bcrypt rung ngẫu nhiên khi chạy full suite — nếu chỉ mình nó đỏ, chạy lại riêng `yarn test apps/auth-service` để xác nhận, không phải hồi quy của task này.

- [ ] **Step 10: Commit**

```bash
git add be/apps/config-service/src/quy-phep/
git commit -m "fix(be): quỹ phép giữ chỗ nguyên tử bằng CAS

Cùng lớp lỗi vừa vá cho quỹ giờ. Làm một quỹ mà bỏ quỹ kia là để nguyên
cái bẫy ở chỗ khác, và quỹ phép còn dễ chạm hơn vì ai cũng nghỉ phép."
```

---

## Task 3: Bảng tra hệ số mở — kiểu dữ liệu, hằng số mặc định, seed

**Files:**
- Modify: `be/libs/entities/src/luong/luong.types.ts:44-50` (`CauHinhLamThem`)
- Modify: `be/apps/config-service/src/don-cham-cong/luat-don.ts:13-15` (hằng số + `traHeSo`)
- Modify: `be/apps/config-service/src/quy-gio/luat-quy-gio.ts:13-24` (`HeSoTichQuy` → `Record`)
- Modify: `be/apps/config-service/src/bang-luong/cau-hinh-luong.seed.ts:2, :33-38`
- Test: `be/apps/config-service/src/don-cham-cong/luat-don.spec.ts:45-47`

**Interfaces:**
- Consumes: (không có)
- Produces:
  ```ts
  // luat-don.ts
  export type LoaiNgayOt = string;
  export const HE_SO_OT_MAC_DINH: Record<string, number>;
  export const KHUNG_GIO_DEM_MAC_DINH: { tu: string; den: string };
  export const UU_TIEN_LOAI_MAC_DINH: string[];
  export const MIEN_THUE_CHENH_MAC_DINH: string[];
  export function traHeSo(bang: Record<string, number>, loai: string): number;

  // luat-quy-gio.ts
  export type HeSoTichQuy = Record<string, number>;

  // luong.types.ts — CauHinhLamThem thêm heSoTra, khungGioDem, uuTienLoai, mienThueChenh
  ```

- [ ] **Step 1: Viết test cho `traHeSo` và các hằng số mặc định**

Sửa test hiện có ở `luat-don.spec.ts:45-47` (nó khoá đúng ba con số cũ, giờ phải là bốn) và thêm test mới. Thay khối đó bằng:

```ts
  it('HE_SO_OT_MAC_DINH là nguồn sự thật duy nhất của bốn con số seed', () => {
    expect(HE_SO_OT_MAC_DINH).toEqual({
      ngay_thuong: 1.5,
      ngay_nghi: 2.0,
      ngay_le: 3.0,
      ngay_dem: 1.5,
    });
  });

  describe('traHeSo', () => {
    const bang = { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 };

    it('tra đúng hệ số của loại có trong bảng', () => {
      expect(traHeSo(bang, 'ngay_le')).toBe(3.0);
      expect(traHeSo(bang, 'ngay_dem')).toBe(1.5);
    });

    it('loại lạ rơi về ngày thường — hệ số THẤP nhất, không tự tặng tiền', () => {
      expect(traHeSo(bang, 'ngay_gi_do_moi')).toBe(1.5);
    });

    it('bảng rỗng rơi về 1.0 chứ không NaN', () => {
      // NaN đi qua lamTronGio()/lamTronTheo() vẫn là NaN rồi nằm im trong DB.
      expect(traHeSo({}, 'ngay_le')).toBe(1);
    });
  });
```

Cập nhật dòng `import` đầu file `luat-don.spec.ts:2` — bỏ `HE_SO_OT`, thêm `HE_SO_OT_MAC_DINH, traHeSo`.

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong/luat-don.spec.ts
```
Expected: FAIL — `HE_SO_OT_MAC_DINH is not defined` / `traHeSo is not a function`.

- [ ] **Step 3: Thay hằng số trong `luat-don.ts`**

Thay `luat-don.ts:13-15`:

```ts
/**
 * Giá trị SEED cho `CauHinhLuong.lamThem.heSoTra`. KHÔNG phải nguồn sự thật
 * lúc chạy — nguồn sự thật là cấu hình của từng công ty (nền tảng đa tenant,
 * mỗi công ty một mức thoả thuận). Đọc hằng số này lúc tính là quay lại đúng
 * cái hardcode mà P4.2b tồn tại để gỡ.
 *
 * `ngay_dem: 1.5` theo mẫu 03-LĐTL chủ sản phẩm cung cấp. Thấp hơn sàn
 * NĐ 145/2020 Đ57.2 (ngày thường đêm ≥ 200%) — xem spec P4.2b §8.1.
 */
export const HE_SO_OT_MAC_DINH: Record<string, number> = {
  ngay_thuong: 1.5,
  ngay_nghi: 2.0,
  ngay_le: 3.0,
  ngay_dem: 1.5,
};

/** BLLĐ 2019 Đ106 định nghĩa ban đêm là 22:00–06:00. */
export const KHUNG_GIO_DEM_MAC_DINH = { tu: '22:00', den: '06:00' };

/** Giờ thuộc nhiều loại thì loại đứng TRƯỚC thắng. Lễ > nghỉ > đêm > thường
 *  nghĩa là làm đêm ngày lễ vẫn ăn hệ số lễ — không bao giờ trả thấp hơn làm
 *  ban ngày cùng ngày đó. */
export const UU_TIEN_LOAI_MAC_DINH = ['ngay_le', 'ngay_nghi', 'ngay_dem', 'ngay_thuong'];

/** Loại nào được tách phần chênh miễn thuế TNCN (P4.2c đọc). Mặc định chỉ ca
 *  đêm, theo sheet `Tính thuế TNCN` — hẹp hơn TT 111/2013 Đ3.1.i, xem spec §8.2. */
export const MIEN_THUE_CHENH_MAC_DINH = ['ngay_dem'];

/**
 * Chuỗi mở, KHÔNG phải union suy từ hằng số: thêm một loại ngày phải là thêm
 * một dòng cấu hình, không phải một lần sửa kiểu rồi deploy.
 */
export type LoaiNgayOt = string;

/**
 * Tra hệ số của một loại ngày. MỌI nơi đọc hệ số phải đi qua đây — `if/else`
 * ba nhánh (khuôn cũ ở `luat-quy-gio.ts`) là thứ khoá cứng danh sách loại vào
 * mã nguồn.
 *
 * Loại lạ rơi về `ngay_thuong` — hệ số THẤP nhất. Rơi về hệ số cao là tự tặng
 * tiền/giờ cho người nộp đơn khi ai đó thêm loại mới mà quên cấu hình. Bảng
 * rỗng rơi về 1.0 thay vì `undefined`: `undefined` nhân ra `NaN`, mà `NaN` đi
 * qua `lamTronGio()`/`lamTronTheo()` vẫn là `NaN` rồi nằm im trong DB.
 */
export function traHeSo(bang: Record<string, number>, loai: string): number {
  return bang?.[loai] ?? bang?.ngay_thuong ?? 1;
}
```

- [ ] **Step 4: Sửa `suyHeSoOt()` tạm thời để file còn biên dịch**

`suyHeSoOt()` (`:31-53`) đang đọc `HE_SO_OT.*` — Task 4 sẽ thay nó hẳn. Ở task này chỉ đổi ba tham chiếu cho biên dịch được: `HE_SO_OT.ngay_le` → `traHeSo(HE_SO_OT_MAC_DINH, 'ngay_le')`, tương tự `ngay_nghi` và `ngay_thuong`.

- [ ] **Step 5: `HeSoTichQuy` thành `Record`**

Thay `luat-quy-gio.ts:13-24`:

```ts
/** Bảng tra mở — thêm loại ngày là thêm một khoá, không phải sửa kiểu. */
export type HeSoTichQuy = Record<string, number>;

/** Mặc định bằng đúng sàn BLLĐ 2019 Đ98.1 cho ba loại ngày; `ngay_dem` theo
 *  mẫu 03-LĐTL (xem spec P4.2b §8.1). Công ty khai khác thì sửa cấu hình. */
export const HE_SO_TICH_MAC_DINH: HeSoTichQuy = {
  ngay_thuong: 1.5,
  ngay_nghi: 2.0,
  ngay_le: 3.0,
  ngay_dem: 1.5,
};
```

- [ ] **Step 6: Mở rộng `CauHinhLamThem`**

Thay `be/libs/entities/src/luong/luong.types.ts:44-50`:

```ts
export interface CauHinhLamThem {
  cheDoBu: CheDoBuLamThem;
  /** Hệ số TRẢ TIỀN từng loại ngày. Tách khỏi `heSoTichQuy` vì hai con số
   *  không nhất thiết bằng nhau: công ty có thể trả 1.5 nhưng chỉ cho nghỉ bù 1.0. */
  heSoTra: Record<string, number>;
  /** Hệ số quy đổi vào quỹ nghỉ bù. */
  heSoTichQuy: Record<string, number>;
  /** Khung giờ tính là ban đêm; `null` = công ty không có ca đêm. */
  khungGioDem: { tu: string; den: string } | null;
  /** Giờ thuộc nhiều loại thì loại đứng trước trong mảng này thắng. */
  uuTienLoai: string[];
  /** Loại nào được tách phần chênh miễn thuế TNCN — P4.2c đọc. */
  mienThueChenh: string[];
  /** null = quỹ không bao giờ hết hạn. */
  soThangHanDung: number | null;
  khiHetHan: 'quy_ra_tien' | 'huy_bo';
}
```

- [ ] **Step 7: Seed bốn trường mới**

Sửa `cau-hinh-luong.seed.ts` — thêm vào import ở `:2`:

```ts
import { HE_SO_TICH_MAC_DINH } from '../quy-gio/luat-quy-gio';
import {
  HE_SO_OT_MAC_DINH,
  KHUNG_GIO_DEM_MAC_DINH,
  MIEN_THUE_CHENH_MAC_DINH,
  UU_TIEN_LOAI_MAC_DINH,
} from '../don-cham-cong/luat-don';
```

và thay khối `lamThem` (`:32-38`):

```ts
  // Chỉ hỗ trợ "chỉ nghỉ bù" ở chặng này — xem CauHinhLamThemHopLe. Bốn trường
  // hệ số/khung giờ là SEED, công ty sửa được ở màn Cấu hình lương.
  lamThem: {
    cheDoBu: 'chi_nghi_bu',
    heSoTra: { ...HE_SO_OT_MAC_DINH },
    heSoTichQuy: { ...HE_SO_TICH_MAC_DINH },
    khungGioDem: { ...KHUNG_GIO_DEM_MAC_DINH },
    uuTienLoai: [...UU_TIEN_LOAI_MAC_DINH],
    mienThueChenh: [...MIEN_THUE_CHENH_MAC_DINH],
    soThangHanDung: null,
    khiHetHan: 'quy_ra_tien',
  },
```

Sao chép nông (`{ ...X }` / `[...X]`) chứ không dùng thẳng hằng số: seed đi vào một document được sửa tại chỗ, dùng chung tham chiếu là để một tenant sửa cấu hình làm bẩn hằng số của tiến trình.

- [ ] **Step 8: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong/luat-don.spec.ts apps/config-service/src/quy-gio
```
Expected: PASS. Test nào của `luat-quy-gio.spec.ts` khoá `HE_SO_TICH_MAC_DINH` bằng `toEqual` ba khoá sẽ đỏ — sửa nó thành bốn khoá (đây là thay đổi cố ý, không phải hồi quy).

- [ ] **Step 9: Kiểm biên dịch toàn backend**

```bash
cd be && nvm use v22.0.0 && npx tsc --noEmit -p tsconfig.json
```
Expected: không lỗi. Chỗ nào còn đọc `HE_SO_OT` (tên cũ) sẽ báo ở đây — đổi sang `HE_SO_OT_MAC_DINH`.

- [ ] **Step 10: Commit**

```bash
git add be/libs/entities/src/luong/luong.types.ts be/apps/config-service/src/don-cham-cong/ be/apps/config-service/src/quy-gio/ be/apps/config-service/src/bang-luong/cau-hinh-luong.seed.ts
git commit -m "feat(be): hệ số làm thêm chuyển vào cấu hình theo công ty, thêm loại ca đêm

HE_SO_OT là hằng số dùng chung mọi tenant và LoaiNgayOt suy ra từ chính nó,
nên thêm một loại ngày là sửa kiểu rồi deploy. Nền tảng đa tenant thì hệ số
trả lương không được nằm trong mã nguồn.

traHeSo() thay if/else ba nhánh: loại lạ rơi về ngày thường (hệ số thấp
nhất), bảng rỗng rơi về 1.0 chứ không NaN — NaN đi qua lamTronGio() vẫn là
NaN rồi nằm im trong DB."
```

---

## Task 4: `suyLoaiNgay()` + `chiaGioOtTheoLoai()` + `gopPhanBoOt()`

**Files:**
- Modify: `be/apps/config-service/src/don-cham-cong/luat-don.ts:31-53` (`suyHeSoOt` → `suyLoaiNgay`), thêm hàm mới
- Test: `be/apps/config-service/src/don-cham-cong/luat-don.spec.ts`

**Interfaces:**
- Consumes: `traHeSo`, `LoaiNgayOt` (Task 3); `hhmmSangPhut` (`../ban-ghi-cham-cong/thoi-gian.util`); `tinhSoGioOt` (đã có, `luat-don.ts:60`)
- Produces:
  ```ts
  export function suyLoaiNgay(input: {
    ngay: string; laNgayLe: boolean; ngayLamViecTrongTuan?: number[];
  }): LoaiNgayOt;

  export function chiaGioOtTheoLoai(input: {
    gioTu: string; gioDen: string; loaiNgay: LoaiNgayOt;
    khungGioDem: { tu: string; den: string } | null;
    uuTienLoai: string[];
    heSoTra: Record<string, number>;
    heSoTichQuy: Record<string, number>;
  }): PhanBoOt[];

  export function gopPhanBoOt(phanBoOt: PhanBoOt[]): {
    soGioOt: number; loaiNgayOt: LoaiNgayOt; heSoOt: number;
  };
  ```
  `PhanBoOt` khai ở Task 6 trong `@app/entities`; task này khai **tạm** trong `luat-don.ts` rồi Task 6 chuyển sang `import type`. Hình dạng: `{ loaiNgayOt: string; soGio: number; heSoTra: number; heSoTichQuy: number }`.

- [ ] **Step 1: Viết test cho `suyLoaiNgay`**

Thêm vào `luat-don.spec.ts`:

```ts
describe('suyLoaiNgay', () => {
  it('lễ thắng ngày nghỉ tuần — lễ rơi Chủ nhật vẫn là ngay_le', () => {
    // 2026-01-04 là Chủ nhật
    expect(suyLoaiNgay({ ngay: '2026-01-04', laNgayLe: true, ngayLamViecTrongTuan: [1, 2, 3, 4, 5] }))
      .toBe('ngay_le');
  });

  it('ngoài lịch làm việc trong tuần = ngay_nghi', () => {
    expect(suyLoaiNgay({ ngay: '2026-01-04', laNgayLe: false, ngayLamViecTrongTuan: [1, 2, 3, 4, 5] }))
      .toBe('ngay_nghi');
  });

  it('lịch làm việc rỗng = CHƯA CẤU HÌNH, không phải nghỉ tất cả', () => {
    expect(suyLoaiNgay({ ngay: '2026-01-04', laNgayLe: false, ngayLamViecTrongTuan: [] }))
      .toBe('ngay_thuong');
    expect(suyLoaiNgay({ ngay: '2026-01-04', laNgayLe: false }))
      .toBe('ngay_thuong');
  });
});
```

- [ ] **Step 2: Viết test cho `chiaGioOtTheoLoai`**

Thêm tiếp vào `luat-don.spec.ts`:

```ts
describe('chiaGioOtTheoLoai', () => {
  const heSoTra = { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 };
  const heSoTichQuy = { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 };
  const khungGioDem = { tu: '22:00', den: '06:00' };
  const uuTienLoai = ['ngay_le', 'ngay_nghi', 'ngay_dem', 'ngay_thuong'];
  const nen = { khungGioDem, uuTienLoai, heSoTra, heSoTichQuy };

  it('ca hoàn toàn ban ngày = một phần, đúng loại ngày', () => {
    expect(chiaGioOtTheoLoai({ ...nen, gioTu: '14:00', gioDen: '18:00', loaiNgay: 'ngay_thuong' }))
      .toEqual([{ loaiNgayOt: 'ngay_thuong', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 }]);
  });

  it('ca vắt nửa đêm chẻ làm hai — 20:00→02:00 ngày thường', () => {
    expect(chiaGioOtTheoLoai({ ...nen, gioTu: '20:00', gioDen: '02:00', loaiNgay: 'ngay_thuong' }))
      .toEqual([
        { loaiNgayOt: 'ngay_thuong', soGio: 2, heSoTra: 1.5, heSoTichQuy: 1.5 },
        { loaiNgayOt: 'ngay_dem', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 },
      ]);
  });

  it('22:00→02:00 nằm TRỌN trong khung đêm — khung vắt nửa đêm phải cắt theo mốc tuyệt đối', () => {
    // Nếu so chuỗi "HH:mm" thì '02:00' < '22:00' và ra 0 giờ đêm.
    expect(chiaGioOtTheoLoai({ ...nen, gioTu: '22:00', gioDen: '02:00', loaiNgay: 'ngay_thuong' }))
      .toEqual([{ loaiNgayOt: 'ngay_dem', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 }]);
  });

  it('ca chạm rìa SÁNG của khung đêm — 05:00→09:00', () => {
    expect(chiaGioOtTheoLoai({ ...nen, gioTu: '05:00', gioDen: '09:00', loaiNgay: 'ngay_thuong' }))
      .toEqual([
        { loaiNgayOt: 'ngay_dem', soGio: 1, heSoTra: 1.5, heSoTichQuy: 1.5 },
        { loaiNgayOt: 'ngay_thuong', soGio: 3, heSoTra: 1.5, heSoTichQuy: 1.5 },
      ]);
  });

  it('ngày lễ thắng ca đêm — gộp lại thành MỘT phần ngay_le', () => {
    expect(chiaGioOtTheoLoai({ ...nen, gioTu: '20:00', gioDen: '02:00', loaiNgay: 'ngay_le' }))
      .toEqual([{ loaiNgayOt: 'ngay_le', soGio: 6, heSoTra: 3.0, heSoTichQuy: 3.0 }]);
  });

  it('uuTienLoai đảo lại thì ca đêm thắng ngày lễ — không hardcode thứ tự', () => {
    const uuTienDem = ['ngay_dem', 'ngay_le', 'ngay_nghi', 'ngay_thuong'];
    expect(chiaGioOtTheoLoai({ ...nen, uuTienLoai: uuTienDem, gioTu: '23:00', gioDen: '01:00', loaiNgay: 'ngay_le' }))
      .toEqual([{ loaiNgayOt: 'ngay_dem', soGio: 2, heSoTra: 1.5, heSoTichQuy: 1.5 }]);
  });

  it('khungGioDem null = công ty không có ca đêm', () => {
    expect(chiaGioOtTheoLoai({ ...nen, khungGioDem: null, gioTu: '22:00', gioDen: '02:00', loaiNgay: 'ngay_thuong' }))
      .toEqual([{ loaiNgayOt: 'ngay_thuong', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 }]);
  });

  it('BẤT BIẾN: tổng giờ sau khi chẻ bằng ĐÚNG tinhSoGioOt(), không sai số', () => {
    const cases: Array<[string, string]> = [
      ['20:00', '02:00'], ['22:00', '06:00'], ['05:00', '09:00'],
      ['21:40', '02:20'], ['23:59', '00:01'], ['08:00', '17:30'],
      ['06:00', '22:00'], ['00:00', '23:59'],
    ];
    for (const [gioTu, gioDen] of cases) {
      const phan = chiaGioOtTheoLoai({ ...nen, gioTu, gioDen, loaiNgay: 'ngay_thuong' });
      const tong = phan.reduce((s, p) => s + p.soGio, 0);
      expect(tong).toBe(tinhSoGioOt(gioTu, gioDen));
    }
  });

  it('ca 0 giờ trả mảng rỗng', () => {
    expect(chiaGioOtTheoLoai({ ...nen, gioTu: '09:00', gioDen: '09:00', loaiNgay: 'ngay_thuong' }))
      .toEqual([]);
  });
});

describe('gopPhanBoOt', () => {
  it('soGioOt là tổng, loại/hệ số lấy phần chiếm NHIỀU giờ nhất', () => {
    expect(gopPhanBoOt([
      { loaiNgayOt: 'ngay_thuong', soGio: 2, heSoTra: 1.5, heSoTichQuy: 1.5 },
      { loaiNgayOt: 'ngay_dem', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 },
    ])).toEqual({ soGioOt: 6, loaiNgayOt: 'ngay_dem', heSoOt: 1.5 });
  });

  it('hoà giờ thì lấy phần ĐẦU — thứ tự đã theo uuTienLoai từ chiaGioOtTheoLoai', () => {
    expect(gopPhanBoOt([
      { loaiNgayOt: 'ngay_le', soGio: 3, heSoTra: 3.0, heSoTichQuy: 3.0 },
      { loaiNgayOt: 'ngay_dem', soGio: 3, heSoTra: 1.5, heSoTichQuy: 1.5 },
    ])).toEqual({ soGioOt: 6, loaiNgayOt: 'ngay_le', heSoOt: 3.0 });
  });

  it('mảng rỗng trả 0 / ngay_thuong / 1', () => {
    expect(gopPhanBoOt([])).toEqual({ soGioOt: 0, loaiNgayOt: 'ngay_thuong', heSoOt: 1 });
  });
});
```

Thêm `suyLoaiNgay, chiaGioOtTheoLoai, gopPhanBoOt` vào dòng import đầu file.

- [ ] **Step 3: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong/luat-don.spec.ts
```
Expected: FAIL — `suyLoaiNgay is not a function`.

- [ ] **Step 4: Viết `suyLoaiNgay()` thay `suyHeSoOt()`**

Thay toàn bộ `suyHeSoOt()` (`luat-don.ts:23-53`, kể cả doc-comment) bằng:

```ts
/**
 * Suy LOẠI NGÀY từ ngày — không trả hệ số, vì hệ số nay phụ thuộc cấu hình
 * của công ty và phụ thuộc cả việc giờ đó có rơi vào khung đêm không. Tách
 * hai việc: hàm này biết lịch, `chiaGioOtTheoLoai()` biết đồng hồ.
 *
 * Không cho người nộp đơn tự chọn — chọn tay thì họ chọn sai (không thuộc
 * luật) hoặc chọn cao hơn thực tế.
 *
 * Thứ tự kiểm là phần cốt lõi: `laNgayLe` phải kiểm TRƯỚC ngày nghỉ tuần, vì
 * lễ rơi vào Chủ nhật vẫn phải là lễ chứ không phải ngày nghỉ — kiểm ngược
 * lại là trả thiếu tiền cho đúng ngày đáng được trả cao nhất.
 */
export function suyLoaiNgay(input: {
  ngay: string;
  laNgayLe: boolean;
  ngayLamViecTrongTuan?: number[];
}): LoaiNgayOt {
  if (input.laNgayLe) return 'ngay_le';

  // Rỗng/undefined = CHƯA CẤU HÌNH, không phải "nghỉ tất cả các ngày" — cùng
  // quy ước đã ghi ở màn hồ sơ nhân viên. Hiểu ngược lại thì mọi đơn OT của
  // người chưa được gán lịch làm việc sẽ âm thầm thành ngày nghỉ.
  const daCauHinh =
    !!input.ngayLamViecTrongTuan && input.ngayLamViecTrongTuan.length > 0;
  if (daCauHinh) {
    const thu = thuTrongTuan(input.ngay);
    if (!input.ngayLamViecTrongTuan!.includes(thu)) return 'ngay_nghi';
  }

  return 'ngay_thuong';
}
```

- [ ] **Step 5: Viết `chiaGioOtTheoLoai()` và `gopPhanBoOt()`**

Thêm vào cuối `luat-don.ts` (sau `tinhSoGioOt`):

```ts
/** Một phần của đơn OT sau khi chẻ theo loại ngày/đêm. Task 6 chuyển sang
 *  `import type { PhanBoOt } from '@app/entities'`. */
export interface PhanBoOt {
  loaiNgayOt: string;
  soGio: number;
  heSoTra: number;
  heSoTichQuy: number;
}

const PHUT_MOT_NGAY = 24 * 60;

/** Loại đứng trước trong `uuTienLoai` thắng; loại không có trong danh sách
 *  xếp cuối (chứ không loại bỏ — mất giờ còn tệ hơn xếp sai thứ tự). */
function chonTheoUuTien(ungVien: string[], uuTien: string[]): string {
  let tot = ungVien[0];
  let hang = uuTien.indexOf(tot);
  if (hang < 0) hang = Number.MAX_SAFE_INTEGER;
  for (const uv of ungVien.slice(1)) {
    let h = uuTien.indexOf(uv);
    if (h < 0) h = Number.MAX_SAFE_INTEGER;
    if (h < hang) { hang = h; tot = uv; }
  }
  return tot;
}

/**
 * Chẻ một đơn OT thành các phần theo loại ngày và khung giờ đêm.
 *
 * Cắt theo MỐC TUYỆT ĐỐI (phút kể từ nửa đêm của ngày đơn), không so chuỗi
 * "HH:mm": khung đêm 22:00–06:00 vắt qua nửa đêm, nên ca 22:00→02:00 giao với
 * nó ở hai đoạn nếu nhìn theo mặt đồng hồ. So chuỗi thì '02:00' < '22:00' và
 * ra 0 giờ đêm.
 *
 * KHÔNG làm tròn `soGio`. Docblock `SO_LE_GIO` (`luat-quy-gio.ts`) đã đo được
 * hậu quả: làm tròn giờ trước khi nhân hệ số biến 2h20' × 3.0 = 7.00 thành
 * 2.33 × 3 = 6.99. Chẻ thành nhiều phần rồi làm tròn từng phần là đúng cái
 * bẫy đó, nhân lên theo số phần. Biên làm tròn ở nơi tiêu thụ, sau phép nhân.
 */
export function chiaGioOtTheoLoai(input: {
  gioTu: string;
  gioDen: string;
  loaiNgay: LoaiNgayOt;
  khungGioDem: { tu: string; den: string } | null;
  uuTienLoai: string[];
  heSoTra: Record<string, number>;
  heSoTichQuy: Record<string, number>;
}): PhanBoOt[] {
  const batDau = hhmmSangPhut(input.gioTu);
  let ketThuc = hhmmSangPhut(input.gioDen);
  if (ketThuc < batDau) ketThuc += PHUT_MOT_NGAY;

  const moc = new Set<number>([batDau, ketThuc]);
  const khoangDem: Array<[number, number]> = [];

  if (input.khungGioDem) {
    const demTu = hhmmSangPhut(input.khungGioDem.tu);
    let demDen = hhmmSangPhut(input.khungGioDem.den);
    if (demDen <= demTu) demDen += PHUT_MOT_NGAY; // khung vắt qua nửa đêm

    // Khung đêm lặp mỗi 24h. Ca dài tối đa 24h (tinhSoGioOt cộng đúng một
    // ngày), nên ba bản sao k ∈ {-1,0,1} phủ hết mọi giao cắt có thể.
    for (const k of [-1, 0, 1]) {
      const t = demTu + k * PHUT_MOT_NGAY;
      const d = demDen + k * PHUT_MOT_NGAY;
      khoangDem.push([t, d]);
      if (t > batDau && t < ketThuc) moc.add(t);
      if (d > batDau && d < ketThuc) moc.add(d);
    }
  }

  const cacMoc = [...moc].sort((a, b) => a - b);
  const ketQua: PhanBoOt[] = [];

  for (let i = 0; i < cacMoc.length - 1; i++) {
    const tu = cacMoc[i];
    const den = cacMoc[i + 1];
    const laDem = khoangDem.some(([t, d]) => tu >= t && den <= d);
    const loai = chonTheoUuTien(
      laDem ? [input.loaiNgay, 'ngay_dem'] : [input.loaiNgay],
      input.uuTienLoai,
    );
    const soGio = (den - tu) / 60;

    // Gộp hai đoạn liền kề cùng loại: ngày lễ thắng ca đêm nên 20:00→02:00
    // ra ba đoạn nhưng cùng là ngay_le — biểu mẫu 03-LĐTL cần một dòng.
    const cuoi = ketQua[ketQua.length - 1];
    if (cuoi && cuoi.loaiNgayOt === loai) {
      cuoi.soGio += soGio;
    } else {
      ketQua.push({
        loaiNgayOt: loai,
        soGio,
        heSoTra: traHeSo(input.heSoTra, loai),
        heSoTichQuy: traHeSo(input.heSoTichQuy, loai),
      });
    }
  }

  return ketQua;
}

/**
 * Ba trường dẫn xuất (`soGioOt`/`loaiNgayOt`/`heSoOt`) mà màn danh sách đơn,
 * bộ lọc và đường tích quỹ cũ đang đọc. Giữ chúng để không phải đổi cùng lúc
 * cả hình dạng dữ liệu lẫn mọi nơi tiêu thụ.
 *
 * Loại "đại diện" là phần chiếm NHIỀU GIỜ nhất; hoà thì lấy phần đầu, và
 * `chiaGioOtTheoLoai()` đã trả các phần theo thứ tự thời gian nên phần đầu là
 * phần bắt đầu sớm nhất — ổn định, không phụ thuộc thứ tự duyệt.
 */
export function gopPhanBoOt(phanBoOt: PhanBoOt[]): {
  soGioOt: number;
  loaiNgayOt: LoaiNgayOt;
  heSoOt: number;
} {
  if (!phanBoOt?.length) {
    return { soGioOt: 0, loaiNgayOt: 'ngay_thuong', heSoOt: 1 };
  }
  const soGioOt = phanBoOt.reduce((s, p) => s + p.soGio, 0);
  const troi = phanBoOt.reduce((a, b) => (b.soGio > a.soGio ? b : a));
  return { soGioOt, loaiNgayOt: troi.loaiNgayOt, heSoOt: troi.heSoTra };
}
```

- [ ] **Step 6: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong/luat-don.spec.ts
```
Expected: PASS toàn bộ, kể cả test bất biến tổng giờ.

- [ ] **Step 7: Sửa nơi gọi `suyHeSoOt` cho biên dịch được**

`don-cham-cong.service.ts:232` còn gọi `suyHeSoOt()`. Task 6 viết lại hẳn khối đó; ở task này chỉ cần biên dịch được — thay tạm:

```ts
      const loaiNgayOt = suyLoaiNgay({
        ngay: dto.ngay,
        laNgayLe: ngayLe !== null,
        ngayLamViecTrongTuan: emp.ngayLamViecTrongTuan,
      });
      const heSoOt = traHeSo(HE_SO_OT_MAC_DINH, loaiNgayOt);
      return { soGioOt, heSoOt, loaiNgayOt };
```

Sửa import ở đầu `don-cham-cong.service.ts`: bỏ `suyHeSoOt`, thêm `suyLoaiNgay, traHeSo, HE_SO_OT_MAC_DINH`.

```bash
cd be && nvm use v22.0.0 && npx tsc --noEmit -p tsconfig.json
```
Expected: không lỗi.

- [ ] **Step 8: Chạy suite đơn chấm công**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong
```
Expected: PASS. Test nào import `suyHeSoOt` đổi sang `suyLoaiNgay` và bỏ kỳ vọng về `heSoOt` trả kèm.

- [ ] **Step 9: Commit**

```bash
git add be/apps/config-service/src/don-cham-cong/
git commit -m "feat(be): chẻ đơn làm thêm theo khung giờ đêm

suyHeSoOt() tách làm hai: suyLoaiNgay() biết lịch, chiaGioOtTheoLoai() biết
đồng hồ. Cắt theo mốc tuyệt đối chứ không so chuỗi HH:mm — khung 22:00-06:00
vắt qua nửa đêm nên ca 22:00->02:00 giao với nó ở hai đoạn; so chuỗi thì
'02:00' < '22:00' và ra 0 giờ đêm.

Giờ trong phanBoOt KHÔNG làm tròn: docblock SO_LE_GIO đã đo được hậu quả của
việc làm tròn trước khi nhân hệ số (2h20' x 3.0 = 7.00 thành 6.99). Test khoá
bất biến tổng giờ bằng đúng tinhSoGioOt(), so sánh chính xác."
```

---

## Task 5: `gioTichTuDonOt()` cộng theo `phanBoOt`

**Files:**
- Modify: `be/apps/config-service/src/quy-gio/luat-quy-gio.ts:115-139`
- Test: `be/apps/config-service/src/quy-gio/luat-quy-gio.spec.ts`

**Interfaces:**
- Consumes: `traHeSo` (Task 3), `PhanBoOt` (Task 4), `lamTronGio` (đã có)
- Produces: `gioTichTuDonOt({ soGioOt, loaiNgayOt, heSoTichQuy, phanBoOt? }): number` — chữ ký cũ vẫn chạy, thêm `phanBoOt` tuỳ chọn

- [ ] **Step 1: Viết test**

Thêm vào `luat-quy-gio.spec.ts`:

```ts
describe('gioTichTuDonOt với phanBoOt (P4.2b)', () => {
  const heSoTichQuy = { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 };

  it('cộng theo từng phần, dùng hệ số SNAPSHOT trên phần chứ không tra lại cấu hình', () => {
    // Cấu hình hiện tại đã đổi ngay_dem lên 2.0, nhưng đơn snapshot 1.5 lúc
    // nộp — phải giữ 1.5, nếu không sửa cấu hình là đổi giờ của đơn đã duyệt.
    expect(
      gioTichTuDonOt({
        soGioOt: 6,
        loaiNgayOt: 'ngay_dem',
        heSoTichQuy: { ...heSoTichQuy, ngay_dem: 2.0 },
        phanBoOt: [
          { loaiNgayOt: 'ngay_thuong', soGio: 2, heSoTra: 1.5, heSoTichQuy: 1.5 },
          { loaiNgayOt: 'ngay_dem', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 },
        ],
      }),
    ).toBe(9); // 2×1.5 + 4×1.5
  });

  it('HỒI QUY: đơn một phần cho ra ĐÚNG con số như đường cũ — 2h20 ngày lễ = 7.00', () => {
    const soGioOt = 2 + 20 / 60;
    const cu = gioTichTuDonOt({ soGioOt, loaiNgayOt: 'ngay_le', heSoTichQuy });
    const moi = gioTichTuDonOt({
      soGioOt,
      loaiNgayOt: 'ngay_le',
      heSoTichQuy,
      phanBoOt: [{ loaiNgayOt: 'ngay_le', soGio: soGioOt, heSoTra: 3.0, heSoTichQuy: 3.0 }],
    });
    expect(cu).toBe(7);
    expect(moi).toBe(7); // KHÔNG phải 6.99 — làm tròn sau khi nhân
  });

  it('đơn cũ không có phanBoOt rơi về đường cũ, tra bảng chứ không if/else', () => {
    expect(gioTichTuDonOt({ soGioOt: 4, loaiNgayOt: 'ngay_dem', heSoTichQuy })).toBe(6);
    expect(gioTichTuDonOt({ soGioOt: 4, loaiNgayOt: 'loai_la', heSoTichQuy })).toBe(6); // → ngay_thuong
  });

  it('phanBoOt rỗng xử như không có', () => {
    expect(gioTichTuDonOt({ soGioOt: 4, loaiNgayOt: 'ngay_le', heSoTichQuy, phanBoOt: [] })).toBe(12);
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/quy-gio/luat-quy-gio.spec.ts
```
Expected: FAIL — `gioTichTuDonOt` chưa nhận `phanBoOt`, test đầu ra 9 nhưng nhận `lamTronGio(6 × 2.0) = 12`.

- [ ] **Step 3: Viết lại `gioTichTuDonOt()`**

Thay `luat-quy-gio.ts:115-139`:

```ts
/**
 * Giờ nghỉ được hưởng sinh ra từ MỘT đơn làm thêm đã duyệt.
 *
 * Cộng theo `phanBoOt` khi đơn có (đơn nộp từ P4.2b trở đi): mỗi phần dùng
 * hệ số SNAPSHOT trên chính nó, không tra lại cấu hình — sửa cấu hình sau
 * không được đổi giờ của đơn đã duyệt, cùng lập luận đã ghi ở
 * `don-cham-cong.service.ts` về việc snapshot lúc tạo đơn.
 *
 * Đơn cũ (chưa backfill) rơi về đường một-phần, tra `heSoTichQuy` qua
 * `traHeSo()` — bảng tra, không `if/else` ba nhánh: loại lạ rơi về ngày
 * thường, hệ số THẤP nhất, để việc ai đó thêm loại mới mà quên cấu hình
 * không thành tự tặng giờ cho người nộp đơn.
 *
 * BIÊN LÀM TRÒN của chiều tích quỹ: nhân hệ số XONG rồi mới làm tròn, TỪNG
 * PHẦN một (xem `SO_LE_GIO`). 2h20' ngày lễ = 2.3333…× 3.0 = 6.99999…→ 7.00,
 * đúng con số con người tính nhẩm ra; làm tròn trước khi nhân cho 6.99.
 */
export function gioTichTuDonOt(input: {
  soGioOt: number;
  loaiNgayOt: string;
  heSoTichQuy: HeSoTichQuy;
  phanBoOt?: PhanBoOt[];
}): number {
  if (input.phanBoOt?.length) {
    return input.phanBoOt.reduce(
      (tong, p) => tong + lamTronGio(p.soGio * p.heSoTichQuy),
      0,
    );
  }

  return lamTronGio(input.soGioOt * traHeSo(input.heSoTichQuy, input.loaiNgayOt));
}
```

Thêm vào đầu `luat-quy-gio.ts`:

```ts
import { PhanBoOt, traHeSo } from '../don-cham-cong/luat-don';
```

Kiểm không tạo vòng: `luat-don.ts` chỉ import `../ban-ghi-cham-cong/thoi-gian.util`, không import `luat-quy-gio` — nên cạnh mới là một chiều.

- [ ] **Step 4: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/quy-gio
```
Expected: PASS toàn bộ, kể cả test hồi quy 7.00.

- [ ] **Step 5: Commit**

```bash
git add be/apps/config-service/src/quy-gio/luat-quy-gio.ts be/apps/config-service/src/quy-gio/luat-quy-gio.spec.ts
git commit -m "feat(be): quỹ giờ tích theo phanBoOt, bỏ if/else ba nhánh

Mỗi phần dùng hệ số snapshot trên chính nó — sửa cấu hình sau không được đổi
giờ của đơn đã duyệt. Đơn cũ rơi về đường một-phần qua traHeSo().

Test hồi quy khoá 2h20' ngày lễ = 7.00 (không phải 6.99): làm tròn vẫn ở sau
phép nhân, từng phần một."
```

---

## Task 6: `phanBoOt` trên đơn + nối vào lúc tạo đơn

**Files:**
- Modify: `be/libs/entities/src/cham-cong/attendance-request.entity.ts:42-48`
- Modify: `be/apps/config-service/src/don-cham-cong/luat-don.ts` (bỏ `PhanBoOt` cục bộ, `import type` từ `@app/entities`)
- Modify: `be/apps/config-service/src/don-cham-cong/don-cham-cong.service.ts:218-238`
- Modify: `be/apps/config-service/src/quy-gio/quy-gio.service.ts` (`layCauHinh` trả thêm 4 trường, `tichTuDonOt` truyền `phanBoOt`)
- Test: `be/apps/config-service/src/don-cham-cong/don-cham-cong.service.spec.ts`

**Interfaces:**
- Consumes: `suyLoaiNgay`, `chiaGioOtTheoLoai`, `gopPhanBoOt` (Task 4); `gioTichTuDonOt` (Task 5)
- Produces: `AttendanceRequest.phanBoOt?: PhanBoOt[]`; `PhanBoOt` export từ `@app/entities`; `QuyGio_Service.cauHinhLamThem()` trả `{ heSoTra, heSoTichQuy, khungGioDem, uuTienLoai } | null`

- [ ] **Step 1: Viết test tích hợp lúc tạo đơn**

Thêm vào `don-cham-cong.service.spec.ts` (dùng helper dựng service sẵn có trong file):

```ts
describe('phanBoOt khi tạo đơn làm thêm (P4.2b Task 6)', () => {
  it('đơn vắt nửa đêm được chẻ và snapshot vào phanBoOt', async () => {
    const { service, donRepo } = await dungService({
      nhanVien: { employeeId: 'nv1', ngayLamViecTrongTuan: [1, 2, 3, 4, 5] },
    });

    await service.create({
      employeeId: 'nv1',
      loaiDon: 'lam_them_gio',
      ngay: '2026-06-01', // thứ Hai, không lễ
      gioTu: '20:00',
      gioDen: '02:00',
      lyDo: 'chạy kịp deadline',
    } as any);

    const don = donRepo.rows[0];
    expect(don.phanBoOt).toEqual([
      { loaiNgayOt: 'ngay_thuong', soGio: 2, heSoTra: 1.5, heSoTichQuy: 1.5 },
      { loaiNgayOt: 'ngay_dem', soGio: 4, heSoTra: 1.5, heSoTichQuy: 1.5 },
    ]);
    // Ba trường dẫn xuất vẫn đúng cho màn danh sách và bộ lọc cũ.
    expect(don.soGioOt).toBe(6);
    expect(don.loaiNgayOt).toBe('ngay_dem');
    expect(don.heSoOt).toBe(1.5);
  });

  it('công ty chưa khai lamThem thì không chẻ, giữ nguyên hành vi cũ', async () => {
    const { service, donRepo } = await dungService({
      nhanVien: { employeeId: 'nv1', ngayLamViecTrongTuan: [1, 2, 3, 4, 5] },
      cauHinh: null,
    });

    await service.create({
      employeeId: 'nv1',
      loaiDon: 'lam_them_gio',
      ngay: '2026-06-01',
      gioTu: '20:00',
      gioDen: '02:00',
      lyDo: 'x',
    } as any);

    const don = donRepo.rows[0];
    expect(don.phanBoOt).toBeUndefined();
    expect(don.soGioOt).toBe(6);
    expect(don.loaiNgayOt).toBe('ngay_thuong');
  });
});
```

Nếu helper dựng service trong file đó chưa nhận `cauHinh`, thêm tham số theo đúng khuôn `quy-gio.service.spec.ts:52-60` (phân biệt `undefined` = mặc định / `null` = repo rỗng).

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong/don-cham-cong.service.spec.ts
```
Expected: FAIL — `don.phanBoOt` là `undefined` ở test đầu.

- [ ] **Step 3: Khai `PhanBoOt` + cột trên entity**

Thêm vào `attendance-request.entity.ts`, ngay trên `@Entity`:

```ts
/**
 * Một phần của đơn OT sau khi chẻ theo loại ngày và khung giờ đêm (P4.2b).
 *
 * Lưu CẢ HAI hệ số vì cả hai đều là cấu hình đổi được: đọc lại lúc tính là
 * để việc HR sửa cấu hình tháng sau âm thầm đổi số của đơn đã duyệt tháng
 * trước, và không ai biết số nào là số đã trả lương.
 */
export interface PhanBoOt {
  loaiNgayOt: string;
  /** Giờ CHÍNH XÁC, không làm tròn — xem `SO_LE_GIO` ở `luat-quy-gio.ts`. */
  soGio: number;
  heSoTra: number;
  heSoTichQuy: number;
}
```

và trong class, ngay dưới `loaiNgayOt` (`:48`):

```ts
  /**
   * Phân bổ giờ OT theo loại ngày/đêm — nguồn sự thật của cả tích quỹ (P4.2b)
   * lẫn tiền làm thêm (P4.2c). Ba trường trên (`soGioOt`/`heSoOt`/`loaiNgayOt`)
   * là DẪN XUẤT từ đây qua `gopPhanBoOt()`, giữ lại cho màn danh sách và bộ
   * lọc cũ. Vắng mặt = đơn nộp trước P4.2b hoặc công ty chưa khai `lamThem`.
   * CỐ Ý không có trong DTO — cùng lý do ba trường trên.
   */
  @Column('json', { nullable: true }) phanBoOt?: PhanBoOt[];
```

Kiểm `PhanBoOt` được export ra `@app/entities` — file này đã export các interface khác qua barrel, nên `export interface` là đủ; xác nhận bằng `grep -n "attendance-request" be/libs/entities/src/index.ts` (hoặc barrel tương đương).

- [ ] **Step 4: `luat-don.ts` dùng kiểu chung**

Bỏ khối `export interface PhanBoOt {...}` đã thêm tạm ở Task 4 Step 5, thay bằng dòng import ở đầu file:

```ts
import type { PhanBoOt } from '@app/entities';
export type { PhanBoOt };
```

`import type` bị xoá lúc biên dịch nên file luật vẫn không có phụ thuộc runtime vào TypeORM.

- [ ] **Step 5: `QuyGio_Service` trả đủ cấu hình làm thêm**

Trong `quy-gio.service.ts`, mở rộng interface `CauHinhLamThemApDung` (`:26-32`) và `layCauHinh()` (`:54-66`):

```ts
interface CauHinhLamThemApDung {
  soGioMoiNgay: number;
  cheDoBu: string;
  heSoTra: Record<string, number>;
  heSoTichQuy: HeSoTichQuy;
  khungGioDem: { tu: string; den: string } | null;
  uuTienLoai: string[];
  soThangHanDung: number | null;
  khiHetHan: string;
}
```

```ts
    return {
      soGioMoiNgay: ch.soGioMoiNgay ?? 8,
      cheDoBu: ch.lamThem.cheDoBu,
      // Công ty khai `lamThem` từ P4.2a chưa có bốn trường này — rơi về mặc
      // định thay vì `undefined`, để đơn nộp ngay sau deploy (trước khi HR kịp
      // vào lưu lại cấu hình) không chẻ ra hệ số NaN.
      heSoTra: ch.lamThem.heSoTra ?? { ...HE_SO_OT_MAC_DINH },
      heSoTichQuy: ch.lamThem.heSoTichQuy,
      khungGioDem: ch.lamThem.khungGioDem ?? { ...KHUNG_GIO_DEM_MAC_DINH },
      uuTienLoai: ch.lamThem.uuTienLoai ?? [...UU_TIEN_LOAI_MAC_DINH],
      soThangHanDung: ch.lamThem.soThangHanDung ?? null,
      khiHetHan: ch.lamThem.khiHetHan ?? 'quy_ra_tien',
    };
```

Thêm import: `import { HE_SO_OT_MAC_DINH, KHUNG_GIO_DEM_MAC_DINH, UU_TIEN_LOAI_MAC_DINH } from '../don-cham-cong/luat-don';`

Thêm accessor công khai ngay dưới `soGioMoiNgay()` (`:69-71`):

```ts
  /**
   * Bảng hệ số + khung giờ đêm cho `chiaGioOtTheoLoai()`. `null` = công ty
   * chưa khai `lamThem` — nơi gọi giữ nguyên hành vi cũ (không chẻ), cùng
   * cửa `layCauHinh()` mà `tichTuDonOt()` dùng để quyết có tích hay không.
   */
  async cauHinhChiaGio(): Promise<{
    heSoTra: Record<string, number>;
    heSoTichQuy: HeSoTichQuy;
    khungGioDem: { tu: string; den: string } | null;
    uuTienLoai: string[];
  } | null> {
    const ch = await this.layCauHinh();
    if (!ch) return null;
    const { heSoTra, heSoTichQuy, khungGioDem, uuTienLoai } = ch;
    return { heSoTra, heSoTichQuy, khungGioDem, uuTienLoai };
  }
```

- [ ] **Step 6: Nối vào `tinhCacTruongSnapshot()`**

Thay nhánh `lam_them_gio` trong `don-cham-cong.service.ts` (`:229-238`, gồm cả bản vá tạm của Task 4 Step 7):

```ts
    if (dto.loaiDon === 'lam_them_gio') {
      const ngayLe = await this.ngayLeService.timTheoNgay(dto.ngay);
      const loaiNgay = suyLoaiNgay({
        ngay: dto.ngay,
        laNgayLe: ngayLe !== null,
        ngayLamViecTrongTuan: emp.ngayLamViecTrongTuan,
      });

      const chia = await this.quyGio_Service.cauHinhChiaGio();
      if (!chia) {
        // Công ty chưa khai `lamThem`: giữ nguyên hành vi trước P4.2b — một
        // loại, hệ số mặc định, không có `phanBoOt`. Không chẻ theo mặc định
        // ngầm, vì chẻ hay không là quyết định của công ty.
        return {
          soGioOt: tinhSoGioOt(dto.gioTu!, dto.gioDen!),
          heSoOt: traHeSo(HE_SO_OT_MAC_DINH, loaiNgay),
          loaiNgayOt: loaiNgay,
        };
      }

      const phanBoOt = chiaGioOtTheoLoai({
        gioTu: dto.gioTu!,
        gioDen: dto.gioDen!,
        loaiNgay,
        khungGioDem: chia.khungGioDem,
        uuTienLoai: chia.uuTienLoai,
        heSoTra: chia.heSoTra,
        heSoTichQuy: chia.heSoTichQuy,
      });

      return { ...gopPhanBoOt(phanBoOt), phanBoOt };
    }
```

Mở rộng kiểu trả về của `tinhCacTruongSnapshot()` (`:221-228`) — thêm `'phanBoOt'` vào danh sách `Pick<AttendanceRequest, …>`.

Sửa import ở đầu file: `suyLoaiNgay, chiaGioOtTheoLoai, gopPhanBoOt, tinhSoGioOt, traHeSo, HE_SO_OT_MAC_DINH`.

- [ ] **Step 7: `tichTuDonOt()` truyền `phanBoOt` xuống**

Trong `quy-gio.service.ts`, tìm nơi gọi `gioTichTuDonOt({ soGioOt, loaiNgayOt, heSoTichQuy })` trong `tichTuDonOt()` và thêm `phanBoOt: don.phanBoOt`. Đối tượng đơn đã có sẵn ở đó (nơi lấy `soGioOt`/`loaiNgayOt`).

- [ ] **Step 8: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/don-cham-cong apps/config-service/src/quy-gio
```
Expected: PASS toàn bộ, gồm cả `don-cham-cong.quy-gio.tich-hop.spec.ts`.

- [ ] **Step 9: Kiểm biên dịch + chạy full backend**

```bash
cd be && nvm use v22.0.0 && npx tsc --noEmit -p tsconfig.json && yarn test
```
Expected: không lỗi biên dịch; test PASS (trừ bcrypt auth-service rung — xác nhận bằng cách chạy riêng).

- [ ] **Step 10: Commit**

```bash
git add be/libs/entities/src/cham-cong/attendance-request.entity.ts be/apps/config-service/src/don-cham-cong/ be/apps/config-service/src/quy-gio/
git commit -m "feat(be): snapshot phanBoOt vào đơn làm thêm lúc tạo

Snapshot chứ không tính lại lúc đọc: HR sửa lịch lễ hay sửa hệ số tháng sau
sẽ âm thầm đổi số của đơn đã duyệt tháng trước, và không ai biết số nào là số
đã trả lương — cùng lập luận đã ghi ở tinhCacTruongSnapshot().

soGioOt/heSoOt/loaiNgayOt giữ lại làm trường dẫn xuất qua gopPhanBoOt() để
màn danh sách, bộ lọc và đường tích quỹ cũ không phải đổi cùng lúc.

Công ty chưa khai lamThem giữ nguyên hành vi trước P4.2b — chẻ hay không là
quyết định của công ty, không phải hệ quả phụ của một lần deploy."
```

---

## Task 7: DTO cấu hình làm thêm

**Files:**
- Modify: `be/apps/config-service/src/bang-luong/dto/cap-nhat-cau-hinh-luong.dto.ts:18-22, :33, :110-119`
- Test: `be/apps/config-service/src/bang-luong/dto/cap-nhat-cau-hinh-luong.dto.spec.ts`

**Interfaces:**
- Consumes: `CauHinhLamThem` (Task 3)
- Produces: `CauHinhLamThemDto` nhận `heSoTra`, `khungGioDem`, `uuTienLoai`, `mienThueChenh`; `CauHinhLamThemHopLe` thêm bốn ràng buộc mới

**Bối cảnh:** `main.ts` bật `forbidNonWhitelisted` — bốn trường mới không khai ở DTO thì **cả form** cấu hình lương trả 400, không riêng trường mới. `HeSoTichQuyDto` hiện khai cứng ba trường nên `ngay_dem` cũng bị chặn.

- [ ] **Step 1: Viết test DTO**

Thêm vào `cap-nhat-cau-hinh-luong.dto.spec.ts` (dùng đúng helper dựng `ValidationPipe` của file — nó phải khớp cấu hình ở `main.ts`, gồm `forbidNonWhitelisted: true`):

```ts
describe('CauHinhLamThemDto — bảng hệ số mở (P4.2b)', () => {
  const lamThemHopLe = {
    cheDoBu: 'chi_nghi_bu',
    heSoTra: { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 },
    heSoTichQuy: { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 },
    khungGioDem: { tu: '22:00', den: '06:00' },
    uuTienLoai: ['ngay_le', 'ngay_nghi', 'ngay_dem', 'ngay_thuong'],
    mienThueChenh: ['ngay_dem'],
    soThangHanDung: null,
    khiHetHan: 'quy_ra_tien',
  };

  it('nhận cấu hình đầy đủ bốn loại', async () => {
    await expect(kiemTra({ lamThem: lamThemHopLe })).resolves.toBeDefined();
  });

  it('từ chối khi uuTienLoai có khoá thiếu trong heSoTra', async () => {
    // Thiếu khoá → hệ số undefined → NaN, mà NaN đi qua lamTronGio() vẫn là
    // NaN rồi nằm im trong DB.
    await expect(
      kiemTra({
        lamThem: {
          ...lamThemHopLe,
          heSoTra: { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0 },
        },
      }),
    ).rejects.toThrow(/heSoTra.*ngay_dem/i);
  });

  it('từ chối khi uuTienLoai có khoá thiếu trong heSoTichQuy', async () => {
    await expect(
      kiemTra({
        lamThem: {
          ...lamThemHopLe,
          heSoTichQuy: { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0 },
        },
      }),
    ).rejects.toThrow(/heSoTichQuy.*ngay_dem/i);
  });

  it('từ chối uuTienLoai rỗng hoặc có khoá trùng', async () => {
    await expect(kiemTra({ lamThem: { ...lamThemHopLe, uuTienLoai: [] } })).rejects.toThrow();
    await expect(
      kiemTra({ lamThem: { ...lamThemHopLe, uuTienLoai: ['ngay_dem', 'ngay_dem', 'ngay_thuong'] } }),
    ).rejects.toThrow(/trùng/i);
  });

  it('từ chối hệ số ≤ 0 — hệ số 0 làm dòng lương bằng 0, hệ số âm làm nó âm', async () => {
    await expect(
      kiemTra({ lamThem: { ...lamThemHopLe, heSoTra: { ...lamThemHopLe.heSoTra, ngay_dem: 0 } } }),
    ).rejects.toThrow();
  });

  it('từ chối mienThueChenh có khoá ngoài uuTienLoai', async () => {
    await expect(
      kiemTra({ lamThem: { ...lamThemHopLe, mienThueChenh: ['ngay_dem', 'ngay_go_nham'] } }),
    ).rejects.toThrow(/mienThueChenh/i);
  });

  it('khungGioDem null hợp lệ — công ty không có ca đêm', async () => {
    await expect(
      kiemTra({ lamThem: { ...lamThemHopLe, khungGioDem: null } }),
    ).resolves.toBeDefined();
  });

  it('từ chối khungGioDem sai dạng hoặc hai mốc bằng nhau', async () => {
    await expect(
      kiemTra({ lamThem: { ...lamThemHopLe, khungGioDem: { tu: '25:00', den: '06:00' } } }),
    ).rejects.toThrow();
    await expect(
      kiemTra({ lamThem: { ...lamThemHopLe, khungGioDem: { tu: '22:00', den: '22:00' } } }),
    ).rejects.toThrow(/bằng nhau|khác nhau/i);
  });

  it('vẫn giữ sàn BLLĐ Đ98.1 cho chi_nghi_bu', async () => {
    await expect(
      kiemTra({
        lamThem: {
          ...lamThemHopLe,
          heSoTichQuy: { ...lamThemHopLe.heSoTichQuy, ngay_le: 2.0 },
        },
      }),
    ).rejects.toThrow(/3\.0/);
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong/dto
```
Expected: FAIL ngay test đầu — `heSoTra`/`khungGioDem`/`uuTienLoai`/`mienThueChenh` bị `forbidNonWhitelisted` chặn.

- [ ] **Step 3: Thay `HeSoTichQuyDto` bằng bảng tra mở**

Thay `cap-nhat-cau-hinh-luong.dto.ts:18-22` và `:33`:

```ts
/** Khung giờ đêm — hai mốc "HH:mm". */
export class KhungGioDemDto {
  @Matches(/^([01]\d|2[0-3]):[0-5]\d$/, { message: 'khungGioDem.tu phải đúng dạng HH:mm' })
  tu: string;

  @Matches(/^([01]\d|2[0-3]):[0-5]\d$/, { message: 'khungGioDem.den phải đúng dạng HH:mm' })
  den: string;
}

/** Sàn hệ số TÍCH QUỸ khi `cheDoBu = 'chi_nghi_bu'` — đúng BẰNG sàn trả tiền
 *  BLLĐ 2019 Đ98.1. Nghỉ bù là hình thức bù DUY NHẤT ở chế độ này (không trả
 *  thêm tiền), nên tích dưới sàn này là trả thiếu công cho người lao động.
 *  `ngay_dem` KHÔNG có sàn luật riêng ở đây — hệ số ca đêm chủ sản phẩm chốt
 *  1.5, thấp hơn NĐ 145/2020 Đ57.2, ghi nhận ở spec P4.2b §8.1. */
const SAN_HE_SO_CHI_NGHI_BU: Record<string, number> = {
  ngay_thuong: 1.5,
  ngay_nghi: 2.0,
  ngay_le: 3.0,
};
```

Xoá hằng `TRUONG_HE_SO` (`:33`) — thay bằng duyệt `Object.keys(SAN_HE_SO_CHI_NGHI_BU)`. Thêm `Matches`, `IsBoolean` (nếu cần) vào import từ `class-validator`.

- [ ] **Step 4: Bổ sung ràng buộc vào `CauHinhLamThemHopLe.lyDoTuChoi()`**

Chèn vào **đầu** `lyDoTuChoi()` (ngay sau `if (!lt) return null;`), trước các nhánh `cheDoBu`:

```ts
    const uuTien: string[] = lt.uuTienLoai ?? [];
    if (!Array.isArray(uuTien) || uuTien.length === 0) {
      return 'uuTienLoai phải có ít nhất một loại ngày';
    }
    if (new Set(uuTien).size !== uuTien.length) {
      return 'uuTienLoai có khoá trùng — mỗi loại ngày chỉ được xuất hiện một lần';
    }

    // Thiếu một khoá là hệ số `undefined`, nhân ra NaN; NaN đi qua
    // lamTronGio()/lamTronTheo() vẫn là NaN, ghi vào DB im lặng, và chỉ lộ ra
    // khi kế toán nhìn bảng lương.
    for (const [ten, bang] of [
      ['heSoTra', lt.heSoTra],
      ['heSoTichQuy', lt.heSoTichQuy],
    ] as const) {
      for (const loai of uuTien) {
        const v = bang?.[loai];
        if (typeof v !== 'number' || !Number.isFinite(v)) {
          return `${ten} thiếu hệ số cho loại "${loai}"`;
        }
        if (v <= 0) {
          return `${ten}."${loai}" phải > 0 — hệ số 0 làm dòng lương bằng 0, hệ số âm làm nó âm (đang là ${v})`;
        }
      }
    }

    for (const loai of lt.mienThueChenh ?? []) {
      if (!uuTien.includes(loai)) {
        return `mienThueChenh chứa loại "${loai}" không có trong uuTienLoai — gần như chắc chắn là gõ nhầm, và hậu quả là miễn thuế hụt trong im lặng`;
      }
    }

    if (lt.khungGioDem) {
      if (lt.khungGioDem.tu === lt.khungGioDem.den) {
        return 'khungGioDem.tu và khungGioDem.den phải khác nhau — hai mốc bằng nhau không rõ là khung rỗng hay khung 24 giờ';
      }
    }
```

Sửa hai vòng lặp `for (const truong of TRUONG_HE_SO)` còn lại trong hàm thành `for (const truong of Object.keys(SAN_HE_SO_CHI_NGHI_BU))`.

Trong nhánh `nghi_bu_va_chenh`, kiểm hệ số 1.0 phải duyệt **`uuTien`** (mọi loại), không chỉ ba loại có sàn — bằng không thêm `ngay_dem` là mở lại đúng lỗ hổng trả gấp đôi mà ràng buộc đó tồn tại để chặn.

- [ ] **Step 5: Khai bốn trường mới trên `CauHinhLamThemDto`**

Thay `:110-119`:

```ts
export class CauHinhLamThemDto {
  @IsIn(['chi_nghi_bu', 'chi_tien', 'nhan_vien_chon', 'nghi_bu_va_chenh'])
  cheDoBu: string;

  // Bảng tra mở: `@IsObject` chứ không DTO khai cứng từng khoá — khai cứng là
  // quay lại đúng cái hardcode P4.2b tồn tại để gỡ. Nội dung được
  // `CauHinhLamThemHopLe` kiểm chéo với `uuTienLoai`.
  @IsObject() heSoTra: Record<string, number>;
  @IsObject() heSoTichQuy: Record<string, number>;

  @IsOptional()
  @ValidateNested()
  @Type(() => KhungGioDemDto)
  khungGioDem: KhungGioDemDto | null;

  @IsArray() @IsString({ each: true }) uuTienLoai: string[];
  @IsArray() @IsString({ each: true }) mienThueChenh: string[];

  @IsOptional() @IsInt() @Min(1) soThangHanDung: number | null;

  @IsIn(['quy_ra_tien', 'huy_bo']) khiHetHan: string;
}
```

Thêm `IsString` vào import từ `class-validator`.

- [ ] **Step 6: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong
```
Expected: PASS. Test cũ nào dựng `lamThem` chỉ có bốn trường P4.2a sẽ đỏ vì thiếu `heSoTra`/`uuTienLoai` — bổ sung bốn trường mới vào fixture đó (đây là thay đổi cố ý).

- [ ] **Step 7: Commit**

```bash
git add be/apps/config-service/src/bang-luong/dto/
git commit -m "feat(be): DTO nhận bảng hệ số mở + khung giờ đêm

HeSoTichQuyDto khai cứng ba trường nên ngay_dem bị chặn, và forbidNonWhitelisted
làm bốn trường mới trả 400 cho CẢ form cấu hình lương.

Ràng buộc quan trọng nhất: mọi khoá trong uuTienLoai phải có mặt trong cả
heSoTra lẫn heSoTichQuy. Thiếu một khoá là hệ số undefined, nhân ra NaN, mà
NaN đi qua lamTronGio() vẫn là NaN rồi nằm im trong DB — chỉ lộ ra khi kế
toán nhìn bảng lương."
```

---

## Task 8: Script backfill đơn cũ

**Files:**
- Create: `ops/backfill-phan-bo-ot.ts`
- Modify: `ops/README.md`

**Interfaces:**
- Consumes: `AttendanceRequest.phanBoOt` (Task 6); `CauHinhLuong.lamThem.heSoTichQuy` (Task 3)
- Produces: script chạy được trong container `nhan-su-be`

**Bối cảnh:** Đơn nộp trước P4.2b có `soGioOt`/`heSoOt`/`loaiNgayOt` nhưng không có `phanBoOt`. Đường cũ trong `gioTichTuDonOt()` vẫn xử lý được, nhưng P4.2c đọc `phanBoOt` để dựng mẫu 03-LĐTL — thiếu là đơn cũ biến mất khỏi biểu mẫu.

**CỐ Ý không chẻ lại theo khung đêm**, dù đơn cũ có đủ `gioTu`/`gioDen`: chẻ lại là đổi hệ số của đơn đã duyệt và đã tích quỹ, quỹ sẽ lệch với sổ và `doiSoat()` báo đỏ hàng loạt. Ca đêm áp cho đơn nộp **từ ngày deploy trở đi**.

- [ ] **Step 1: Đọc khuôn script sẵn có**

Đọc `ops/grant-quyen-module-moi.ts` để lấy đúng khuôn kết nối Mongo, đọc biến môi trường, và cách lặp theo tenant. Script mới bám sát khuôn đó — không tự nghĩ ra cách kết nối thứ hai.

- [ ] **Step 2: Viết script**

Tạo `ops/backfill-phan-bo-ot.ts`:

```ts
/**
 * Backfill `phanBoOt` cho đơn làm thêm nộp TRƯỚC P4.2b.
 *
 * CỐ Ý không chẻ lại theo khung giờ đêm dù đơn cũ có đủ gioTu/gioDen: chẻ lại
 * là đổi hệ số của đơn đã duyệt và đã tích quỹ — quỹ sẽ lệch với sổ và
 * doiSoat() báo đỏ hàng loạt. Ca đêm chỉ áp cho đơn nộp từ ngày deploy.
 *
 * An toàn khi chạy lại: chỉ đụng đơn CHƯA có `phanBoOt`.
 *
 * Chạy TRONG container (không chạy từ máy trạm — `ts-node` ép commonjs, cùng
 * khuôn với ops/grant-quyen-module-moi.ts):
 *   docker exec -it nhan-su-be npx ts-node --compiler-options '{"module":"commonjs"}' ops/backfill-phan-bo-ot.ts
 *   docker exec -it nhan-su-be npx ts-node --compiler-options '{"module":"commonjs"}' ops/backfill-phan-bo-ot.ts --that   # ghi thật
 */
import { MongoClient } from 'mongodb';

const GHI_THAT = process.argv.includes('--that');

/** Bảng tra — cùng quy ước `traHeSo()`: loại lạ rơi về ngày thường (hệ số
 *  thấp nhất), bảng rỗng rơi về 1.0 chứ không NaN. */
function traHeSo(bang: Record<string, number>, loai: string): number {
  return bang?.[loai] ?? bang?.ngay_thuong ?? 1;
}

async function main() {
  const uri = process.env.MONGODB_URI;
  const dbName = process.env.MONGODB_DATABASE ?? 'nhan_su';
  if (!uri) throw new Error('Thiếu MONGODB_URI');

  const client = await MongoClient.connect(uri);
  const db = client.db(dbName);

  const cauHinhs = await db.collection('cau_hinh_luong').find({}).toArray();
  // Cấu hình theo tenant; đơn cũng theo tenant. Dùng nhầm bảng hệ số của tenant
  // khác là ghi sai giờ nghỉ bù của người lao động.
  const bangTheoTenant = new Map<string, Record<string, number>>();
  for (const ch of cauHinhs) {
    bangTheoTenant.set(String(ch.tenantId ?? ''), ch.lamThem?.heSoTichQuy ?? {});
  }

  const dons = await db
    .collection('attendance_requests')
    .find({ loaiDon: 'lam_them_gio', phanBoOt: { $exists: false } })
    .toArray();

  let da = 0;
  let boQua = 0;

  for (const don of dons) {
    const bang = bangTheoTenant.get(String(don.tenantId ?? ''));
    if (!bang) {
      console.warn(`Bỏ qua đơn ${don._id}: tenant ${don.tenantId} chưa khai cấu hình lương`);
      boQua++;
      continue;
    }
    if (typeof don.soGioOt !== 'number' || !don.loaiNgayOt) {
      console.warn(`Bỏ qua đơn ${don._id}: thiếu soGioOt/loaiNgayOt`);
      boQua++;
      continue;
    }

    const phanBoOt = [
      {
        loaiNgayOt: don.loaiNgayOt,
        soGio: don.soGioOt,
        // heSoOt đã snapshot lúc tạo đơn — giữ nguyên, KHÔNG tra lại bảng
        // hiện tại, nếu không là đổi số của đơn đã duyệt.
        heSoTra: typeof don.heSoOt === 'number' ? don.heSoOt : traHeSo(bang, don.loaiNgayOt),
        heSoTichQuy: traHeSo(bang, don.loaiNgayOt),
      },
    ];

    if (GHI_THAT) {
      await db
        .collection('attendance_requests')
        .updateOne({ _id: don._id }, { $set: { phanBoOt } });
    }
    da++;
  }

  console.log(
    `${GHI_THAT ? 'ĐÃ GHI' : 'CHẠY THỬ (thêm --that để ghi thật)'}: ${da} đơn được backfill, ${boQua} đơn bỏ qua, tổng ${dons.length}.`,
  );

  await client.close();
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 3: Chạy thử (chế độ không ghi)**

```bash
docker exec -it nhan-su-be npx ts-node --compiler-options '{"module":"commonjs"}' ops/backfill-phan-bo-ot.ts
```
Expected: in ra `CHẠY THỬ (thêm --that để ghi thật): N đơn được backfill, …`. Không có container đang chạy thì bỏ qua bước này và ghi lại trong PR rằng script chưa được chạy thật.

- [ ] **Step 4: Thêm mục vào runbook**

Thêm vào `ops/README.md` một mục "Backfill `phanBoOt` (P4.2b)", theo đúng khuôn các mục có sẵn: khi nào phải chạy (ngay sau khi deploy P4.2b, trước khi bật P4.2c), lệnh chạy thử, lệnh ghi thật, và câu cảnh báo rằng script CỐ Ý không chẻ lại đơn cũ theo khung đêm.

- [ ] **Step 5: Commit**

```bash
git add ops/backfill-phan-bo-ot.ts ops/README.md
git commit -m "feat(ops): script backfill phanBoOt cho đơn làm thêm cũ

Cố ý không chẻ lại theo khung giờ đêm dù đơn cũ có đủ gioTu/gioDen: chẻ lại
là đổi hệ số của đơn đã duyệt và đã tích quỹ, quỹ sẽ lệch với sổ và doiSoat()
báo đỏ hàng loạt. Ca đêm áp cho đơn nộp từ ngày deploy trở đi.

Mặc định chạy thử; phải thêm --that mới ghi. An toàn khi chạy lại."
```

---

## Task 9: Frontend — bảng hệ số và khung giờ đêm

**Files:**
- Modify: `fe/src/services/cauHinhLuongService.ts:52-72`
- Modify: `fe/src/pages/luong/cau-hinh-luong/components/LamThemEditor.tsx:33-37, :150-172`
- Test: `fe/src/pages/luong/cau-hinh-luong/LamThemEditor.render.test.tsx`

**Interfaces:**
- Consumes: `CauHinhLamThemDto` (Task 7) — hình dạng gửi lên phải khớp đúng, sai một trường là 400 cho cả form
- Produces: `CauHinhLamThem` phía FE có `heSoTra`, `khungGioDem`, `uuTienLoai`, `mienThueChenh`; `LAM_THEM_MAC_DINH` seed đủ bốn loại

- [ ] **Step 1: Cập nhật kiểu và mặc định phía FE**

Thay `fe/src/services/cauHinhLuongService.ts:52-72`:

```ts
export interface CauHinhLamThem {
  cheDoBu: CheDoBuLamThem;
  /** Hệ số TRẢ TIỀN từng loại ngày (P4.2c dùng). */
  heSoTra: Record<string, number>;
  /** Hệ số quy đổi vào quỹ nghỉ bù. */
  heSoTichQuy: Record<string, number>;
  /** null = công ty không có ca đêm. */
  khungGioDem: { tu: string; den: string } | null;
  /** Giờ thuộc nhiều loại thì loại đứng trước thắng. */
  uuTienLoai: string[];
  /** Loại nào được tách phần chênh miễn thuế TNCN. */
  mienThueChenh: string[];
  /** null = quỹ không bao giờ hết hạn. */
  soThangHanDung: number | null;
  khiHetHan: 'quy_ra_tien' | 'huy_bo';
}

/** Sàn BLLĐ 2019 Đ98.1 — backend chặn nếu hệ số TÍCH QUỸ khai thấp hơn ở chế
 *  độ chi_nghi_bu. `ngay_dem` không có sàn ở đây (xem spec P4.2b §8.1). */
export const HE_SO_TICH_SAN: Record<string, number> = {
  ngay_thuong: 1.5,
  ngay_nghi: 2.0,
  ngay_le: 3.0,
};

/** Nhãn tiếng Việt + thứ tự hiển thị của các loại ngày mặc định. Loại do công
 *  ty tự thêm không có trong bảng này thì hiện chính khoá của nó. */
export const NHAN_LOAI_NGAY: Record<string, string> = {
  ngay_thuong: 'Ngày thường',
  ngay_nghi: 'Ngày nghỉ hằng tuần',
  ngay_le: 'Ngày lễ / Tết',
  ngay_dem: 'Buổi đêm',
};

export const LAM_THEM_MAC_DINH: CauHinhLamThem = {
  cheDoBu: 'chi_nghi_bu',
  heSoTra: { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 },
  heSoTichQuy: { ngay_thuong: 1.5, ngay_nghi: 2.0, ngay_le: 3.0, ngay_dem: 1.5 },
  khungGioDem: { tu: '22:00', den: '06:00' },
  uuTienLoai: ['ngay_le', 'ngay_nghi', 'ngay_dem', 'ngay_thuong'],
  mienThueChenh: ['ngay_dem'],
  soThangHanDung: null,
  khiHetHan: 'quy_ra_tien',
};
```

- [ ] **Step 2: Viết test render**

Thêm vào `LamThemEditor.render.test.tsx` (theo đúng khuôn dựng context sẵn có trong file):

```tsx
describe('LamThemEditor — bảng hệ số (P4.2b)', () => {
  it('hiện một dòng cho mỗi loại trong uuTienLoai, kể cả ca đêm', () => {
    renderVoiCauHinh({ lamThem: LAM_THEM_MAC_DINH, soGioMoiNgay: 8 });

    expect(screen.getByText('Ngày lễ / Tết')).toBeInTheDocument();
    expect(screen.getByText('Ngày nghỉ hằng tuần')).toBeInTheDocument();
    expect(screen.getByText('Buổi đêm')).toBeInTheDocument();
    expect(screen.getByText('Ngày thường')).toBeInTheDocument();
  });

  it('hiện loại do công ty tự thêm bằng chính khoá của nó', () => {
    renderVoiCauHinh({
      soGioMoiNgay: 8,
      lamThem: {
        ...LAM_THEM_MAC_DINH,
        uuTienLoai: [...LAM_THEM_MAC_DINH.uuTienLoai, 'ngay_bao'],
        heSoTra: { ...LAM_THEM_MAC_DINH.heSoTra, ngay_bao: 2.5 },
        heSoTichQuy: { ...LAM_THEM_MAC_DINH.heSoTichQuy, ngay_bao: 2.5 },
      },
    });

    expect(screen.getByText('ngay_bao')).toBeInTheDocument();
  });

  it('tắt khung giờ đêm thì ẩn hai ô mốc giờ', () => {
    renderVoiCauHinh({
      soGioMoiNgay: 8,
      lamThem: { ...LAM_THEM_MAC_DINH, khungGioDem: null },
    });

    expect(screen.queryByLabelText(/Từ/i)).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Chạy test, xác nhận ĐỎ**

```bash
cd fe && nvm use && npm run test -- src/pages/luong/cau-hinh-luong/LamThemEditor.render.test.tsx
```
Expected: FAIL — `Buổi đêm` không có trong DOM (component đang lặp `TRUONG_HE_SO` cứng ba loại).

- [ ] **Step 4: Viết lại khối hệ số trong `LamThemEditor.tsx`**

Xoá hằng `TRUONG_HE_SO` (`:33-37`). Thêm import `NHAN_LOAI_NGAY` từ `@/services/cauHinhLuongService`, và `TimePicker` từ `antd`, `dayjs` (đã là phụ thuộc của antd).

Thay khối `{TRUONG_HE_SO.map(...)}` (`:150-172`) bằng một bảng lặp theo `lamThem.uuTienLoai`:

```tsx
        <Col span={24}>
          <FieldLabel>Hệ số theo loại ngày</FieldLabel>
          <p className="mb-2 text-xs text-muted-foreground">
            Thứ tự từ trên xuống là thứ tự ưu tiên: giờ thuộc nhiều loại thì
            loại ở trên thắng. Mặc định lễ &gt; nghỉ &gt; đêm &gt; thường, nên
            làm đêm ngày lễ vẫn ăn hệ số lễ.
          </p>
          <table className="w-full text-sm">
            <thead>
              <tr className="text-left">
                <th className="py-1">Loại ngày</th>
                <th className="py-1">Hệ số trả tiền</th>
                <th className="py-1">Hệ số tích quỹ</th>
                <th className="py-1">Miễn thuế phần chênh</th>
              </tr>
            </thead>
            <tbody>
              {lamThem.uuTienLoai.map((khoa) => (
                <tr key={khoa}>
                  <td className="py-1 pr-2">{NHAN_LOAI_NGAY[khoa] ?? khoa}</td>
                  <td className="py-1 pr-2">
                    <InputNumber
                      className="w-full"
                      min={0.1}
                      step={0.5}
                      value={lamThem.heSoTra[khoa]}
                      disabled={!canEdit}
                      onChange={(v) =>
                        capNhatLamThem({
                          heSoTra: { ...lamThem.heSoTra, [khoa]: v ?? 1 },
                        })
                      }
                    />
                  </td>
                  <td className="py-1 pr-2">
                    <InputNumber
                      className="w-full"
                      min={HE_SO_TICH_SAN[khoa] ?? 0.1}
                      step={0.5}
                      value={lamThem.heSoTichQuy[khoa]}
                      disabled={!canEdit}
                      onChange={(v) =>
                        capNhatLamThem({
                          heSoTichQuy: {
                            ...lamThem.heSoTichQuy,
                            [khoa]: v ?? HE_SO_TICH_SAN[khoa] ?? 1,
                          },
                        })
                      }
                    />
                  </td>
                  <td className="py-1">
                    <Switch
                      checked={lamThem.mienThueChenh.includes(khoa)}
                      disabled={!canEdit}
                      onChange={(bat) =>
                        capNhatLamThem({
                          mienThueChenh: bat
                            ? [...lamThem.mienThueChenh, khoa]
                            : lamThem.mienThueChenh.filter((x) => x !== khoa),
                        })
                      }
                    />
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
          <p className="mt-1 text-xs text-muted-foreground">
            Hệ số tích quỹ tối thiểu 1,5 / 2,0 / 3,0 cho ngày thường / ngày
            nghỉ / ngày lễ (sàn BLLĐ 2019 Đ98.1) khi chế độ là “chỉ nghỉ bù”.
          </p>
        </Col>

        <Col span={8} className="flex items-end gap-2">
          <Switch
            checked={lamThem.khungGioDem !== null}
            disabled={!canEdit}
            onChange={(bat) =>
              capNhatLamThem({
                khungGioDem: bat ? { tu: "22:00", den: "06:00" } : null,
              })
            }
          />
          <span className="text-sm">Công ty có ca đêm</span>
        </Col>

        {lamThem.khungGioDem && (
          <Col span={16}>
            <FieldLabel>Khung giờ ban đêm</FieldLabel>
            <div className="flex items-center gap-2">
              <TimePicker
                aria-label="Từ"
                format="HH:mm"
                minuteStep={15}
                value={dayjs(lamThem.khungGioDem.tu, "HH:mm")}
                disabled={!canEdit}
                onChange={(v) =>
                  capNhatLamThem({
                    khungGioDem: {
                      ...lamThem.khungGioDem!,
                      tu: v?.format("HH:mm") ?? "22:00",
                    },
                  })
                }
              />
              <span>→</span>
              <TimePicker
                aria-label="Đến"
                format="HH:mm"
                minuteStep={15}
                value={dayjs(lamThem.khungGioDem.den, "HH:mm")}
                disabled={!canEdit}
                onChange={(v) =>
                  capNhatLamThem({
                    khungGioDem: {
                      ...lamThem.khungGioDem!,
                      den: v?.format("HH:mm") ?? "06:00",
                    },
                  })
                }
              />
            </div>
            <p className="mt-1 text-xs text-muted-foreground">
              BLLĐ 2019 Đ106 định nghĩa ban đêm là 22:00–06:00. Giờ làm thêm
              rơi vào khung này được tách riêng khi tính lương.
            </p>
          </Col>
        )}
```

- [ ] **Step 5: Chạy test, xác nhận XANH**

```bash
cd fe && nvm use && npm run test -- src/pages/luong/cau-hinh-luong
```
Expected: PASS. Test cũ nào dựng `lamThem` thiếu bốn trường mới sẽ đỏ khi component đọc `lamThem.uuTienLoai.map` — sửa fixture đó dùng `LAM_THEM_MAC_DINH`.

- [ ] **Step 6: Chạy toàn bộ test FE + kiểm biên dịch**

```bash
cd fe && nvm use && npx tsc --noEmit && npm run test
```
Expected: không lỗi biên dịch, test PASS.

- [ ] **Step 7: Commit**

```bash
git add fe/src/services/cauHinhLuongService.ts fe/src/pages/luong/cau-hinh-luong/
git commit -m "feat(fe): màn cấu hình bảng hệ số làm thêm + khung giờ đêm

Bảng lặp theo uuTienLoai chứ không ba dòng cứng — công ty thêm loại ngày thì
màn tự có thêm dòng, và loại không có nhãn tiếng Việt hiện chính khoá của nó.

Tắt công tắc ca đêm đặt khungGioDem = null; backend hiểu là công ty không có
ca đêm và không chẻ đơn."
```

---

## Self-Review

**1. Spec coverage**

| Mục spec | Task |
|---|---|
| §2 bốn quyết định nghiệp vụ → giá trị seed | 3 (be), 9 (fe) |
| §3.1 ba chỗ hardcode | 3 (`luat-don.ts:14-15`), 5 (`luat-quy-gio.ts:131-136`) |
| §3.2 hình dạng cấu hình mới | 3 |
| §3.3 seed mặc định | 3 (be), 9 (fe) |
| §3.4 kiểm tra hợp lệ khi lưu | 7 |
| §4.1 `chiaGioOtTheoLoai()`, mốc tuyệt đối, bất biến tổng giờ, không làm tròn | 4 |
| §4.2 `phanBoOt`, trường dẫn xuất, `gioTichTuDonOt` | 5, 6 |
| §4.3 backfill, không chẻ lại đơn cũ | 8 |
| §5 giữ chỗ nguyên tử cả hai quỹ | 1, 2 |
| §6 màn cấu hình FE | 9 |
| §7 ngoài phạm vi | Global Constraints (cấm đụng `tinh-luong.ts`) |
| §8 rủi ro đã ghi nhận | doc-comment ở Task 3 Step 3, Task 7 Step 3 |

Không có mục nào thiếu task.

**2. Placeholder scan** — không có "TBD"/"TODO"/"tương tự Task N". Ba chỗ nói "theo đúng khuôn sẵn có trong file" (Task 2 Step 1, Task 6 Step 1, Task 9 Step 2) là chỉ dẫn tới khuôn cụ thể đã dẫn số dòng ở nơi khác, kèm code đầy đủ cho phần mới.

**3. Type consistency**

- `PhanBoOt` — Task 4 khai tạm trong `luat-don.ts`, Task 6 Step 4 chuyển sang `import type` từ `@app/entities`. Cùng bốn trường ở cả hai chỗ. ✓
- `traHeSo(bang, loai)` — khai Task 3, dùng ở Task 4 (`chiaGioOtTheoLoai`), Task 5 (`gioTichTuDonOt`), Task 6 (nhánh chưa khai `lamThem`), Task 8 (chép vào script ops vì script không import mã app). ✓
- `HeSoTichQuy` = `Record<string, number>` (Task 3) — `luat-quy-gio.ts`, `quy-gio.service.ts`, DTO đều dùng `Record`. ✓
- `suyLoaiNgay` trả `LoaiNgayOt` (= `string`); `gopPhanBoOt` trả `{soGioOt, loaiNgayOt, heSoOt}` khớp đúng ba cột entity. ✓
- `cauHinhChiaGio()` (Task 6 Step 5) trả bốn trường đúng bằng bốn tham số `chiaGioOtTheoLoai` cần ngoài `gioTu`/`gioDen`/`loaiNgay`. ✓
- `MA_LOI_QUY_GIO.DANG_SUA_DONG_THOI` / `MA_LOI_QUY_PHEP.DANG_SUA_DONG_THOI` — tên hằng giống nhau, giá trị chuỗi khác nhau theo tiền tố quỹ, khớp lệ đặt tên sẵn có. ✓

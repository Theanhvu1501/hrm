# P4.3 — Phiếu lương — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Nhân viên tự xem phiếu lương của mình trên vỏ `/toi`, kế toán in được phiếu từ Bảng lương.

**Architecture:** Một hàm thuần dựng `PhieuLuong` từ `DongLuong` + danh mục khoản (lọc bỏ mức khai báo, ghép nhãn tiếng Việt); hai route tự phục vụ `cua-toi` trên controller bảng lương, `employeeId` suy từ token; một màn `/toi/phieu-luong` và một nút in dùng lại cơ chế iframe sandbox sẵn có.

**Tech Stack:** NestJS 10 + TypeORM (MongoDB), Jest; React 18 + Vite + antd + Vitest; yarn (be), npm (fe); Node 22.

**Spec:** `docs/superpowers/specs/2026-08-03-hrm-p4.3-phieu-luong-design.md`

## Global Constraints

- **Node 22.** `cd be && nvm use v22.0.0`; `cd fe && nvm use`.
- **Baseline:** backend `yarn test` xanh toàn bộ (một test `AuthServiceService › Property 8: Password Hashing` nằm sát mốc timeout 5s của Jest, đỏ ngẫu nhiên và đỏ cả trên `main` — không phải hồi quy). `tsc --noEmit -p tsconfig.json` ra đúng **70** dòng. FE `npm run test` xanh, `tsc --noEmit` sạch.
- **Payload phiếu lương TUYỆT ĐỐI không mang `khaiBao`, `mucKhaiBao`, `luongThoaThuan`.** Lọc ở service — lọc phía FE nghĩa là dữ liệu vẫn đi qua đường truyền và nằm trong tab Network của mọi nhân viên.
- **`employeeId` LUÔN suy từ token** qua `NhanVien_Service.resolveEmployeeFromUser(req.user)`, không bao giờ đọc từ query/body. Đây là toàn bộ ranh giới ngăn xem lương đồng nghiệp.
- **Route `cua-toi` KHÔNG khai `@Permissions`** — `quetPhanQuyenRoute()` nhận tiền tố `cua-toi` là tự phục vụ và ép đúng luật đó; khai `@Permissions` vào là test đỏ.
- **Route `cua-toi` phải khai TRƯỚC mọi route `:id`** — nếu không NestJS khớp chuỗi `"cua-toi"` thành `:id`, và vì route đó đòi quyền nên nhân viên nhận nhầm 403.
- **Chỉ kỳ `trangThai === 'chot'`.** Kỳ `nhap` coi như chưa có phiếu.
- **Commit sau mỗi task**, message tiếng Việt theo lệ repo.

---

## File Structure

| File | Trách nhiệm | Task |
|---|---|---|
| `be/libs/entities/src/luong/luong.types.ts` | kiểu `PhieuLuong` | 1 |
| `be/libs/core/src/luong/dung-phieu-luong.ts` | hàm thuần dựng phiếu từ `DongLuong` | 1 |
| `be/apps/config-service/src/bang-luong/bang-luong.service.ts` | `phieuLuongCuaToi`, `kyCoPhieuLuong` | 2 |
| `be/apps/config-service/src/bang-luong/bang-luong.controller.ts` | hai route `cua-toi` | 2 |
| `fe/src/utils/printHtml.ts` | cơ chế in dùng chung (tách từ `printHopDong`) | 3 |
| `fe/src/pages/nhan-su/hop-dong-lao-dong/lib/printHopDong.ts` | gọi lại util chung | 3 |
| `fe/src/services/phieuLuongService.ts` | client | 4 |
| `fe/src/pages/toi/phieu-luong/**` | màn nhân viên | 4 |
| `fe/src/pages/luong/bang-luong/lib/phieuLuongHtml.ts` | dựng HTML phiếu để in | 5 |
| `fe/src/pages/luong/bang-luong/components/BangLuongTable.tsx` | nút In phiếu lương | 5 |

---

## Task 1: Hàm thuần `dungPhieuLuong()`

**Files:**
- Modify: `be/libs/entities/src/luong/luong.types.ts`
- Create: `be/libs/core/src/luong/dung-phieu-luong.ts`
- Create: `be/libs/core/src/luong/dung-phieu-luong.spec.ts`
- Modify: `be/libs/core/src/index.ts`

**Interfaces:**
- Consumes: `DongLuong` (entity), `KhoanLuong` (`luong.types.ts`)
- Produces:
  ```ts
  export interface KhoanPhieuLuong { ma: string; ten: string; soTien: number }
  export interface PhieuLuong {
    thang: string; hoTen: string; maNhanVien: string;
    congThuong: number; congThuViec: number; congKhac: number;
    khoan: KhoanPhieuLuong[];
    tongThuNhap: number; bhxh: number; thue: number; phiCongDoan: number;
    tamUng: number; khauTruKhac: number; thucLinh: number;
    thuNhapMienThue: number; giamTru: number; thuNhapTinhThue: number;
  }
  export function dungPhieuLuong(dong: DongLuong, khoanLuong: KhoanLuong[]): PhieuLuong;
  ```

- [ ] **Step 1: Viết test**

Tạo `be/libs/core/src/luong/dung-phieu-luong.spec.ts`:

```ts
import { dungPhieuLuong } from './dung-phieu-luong';
import type { KhoanLuong } from '@app/entities';

const KHOAN: KhoanLuong[] = [
  { ma: 'LUONG_CONG', ten: 'Lương theo công', loaiCongThuc: 'LUONG_THEO_CONG', thamSo: {}, chiuThue: true, tranMienThue: null, vaoTongThuNhap: true, vaoBHXH: true, thuTu: 1 },
  { ma: 'AN_CA', ten: 'Ăn ca', loaiCongThuc: 'DINH_MUC_x_CONG', thamSo: {}, chiuThue: true, tranMienThue: 1_200_000, vaoTongThuNhap: true, vaoBHXH: false, thuTu: 2 },
  { ma: 'CP_NOI_BO', ten: 'Chi phí nội bộ', loaiCongThuc: 'NHAP_THEO_KY', thamSo: {}, chiuThue: false, tranMienThue: null, vaoTongThuNhap: false, vaoBHXH: false, thuTu: 8 },
];

const dong = (over: any = {}) => ({
  thang: '2026-07',
  employeeName: 'Đào Thị Kiều Oanh',
  employeeCode: 'NV0001',
  congThuong: 24, congThuViec: 0, congKhac: 0,
  mucKhaiBao: 5_500_000,
  luongThoaThuan: 15_000_000,
  tamUng: 500_000,
  khauTruKhac: 0,
  khaiBao: { tongThuNhap: 5_500_000, thucLinh: 4_922_500 },
  thucTe: {
    giaTriTungKhoan: { LUONG_CONG: 12_000_000, AN_CA: 1_200_000, CP_NOI_BO: 999_000 },
    tongThuNhap: 13_200_000,
    thuNhapMienThue: 16_900_000,
    mienThueKhoan: 1_200_000,
    otMienThue: 0,
    bhxh: 577_500,
    giamTru: 15_500_000,
    thuNhapTinhThue: 0,
    thue: 0,
    phiCongDoan: 110_000,
    thucLinh: 12_012_500,
    chiPhiBHCongTy: 1_182_500,
    tongChiPhiCongTy: 14_382_500,
  },
  trangThai: 'chot',
  ...over,
});

describe('dungPhieuLuong', () => {
  it('lấy số từ mức THỰC TẾ, không phải khai báo', () => {
    const p = dungPhieuLuong(dong() as any, KHOAN);

    expect(p.tongThuNhap).toBe(13_200_000);
    expect(p.thucLinh).toBe(12_012_500);
  });

  it('KHÔNG mang mức khai báo ra ngoài — kiểm trên CHÍNH object trả về', () => {
    // Lọc ở service chứ không ở FE: lọc phía FE thì dữ liệu vẫn đi qua đường
    // truyền và nằm trong tab Network của mọi nhân viên.
    const p = dungPhieuLuong(dong() as any, KHOAN) as any;
    const chuoi = JSON.stringify(p);

    expect(p.khaiBao).toBeUndefined();
    expect(p.mucKhaiBao).toBeUndefined();
    expect(p.luongThoaThuan).toBeUndefined();
    expect(chuoi).not.toContain('5500000');
    expect(chuoi).not.toContain('4922500');
  });

  it('ghép NHÃN tiếng Việt cho từng khoản — nhân viên không đọc được cấu hình', () => {
    const p = dungPhieuLuong(dong() as any, KHOAN);

    expect(p.khoan).toEqual([
      { ma: 'LUONG_CONG', ten: 'Lương theo công', soTien: 12_000_000 },
      { ma: 'AN_CA', ten: 'Ăn ca', soTien: 1_200_000 },
    ]);
  });

  it('BỎ khoản không vào tổng thu nhập — không phải thứ nhân viên nhận', () => {
    const p = dungPhieuLuong(dong() as any, KHOAN);
    expect(p.khoan.some((k) => k.ma === 'CP_NOI_BO')).toBe(false);
  });

  it('khoản có trong dòng lương nhưng KHÔNG còn trong cấu hình vẫn hiện, nhãn = mã', () => {
    // Cấu hình đổi giữa chừng: xoá một khoản khỏi danh mục KHÔNG được làm
    // biến mất một dòng tiền đã trả — nhân viên sẽ thấy tổng không khớp.
    const p = dungPhieuLuong(
      dong({
        thucTe: {
          ...dong().thucTe,
          giaTriTungKhoan: { ...dong().thucTe.giaTriTungKhoan, KHOAN_CU: 300_000 },
        },
      }) as any,
      KHOAN,
    );

    expect(p.khoan).toContainEqual({ ma: 'KHOAN_CU', ten: 'KHOAN_CU', soTien: 300_000 });
  });

  it('bỏ khoản giá trị 0 — phiếu lương không liệt kê thứ không có', () => {
    const p = dungPhieuLuong(
      dong({
        thucTe: { ...dong().thucTe, giaTriTungKhoan: { LUONG_CONG: 12_000_000, AN_CA: 0 } },
      }) as any,
      KHOAN,
    );

    expect(p.khoan.map((k) => k.ma)).toEqual(['LUONG_CONG']);
  });

  it('dòng chốt TRƯỚC P4.2c-2 không có phiCongDoan → 0, không undefined', () => {
    const t = { ...dong().thucTe } as any;
    delete t.phiCongDoan;

    const p = dungPhieuLuong(dong({ thucTe: t }) as any, KHOAN);
    expect(p.phiCongDoan).toBe(0);
  });

  it('mang đủ công, tạm ứng, khấu trừ khác', () => {
    const p = dungPhieuLuong(dong() as any, KHOAN);

    expect(p.congThuong).toBe(24);
    expect(p.tamUng).toBe(500_000);
    expect(p.khauTruKhac).toBe(0);
    expect(p.maNhanVien).toBe('NV0001');
    expect(p.hoTen).toBe('Đào Thị Kiều Oanh');
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test libs/core/src/luong/dung-phieu-luong.spec.ts
```
Expected: FAIL — `Cannot find module './dung-phieu-luong'`.

- [ ] **Step 3: Khai kiểu trong `luong.types.ts`**

```ts
/** Một dòng khoản cộng trên phiếu lương — đã ghép nhãn, FE không phải tra. */
export interface KhoanPhieuLuong {
  ma: string;
  ten: string;
  soTien: number;
}

/**
 * Phiếu lương giao cho NGƯỜI LAO ĐỘNG.
 *
 * CỐ Ý không có `khaiBao`/`mucKhaiBao`/`luongThoaThuan`: hiện mức khai báo là
 * phơi bày chiến lược khai báo BHXH của công ty cho toàn bộ nhân viên — một
 * quyết định kinh doanh, không phải lựa chọn giao diện (spec P4.3 §2.1).
 */
export interface PhieuLuong {
  thang: string;
  hoTen: string;
  maNhanVien: string;
  congThuong: number;
  congThuViec: number;
  congKhac: number;
  khoan: KhoanPhieuLuong[];
  tongThuNhap: number;
  bhxh: number;
  thue: number;
  phiCongDoan: number;
  tamUng: number;
  khauTruKhac: number;
  thucLinh: number;
  /** Ba số giải thích cách ra thuế — không phải để đối chiếu tờ khai. */
  thuNhapMienThue: number;
  giamTru: number;
  thuNhapTinhThue: number;
}
```

- [ ] **Step 4: Viết hàm thuần**

`be/libs/core/src/luong/dung-phieu-luong.ts`:

```ts
import type {
  DongLuong,
  KhoanLuong,
  KhoanPhieuLuong,
  PhieuLuong,
} from '@app/entities';

/**
 * Dựng phiếu lương giao cho người lao động từ một dòng bảng lương.
 *
 * Chỉ đọc `thucTe` — mức lương thật họ nhận. `khaiBao` không bao giờ đi ra
 * khỏi hàm này (spec P4.3 §2.1).
 *
 * Nhãn khoản ghép ở đây vì nhân viên KHÔNG có quyền `/luong/cau-hinh:xem` —
 * để FE tự tra thì hoặc màn phiếu lương 403, hoặc phải nới quyền cấu hình
 * lương cho mọi nhân viên. Cả hai đều tệ hơn ghép một mảng ở server.
 */
export function dungPhieuLuong(
  dong: DongLuong,
  khoanLuong: KhoanLuong[],
): PhieuLuong {
  const t: any = dong.thucTe ?? {};
  const theoMa = new Map(khoanLuong.map((k) => [k.ma, k]));

  const khoan: KhoanPhieuLuong[] = Object.entries(
    (t.giaTriTungKhoan ?? {}) as Record<string, number>,
  )
    .filter(([ma, soTien]) => {
      if (!soTien) return false; // không liệt kê thứ không có
      const k = theoMa.get(ma);
      // Khoản đã bị xoá khỏi danh mục vẫn giữ lại: xoá một khoản khỏi cấu
      // hình KHÔNG được làm biến mất một dòng tiền đã trả, nếu không nhân
      // viên thấy các khoản cộng lại không ra tổng thu nhập.
      return k ? k.vaoTongThuNhap : true;
    })
    .map(([ma, soTien]) => ({
      ma,
      ten: theoMa.get(ma)?.ten ?? ma,
      soTien,
    }));

  return {
    thang: dong.thang,
    hoTen: dong.employeeName ?? '',
    maNhanVien: dong.employeeCode ?? '',
    congThuong: dong.congThuong ?? 0,
    congThuViec: dong.congThuViec ?? 0,
    congKhac: dong.congKhac ?? 0,
    khoan,
    tongThuNhap: t.tongThuNhap ?? 0,
    bhxh: t.bhxh ?? 0,
    thue: t.thue ?? 0,
    // `?? 0`: dòng chốt trước P4.2c-2 không có trường này.
    phiCongDoan: t.phiCongDoan ?? 0,
    tamUng: dong.tamUng ?? 0,
    khauTruKhac: dong.khauTruKhac ?? 0,
    thucLinh: t.thucLinh ?? 0,
    thuNhapMienThue: t.thuNhapMienThue ?? 0,
    giamTru: t.giamTru ?? 0,
    thuNhapTinhThue: t.thuNhapTinhThue ?? 0,
  };
}
```

Export ra `libs/core/src/index.ts`.

- [ ] **Step 5: Chạy test, xác nhận XANH**

```bash
cd be && nvm use v22.0.0 && yarn test libs/core
```

- [ ] **Step 6: Commit**

```bash
git add be/libs
git commit -m "feat(be): hàm thuần dungPhieuLuong — chỉ mức thực tế, ghép nhãn khoản

khaiBao/mucKhaiBao/luongThoaThuan không bao giờ đi ra khỏi hàm này: hiện mức
khai báo là phơi bày chiến lược khai báo BHXH của công ty cho toàn bộ nhân
viên. Test kiểm trên chính chuỗi JSON trả về, không chỉ kiểm trường vắng mặt.

Nhãn khoản ghép ở server vì nhân viên không có quyền /luong/cau-hinh:xem.
Khoản đã xoá khỏi danh mục vẫn giữ lại (nhãn = mã): xoá khỏi cấu hình không
được làm biến mất một dòng tiền đã trả."
```

---

## Task 2: Hai route tự phục vụ

**Files:**
- Modify: `be/apps/config-service/src/bang-luong/bang-luong.service.ts`
- Modify: `be/apps/config-service/src/bang-luong/bang-luong.controller.ts`
- Modify: `be/apps/config-service/src/bang-luong/bang-luong.module.ts`
- Test: `be/apps/config-service/src/bang-luong/bang-luong.service.spec.ts`, `bang-luong.controller.spec.ts`

**Interfaces:**
- Consumes: `dungPhieuLuong` (Task 1), `NhanVien_Service.resolveEmployeeFromUser`
- Produces:
  ```ts
  BangLuong_Service.phieuLuongCuaToi(employeeId: string, thang: string): Promise<PhieuLuong | null>
  BangLuong_Service.kyCoPhieuLuong(employeeId: string): Promise<string[]>   // ['2026-07', '2026-06'] mới nhất trước
  ```
  Routes trên `@Controller('bang-luong')`:
  | Method | Path | Guard |
  |---|---|---|
  | GET | `cua-toi/ky` | chỉ `JwtGuard` |
  | GET | `cua-toi` | chỉ `JwtGuard` |

- [ ] **Step 1: Viết test service**

Thêm vào `bang-luong.service.spec.ts`:

```ts
  describe('phiếu lương tự phục vụ (P4.3)', () => {
    const dongChot = (over: any = {}) => ({
      _id: 'd1', thang: '2026-07', employeeId: EMP1,
      employeeName: 'A', employeeCode: 'NV0001',
      congThuong: 24, congThuViec: 0, congKhac: 0,
      mucKhaiBao: 5_500_000, luongThoaThuan: 15_000_000,
      tamUng: 0, khauTruKhac: 0,
      khaiBao: { tongThuNhap: 5_500_000, thucLinh: 4_922_500 },
      thucTe: {
        giaTriTungKhoan: { LUONG_CONG: 12_000_000 },
        tongThuNhap: 12_000_000, thuNhapMienThue: 15_500_000,
        mienThueKhoan: 0, otMienThue: 0, bhxh: 0, giamTru: 15_500_000,
        thuNhapTinhThue: 0, thue: 0, phiCongDoan: 110_000,
        thucLinh: 11_890_000, chiPhiBHCongTy: 0, tongChiPhiCongTy: 12_000_000,
      },
      trangThai: 'chot', isActive: true, ...over,
    });

    it('trả phiếu của kỳ ĐÃ CHỐT', async () => {
      seedCauHinh();
      dongLuongStore.push(dongChot());

      const p = await service.phieuLuongCuaToi(EMP1, '2026-07');
      expect(p?.thucLinh).toBe(11_890_000);
    });

    it('kỳ còn NHÁP trả null — con số ở đó còn thay đổi được', async () => {
      seedCauHinh();
      dongLuongStore.push(dongChot({ trangThai: 'nhap' }));

      await expect(service.phieuLuongCuaToi(EMP1, '2026-07')).resolves.toBeNull();
    });

    it('không có dòng thì trả null, không ném', async () => {
      seedCauHinh();
      await expect(service.phieuLuongCuaToi(EMP1, '2026-07')).resolves.toBeNull();
    });

    it('KHÔNG trả phiếu của người khác dù truyền đúng tháng', async () => {
      seedCauHinh();
      dongLuongStore.push(dongChot({ employeeId: EMP2 }));

      await expect(service.phieuLuongCuaToi(EMP1, '2026-07')).resolves.toBeNull();
    });

    it('payload không mang mức khai báo', async () => {
      seedCauHinh();
      dongLuongStore.push(dongChot());

      const p = await service.phieuLuongCuaToi(EMP1, '2026-07');
      const chuoi = JSON.stringify(p);
      expect(chuoi).not.toContain('4922500');
      expect(chuoi).not.toContain('5500000');
    });

    it('kyCoPhieuLuong chỉ liệt kê kỳ đã chốt, mới nhất trước', async () => {
      seedCauHinh();
      dongLuongStore.push(
        dongChot({ _id: 'd1', thang: '2026-06' }),
        dongChot({ _id: 'd2', thang: '2026-07' }),
        dongChot({ _id: 'd3', thang: '2026-08', trangThai: 'nhap' }),
      );

      await expect(service.kyCoPhieuLuong(EMP1)).resolves.toEqual([
        '2026-07',
        '2026-06',
      ]);
    });

    it('kyCoPhieuLuong KHÔNG lẫn kỳ của người khác', async () => {
      seedCauHinh();
      dongLuongStore.push(
        dongChot({ _id: 'd1', thang: '2026-06' }),
        dongChot({ _id: 'd2', thang: '2026-07', employeeId: EMP2 }),
      );

      await expect(service.kyCoPhieuLuong(EMP1)).resolves.toEqual(['2026-06']);
    });
  });
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong/bang-luong.service.spec.ts
```
Expected: FAIL — `service.phieuLuongCuaToi is not a function`.

- [ ] **Step 3: Viết hai method**

```ts
  /**
   * Phiếu lương của CHÍNH nhân viên đó cho một kỳ. `null` khi chưa có dòng
   * hoặc kỳ còn `nhap`.
   *
   * `employeeId` do controller suy từ token truyền xuống — service KHÔNG
   * nhận nó từ client ở bất kỳ đường nào khác.
   */
  async phieuLuongCuaToi(
    employeeId: string,
    thang: string,
  ): Promise<PhieuLuong | null> {
    const rows = await this.dongLuongRepo.find({
      where: { thang, employeeId } as any,
    });
    const dong = rows[0];
    if (!dong || dong.trangThai !== 'chot') return null;

    const ch = this.toCauHinhData(await this.layCauHinh());
    return dungPhieuLuong(dong, ch.khoanLuong ?? []);
  }

  /** Các kỳ nhân viên này CÓ phiếu (đã chốt), mới nhất trước. */
  async kyCoPhieuLuong(employeeId: string): Promise<string[]> {
    const rows = await this.dongLuongRepo.find({
      where: { employeeId } as any,
    });
    return rows
      .filter((r) => r.trangThai === 'chot')
      .map((r) => r.thang)
      .sort()
      .reverse();
  }
```

- [ ] **Step 4: Viết hai route + test phân quyền**

Trong `bang-luong.controller.ts`, khai **NGAY SAU** khối `cau-hinh` và **TRƯỚC** mọi route `:id`:

```ts
  // ── Tự phục vụ — PHẢI khai TRƯỚC route ':id' bên dưới. Nếu không, NestJS
  // khớp chuỗi "cua-toi" thành :id, và vì route đó đòi quyền nên nhân viên
  // nhận nhầm 403 (cùng bẫy đã ghi ở don-cham-cong.controller.ts).
  //
  // CỐ Ý không có `@Permissions`: bắt HR gán quyền cho từng người thì người
  // mới không xem được lương tháng đầu. Phạm vi khoá bằng `employeeId` suy từ
  // token — chặt hơn quyền. `quetPhanQuyenRoute()` ép đúng luật này.

  @Get('cua-toi/ky')
  async kyCoPhieuLuongCuaToi(@Req() req: any) {
    const emp = await this.nhanVien_Service.resolveEmployeeFromUser(req.user);
    const data = await this.bangLuong_Service.kyCoPhieuLuong(
      String((emp as any)._id),
    );
    return { success: true, data };
  }

  @Get('cua-toi')
  async phieuLuongCuaToi(@Query('thang') thang: string, @Req() req: any) {
    const emp = await this.nhanVien_Service.resolveEmployeeFromUser(req.user);
    const data = await this.bangLuong_Service.phieuLuongCuaToi(
      String((emp as any)._id),
      thang,
    );
    return { success: true, data };
  }
```

Tiêm `NhanVien_Service` vào controller và import `NhanVien_Module` trong `bang-luong.module.ts` (dùng `forwardRef` nếu Nest báo circular — khuôn ở `nhan-vien.module.ts`).

Trong `bang-luong.controller.spec.ts`, thêm hai route vào `BANG_QUYEN`? **KHÔNG** — chúng là tự phục vụ, không có quyền. Thay vào đó sửa bài "bảng quyền phủ đúng toàn bộ route" để loại trừ hai route đó, và thêm bài mới:

```ts
const ROUTE_TU_PHUC_VU = ['kyCoPhieuLuongCuaToi', 'phieuLuongCuaToi'];

it('route tự phục vụ KHÔNG khai @Permissions', () => {
  for (const ten of ROUTE_TU_PHUC_VU) {
    expect(permissionsOf(proto[ten])).toBeUndefined();
  }
});

it('bảng quyền ở trên phủ đúng toàn bộ route KHÔNG tự phục vụ', () => {
  const routeThucTe = Object.getOwnPropertyNames(proto).filter(
    (ten) => httpMethodOf(proto[ten]) !== undefined && !ROUTE_TU_PHUC_VU.includes(ten),
  );
  expect(routeThucTe.sort()).toEqual(BANG_QUYEN.map(([t]) => t).sort());
});
```

Bài `quetPhanQuyenRoute(BangLuong_Controller)` sẵn có **giữ nguyên** — nó tự nhận `cua-toi` là tự phục vụ và sẽ đỏ nếu ai đó gắn `@Permissions` vào. Bài "không route nào được miễn trừ theo diện tự phục vụ" (`quetPhanQuyenRoute(Controller, [])`) phải **xoá** — controller này nay CÓ route tự phục vụ; giữ lại là mâu thuẫn.

- [ ] **Step 5: Chạy test**

```bash
cd be && nvm use v22.0.0 && yarn test apps/config-service/src/bang-luong && yarn test && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | grep -c ""
```
Expected: PASS; tsc **70**.

- [ ] **Step 6: Commit**

```bash
git add be/apps/config-service/src/bang-luong
git commit -m "feat(be): hai route tự phục vụ cho phiếu lương

employeeId LUÔN suy từ token, không bao giờ đọc từ query — đây là toàn bộ
ranh giới ngăn xem lương đồng nghiệp. Route khai TRƯỚC ':id' vì NestJS khớp
chuỗi 'cua-toi' thành :id và route đó đòi quyền nên nhân viên nhận nhầm 403.

Không khai @Permissions: bắt HR gán quyền từng người thì người mới không xem
được lương tháng đầu; quetPhanQuyenRoute() ép đúng luật này."
```

---

## Task 3: Tách cơ chế in dùng chung

**Files:**
- Create: `fe/src/utils/printHtml.ts`
- Create: `fe/src/utils/printHtml.test.ts`
- Modify: `fe/src/pages/nhan-su/hop-dong-lao-dong/lib/printHopDong.ts`

**Interfaces:**
- Produces: `buildPrintableDocument(html, title)`, `printHtml(html, title)`

Chép nguyên `printHopDong.ts` sang `fe/src/utils/printHtml.ts`, đổi tên hàm `printHopDong` → `printHtml`, giữ **toàn bộ** doc-comment về `sandbox="allow-same-origin allow-modals"` (thiếu `allow-modals` thì Chrome **âm thầm** nuốt `window.print()`). Rồi `printHopDong.ts` rút gọn thành:

```ts
import { printHtml, buildPrintableDocument } from '@/utils/printHtml';

export { buildPrintableDocument };

/** In hợp đồng lao động — HTML đã ghép + sanitize ở BE (`renderHopDong`). */
export function printHopDong(html: string, title = 'Hợp đồng lao động'): void {
  printHtml(html, title);
}
```

- [ ] **Step 1: Chép test sẵn có sang util mới**

`fe/src/pages/nhan-su/hop-dong-lao-dong/lib/__tests__/printHopDong.test.ts` đã có test cho cơ chế này. Chép sang `fe/src/utils/printHtml.test.ts`, đổi import và tên hàm. **Giữ nguyên** bài khẳng định thuộc tính `sandbox` — đó là bài chặn hồi quy quan trọng nhất.

- [ ] **Step 2: Chạy cả hai suite**

```bash
cd fe && nvm use && npm run test -- src/utils/printHtml.test.ts src/pages/nhan-su/hop-dong-lao-dong
```
Expected: PASS cả hai — test hợp đồng cũ vẫn xanh sau khi delegate.

- [ ] **Step 3: Commit**

```bash
git add fe/src/utils fe/src/pages/nhan-su/hop-dong-lao-dong
git commit -m "refactor(fe): tách printHtml dùng chung khỏi printHopDong

Hiểu biết tinh tế nhất của cơ chế này nằm ở sandbox='allow-same-origin
allow-modals' — thiếu allow-modals thì Chrome âm thầm nuốt window.print(),
không báo lỗi gì lên JS. Kiến thức đó tồn tại hai bản là chuyện sớm muộn một
bản bị sửa sai."
```

---

## Task 4: Màn `/toi/phieu-luong`

**Files:**
- Create: `fe/src/services/phieuLuongService.ts`
- Create: `fe/src/pages/toi/phieu-luong/PhieuLuongCuaToi.tsx`
- Create: `fe/src/pages/toi/phieu-luong/PhieuLuongCuaToi.test.tsx`
- Modify: `fe/src/pages/loadable.tsx`, `fe/src/App.tsx`, vỏ `EmployeeLayout` (menu)

**Interfaces:**
- Consumes: API Task 2
- Produces: route `/toi/phieu-luong`

- [ ] **Step 1: Viết service FE**

```ts
import { ServiceBase } from './base/service-base';

export interface KhoanPhieuLuong { ma: string; ten: string; soTien: number }

export interface PhieuLuong {
  thang: string; hoTen: string; maNhanVien: string;
  congThuong: number; congThuViec: number; congKhac: number;
  khoan: KhoanPhieuLuong[];
  tongThuNhap: number; bhxh: number; thue: number; phiCongDoan: number;
  tamUng: number; khauTruKhac: number; thucLinh: number;
  thuNhapMienThue: number; giamTru: number; thuNhapTinhThue: number;
}

class PhieuLuongService extends ServiceBase {
  constructor() {
    super({ endpoint: '/config/bang-luong' });
  }

  async cacKy(): Promise<string[]> {
    return super.get<string[]>({ endpoint: '/cua-toi/ky' });
  }

  /** `null` = kỳ chưa chốt hoặc chưa có dòng. */
  async phieu(thang: string): Promise<PhieuLuong | null> {
    return super.get<PhieuLuong | null>({
      endpoint: '/cua-toi',
      params: { thang },
    });
  }
}

export const phieuLuongService = new PhieuLuongService();
```

Kiểm chữ ký `ServiceBase.get` với cả `endpoint` lẫn `params` bằng cách đọc một service sẵn có (`grep -n "super.get<" fe/src/services/bangLuongService.ts`); dùng chữ ký THẬT của repo, đừng ép repo theo phác thảo này.

- [ ] **Step 2: Viết test render**

```tsx
// @vitest-environment jsdom
const PHIEU = {
  thang: '2026-07', hoTen: 'Đào Thị Kiều Oanh', maNhanVien: 'NV0001',
  congThuong: 24, congThuViec: 0, congKhac: 0,
  khoan: [
    { ma: 'LUONG_CONG', ten: 'Lương theo công', soTien: 12_000_000 },
    { ma: 'TIEN_OT', ten: 'Tiền làm thêm giờ', soTien: 1_504_000 },
  ],
  tongThuNhap: 13_504_000, bhxh: 577_500, thue: 45_000, phiCongDoan: 110_000,
  tamUng: 0, khauTruKhac: 0, thucLinh: 12_771_500,
  thuNhapMienThue: 15_500_000, giamTru: 15_500_000, thuNhapTinhThue: 0,
};

describe('Phiếu lương của tôi', () => {
  it('hiện từng khoản kèm nhãn tiếng Việt và thực lĩnh', async () => {
    vi.spyOn(phieuLuongService, 'cacKy').mockResolvedValue(['2026-07']);
    vi.spyOn(phieuLuongService, 'phieu').mockResolvedValue(PHIEU);
    render(<PhieuLuongCuaToi />);

    expect(await screen.findByText('Lương theo công')).toBeTruthy();
    expect(screen.getByText('Tiền làm thêm giờ')).toBeTruthy();
    expect(screen.getAllByText(/12\.771\.500/).length).toBeGreaterThan(0);
  });

  it('KHÔNG hiện chữ nào về mức khai báo', async () => {
    vi.spyOn(phieuLuongService, 'cacKy').mockResolvedValue(['2026-07']);
    vi.spyOn(phieuLuongService, 'phieu').mockResolvedValue(PHIEU);
    const { container } = render(<PhieuLuongCuaToi />);

    await screen.findByText('Lương theo công');
    expect(container.textContent).not.toMatch(/khai báo/i);
  });

  it('chưa có kỳ nào thì báo rõ, không vỡ', async () => {
    vi.spyOn(phieuLuongService, 'cacKy').mockResolvedValue([]);
    render(<PhieuLuongCuaToi />);

    expect(await screen.findByText(/Chưa có phiếu lương nào/)).toBeTruthy();
  });

  it('kỳ chưa chốt (API trả null) báo rõ, không hiện bảng trống', async () => {
    vi.spyOn(phieuLuongService, 'cacKy').mockResolvedValue(['2026-08']);
    vi.spyOn(phieuLuongService, 'phieu').mockResolvedValue(null);
    render(<PhieuLuongCuaToi />);

    expect(await screen.findByText(/Chưa có phiếu lương tháng này/)).toBeTruthy();
  });

  it('tài khoản chưa gắn hồ sơ nhân viên (404) báo rõ, không màn trắng', async () => {
    vi.spyOn(phieuLuongService, 'cacKy').mockRejectedValue({
      response: { status: 404 },
    });
    render(<PhieuLuongCuaToi />);

    expect(await screen.findByText(/chưa gắn hồ sơ nhân viên/i)).toBeTruthy();
  });
});
```

Chép khối mock `matchMedia`/`ResizeObserver`/`AuthContext` từ một test render sẵn có (vd `BangThemGioPage.render.test.tsx`).

- [ ] **Step 3: Chạy test, xác nhận ĐỎ**

```bash
cd fe && nvm use && npm run test -- src/pages/toi/phieu-luong
```

- [ ] **Step 4: Dựng màn**

Component đơn (không cần CHandler — màn chỉ đọc, không có vòng đời tổng hợp/chốt; `BangCongThang.tsx` trong cùng vỏ `/toi` cũng dùng `useState` thuần, theo đúng lệ đó).

Bố cục theo thứ tự người đọc phiếu giấy:

```
Phiếu lương — Tháng 07/2026        [chọn tháng ▾]
Đào Thị Kiều Oanh · NV0001 · Công: 24

CÁC KHOẢN                              Số tiền
  Lương theo công                  12.000.000
  Tiền làm thêm giờ                 1.504.000
  ─────────────────────────────────────────
  Tổng thu nhập                    13.504.000

CÁC KHOẢN TRỪ
  BHXH                               -577.500
  Thuế TNCN                           -45.000
  Phí công đoàn                      -110.000
  Tạm ứng                                   0
  ─────────────────────────────────────────
  THỰC LĨNH                        12.771.500

▸ Cách tính thuế   (thu gọn)
```

- [ ] **Step 5: Route + menu**

Thêm `PhieuLuongCuaToiPage` vào `loadable.tsx`, route `phieu-luong` trong khối `/toi` của `App.tsx` (**không** khai trong `routePermissions.ts` — vỏ `/toi` cố ý không kiểm quyền, xem chú thích tại chỗ), và mục menu trong vỏ nhân viên (tìm bằng `grep -rn "Bảng công" fe/src/components/layout`).

- [ ] **Step 6: Chạy test + tsc**

```bash
cd fe && nvm use && npm run test && ./node_modules/.bin/tsc --noEmit
```

- [ ] **Step 7: Commit**

```bash
git add fe/src
git commit -m "feat(fe): màn Phiếu lương của tôi trên vỏ nhân viên

Chỉ đọc, dùng useState thuần theo đúng lệ BangCongThang trong cùng vỏ /toi —
không có vòng đời tổng hợp/chốt nên không cần CHandler.

Ba trạng thái rỗng đều báo rõ thay vì bảng trống: chưa có kỳ nào, kỳ chưa
chốt, và tài khoản chưa gắn hồ sơ nhân viên (404 từ
resolveEmployeeFromUser)."
```

---

## Task 5: Nút In phiếu lương trên Bảng lương

**Files:**
- Create: `fe/src/pages/luong/bang-luong/lib/phieuLuongHtml.ts`
- Create: `fe/src/pages/luong/bang-luong/lib/phieuLuongHtml.test.ts`
- Modify: `fe/src/pages/luong/bang-luong/components/BangLuongTable.tsx`

**Interfaces:**
- Consumes: `printHtml` (Task 3), `DongLuong` (FE)
- Produces: `dungPhieuLuongHtml(dong, khoanLuong, tenCongTy): string`

Kế toán in từ `DongLuong` đã có sẵn trên màn (không gọi API tự phục vụ — route đó khoá theo token của **chính họ**).

- [ ] **Step 1: Viết test**

```ts
describe('dungPhieuLuongHtml', () => {
  it('có tên công ty, tháng, tên nhân viên và thực lĩnh', () => {
    const html = dungPhieuLuongHtml(DONG, KHOAN, 'Công ty cổ phần Master CEO');

    expect(html).toContain('Công ty cổ phần Master CEO');
    expect(html).toContain('07/2026');
    expect(html).toContain('Đào Thị Kiều Oanh');
    expect(html).toContain('12.771.500');
  });

  it('KHÔNG in mức khai báo', () => {
    const html = dungPhieuLuongHtml(DONG, KHOAN, 'X');
    expect(html).not.toContain('4.922.500');
  });

  it('escape tên nhân viên — không nội suy chuỗi chưa escape vào HTML', () => {
    const html = dungPhieuLuongHtml(
      { ...DONG, employeeName: '<img src=x onerror=alert(1)>' } as never,
      KHOAN, 'X',
    );
    expect(html).not.toContain('<img');
    expect(html).toContain('&lt;img');
  });

  it('có chỗ ký Người lập / Người nhận', () => {
    const html = dungPhieuLuongHtml(DONG, KHOAN, 'X');
    expect(html).toMatch(/Người lập/);
    expect(html).toMatch(/Người nhận/);
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận ĐỎ**, rồi viết `dungPhieuLuongHtml`

Dùng lại `dungPhieuLuong`-style lọc ở FE (đọc `dong.thucTe`, bỏ `khaiBao`), escape mọi giá trị chuỗi bằng hàm `escapeHtml` cùng khuôn `printHtml.ts`. HTML là một bảng đơn có `<style>` inline cho khổ A5.

- [ ] **Step 3: Nút trên bảng**

Thêm cột thao tác cuối `BangLuongTable`: nút "In phiếu", **`disabled` khi `record.trangThai !== 'chot'`** kèm tooltip "Chốt kỳ trước khi in" — in một phiếu mà số còn đổi được là phát ra ngoài một con số sẽ sai.

- [ ] **Step 4: Chạy test + tsc + commit**

```bash
cd fe && nvm use && npm run test && ./node_modules/.bin/tsc --noEmit
git add fe/src/pages/luong/bang-luong
git commit -m "feat(fe): in phiếu lương từ Bảng lương

Nút bị khoá khi dòng chưa chốt — in một phiếu mà số còn đổi được là phát ra
ngoài một con số sẽ sai. Escape mọi giá trị chuỗi: không nội suy chuỗi chưa
escape vào HTML."
```

---

## Self-Review

**1. Spec coverage**

| Mục spec | Task |
|---|---|
| §2.1 chỉ thực tế, chỉ kỳ đã chốt | 1 (hàm thuần), 2 (lọc `chot`) |
| §2.2 không email, in từ Bảng lương | 3, 5 |
| §3 route tự phục vụ, `employeeId` từ token | 2 |
| §3.1 payload tự chứa nhãn khoản | 1 |
| §3.2 chưa gắn hồ sơ nhân viên → báo rõ | 4 (test 404) |
| §4.1 màn `/toi/phieu-luong` | 4 |
| §4.2 nút in, tách `printHtml` | 3, 5 |
| §6.1 test khẳng định không đọc `employeeId` từ query | 2 (bài "KHÔNG trả phiếu của người khác") |

**2. Placeholder scan** — không có "TBD"/"TODO". Ba chỗ yêu cầu đọc mã trước khi viết (chữ ký `ServiceBase` Task 4 Step 1, mock render Task 4 Step 2, menu vỏ `/toi` Task 4 Step 5) đều nêu lệnh grep cụ thể.

**3. Type consistency**

- `PhieuLuong` / `KhoanPhieuLuong` khai ở `luong.types.ts` (Task 1), FE mirror y hệt (Task 4 Step 1). ✓
- `dungPhieuLuong(dong, khoanLuong)` — Task 2 Step 3 gọi đúng hai tham số. ✓
- `phieuLuongCuaToi(employeeId, thang)` / `kyCoPhieuLuong(employeeId)` — controller Task 2 Step 4 gọi đúng thứ tự. ✓
- `printHtml(html, title)` (Task 3) — Task 5 gọi đúng chữ ký. ✓
- `dungPhieuLuongHtml(dong, khoanLuong, tenCongTy)` — Task 5 test và nút gọi cùng thứ tự. ✓
